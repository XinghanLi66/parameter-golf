# Experiment Ledger

Use one row per meaningful experiment or proposal revision. Prefer short factual entries over prose.

| Date (UTC) | ID | Category | Baseline / Comparison | Hypothesis | Mechanism / Rationale | Changed Variables | Held Fixed | Expected Effect | Actual Result | Interpretation | Next Step |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-03-27 | `eval_051_eval050_buckets_pair_confirm_patched_7gpu` | evaluation | Fresh same-session patched-helper `2097152` control on GPUs `1..7` versus immediate `1048576` candidate, with admissibility anchors `eval_049=0.19974316` and `eval_048=0.19974338`; `eval_050=0.14602989` remained candidate-only context, not the primary decision baseline | If `NGRAM_EVAL_BUCKETS=1048576` is a real improvement on the validated patched `1..7` surrogate path, then a fresh same-session pair should show an admissible `2097152` control and a clean `1048576` candidate completion that beats the fresh control by at least `0.00010 BPB` | `eval_049` already validated the patched control and `eval_050` already showed candidate-only operational viability, so the missing discriminating evidence was the exact same-session control/candidate comparison with only bucket count changed | Reused the exact patched helper unchanged in fresh `eval_051`; changed only fresh operational identifiers plus `NGRAM_EVAL_BUCKETS 2097152 -> 1048576` between arms; recorded fresh gate, clean handoff, and separate external telemetry for control and candidate on GPUs `1,2,3,4,5,6,7` | Patched helper `126026 / c4a687b6...`, checkpoint `106178569 / b8291ad1...`, artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_ENABLED=1`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `EVAL_STRIDE=64`, `NGRAM_EVAL_MIN_ORDER=2`, `NGRAM_EVAL_MAX_ORDER=9`, batched lookup, batched torch stats, vectorized postlookup, tokenizer/data, runner path, and GPU subset `1..7` | Fresh control should stay admissible versus `eval_049` / `eval_048`, then the immediate `1048576` candidate should remain clean and either beat the fresh control by at least `0.00010 BPB` or fail comparison-cleanly enough to justify a bucket decision | The initial gate passed at `2026-03-27T16:39:09Z` with GPUs `1..7` idle at about `81007 MiB` free and no subset compute apps. The fresh control then completed cleanly with `legal_ttt_exact val_loss=0.33725841`, `val_bpb=0.19974343`, script `617197ms`, runner `665584ms`, external `665.848s`, `runner_start_to_child_spawn_ms=649`, `child_runtime_ms=664935`, any-match `0.98387585`, avg alpha `0.65461559`, matched-order histogram `order_2=60166`, `order_3=346841`, `order_4=373090`, `order_5=332096`, `order_6=395043`, `order_7=644544`, `order_8=1634957`, `order_9=57234849`, `ngram_batch_lookup_elapsed_ms=17414`, `ngram_torch_stats_elapsed_ms=4366`, `ngram_postlookup_vectorized_elapsed_ms=1109722`, and `ngram_update_batch_timing elapsed_ms=30758`; control admissibility passed versus `eval_049` with `+0.00000027 BPB` and versus `eval_048` with `+0.00000005 BPB`. The immediate handoff sample at `2026-03-27T16:51:56Z` also passed cleanly, so the `1048576` candidate launched at `2026-03-27T16:52:21Z`. But the candidate arm then became contaminated: the first external telemetry evidence at `2026-03-27T16:53:37Z` showed foreign PIDs `1873822..1873828` appear on GPUs `1..7` alongside the candidate PIDs `1869880..1869886`, initially at about `714..756 MiB` foreign memory per reviewed GPU. Contamination escalated across later samples; by `2026-03-27T16:58:43Z` and `16:58:48Z`, GPUs `1..7` were at `100%` utilization with about `36.5..38.9 GiB` of foreign memory plus about `5.3..5.6 GiB` of candidate memory per GPU. I therefore interrupted the candidate rather than let a dirty comparison finish. Latest captured candidate stdout only reached chunk `221/1893` at running `bpb=0.509815`, any-match `0.860559`, avg alpha `0.629725`, helper chunk-time `321.8s`; no `metadata.json` or final post-export metric was produced | This is a controlled `invalid pair` result. The fresh control and immediate handoff were valid, but the candidate arm lost comparison cleanliness after launch, so the round still does not answer whether `1048576` should replace `2097152` on the patched `1..7` surrogate path | Rerun the same reviewed patched-helper `2097152 -> 1048576` pair only when GPUs `1..7` can remain isolated through both arms. Do not use the partial dirty candidate trace as promotability evidence |
| 2026-03-27 | `eval_050_eval049_buckets1048576_operational_rerun_patched_7gpu` | evaluation | Prior failed `eval_049` patched-helper `NGRAM_EVAL_BUCKETS=1048576` candidate on GPUs `1..7`; context anchors remained `eval_049` / `eval_048` / `eval_047` admissible `2097152` patched-helper controls | On the validated patched `1..7` surrogate regime, the late `1048576` failure from `eval_049` is either reproducibly unstable, in which case one exact rerun should fail again with comparable late-path evidence, or operationally viable, in which case one exact rerun should finish cleanly and emit a final post-export metric | `eval_049` had already answered the fresh-control admissibility question, so the only unresolved discriminating variable was failure reproducibility itself on the exact same patched-helper / checkpoint / artifact / launch path | Reused the exact patched helper from `eval_049` unchanged in fresh `eval_050`; kept `NGRAM_EVAL_BUCKETS=1048576`; changed only fresh `RUN_ID`, fresh runner log dir, fresh run name, fresh gate evidence, and fresh external telemetry capture on the same reviewed GPU subset `1,2,3,4,5,6,7` | Patched helper `126026 / c4a687b6...`, checkpoint `106178569 / b8291ad1...`, artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_ENABLED=1`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `EVAL_STRIDE=64`, `NGRAM_EVAL_MIN_ORDER=2`, `NGRAM_EVAL_MAX_ORDER=9`, batched lookup, batched torch stats, vectorized postlookup, tokenizer/data, runner path, and GPU subset `1..7` | If the path were unstable, the rerun should fail again near the same late chunk; if transient, it should complete cleanly and reopen the bucket question for a later fresh same-session control/candidate comparison | The first fresh gate sample at `2026-03-27T16:10:03Z` failed because a foreign 8-GPU workload occupied GPUs `1..7` with about `37.5..39.1 GiB` used and `100%` utilization, but the bounded gate watch passed at `2026-03-27T16:10:35Z` with GPUs `1..7` idle and no subset processes. The exact rerun then launched at `2026-03-27T16:11:17Z` and completed cleanly with `legal_ttt_exact val_loss=0.24656535`, `val_bpb=0.14602989`, script `618438ms`, runner `666370ms`, external `666.594s`, `runner_start_to_child_spawn_ms=395`, `child_runtime_ms=665975`, any-match `0.98387635`, avg alpha `0.66088724`, matched-order histogram `order_2=17786`, `order_3=95312`, `order_4=113192`, `order_5=124743`, `order_6=175760`, `order_7=309994`, `order_8=810348`, `order_9=59374482`, `ngram_batch_lookup_elapsed_ms=14132`, `ngram_torch_stats_elapsed_ms=4303`, `ngram_postlookup_vectorized_elapsed_ms=1108473`, and `ngram_update_batch_timing elapsed_ms=29799`; metadata exit code was `0` with timeout `false`. Critically, the rerun passed the old failed locus cleanly: at chunk `1521/1893` it had the same running `bpb=0.161550`, any-match `0.979712`, and nearly identical avg alpha `0.659664`, but reached that locus at `487.0s` instead of `637.6s`, then finished the remaining `372` chunks and wrote `metadata.json`. External telemetry showed a normal active tail near `16:22:19Z` with about `5.3..5.6 GiB` used and `68..82%` utilization on GPUs `1..7`, followed by clean release back to `0 MiB` used / `81007 MiB` free / `0%` utilization at `16:22:24Z` and `16:22:30Z` | This is a controlled `operationally viable` result. The prior `eval_049` late crash is not reproducible enough to close `1048576` as operationally unstable on the patched `1..7` path. However, because this round intentionally omitted a fresh same-session control arm, it still does not justify a bucket-geometry promotion on its own | If the bucket question still matters, schedule a later reviewed same-session patched-helper `2097152 -> 1048576` control/candidate comparison on the validated `1..7` surrogate path; otherwise move to a different single-variable evaluation question and treat `eval_050` as operational evidence rather than a promotion |
| 2026-03-27 | `eval_049_eval048_buckets_pair_patched_7gpu` | evaluation | Fresh same-session patched-helper 7-GPU control with `NGRAM_EVAL_BUCKETS=2097152` on GPUs `1..7` versus immediate candidate with only `NGRAM_EVAL_BUCKETS=1048576`, with admissibility anchors `eval_048=0.19974338`, `eval_047=0.19974198`, `eval_045=0.19974186`, and historical bucket anchor `eval_015=0.19974202` | On the validated patched `1..7` surrogate regime, reducing only `NGRAM_EVAL_BUCKETS` from `2097152` to `1048576` should keep post-export `val_bpb` within `+0.00010` of the fresh control while saving at least `30s` external wallclock | `eval_015` already answered `4194304 -> 2097152` on an older line, but not one more halving on the stronger patched-helper regime with `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, and `TTT_EPOCHS=4`, so this was the cleanest remaining bucket-geometry runtime/promotability question | Reused the exact patched helper from `eval_047` unchanged in fresh `eval_049`; kept the control arm at `NGRAM_EVAL_BUCKETS=2097152`; changed only the candidate arm to `NGRAM_EVAL_BUCKETS=1048576`; used fresh `RUN_ID`s, fresh runner log dirs, fresh run names, and the reviewed clean-idle subset `1,2,3,4,5,6,7` | Patched helper `126026 / c4a687b6...`, checkpoint `106178569 / b8291ad1...`, artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_ENABLED=1`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `EVAL_STRIDE=64`, `NGRAM_EVAL_MIN_ORDER=2`, `NGRAM_EVAL_MAX_ORDER=9`, batched lookup, batched torch stats, vectorized postlookup, tokenizer/data, runner path, and GPU subset `1..7` | Fresh control should stay admissible versus `eval_048` / `eval_047`, then the immediate `1048576` candidate should remain within `+0.00010 BPB` of the fresh control while saving at least `30s` external wallclock if one more bucket halving is safe on the patched promoted-line runtime path | The fresh gate passed at `2026-03-27T15:21:54Z` with GPUs `1..7` idle and no compute-app processes on the selected subset. The fresh control completed with `legal_ttt_exact val_loss=0.33725796`, `val_bpb=0.19974316`, script `616057ms`, runner `661647ms`, external `661.929s`, `runner_start_to_child_spawn_ms=390`, `child_runtime_ms=661256`, any-match `0.98387585`, avg alpha `0.65461727`, unchanged matched-order histogram, and `ngram_postlookup_vectorized_elapsed_ms=1112324`; admissibility passed versus `eval_048` with `-0.00000022 BPB`, `-7745ms` script, `-8262ms` runner, and `-8.212s` external, and versus `eval_047` with `+0.00000118 BPB`, `-268ms` script, `-1161ms` runner, and `-1.163s` external. The immediate `1048576` candidate then launched on the same patched helper but failed late: `stdout.log` reached chunk `1521/1893` at running `bpb=0.161550`, any-match `0.979712`, avg alpha `0.659670`, helper chunk-time `637.6s`, then `stderr.log` recorded `torch.AcceleratorError: CUDA error: unspecified launch failure` in `score_segments()`. Torchrun hung in distributed teardown after the child-side failure and never wrote `metadata.json`; I interrupted the stuck top-level runner at `2026-03-27T15:54:48Z` only after the failure had been captured so GPUs `1..7` were released | This is a controlled `blocked-after-admissible-control` result. The patched `1..7` surrogate path remains valid and repeatable, but the reviewed bucket comparison is unanswered because the `1048576` candidate never produced a final post-export metric or valid final wallclock. The round therefore does not support either `promote 1048576` or `hold 2097152` as a bucket-geometry conclusion | Do not infer a bucket conclusion from round 24. If this question remains important, require a new reviewed brief for a dedicated operational diagnosis or controlled retry of the late `1048576` CUDA failure on the patched `1..7` path; otherwise move the next refinement round to a different single-variable evaluation question |
| 2026-03-27 | `eval_048_eval047_ttt_epochs_pair_patched_7gpu` | evaluation | Fresh same-session patched-helper 7-GPU control with `TTT_EPOCHS=4` on GPUs `1..7` versus immediate candidate with only `TTT_EPOCHS=3`, with admissibility anchors `eval_047=0.19974198`, `eval_045=0.19974186`, and promoted `eval_038=0.19974237` | On the validated patched `1..7` surrogate regime, reducing only `TTT_EPOCHS` from `4` to `3` should keep post-export `val_bpb` within `+0.00005` of the fresh control while saving meaningful eval wallclock | Leaderboard `#1` uses legal score-first TTT with `3` epochs, and `eval_047` had already restored an interpretable patched 7-GPU surrogate path, so epoch count was again the cleanest remaining promoted-line evaluation mismatch | Reused the exact patched helper from `eval_047` unchanged in fresh `eval_048`; kept the control arm at `TTT_EPOCHS=4`; changed only the candidate arm to `TTT_EPOCHS=3`; used fresh `RUN_ID`s, fresh runner log dirs, fresh run names, and the reviewed clean-idle subset `1,2,3,4,5,6,7` | Patched helper `126026 / c4a687b6...`, checkpoint `106178569 / b8291ad1...`, artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_BUCKETS=2097152`, batched lookup, batched torch stats, vectorized postlookup, tokenizer/data, runner path, and GPU subset `1..7` | Fresh control should stay admissible versus `eval_047`, then the immediate `TTT_EPOCHS=3` candidate should remain within `+0.00005 BPB` of the fresh control while saving at least `50s` wallclock if `3` is the better promoted-line runtime setting | The fresh gate passed at `2026-03-27T14:36:53Z` with GPUs `1..7` idle and only GPU `0` occupied outside the reviewed set. The fresh control completed with `legal_ttt_exact val_loss=0.33725832`, `val_bpb=0.19974338`, script `623802ms`, runner `669909ms`, external `670.141s`, `runner_start_to_child_spawn_ms=411`, `child_runtime_ms=669498`, any-match `0.98387585`, avg alpha `0.65461534`, unchanged matched-order histogram, and `ngram_postlookup_vectorized_elapsed_ms=1105413`; its BPB delta vs `eval_047` was only `+0.00000140`, so admissibility passed. The immediate candidate then completed with `legal_ttt_exact val_loss=0.33730055`, `val_bpb=0.19976839`, script `998177ms`, runner `1041138ms`, external `1041.407s`, `runner_start_to_child_spawn_ms=306`, `child_runtime_ms=1040831`, any-match `0.98387585`, avg alpha `0.65467460`, unchanged histogram, and `ngram_postlookup_vectorized_elapsed_ms=1353120`. Candidate minus control was `+0.00002501 BPB`, `+0.00004223 val_loss`, `+374375ms` script, `+371229ms` runner, `+371.266s` external, `+0.00005926` avg alpha, unchanged histogram, and `+247707ms` postlookup | This is a controlled `hold` result. The fresh control revalidated the patched surrogate path, the candidate stayed inside the BPB tolerance band, but the supposed runtime optimization failed outright because `TTT_EPOCHS=3` was much slower and slightly worse on quality. The reviewed hypothesis is therefore answered negatively rather than blocked | Keep `TTT_EPOCHS=4` as the quality default on the patched promoted-line surrogate path. Do not promote `TTT_EPOCHS=3` as a runtime setting; move the next refinement round to a different single-variable question unless a new brief explicitly asks for an operational diagnosis of the slower `3`-epoch path |
| 2026-03-27 | `eval_047_eval045_patched_helper_7gpu_control` | evaluation | Fresh patched-helper 7-GPU promoted-line `TTT_EPOCHS=4` control on GPUs `1..7` versus fresh admissible 8-GPU control `eval_045=0.19974186` and promoted 8-GPU anchor `eval_038=0.19974237` | If `eval_046` was blocked only by the helper’s startup invariant, then a minimal eval-only patch permitting non-divisor `WORLD_SIZE=7` should launch a 7-GPU control that stays semantically in-family with `eval_045` | The copied helper’s eval-only path does not need the training accumulation invariant, so guarding only that startup check is the smallest operational repair that can answer surrogate validity without changing scorer, TTT, export, checkpoint, or dataset semantics | Copied helper into fresh `eval_047`; patched only startup logic so `EVAL_ONLY=1` and non-divisor `WORLD_SIZE` fall back to `grad_accum_steps=1`; added one compatibility log line; operationally used GPUs `1,2,3,4,5,6,7`, runner request count `7`, `torchrun --nproc_per_node=7`, fresh `RUN_ID`, fresh runner log dir, and fresh run name | Checkpoint `106178569 / b8291ad1...`, artifact `15555121 / eb062c96...`, promoted legal-TTT settings, scorer path, n-gram settings, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, tokenizer/data, `physicslm`, runner path, no training changes, no export changes, no non-eval behavior changes, original locked helper left untouched | Patched 7-GPU control should launch and finish, then remain within `±0.00002 BPB` of `eval_045` with in-family telemetry, proving the `1..7` path is an admissible surrogate regime | The gate passed at `2026-03-27T14:12:53Z` with GPUs `1..7` all clean at about `81007 MiB` free while only GPU `0` remained occupied outside the reviewed gate set. The patched helper launched and completed the control: `legal_ttt_exact val_loss=0.33725596`, `val_bpb=0.19974198`, script `616325ms`, runner `662808ms`, external `663.092s`, `runner_start_to_child_spawn_ms=428`, `child_runtime_ms=662380`, any-match `0.98387585`, avg alpha `0.65461744`, unchanged matched-order histogram, and `ngram_postlookup_vectorized_elapsed_ms=1110166`. Deltas vs `eval_045` were `+0.00000012 BPB`, identical any-match, `+0.00001589` avg alpha, identical histogram, `-689683ms` script, `-687719ms` runner, `-687713ms` external, and `-373926ms` postlookup. Deltas vs `eval_038` were `-0.00000039 BPB`, identical any-match, `+0.00001833` avg alpha, identical histogram, `+27512ms` script, `+28123ms` runner, `+28166ms` external, and `-52017ms` postlookup | This is a controlled `surrogate admissible` result. The reviewed operational blocker was real but repairable: the 7-GPU `1..7` path is usable once the helper’s eval-only startup invariant is patched, and the resulting control stays semantically in-family with the promoted 8-GPU line | Reopen the exact same-session promoted-line `TTT_EPOCHS=4 -> 3` comparison on the patched `1..7` path, keeping the patched helper, checkpoint, artifact, scorer settings, and all non-epoch variables fixed |
| 2026-03-27 | `eval_046_eval045_ttt_epochs_pair_7gpu_subset` | evaluation | Reviewed same-session promoted-line 7-GPU surrogate pair on the locked `eval_038=0.19974237` legality line: fresh `TTT_EPOCHS=4` control on GPUs `1..7` vs immediate `TTT_EPOCHS=3` candidate on the same subset, with admissibility anchors `eval_045 control=0.19974186` and promoted `eval_038=0.19974237` | If GPU `0` contamination was the real blocker rather than the epoch-count hypothesis itself, then the same promoted-line pair on the cleaner fixed subset `1..7` would finally yield a valid surrogate control and candidate comparison | The reviewed brief isolated only the launch subset and candidate epoch count; this was the smallest operational retry that could answer the epoch question without mixing architecture, optimization, export, or scorer changes | No code edits. Actual executed changes were helper/checkpoint/artifact re-verification, fresh operational identifiers, fixed subset `1,2,3,4,5,6,7`, runner request count `7`, `torchrun --nproc_per_node=7`, one fresh 7-GPU clean-idle gate, and one fresh 7-GPU control launch attempt; intended candidate-only semantic change `TTT_EPOCHS: 4 -> 3` was not launched after the control startup failure | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, `NGRAM_EVAL_BATCH_TORCH_STATS=1`, `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, tokenizer/data, stride `64`, `physicslm`, cwd, runner path, no helper edits, no retraining, no export rewrite | The fixed `1..7` clean-idle gate should pass, the fresh 7-GPU control should launch and finish, and then the immediate 7-GPU `TTT_EPOCHS=3` candidate should launch if the control is an admissible surrogate regime | The 7-GPU gate passed at `2026-03-27T13:56:56Z`, with GPUs `1..7` all clean at about `81007 MiB` free while only GPU `0` remained occupied. Identity checks stayed clean. The runner then successfully selected GPUs `1..7` and launched the fresh 7-GPU control at `2026-03-27T13:57:15Z`, but the child failed before any evaluation metric was produced because the locked helper raised `ValueError: WORLD_SIZE=7 must divide 8 so grad_accum_steps stays integral` at startup on every rank. Runner timings were external `34.181s`, runner `33914ms`, `runner_start_to_child_spawn_ms=424`, and `child_runtime_ms=33489`; metadata exit code was `1`, and torchrun surfaced `ChildFailedError` with first observed root-cause failure at rank `5`. Because the control never reached admissibility, the immediate `TTT_EPOCHS=3` candidate was not attempted | This is a controlled `operationally blocked` result on the reviewed 7-GPU launch path, not a surrogate-regime or epoch-count decision. The gate itself passed, so GPU `0` cleanliness alone is no longer the only blocker; the locked helper is incompatible with `WORLD_SIZE=7` under the no-code-change reviewed constraints | Do not retry the exact 7-GPU surrogate path unchanged. Require a new reviewed brief that either authorizes a helper patch for non-divisor eval-only world sizes or specifies a different helper-compatible topology before reopening the promoted-line `TTT_EPOCHS=4 -> 3` question |
| 2026-03-27 | `eval_045_eval038_ttt_epochs_pair_clean_idle_continuous_watch` | evaluation | Exact promoted-line same-session pair on the locked `eval_038=0.19974237` legality line: fresh `TTT_EPOCHS=4` control vs immediate `TTT_EPOCHS=3` candidate, with current-regime context anchors `eval_042=0.19974395` at `1556076ms` external and blocked retries `eval_043` / `eval_044` | If reducing only `TTT_EPOCHS` from `4` to `3` on the exact promoted `EVAL_LOGIT_TEMP=1.0`, `NGRAM_EVAL_BUCKETS=2097152` line preserves BPB within `+0.00005` of the same-session control while saving at least `50s` external, then `3` is promotable on the current runtime regime | The reviewed brief identified epoch count as the narrowest remaining promoted-line refinement question, and required one materially longer continuous clean-idle watch so launch admissibility would no longer be confounded by another short bounded retry | No code edits. Actual executed changes were helper/checkpoint/artifact re-verification, fresh operational identifiers, one uninterrupted continuous clean-idle watch on pinned GPUs `0..7`, synchronized telemetry, one fresh exact `TTT_EPOCHS=4` control launch, and one immediate candidate launch attempt with only `TTT_EPOCHS: 4 -> 3`; the candidate never reached child spawn because the pinned set re-contaminated after control completion | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, `NGRAM_EVAL_BATCH_TORCH_STATS=1`, `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no helper edits, no retraining, no export rewrite | Continuous gate should pass, then the fresh `TTT_EPOCHS=4` control should launch and remain semantically in-family versus `eval_042` / `eval_038`, after which the immediate `TTT_EPOCHS=3` candidate should launch and answer `hold 4` vs `runtime-only 3` vs `promote 3` | The continuous gate passed at `2026-03-27T13:05:27Z` after an uninterrupted watch from `2026-03-27T12:58:42Z`, with all eight pinned GPUs clean. Identity checks stayed clean before and after the round. The fresh exact control then finished with `legal_ttt_exact val_loss=0.33725577`, `val_bpb=0.19974186`, script `1306008ms`, runner `1350527ms`, external `1350805ms`, `runner_start_to_child_spawn_ms=372`, `child_runtime_ms=1350155`, any-match `0.98387585`, avg alpha `0.65460155`, the exact promoted matched-order histogram, and `ngram_postlookup_vectorized_elapsed_ms=1484092`, so control admissibility passed versus both `eval_042` and `eval_038`. The immediate candidate launch with only `TTT_EPOCHS=3` then failed before child spawn because a foreign process reoccupied GPU `0`; at `2026-03-27T13:29:49Z` the runner saw only 7 eligible GPUs, with GPU `0` at `6512 MiB` free / `74495 MiB` used / `100%` utilization. Telemetry showed foreign PID `4112518` appear on GPU `0` during the late control tail and persist through the candidate attempt, so no candidate BPB or wallclock was produced | This is a controlled `post-gate contamination / invalid same-session pair`, not an epoch-count decision. The longer continuous watch answered the launch-admissibility question positively and yielded a fresh admissible promoted-line control, but the scientific `TTT_EPOCHS=4 -> 3` comparison still remains unanswered because the candidate handoff was contaminated after gate pass | Retry the exact same promoted-line same-session `TTT_EPOCHS=4 -> 3` pair unchanged when pinned GPUs `0..7` can remain clean through both the continuous gate and the immediate post-control candidate handoff. Do not force a dirty relaunch or infer `hold 4` / `runtime-only 3` / `promote 3` from this invalidated pair |
| 2026-03-27 | `eval_044_eval038_ttt_epochs_pair_clean_idle_retry` | evaluation | Planned exact promoted-line same-session pair on the locked `eval_038=0.19974237` legality line: fresh `TTT_EPOCHS=4` control vs immediate `TTT_EPOCHS=3` candidate, with current-regime context anchor `eval_042=0.19974395` at `1556076ms` external | If reducing only `TTT_EPOCHS` from `4` to `3` on the exact promoted `EVAL_LOGIT_TEMP=1.0`, `NGRAM_EVAL_BUCKETS=2097152` line preserves BPB within `+0.00005` of the same-session control while saving at least `50s` external, then `3` is promotable on the current runtime regime | After `eval_043` failed on a dirty gate, the reviewed brief still left the same exact same-session epoch pair as the smallest unresolved refinement-phase question, so the disciplined next step was a clean-idle retry rather than any semantic change | No code edits and no semantic launches occurred because the clean-idle gate never passed. Actual executed changes were only helper/checkpoint/artifact re-verification, fresh operational identifiers, intended control/candidate command preparation, and a new bounded clean-idle watch on pinned GPUs `0..7`; intended candidate-only semantic change `TTT_EPOCHS: 4 -> 3` was not launched | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, `NGRAM_EVAL_BATCH_TORCH_STATS=1`, `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no helper edits, no retraining, no export rewrite | Clean-idle gate should pass, then the fresh `TTT_EPOCHS=4` control should launch and remain semantically in-family versus `eval_042`, after which the immediate `TTT_EPOCHS=3` candidate should launch and answer `hold 4` vs `runtime-only 3` vs `promote 3` | The pair did not launch. Identity checks stayed clean before and after the round. Repeated UTC samples from `2026-03-27T12:43:36Z` through `2026-03-27T12:46:41Z` showed persistent foreign compute-app PIDs `3796173..3796180` on all eight pinned GPUs. The watch began with a light all-GPU footprint (`2884 MiB` on GPU `0`, `1506 MiB` on GPUs `1..7`) and then ramped into a heavy 8-GPU workload, reaching about `38596..39096 MiB` used per GPU by the end of the watch with `86..100%` utilization. Because the pinned `0..7` set never became clean-idle, no synchronized telemetry, no runner-managed fresh `TTT_EPOCHS=4` control, and no `TTT_EPOCHS=3` candidate were started. Intended commands and gate evidence were recorded in `runs/eval_044_eval038_ttt_epochs_pair_clean_idle_retry/` | This is a controlled `no-launch / clean-idle-gate-fail`, not an epoch-count result. The promoted-line `TTT_EPOCHS=4 -> 3` question remains unanswered again, and this retry establishes that the current blocker can appear as a full 8-GPU foreign workload rather than only the single-GPU occupancy seen in `eval_043` | Retry the exact same promoted-line same-session `TTT_EPOCHS=4 -> 3` pair when GPUs `0..7` can be guaranteed clean-idle. Do not force a dirty launch or infer `hold 4` / `runtime-only 3` / `promote 3` from this blocked retry |
| 2026-03-27 | `eval_043_eval038_ttt_epochs_pair_clean_idle_blocked` | evaluation | Planned exact promoted-line same-session pair on the locked `eval_038=0.19974237` legality line: fresh `TTT_EPOCHS=4` control vs immediate `TTT_EPOCHS=3` candidate, with current-regime context anchor `eval_042=0.19974395` at `1556076ms` external | If reducing only `TTT_EPOCHS` from `4` to `3` on the exact promoted `EVAL_LOGIT_TEMP=1.0`, `NGRAM_EVAL_BUCKETS=2097152` line preserves BPB within `+0.00005` of the same-session control while saving at least `50s` external, then `3` is promotable on the current runtime regime | After `eval_042` re-established a clean-launch current-regime control, the remaining matched SOTA-side evaluation variable was epoch count; a same-session pair is the smallest refinement-phase test that can answer that question without mixing any other change | No code edits and no semantic launches occurred because the clean-idle gate never passed. Actual executed changes were only helper/checkpoint/artifact re-verification, fresh operational identifiers, intended control/candidate command preparation, and a bounded clean-idle watch on pinned GPUs `0..7`; intended candidate-only semantic change `TTT_EPOCHS: 4 -> 3` was not launched | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, `NGRAM_EVAL_BATCH_TORCH_STATS=1`, `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no helper edits, no retraining, no export rewrite | Clean-idle gate should pass, then the fresh `TTT_EPOCHS=4` control should launch and remain semantically in-family versus `eval_042`, after which the immediate `TTT_EPOCHS=3` candidate should launch and answer `hold 4` vs `runtime-only 3` vs `promote 3` | The pair did not launch. Identity checks stayed clean before and after the round. Repeated UTC samples from `2026-03-27T12:27:56Z` through `2026-03-27T12:30:45Z` showed GPU `0` continuously occupied by foreign compute `PID 3676150` / `[Not Found]` using about `74486 MiB`, leaving only `6512 MiB` free at `100%` utilization, while GPUs `1..7` stayed idle with about `81007 MiB` free and `0%` utilization. Because the pinned `0..7` set never became clean-idle, no synchronized telemetry, no runner-managed fresh `TTT_EPOCHS=4` control, and no `TTT_EPOCHS=3` candidate were started. Intended commands and gate evidence were recorded in `runs/eval_043_eval038_ttt_epochs_pair_clean_idle_blocked/` | This is a controlled `no-launch / clean-idle-gate-fail`, not an epoch-count result. The promoted-line `TTT_EPOCHS=4 -> 3` question remains unanswered this round because launching on a dirty pinned set would have violated the reviewed interpretability requirement | Retry the exact same promoted-line same-session `TTT_EPOCHS=4 -> 3` pair when GPUs `0..7` can be guaranteed clean-idle. Do not force a dirty launch or infer `hold 4` / `runtime-only 3` / `promote 3` from this blocked round |
| 2026-03-27 | `eval_042_eval038_clean_idle_telemetry_control` | evaluation | Exact promoted-line runtime-diagnosis control on the promoted `eval_038=0.19974237` legality line, with runtime-restoration target `634926ms` external / `634685ms` runner / `588813ms` script and slowdown anchors `eval_039=0.19974161` at `1237494ms` external, `eval_040 control A=0.19974193` at `1365195ms` external, and `eval_040 control B=0.19974367` at `1298755ms` external | If the promoted helper/checkpoint/artifact stack is still semantically stable and the slowdown is operational rather than algorithmic, one exact clean-idle rerun should either return near the `eval_038` runtime band or prove the shift persists even after a clean launch | Enforcing the full declared clean-idle gate and then launching the exact promoted stack with synchronized telemetry is the smallest refinement-phase intervention that can separate clean-launch child-runtime inflation from launch-side overhead or prelaunch contention | No code edits and no semantic changes. Changed only clean-idle acquisition, synchronized telemetry capture, and fresh operational identifiers (`RUN_ID`, runner log dir, runner run name) | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, `NGRAM_EVAL_BATCH_TORCH_STATS=1`, `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no helper edits, no retraining, no export rewrite | Clean-idle gate should pass, the exact promoted-line run should finish within `+-0.00005 BPB` of promoted `eval_038`, and aligned telemetry should classify the slowdown locus; secondary target was runtime within `+15s` external of promoted `eval_038` | The clean-idle gate passed on the first required sample at `2026-03-27T11:40:19Z`, and the exact promoted-line control finished. Final metrics were `legal_ttt_exact val_loss=0.33725929`, `val_bpb=0.19974395`, script `1507653ms`, runner `1555810ms`, external `1556076ms`, `runner_start_to_child_spawn_ms=479`, and `child_runtime_ms=1555330`. Versus promoted `eval_038`, deltas were `+0.00000266 val_loss`, `+0.00000158 BPB`, `+918840ms` script, `+921125ms` runner, and `+921150ms` external. Any-match `0.98387585`, avg alpha `0.65459237`, and the matched-order histogram stayed in-family. Telemetry showed no meaningful launch-side stall, but `ngram_postlookup_vectorized_elapsed_ms` rose to `1540712`, which is `+378529ms` versus promoted `eval_038`; a late extra foreign PID appeared on GPU `0`, but only after the run was already far behind pace | This is a controlled `clean-launch persistent-runtime-shift / child-runtime-inflation` result. The exact promoted helper/checkpoint/artifact stack remains semantically stable, the clean-idle protocol gap from `eval_041` is now closed, and the dominant slowdown locus sits inside child execution under the current environment/process regime rather than in launch gating | Reopen the dedicated `TTT_EPOCHS=4 -> 3` refinement pair as a same-session current-regime comparison on the exact promoted line. Keep helper/checkpoint/artifact and all non-epoch variables fixed, and keep telemetry if late GPU contamination remains a concern |
| 2026-03-27 | `eval_041_eval038_clean_idle_telemetry_control` | evaluation | Exact promoted-line runtime-diagnosis control on the promoted `eval_038=0.19974237` legality line, with runtime restoration target `634926ms` external / `634685ms` runner / `588813ms` script and recent slowdown anchor `eval_040 control B=0.19974367` at `1298755ms` external | If the promoted helper/checkpoint/artifact stack is still semantically stable and the slowdown is operational, one exact clean-idle rerun with aligned telemetry should localize whether the drift sits in overlap, launch-side overhead, or true child-runtime inflation | Enforcing a verified clean-idle launch on pinned GPUs `0..7` is the smallest refinement-phase intervention that can distinguish external contention from intrinsic runtime drift without changing semantics | No code edits and no semantic changes. Intended operational-only changes were clean-idle gating, timestamped telemetry, and fresh `RUN_ID` / runner log-dir / runner run-name. Actual executed commands were only prelaunch GPU/process probes and a bounded clean-idle watch because the gate never passed | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, `NGRAM_EVAL_BATCH_TORCH_STATS=1`, `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no helper edits, no retraining, no export rewrite | Clean-idle gate should pass, one exact promoted-line control should finish within `±0.00005 BPB` of promoted `eval_038`, and aligned telemetry should classify the slowdown locus | The clean-idle gate failed and no valid diagnostic control was launched. Repeated UTC samples from `11:23:11` through `11:25:26` showed GPU `0` occupied by a foreign compute app `PID 3106138` / `[Not Found]` using about `74486 MiB`, leaving only `6512 MiB` free at `100%` utilization, while GPUs `1..7` stayed idle with about `81007 MiB` free and `0%` utilization. Because the pinned `0..7` set never became clean-idle, no synchronized launch telemetry or runner-managed eval run was started, so no new `val_loss`, `val_bpb`, or wallclock fields were produced | This is a controlled `no-launch / clean-idle-gate-fail`, not a new runtime-localization result. The project still lacks the required clean-idle exact control, so the existing `persistent-runtime-shift` classification from `eval_040` remains the last completed evidence rather than something newly confirmed or refuted here | Retry the exact same clean-idle telemetry control when GPUs `0..7` can be guaranteed idle. Do not reopen `TTT_EPOCHS=4 -> 3` until that diagnostic control succeeds |
| 2026-03-27 | `eval_040_eval038_duplicate_control` | evaluation | Same-session duplicate-control reproducibility check on the promoted `eval_038=0.19974237` legality line: `control A` followed immediately by `control B`; historical runtime anchors were promoted `634926ms` external / `634685ms` runner / `588813ms` script and drifted `eval_039=0.19974161` at `1237494ms` external | If the `eval_039` slowdown was transient rather than persistent, two fresh exact controls should reproduce promoted BPB and telemetry and return to the promoted runtime band | A duplicate-control probe is the smallest refinement-phase intervention that can separate restored runtime from same-session jitter and from a persistent promoted-line runtime shift while keeping semantics fixed | No code edits and no semantic changes. Both successful controls changed only operational identifiers (`RUN_ID`, runner log dir, runner run name`). One pre-launch `control A` attempt and one immediate post-`control A` `control B` attempt failed before child spawn because only 7 GPUs passed the runner free-memory gate | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, `NGRAM_EVAL_BATCH_TORCH_STATS=1`, `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no helper edits, no retraining, no export rewrite | Both fresh controls should stay within `±0.00005 BPB` and `±15000ms` external of promoted `eval_038`, and should also stay within `±15000ms` of each other if the promoted runtime regime is restored cleanly | Identity checks stayed clean before and after the round. Control A scored `legal_ttt_exact val_loss=0.33725588`, `val_bpb=0.19974193`, script `1316129ms`, runner `1364893ms`, external `1365195ms`, any-match `0.98387585`, avg alpha `0.65459876`, histogram `order_2=60166`, `order_3=346841`, `order_4=373090`, `order_5=332096`, `order_6=395043`, `order_7=644544`, `order_8=1634957`, `order_9=57234849`. Control B scored `legal_ttt_exact val_loss=0.33725882`, `val_bpb=0.19974367`, script `1249634ms`, runner `1298509ms`, external `1298755ms`, any-match `0.98387585`, avg alpha `0.65459430`, and the identical histogram. Versus promoted `eval_038`, control A deltas were `-0.00000075 val_loss`, `-0.00000044 BPB`, `+727316ms` script, `+730208ms` runner, `+730269ms` external; control B deltas were `+0.00000219 val_loss`, `+0.00000130 BPB`, `+660821ms` script, `+663824ms` runner, `+663829ms` external. Control B vs control A was `+0.00000174 BPB` and `-66440ms` external. Versus drifted `eval_039`, control A was `+127701ms` external and control B was `+61261ms` external. Control A overlapped with a root-owned external `run_humaneval.py` / `VLLM::EngineCore` workload on GPU `0`, but control B launched only after that workload cleared and still remained deep in the slowdown regime | This is a controlled `persistent-runtime-shift` result. Both exact controls reproduced promoted BPB and telemetry but failed the promoted runtime band badly; the second control removes the external overlap confound from the round-level classification even though control A saw additional within-shift contamination | Do not return to the dedicated `TTT_EPOCHS=4 -> 3` pair yet. First run a narrow runtime-diagnosis round on the exact promoted `eval_038` line to isolate why the promoted wallclock regime no longer reproduces on the pinned `0..7` runner-managed setup |
| 2026-03-27 | `eval_039_eval038_ttt_epochs_pair` | evaluation | Planned fresh same-session control on the promoted `eval_038=0.19974237` legality line at `TTT_EPOCHS=4` vs one immediate candidate with only `TTT_EPOCHS=3`; historical anchors `eval_015=0.19974202` and `eval_034` runtime-only epoch result on the older `EVAL_LOGIT_TEMP=0.95` line | If the fourth TTT epoch no longer buys enough quality on the promoted `EVAL_LOGIT_TEMP=1.0` line, then reducing only `TTT_EPOCHS` from `4` to `3` should preserve post-export `val_bpb` while saving substantial runtime | Leaderboard `#1` uses legal score-first TTT with `3` epochs, and `eval_034` had already shown that `3` epochs can be a strong runtime lever, so the calibrated `EVAL_LOGIT_TEMP=1.0` operating point reopened epoch count as a clean refinement question | No code edits. Actual executed control arm changed only operational identifiers (`RUN_ID`, runner log dir, runner run name`) while rerunning the exact promoted `TTT_EPOCHS=4` line; planned semantic candidate-only change `TTT_EPOCHS: 4 -> 3` was not launched because the control failed admissibility | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=1.0`, `TTT_LR=0.0025`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, `NGRAM_EVAL_BATCH_TORCH_STATS=1`, `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no helper edits, no retraining, no export rewrite | Fresh control should stay in-family versus promoted `eval_038`, then the immediate `TTT_EPOCHS=3` candidate should be compared against it; candidate promotion required no worse than `+0.00005 BPB` and at least `50s` external saved | Identity checks stayed clean before and after the round. Fresh control scored `legal_ttt_exact val_loss=0.33725535`, `val_bpb=0.19974161`, script `1190477ms`, runner `1237270ms`, external `1237494ms`, any-match `0.98387585`, avg alpha `0.65459666`, histogram `order_2=60166`, `order_3=346841`, `order_4=373090`, `order_5=332096`, `order_6=395043`, `order_7=644544`, `order_8=1634957`, `order_9=57234849`. Versus promoted `eval_038`, control deltas were `-0.00000128 val_loss`, `-0.00000076 BPB`, `+601664ms` script, `+602585ms` runner, `+602568ms` external, and `-0.00000245` avg alpha, so semantics stayed in-family but runtime admissibility failed catastrophically. The `TTT_EPOCHS=3` candidate was therefore not launched. Historical context only: fresh control external was `+679050ms` slower than old-line `eval_034` | This is a controlled `drift-stop`, not an epoch-count result. The promoted helper/checkpoint/artifact stack still reproduces the same quality regime, but not the promoted runtime regime under this session, so launching `TTT_EPOCHS=3` would have violated the reviewed same-session admissibility rule | Re-establish a fresh in-gate control on the exact promoted `eval_038` line, then rerun the same epoch-count-only `4 -> 3` pair. Do not treat this round as evidence for promoting or rejecting `TTT_EPOCHS=3` on the promoted `EVAL_LOGIT_TEMP=1.0` legality line |
| 2026-03-27 | `eval_038_eval035_temperature_pair` | evaluation | Fresh admissible same-session runner-managed control on the promoted `eval_035=0.20079980` legality line at `EVAL_LOGIT_TEMP=0.95` vs one immediate same-session candidate with only `EVAL_LOGIT_TEMP=1.0`; historical anchors `eval_036 control=0.20079853` and `eval_015=0.19974202` | If the promoted `2097152`-bucket legality line is slightly over-sharpened at `EVAL_LOGIT_TEMP=0.95`, then restoring only `EVAL_LOGIT_TEMP=1.0` should improve post-export `val_bpb` while staying in the same runtime band | `EVAL_LOGIT_TEMP=0.95` was validated before the bucket-geometry promotion, and the promoted line now trusts matched n-gram signal more strongly, so scorer temperature remained the clean remaining calibration variable on this new operating point | No code edits; changed only `EVAL_LOGIT_TEMP: 0.95 -> 1.0` between the fresh control and immediate candidate plus operational identifiers (`RUN_ID`, runner log dir, runner run name) | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, exact PR809 vectorized n-gram settings, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no retraining, no export rewrite, no helper edits | Fresh control should stay within `0.0002 BPB` of promoted `eval_035`, the candidate should then beat the fresh control by at least `0.0001 BPB`, and runtime should stay within `+15s` external of the fresh control | Identity checks stayed clean before and after both arms. Fresh control scored `legal_ttt_exact val_loss=0.33904065`, `val_bpb=0.20079897`, script `585778ms`, runner `631265ms`, external `631449ms`, any-match `0.98387585`, avg alpha `0.63990741`, histogram `order_2=60166`, `order_3=346841`, `order_4=373090`, `order_5=332096`, `order_6=395043`, `order_7=644544`, `order_8=1634957`, `order_9=57234849`; control deltas vs promoted `eval_035` were `-0.00000140 val_loss`, `-0.00000083 BPB`, `+3506ms` script, `+6252ms` runner, and `+6272ms` external, so admissibility passed cleanly. Immediate fresh candidate scored `legal_ttt_exact val_loss=0.33725663`, `val_bpb=0.19974237`, script `588813ms`, runner `634685ms`, external `634926ms`, any-match `0.98387585`, avg alpha `0.65459911`, and the identical order histogram. Candidate deltas vs fresh control were `-0.00178402 val_loss`, `-0.00105660 BPB`, `+3035ms` script, `+3420ms` runner, and `+3477ms` external; candidate vs `eval_036` control was `-0.00105616 BPB`; candidate vs historical `eval_015` was only `+0.00000035 BPB` | This is a controlled `promote` result. The promoted `2097152`-bucket legality line was indeed over-sharpened at `EVAL_LOGIT_TEMP=0.95`; restoring `EVAL_LOGIT_TEMP=1.0` produces a large same-session gain while keeping runtime comparison-clean and almost exactly matching the strongest same-family quality anchor | Promote `EVAL_LOGIT_TEMP=1.0` as the new default on the promoted `eval_035` lineage and move the next refinement round to a different single-variable question on top of this stronger calibrated baseline, with `TTT_EPOCHS=3` now the cleanest reopened runtime-quality tradeoff to test |
| 2026-03-27 | `eval_037_eval035_temperature_pair` | evaluation | Promoted `eval_035=0.20079980` as the locked promoted legality line at `EVAL_LOGIT_TEMP=0.95`; planned one fresh admissibility control at `0.95` and then one fresh candidate at only `1.0`; historical anchors `eval_036 control=0.20079853` and `eval_015=0.19974202` | If the promoted `2097152`-bucket legality line is slightly over-sharpened at `EVAL_LOGIT_TEMP=0.95`, then restoring only `EVAL_LOGIT_TEMP=1.0` should improve post-export `val_bpb` while staying in the same runtime band | `EVAL_LOGIT_TEMP=0.95` was validated before the bucket-geometry promotion, and the promoted line now trusts matched n-gram signal more strongly, so scorer temperature is the clean remaining calibration variable on this new operating point | No code edits; planned semantic change was only `EVAL_LOGIT_TEMP: 0.95 -> 1.0` plus operational identifiers (`RUN_ID`, runner log dir, runner run name); actual execution stopped after the fresh `0.95` control because the reviewed admissibility gate failed | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, exact PR809 vectorized n-gram settings, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no retraining, no export rewrite, no helper edits | Fresh control should stay within `0.0002 BPB` and `+15s` external of promoted `eval_035`; if so, the fresh `T=1.0` candidate should then be launched and beat the fresh control by at least `0.0001 BPB` | Identity checks stayed clean before and after the control run. Fresh control scored `legal_ttt_exact val_loss=0.33904036`, `val_bpb=0.20079880`, script `603417ms`, runner `648159ms`, external `648378ms`, any-match `0.98387585`, avg alpha `0.63990743`, histogram `order_2=60166`, `order_3=346841`, `order_4=373090`, `order_5=332096`, `order_6=395043`, `order_7=644544`, `order_8=1634957`, `order_9=57234849`. Fresh-control deltas vs promoted `eval_035` were `-0.00000169 val_loss`, `-0.00000100 BPB`, `+21145ms` script, `+23146ms` runner, and `+23201ms` external. Because the reviewed external gate allowed only `+15000ms`, the control failed admissibility by `8201ms`, so the `EVAL_LOGIT_TEMP=1.0` candidate was not launched. Fresh control vs `eval_036` control was `+0.00000027 BPB` and `+22331ms` external; fresh control vs `eval_015` was `+0.00105678 BPB` | This is a controlled `drift-stop`, not a negative temperature result. Promoted-line semantics remain in-family on BPB and telemetry, but the fresh control was too slow externally for the reviewed pairwise comparison to remain admissible, so `EVAL_LOGIT_TEMP=1.0` is still unanswered on the promoted `2097152`-bucket line | Re-establish a fresh in-gate control on the exact promoted `eval_035` line, then rerun the same temperature-only `0.95 -> 1.0` pair. Do not treat this round as evidence for or against changing the promoted temperature default |
| 2026-03-27 | `eval_036_eval035_ttt_lr_pair` | evaluation | Fresh admissible runner-managed control on the promoted `eval_035` legality line with `TTT_LR=0.0025` vs one fresh runner-managed candidate with only `TTT_LR=0.0020`; historical anchors `eval_035=0.20079980` and `eval_015=0.19974202` | If the promoted `2097152`-bucket legality line is slightly over-adapting at `TTT_LR=0.0025`, then reducing only `TTT_LR` to `0.0020` should improve post-export `val_bpb` while staying in the same runtime band | The live SOTA motif still uses legal score-first TTT at lower LR, and the promoted local line now trusts matched n-gram signal more strongly after the `2097152`-bucket promotion, so a smaller step size could have reduced overshoot without mixing in any other changes | No code edits; changed only `TTT_LR: 0.0025 -> 0.0020` between fresh control and candidate plus operational identifiers (`RUN_ID`, runner log dir, runner run name); candidate launch also added a bash `time` wrapper only to capture outer wallclock | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_ONLY=1`, `EVAL_LOGIT_TEMP=0.95`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `NGRAM_EVAL_ENABLED=1`, `NGRAM_EVAL_BUCKETS=2097152`, exact PR809 vectorized n-gram settings, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no retraining, no export rewrite, no helper edits | Candidate should beat the fresh control by at least `0.0001 BPB`, or at minimum show any quality gain while staying inside the reviewed `+20s` external band | Identity checks stayed clean before and after both arms. Fresh control scored `legal_ttt_exact val_loss=0.33903990`, `val_bpb=0.20079853`, script `582012ms`, runner `626047ms`, external `626047ms` via runner-total proxy with metadata UTC timestamps agreeing at about `626000ms`, any-match `0.98387585`, avg alpha `0.63990797`; admissibility vs promoted `eval_035` was `-0.00000127 BPB` and `+870ms` external, so the gate passed. Fresh candidate scored `legal_ttt_exact val_loss=0.33907413`, `val_bpb=0.20081880`, script `591247ms`, runner `633936ms`, external `634143ms`, any-match `0.98387585`, avg alpha `0.63995303`. Candidate deltas vs fresh control were `+0.00003423 val_loss`, `+0.00002027 BPB`, `+9235ms` script, `+7889ms` runner, and `+8096ms` external. Candidate vs historical `eval_015` was `+0.00107678 BPB` | This is a controlled `hold` result. The promoted `2097152`-bucket legality line still prefers `TTT_LR=0.0025`; lowering to `0.0020` neither improved BPB nor runtime, so downward TTT-LR retuning is now answered negatively on this helper lineage | Hold `TTT_LR=0.0025` fixed as the quality default on the promoted `eval_035` line and move the next refinement round to a different single-variable question rather than spending another immediate round on lower-`TTT_LR` retuning |
| 2026-03-27 | `eval_035_eval031_bucket_geometry_pair` | evaluation | Fresh admissible runner-managed control on the promoted `eval_031` legality line at `EVAL_LOGIT_TEMP=0.95` with `NGRAM_EVAL_BUCKETS=4194304` vs one fresh runner-managed candidate with `NGRAM_EVAL_BUCKETS=2097152`; historical anchors `eval_032=0.29081271` and `eval_015=0.19974202` | If the old bucket-geometry win still transfers on the promoted `T=0.95` legality line, then reducing `NGRAM_EVAL_BUCKETS` from `4194304` to `2097152` should improve post-export `val_bpb` while keeping runtime comparable or slightly better | `eval_015` was the strongest unresolved same-family quality signal, and no round had yet re-asked that exact cache-geometry question on the promoted helper with `EVAL_LOGIT_TEMP=0.95`, scorer-batch lookup, batched torch stats, and vectorized postlookup all fixed | No code edits; changed only `NGRAM_EVAL_BUCKETS: 4194304 -> 2097152` between fresh control and candidate plus operational identifiers (`RUN_ID`, runner log dir, runner run name) | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_LOGIT_TEMP=0.95`, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, exact PR809 vectorized n-gram settings, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no retraining, no export rewrite, no helper edits | Candidate should beat the fresh control by at least `0.0001 BPB`, or at minimum show any quality gain with runtime still inside the reviewed `+20s` band | Identity checks stayed clean before and after both arms. Fresh control scored `legal_ttt_exact val_loss=0.49102809`, `val_bpb=0.29081449`, script `591126ms`, runner `634475ms`, external `634633ms`, any-match `0.98387524`, avg alpha `0.62766972`; admissibility vs `eval_033` was only `+0.00000069 BPB` and `+6494ms` external, so the gate passed. Fresh candidate scored `legal_ttt_exact val_loss=0.33904205`, `val_bpb=0.20079980`, script `582272ms`, runner `625013ms`, external `625177ms`, any-match `0.98387585`, avg alpha `0.63990334`. Candidate deltas vs fresh control were `-0.15198604 val_loss`, `-0.09001469 BPB`, `-8854ms` script, `-9462ms` runner, and `-9456ms` external. Candidate vs promoted `eval_032` was `-0.09001291 BPB`, and candidate vs historical `eval_015` was `+0.00105778 BPB` with much faster runtime | This is a controlled `promote` result. The smaller-bucket geometry transfers extremely strongly to the promoted `T=0.95` legality line and should replace `4194304` as the quality default on that line; `eval_015` remains the absolute best same-family bucket result, but the refreshed promoted-line transfer is now very close | Hold `NGRAM_EVAL_BUCKETS=2097152` fixed as the new promoted default on the `eval_031` lineage and move the next refinement round to a different single-variable question on top of this stronger baseline |
| 2026-03-27 | `eval_034_eval031_ttt_epochs3_candidate` | evaluation | Fresh admissible `eval_033` runner control on the promoted `eval_031` legality line at `EVAL_LOGIT_TEMP=0.95` with `TTT_EPOCHS=4` vs one fresh runner-managed candidate with `TTT_EPOCHS=3`; historical promoted anchor `eval_032=0.29081271` | If the fourth TTT epoch is unnecessary or mildly over-adapts each chunk on the promoted `T=0.95` PR809 legality line, then reducing `TTT_EPOCHS` from `4` to `3` should preserve or improve post-export `val_bpb` while reducing eval runtime | Current leaderboard `#1` uses legal score-first TTT with `3` epochs, while the promoted local line still used `4`; temperature calibration and launcher-path variation were already closed, making epoch count the clean remaining evaluation-side mismatch | No code edits; changed only `TTT_EPOCHS: 4 -> 3` plus operational identifiers (`RUN_ID`, runner log dir, runner run name) | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_LOGIT_TEMP=0.95`, `TTT_LR=0.0025`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, exact PR809 vectorized n-gram settings, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, runner path, no retraining, no export rewrite, no helper edits | Improve on fresh `eval_033` by at least `0.0001 BPB`, or at minimum stay within `±0.0001` while saving at least `20s` wallclock | Identity checks stayed clean before and after the run. The fresh candidate scored `legal_ttt_exact val_loss=0.49109611`, `val_bpb=0.29085478`, script `514515ms`, runner `558261ms`, external `558444ms`. Versus fresh `eval_033` control, deltas were `+0.00006919 val_loss`, `+0.00004098 BPB`, `-70937ms` script, `-69707ms` runner, and `-69695ms` external. Versus promoted `eval_032`, the candidate was `+0.00004207 BPB`, `-70361ms` script, `-68921ms` runner, and `-68977ms` external | This is a controlled `runtime-only operational win`. The fourth TTT epoch still buys a small but real BPB gain on the promoted line, so `TTT_EPOCHS=4` should remain the quality default; however, `TTT_EPOCHS=3` is a real speed win that stays well within the reviewed `±0.0001` BPB tolerance | Close TTT epoch count on this line as quality-default `4` vs runtime-optimized `3`, and move the next refinement round to a genuinely different single-variable question rather than another nearby epoch rerun |
| 2026-03-27 | `eval_033_eval031_direct_launch_pair` | evaluation | Fresh runner control vs fresh direct-launch candidate on the locked promoted `eval_031` legality line at `EVAL_LOGIT_TEMP=0.95`; admissibility anchor `eval_032=0.29081271` with external `627421ms` | If materially remaining runtime overhead on the promoted line still sits in the outer launcher path, then bypassing `tools/gpu_experiment_runner.py` should preserve BPB while reducing external wallclock by at least `5s`, ideally `10s` | `eval_028` showed helper-local process time could fit budget, `eval_029` answered runner-internal trimming negatively, and `eval_030` never reached the direct-launch candidate because its fresh control drifted; that left launcher bypass as the clean remaining launcher-path variable on the promoted line | No code edits; changed only launch path (`runner` vs `direct launch`) plus operational identifiers (`RUN_ID`, runner log dir, runner run name, direct-launch capture dir) | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, `EVAL_LOGIT_TEMP=0.95`, exact legal TTT settings, exact PR809 vectorized n-gram settings, tokenizer/data, stride `64`, `physicslm`, cwd, pinned GPUs `0..7`, no retraining, no export rewrite, no helper edits | Fresh control should stay within `0.0002 BPB` and `+15s` external of `eval_032`; direct launch should preserve BPB within `±0.0001` while saving at least `5s` externally if launcher overhead remains material | Identity checks stayed clean before and after both runs. Fresh runner control scored `legal_ttt_exact val_loss=0.49102692`, `val_bpb=0.29081380`, script `585452ms`, runner `627968ms`, external `628139ms`; drift vs `eval_032` was only `+0.00000109 BPB` and `+718ms`, so the gate passed. Fresh direct-launch candidate reused the same wrapped child command, same cwd, same `physicslm`, and same `CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7`, differing only at `RUN_ID`, and scored `legal_ttt_exact val_loss=0.49102993`, `val_bpb=0.29081558`, script `583370ms`, external `625537ms`. Candidate deltas vs fresh control were `+0.00000178 BPB`, `-2082ms` script, and only `-2602ms` external; vs `eval_032` anchor the candidate was `+0.00000287 BPB` and `-1884ms` external | This is a controlled negative result on launcher bypass for the promoted `eval_031` `T=0.95` line. BPB stayed comparable, but the external runtime saving was below the reviewed `3s` threshold, so direct launch should not be promoted as the new legality/runtime baseline on this line | Hold promoted `T=0.95` fixed and stop spending immediate rounds on runner-vs-direct-launch path changes for this line; move to a genuinely different single-variable refinement question |
| 2026-03-27 | `eval_032_eval031_temperature_confirmation_pair` | evaluation | Fresh same-helper official control `T=1.0` vs fresh same-helper official candidate `T=0.95` on the locked `eval_031` legality line; historical anchors `eval_027=0.29117839`, `eval_030=0.29117961`, prior `eval_031=0.29081485` | If the `eval_031` `T=0.95` gain is real rather than a one-off selected run, then a fresh same-helper full official candidate at `T=0.95` should again beat a fresh same-helper full official control at `T=1.0` by at least `0.0002 BPB` | `eval_031` already gave one fixed-slice selector win and one official candidate win; the only unresolved variable was fresh official repeatability on the exact same helper/checkpoint/artifact/runner line | No code edits; changed only `EVAL_LOGIT_TEMP` between `1.0` and `0.95`, plus `RUN_ID`, log dir, and run name required for separate launches | Exact helper `125663 / 2dea839e...`, exact checkpoint `106178569 / b8291ad1...`, exact artifact `15555121 / eb062c96...`, exact PR809 vectorized n-gram settings, exact legal TTT settings, tokenizer/data, stride `64`, runner path, `physicslm`, pinned GPUs `0..7`, no retraining, no export rewrite, no other calibration change | Fresh control should stay within `0.0002` of `eval_027` and `eval_030`, and fresh `T=0.95` should beat fresh `T=1.0` by at least `0.0002 BPB` if the prior gain is stable | Identity checks stayed clean before and after both runs. Fresh control `T=1.0` scored `legal_ttt_exact val_loss=0.49164677`, `val_bpb=0.29118091`, script `591399ms`, runner `633253ms`, external `633409ms`. Fresh candidate `T=0.95` scored `legal_ttt_exact val_loss=0.49102508`, `val_bpb=0.29081271`, script `584876ms`, runner `627182ms`, external `627421ms`. Fresh candidate beat fresh control by `-0.00036820 BPB` and `-0.00062169 val_loss`, beat prior `eval_031` by `-0.00000214 BPB`, and the fresh control drifted only `+0.00000252` vs `eval_027` and `+0.00000130` vs `eval_030`; runtime deltas vs `eval_027` were `+17197ms` script / `+18253ms` runner for fresh control and `+10674ms` script / `+12182ms` runner for fresh candidate | This is a clean confirmatory promote result. `EVAL_LOGIT_TEMP=0.95` is stable enough to promote as the default on the locked `eval_031` legality line, and nearby scalar-temperature tuning on this line should now be considered closed | Hold promoted `T=0.95` fixed and move to a genuinely different single-variable refinement question; if launcher-path runtime work returns, first obtain a fresh admissible control and do not reuse this temperature-confirmation round as launcher evidence |
| 2026-03-27 | `eval_031_eval027_global_temperature_calibration` | evaluation | Locked official baseline `eval_027=0.29117839` with script eval `574202ms` and managed `615s`; fresh stability anchor `eval_030=0.29117961`; calibration-slice parity anchor was the locked `eval_027` same-helper held-out control `1.20586072` on the frozen `eval_009` slice | If the locked `eval_027` PR809 legality line is still slightly miscalibrated on the neural side after legal TTT and vectorized n-gram mixing, then one global scoring temperature applied only to neural logits should improve official post-export `val_bpb` without changing training, export, or cache behavior | Global temperature already failed on the plain legal-TTT line, but `eval_027` adds the PR809 neural-plus-n-gram scoring path, so calibration may differ; this stays strictly in the evaluation scorer and changes no architecture, optimization, export, or cache-update mechanism | Copied the exact `eval_027` helper into fresh `eval_031`; added default-off `EVAL_LOGIT_TEMP`; applied it only to neural scoring logits before `log_softmax` / `softmax` in the PR809 scorer; reused the frozen non-official `eval_009` held-out slice and swept `T={0.95,0.975,1.00,1.025,1.05}`; then ran exactly one full official candidate at selected `T=0.95` through `tools/gpu_experiment_runner.py` | Exact checkpoint/artifact lineage (`final_model.pt` `106178569 / b8291ad1...`, `final_model.int6.ptz` `15555121 / eb062c96...`), exact PR809 vectorized chunked n-gram settings, exact legal TTT settings, tokenizer/data format, stride `64`, `physicslm`, pinned GPUs `0..7`, no retraining, no export rewrite, no n-gram alpha/backoff changes, no runner-path changes | `T=1.0` should preserve held-out parity; one non-`1.0` temperature should beat `T=1.0` on the fixed slice by at least `0.0002`; the selected temperature should then beat official `eval_027` while staying in the same runtime band | The copied helper changed from `125178 / bbfe961c...` to `125663 / 2dea839e...`; checkpoint and artifact bytes/hashes stayed unchanged before and after all runs. Reused held-out slice identity: `fineweb_train_000000.bin`, offset `8388608`, copied `2097153`, scored `2097152`, sha `8b5e92cc...`. Held-out results were `0.95 -> 1.20029274`, `0.975 -> 1.20275434`, `1.00 -> 1.20587508`, `1.025 -> 1.20954568`, `1.05 -> 1.21377823`; `T=1.0` stayed within `+0.00001436` of the locked same-helper held-out control, and `T=0.95` beat `T=1.0` by `0.00558234`, so the gate passed. The single official candidate at `T=0.95` scored `legal_ttt_exact val_loss=0.49102869`, `val_bpb=0.29081485`, with script eval wallclock `584865ms`, managed wallclock `628844ms`, and external wallclock `629.130s`; deltas were `-0.00036354` vs `eval_027` and `-0.00036476` vs fresh `eval_030`, while runtime moved to `+10663ms` script and `+13844ms` managed vs `eval_027` | This is a controlled positive calibration result on the locked PR809 legality line, not selector overfit: the held-out-selected `T=0.95` transferred cleanly to full validation. The primary success criterion `val_bpb < 0.29117839` was met, but the more ambitious `>=0.0005` improvement target was missed, so this is a real but moderate win rather than a decisive leap | Keep `eval_031` as the new active single-seed legality baseline; if more confidence is needed, run one fresh official confirmation of `T=0.95` against `T=1.0` on the same locked artifact before spending another round on nearby scalar-temperature sweeps |
| 2026-03-27 | `eval_030_eval027_direct_launcher_ablation` | evaluation | Primary historical anchor was `eval_027` at `val_bpb=0.29117839`, script eval wallclock `574202ms`, managed wallclock `615s`; nearest fresh-control context was `eval_029` control at `val_bpb=0.29117992`, script eval wallclock `582051ms`, managed wallclock `624825ms`; reviewed control gate required fresh-control `val_bpb` within `±0.0001` of historical `eval_027` and managed wallclock no worse than `+10s` vs historical `eval_027` | If a material share of the remaining managed overrun comes from the outer launcher path rather than the child eval itself, then bypassing `tools/gpu_experiment_runner.py` with a direct launch of the exact locked child command should reduce end-to-end wallclock by at least `10s` while preserving `val_bpb` within noise | `eval_027` already made the script path legal, `eval_028` showed helper-local total process time can fit under `600s`, and `eval_029` answered nearby runner-internal trimming negatively, leaving a genuinely different direct launcher path as the last unresolved repo-adjacent runtime lever | No repo code changes; changed only the planned outer launcher path. Recovered the exact locked `eval_027` child command and helper/artifact lineage from prior metadata; ran one fresh control through the default `tools/gpu_experiment_runner.py` path with a fresh shared `RUN_ID` and external top-level timing; planned but did not run the direct-launch candidate after the reviewed gate failed | Exact `eval_027` helper bytes `125178 / bbfe961c...`, exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `legal_ttt_exact` child command semantics, tokenizer/dataset, stride `64`, exact n-gram and TTT settings from `eval_027`, shim-free `physicslm`, exact `8x NVIDIA L20Z` shape, same shared `RUN_ID`, no retraining, no export rewrite, no scorer-side or helper-side edits | Fresh control should pass the admissibility gate, then the direct-launch candidate should preserve `val_bpb` while reducing outer wallclock by at least `10s`, ideally to `<=600s` | Identity checks stayed clean before and after launch. The fresh control used GPUs `0,1,2,3,4,5,6,7` and scored `legal_ttt_exact val_loss=0.49164458`, `val_bpb=0.29117961`, with script eval wallclock `583199ms`, runner managed wallclock `626554ms`, and external top-level wallclock `626721ms`. Drift vs historical `eval_027` was `+0.00000122 val_bpb` and `+11554ms` managed, so the BPB gate passed but the managed-wallclock gate failed by `1554ms`. Per the reviewed brief, the direct-launch candidate was not run and the round stopped as drift rather than launcher evidence | This is a controlled drift stop, not a negative result on direct launch. The direct-launcher hypothesis remains unanswered because the comparison never became admissible under the reviewed gate. `eval_027` remains the active legality baseline | If launcher-path work is revisited, first obtain a fresh in-gate control on the exact locked `eval_027` line and only then rerun the direct-launch candidate on the same pinned GPUs; if that cannot be done reliably, deprioritize further runtime-legality work rather than treating this drift-stopped round as evidence |
| 2026-03-27 | `eval_029_eval027_runner_minpath_seed1337` | evaluation | Fresh control baseline was the unchanged `eval_027_arch010_pr809_chunk_ngram_ttt_vectorized_postlookup_seed1337` line rerun through the current default runner; historical anchors were `eval_027` at `val_bpb=0.29117839`, script eval wallclock `574202ms`, managed wallclock `615s`, and `eval_028` at `val_bpb=0.29118133`, helper `process_total_ms=594842`, inferred runner-managed outside-helper overhead `21158ms`, managed wallclock `616s`; official target `0.4416`; legality-pending reference `0.2952` | If the remaining managed overrun is mainly runner-side setup, teardown, and wrapper bookkeeping outside the helper, then a default-off minimal runner mode that preserves identical child execution should reduce managed wallclock to `<=600s` while preserving `val_bpb` within `±0.0001` of fresh control and historical `eval_027` | `eval_027` already proved the exact script path is legal, and `eval_028` showed helper-local total process time can already fit under budget, so the clean remaining repo-controlled refinement question was whether the runner itself still held meaningful removable overhead outside the helper | Edited only `tools/gpu_experiment_runner.py`; added one default-off `--minimal-runner` switch; added runner timing fields `runner_start_to_child_spawn_ms`, `child_runtime_ms`, `child_exit_to_runner_exit_ms`, and `runner_total_ms` in both arms; minimal mode skipped only console mirroring and extra pre-query bookkeeping; ran one fresh official control through the default runner path, then one corrected official candidate with the exact same child command and changed only the runner switch. One initial minimal-mode draft candidate with a different `RUN_ID` was discarded because the child command and helper log destination were not strictly identical | Exact `eval_027` helper bytes `125178 / bbfe961c...`, exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact official `legal_ttt_exact` child command, tokenizer/dataset, stride `64`, exact n-gram and TTT settings from `eval_027`, exact `physicslm` environment, exact `8x NVIDIA L20Z` launch shape, no retraining, no export rewrite, no scorer-side or helper-side code edits | Candidate managed wallclock `<=600s`, `val_bpb` within `±0.0001` of fresh control and historical `eval_027`, and at least a meaningful reduction in runner-managed overhead on the real path | Identity checks stayed clean: helper and artifact hashes/bytes matched exactly. The fresh control scored `legal_ttt_exact val_loss=0.49164510`, `val_bpb=0.29117992`, with `582051ms` script wallclock and `624825ms` managed; control drift vs historical `eval_027` was `+0.00000153 val_bpb` and `+9825ms` managed, so the reviewed gate passed. The corrected final candidate used the exact same child command as control and scored `legal_ttt_exact val_loss=0.49164536`, `val_bpb=0.29118007`, with `583407ms` script wallclock and `625998ms` managed. Runner splits were control `427 / 624397 / 0 / 624825ms` and candidate `180 / 625818 / 0 / 625998ms`, so deltas were `+0.00000015 val_bpb`, `+1356ms` script eval, `+1173ms` managed, `-247ms` start-to-spawn, and `+1421ms` child runtime. The exact `eval_027` helper intentionally remained untouched, so fresh-arm `process_total_ms` was unavailable; the nearest helper-timed historical anchor remained `eval_028` with inferred outside-helper residual `21158ms` | This is a controlled negative refinement result for the repo-controlled runner-path hypothesis. The minimal runner preserved quality and exact child-command identity, but it did not reduce managed wallclock and in fact finished slightly slower. `eval_027` remains the active legality baseline. Classification: `runner-trim-negative / likely-platform-overhead` | Keep `eval_027` as the active legality baseline; do not spend another immediate round on a nearby repo-controlled runner micro-trim unless a genuinely different launcher path or platform-side lever becomes available |
| 2026-03-27 | `eval_028_arch010_pr809_chunk_ngram_ttt_official_eval_only_seed1337` | evaluation | Primary runtime baseline `eval_027_arch010_pr809_chunk_ngram_ttt_vectorized_postlookup_seed1337` at `val_bpb=0.29117839`, script eval wallclock `574202ms`, managed wallclock `615s`, managed minus script overhead `40.798s`; official `0.4416`; legality-pending reference `0.2952` | If the remaining `15s` managed overrun after `eval_027` is mostly outside the locked scorer path, then an official-eval-only helper fast path should reduce managed wallclock to `<=600s` while keeping `val_bpb` within `±0.0001` of `eval_027` and leaving script eval roughly unchanged | `eval_027` already solved script legality, so the clean remaining refinement question was whether inherited helper orchestration rather than the scorer hot path was still responsible for the managed miss | Copied the exact `eval_027` helper into a fresh `eval_028` run dir; added one default-off env control `NGRAM_EVAL_OFFICIAL_ONLY`; changed only top-level `main()` / eval-only orchestration so the enabled path suppresses inherited code-dump, `nvidia-smi`, and train-shard logs, runs only the saved-artifact official eval, and emits coarse helper phase timings; ran one full official candidate via `tools/gpu_experiment_runner.py` in `physicslm` on `8x NVIDIA L20Z` | Exact `eval_027` scorer code and n-gram mechanism, exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, exact `NGRAM_EVAL_BATCH_TORCH_STATS=1`, exact `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT settings, tokenizer/dataset, no retraining, no export rewrite, no scorer-side algorithm change | Managed wallclock `<=600s`, full official `val_bpb` within `±0.0001` of `eval_027`, script eval no worse than `eval_027` by more than `5000ms`, and managed minus script overhead `<=26s` | The copied source helper from `eval_027` was `125178` bytes / `bbfe961c...`; the new `eval_028` helper was `126904` bytes / `e1e55335...`; artifact identities stayed unchanged. The full official candidate scored `legal_ttt_exact val_loss=0.49164748`, `val_bpb=0.29118133`, with `579356ms` script wallclock and `616s` managed; helper-local phase timings were `process_to_official_eval_start_ms=14940`, `official_eval_duration_ms=579356`, `official_eval_end_to_process_exit_ms=547`, `process_total_ms=594842`; inferred runner-managed overhead outside the helper was about `21158ms`; deltas vs `eval_027` were `+0.00000294` `val_bpb`, `+5154ms` script, `+1s` managed, and `-4.154s` managed minus script overhead; reused scorer telemetry stayed in family with `ngram_ctx_key_precompute elapsed_ms=20645`, `ngram_batch_lookup_elapsed_ms=20226`, `ngram_torch_stats_elapsed_ms=3297`, `ngram_postlookup_vectorized_elapsed_ms=1171231`, `ngram_update_batch_timing elapsed_ms=32505`, any-match `0.98387524`, and avg alpha `0.64246944`; total bytes were `15682025` | This is a controlled negative refinement result. BPB stayed locked, and helper-local total process time fell under `600s`, but managed wallclock did not improve on the real path. The remaining miss now localizes more to runner-managed overhead outside the helper than to helper-local pre/post work. Classification: `helper-trim-negative / localizes-runner-overhead` | Keep `eval_027` as the active legality baseline; if managed legality still matters, target runner-managed overhead outside the helper rather than another scorer-side or helper-orchestration trim |
| 2026-03-27 | `eval_027_arch010_pr809_chunk_ngram_ttt_vectorized_postlookup_seed1337` | evaluation | Primary runtime baseline `eval_026_arch010_pr809_chunk_ngram_ttt_batch_torch_stats_seed1337` at `val_bpb=0.29117999`, script eval wallclock `606325ms`, managed wallclock `647s`; required disabled parity reference `1.11934940`; same-helper held-out control for this round `1.20586072`; official `0.4416` | If the remaining `6325ms` legality miss after `eval_026` is dominated by Python-side post-lookup scorer bookkeeping, then vectorizing the remaining scorer-side matched-order, alpha, probability-mixing, byte-accounting, and histogram work across the scorer batch should recover the missing runtime without materially changing BPB | `eval_025` already removed most outer lookup-call overhead and `eval_026` already removed repeated per-row exact neural-stat extraction, leaving the Python row loop after lookup plus torch-stats as the clean residual scorer-side cost center | Copied the exact `eval_026` helper into a fresh `eval_027` run dir; added one default-off env control `NGRAM_EVAL_VECTORIZE_POSTLOOKUP`; changed only `score_segments()` so the switch-off path preserves exact `eval_026` behavior, while the switch-on path vectorizes the remaining post-lookup matched-order, alpha, probability mixing, byte accounting, and histogram updates across a flattened scorer batch; added only the requested `ngram_postlookup_vectorized_*` telemetry; ran one disabled parity eval, one enabled same-helper held-out control with `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=0`, one enabled held-out candidate screen with `NGRAM_EVAL_VECTORIZE_POSTLOOKUP=1`, and one enabled full official candidate eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_026` helper outside the new scorer switch, exact `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1`, exact `NGRAM_EVAL_BATCH_TORCH_STATS=1`, exact `NgramEvalCache` lookup/update semantics, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT settings, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite, no order/bucket/chunk/backend/lookup-policy/scorer-batch-size sweep | Disabled parity within the locked band, enabled held-out candidate within `±0.0001` of the same-helper control, full official `val_bpb` within `±0.0005` of `eval_026`, and script eval wallclock `<= 600000ms` | The copied source helper from `eval_026` was `119346` bytes / `62c19517...`; the new `eval_027` helper stayed fixed before/after launch at `125178` bytes / `bbfe961c...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11934901`, only `-0.00000039` vs the locked `eval_026` parity reference, with `468349ms` script wallclock and `511s` managed. The enabled same-helper held-out control scored `1.20586072`, with `ngram_batch_lookup_call_count=1040`, `ngram_batch_lookup_elapsed_ms=1769`, `ngram_torch_stats_batches=1040`, `ngram_torch_stats_elapsed_ms=469`, `ngram_postlookup_vectorized_elapsed_ms=0`, and `ngram_update_batch_timing calls=3 elapsed_ms=1139`. The enabled held-out candidate scored `1.20586294`, only `+0.00000222` vs the same-helper control, with `ngram_batch_lookup_call_count=1040`, `ngram_batch_lookup_elapsed_ms=1866`, `ngram_torch_stats_batches=1040`, `ngram_torch_stats_elapsed_ms=480`, `ngram_postlookup_vectorized_batches=1040`, `ngram_postlookup_vectorized_positions_total=2097152`, `ngram_postlookup_vectorized_elapsed_ms=40753`, and `ngram_update_batch_timing calls=3 elapsed_ms=1157`, so the reviewed stop gate did not trigger. Enabled full-val scored `legal_ttt_exact val_loss=0.49164251`, `val_bpb=0.29117839`, with `574202ms` script wallclock and `615s` managed; full-val telemetry was `ngram_ctx_key_precompute resident_bytes=1984692256 elapsed_ms=20904`, `ngram_batch_lookup_call_count=30770`, `ngram_batch_lookup_elapsed_ms=20336`, `ngram_batch_lookup_positions_total=62021632`, `ngram_batch_lookup_max_positions_per_call=4032`, `ngram_torch_stats_batches=30770`, `ngram_torch_stats_elapsed_ms=3223`, `ngram_torch_stats_scored_positions_total=62021632`, `ngram_postlookup_vectorized_batches=30770`, `ngram_postlookup_vectorized_positions_total=62021632`, `ngram_postlookup_vectorized_elapsed_ms=1177650`, `ngram_update_batch_timing calls=63 elapsed_ms=32130`, any-match `0.98387524`, avg alpha `0.64247077`, and the same matched-order histogram as `eval_026`; deltas were `-0.00000160` and `-32123ms` vs `eval_026`, and `-0.15042161` vs official `0.4416`; total bytes were `15680299` | This is a controlled positive legality-recovery result. The controls passed, BPB stayed effectively locked, and the full official script eval became legal with `25798ms` of headroom. Managed wallclock still sits `15s` above `600s`, but the locked evaluator line is no longer blocked on script legality. Classification: `legal-script-runtime / promote` | Promote `eval_027` as the new active legality baseline; if more runtime headroom is still useful, target a different non-overlapping cost center such as `update_batch()` or managed-path overhead rather than scorer-side post-lookup work again |
| 2026-03-26 | `eval_025_arch010_pr809_chunk_ngram_ttt_batch_lookup_by_batch_seed1337` | evaluation | Primary baseline `eval_018_arch010_pr809_chunk_ngram_ttt_sparse_updates_seed1337` at `val_bpb=0.29117845`, script eval wallclock `630501ms`, managed wallclock `671s`; lookup-family context `eval_021=0.29117893`, script `641669ms`, managed `682s`, `batch_lookup timing 969057 calls / 111576ms`; newest nearby runtime probe `eval_024=0.29117973`, script `637282ms`, managed `679s`; required same-slice control baseline `1.20585497`; official `0.4416` | If the remaining legality miss after `eval_018` is materially driven by scorer-side `batch_lookup()` outer-call overhead rather than table math or update cost, then reducing lookup invocation frequency from per-segment to per-scorer-batch should improve end-to-end runtime without materially changing BPB | `eval_021` showed `batch_lookup()` still consumed `111576ms` across `969057` calls after the scratch rewrite failed, so changing only invocation granularity from the scorer was a clean non-redundant refinement-phase test on the validated PR `#809` algorithmic shape | Copied the exact `eval_018` helper into a fresh `eval_025` run dir; added one default-off env control `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH`; changed only `score_segments()` so the batching-off path preserves the exact one-call-per-row behavior, while the batching-on path flattens the current scorer batch into one lookup workload, calls `cache.batch_lookup()` once, and scatters the results back into the existing accounting; left `NgramEvalCache.update_batch()` untouched; added only the requested lookup telemetry; ran one disabled parity eval, one enabled same-helper held-out batch-off control, one enabled held-out batch-on screen, and one enabled full official batch-on eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_018` helper outside the scorer batching change, exact `NgramEvalCache` lookup/update semantics, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT settings, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite, no order/bucket/chunk/backend/lookup-policy sweep | Disabled parity within the locked band, enabled same-slice batch-off control within `+-0.0001` of the locked baseline, enabled full-val within `+-0.0005` of `eval_018`, and runtime legal under `600000ms` script / `600s` managed with materially fewer lookup calls | The copied source helper from `eval_018` was `111179` bytes / `7471e15d...`; the new `eval_025` helper stayed fixed before/after launch at `116390` bytes / `2d32bdbf...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11934793`, only `-0.00000384` vs the locked `eval_018` disabled baseline `1.11935177`, with `467990ms` script wallclock and `509s` managed. The enabled same-helper batch-off control scored `1.20582638`, only `-0.00002859` vs the locked `eval_018` same-slice baseline `1.20585497`, with `ngram_batch_lookup_call_count=32737`, `ngram_batch_lookup_elapsed_ms=5976`, `ngram_batch_lookup_positions_total=2097152`, `ngram_batch_lookup_max_positions_per_call=2048`, and `ngram_update_batch_timing calls=3 elapsed_ms=1178`. The enabled batch-on held-out screen scored `1.20584885`, only `+0.00002247` vs the same-helper control, with `ngram_batch_lookup_call_count=1040`, `ngram_batch_lookup_elapsed_ms=1977`, `ngram_batch_lookup_positions_total=2097152`, and `ngram_batch_lookup_max_positions_per_call=4032`, so the reviewed stop gate did not trigger. Enabled full-val scored `legal_ttt_exact val_loss=0.49164495`, `val_bpb=0.29117983`, with `614228ms` script wallclock and `657s` managed; full-val telemetry was `ngram_ctx_key_precompute resident_bytes=1984692256 elapsed_ms=21285`, `ngram_batch_lookup_call_count=30770`, `ngram_batch_lookup_elapsed_ms=20386`, `ngram_batch_lookup_positions_total=62021632`, `ngram_batch_lookup_max_positions_per_call=4032`, `ngram_update_batch_timing calls=63 elapsed_ms=32269`, any-match `0.98387524`, avg alpha `0.64246741`, and the same matched-order histogram as `eval_018`; deltas were `+0.00000138` and `-16273ms` vs `eval_018`, `+0.00000090` and `-27441ms` vs `eval_021`, `+0.00000010` and `-23054ms` vs `eval_024`, and `-0.15042017` vs official `0.4416`; total bytes were `15671511` | This is a controlled positive runtime probe but not full legality recovery. The controls passed and full-val BPB stayed effectively locked, while scorer-batch lookup batching cut full lookup calls by `938287` and cumulative lookup elapsed by `91190ms` versus the closest full-val lookup-family context. End-to-end runtime improved materially, but script eval still missed the `600000ms` budget by `14228ms` and managed wallclock still missed `600s` by `57s`. Classification: `runtime-improved-but-still-illegal` | Promote `eval_025` to the active legality-recovery baseline and test a different non-overlapping runtime lever on top of this batched-scorer line rather than abandoning outer scorer granularity |
| 2026-03-23 | `eval_001_sliding_window_lockin` | evaluation | Root `train_gpt.py` non-overlapping eval vs sliding-window eval on the same weights | Sliding-window eval should materially improve measured post-export `val_bpb` and should become the fixed local evaluation protocol | Published records show large gains from richer left context during scoring; current root script still appears baseline-like | Add sliding eval function, stride setting, and comparison reporting only | Architecture, training hyperparameters, export path, dataset, tokenizer, checkpoint weights | Large `val_bpb` gain with acceptable eval time | Planned; not run yet | Highest-confidence local gap to close before export or architecture search | Implement and validate sliding-window evaluation in the root script |
| 2026-03-23 | `eval_001_sliding_window_lockin_smoke` | evaluation | Same tiny synthetic checkpoint and same int8+zlib artifact scored with `non_overlapping` vs `sliding_window` in updated root `train_gpt.py` | If the new evaluation selector is wired correctly, both modes should run on the same weights and exported bytes and produce sane, directly comparable metrics | The implementation adds a separate sliding-window token-accounting path while leaving export and model state untouched | `EVAL_MODE=both`; `EVAL_STRIDE=64`; tiny synthetic tokenizer/shards; smoke launcher bypassed `torch.compile` only to avoid local env issues | Weights, exported artifact bytes, architecture, export path, tokenizer within the fixture, dataset split within the fixture, metric definition | Successful execution of both modes pre-export and post-export; synthetic delta may be small or zero | Pre-export: `3.96925568 -> 3.96925571` `val_bpb` (`+0.00000003`) with runtime `467ms -> 168ms`; post-export: `3.96925568 -> 3.96925571` (`+0.00000003`) with runtime `68ms -> 160ms`; real FineWeb comparison blocked because this workspace lacks the intended dataset/tokenizer assets | Implementation is working and preserves apples-to-apples comparison on the same weights/artifact, but the smoke fixture is not informative about the real hypothesis; no protocol lock-in decision should be made from it | Re-run the same comparison on the actual FineWeb validation assets and a real checkpoint/artifact, then decide whether to lock sliding-window as the local standard |
| 2026-03-23 | `eval_001_sliding_window_lockin_followup_asset_audit` | evaluation | Reviewed-brief real-asset comparison on one existing checkpoint and one matching exported artifact under `non_overlapping` vs `sliding_window stride=64` | If the repo already contains a reusable real checkpoint/artifact pair, the real FineWeb evaluation delta can be measured without changing training or export | The local evaluation script, tokenizer, and FineWeb validation shard are already present; only model artifacts are needed for a clean evaluation-only test | Narrowed scope to asset discovery only after confirming the reviewed brief required an existing checkpoint/artifact pair | Evaluation code, tokenizer, validation shard, metric path, stride target, and no fresh training/export generation | Either locate a real reusable checkpoint/artifact pair and run the comparison, or prove the assets are unavailable | Found root `train_gpt.py`, tokenizer `/newcpfs/lxh/parameter-golf/data/tokenizers/fineweb_1024_bpe.model`, and validation shard `/newcpfs/lxh/parameter-golf/data/datasets/fineweb10B_sp1024/fineweb_val_000000.bin`; found no preexisting real `final_model.pt`, no matching exported artifact such as `final_model.int8.ptz`, and no other checkpoint files under `/newcpfs/lxh/parameter-golf` or the research workspace except the synthetic smoke fixture in `tmp/eval_001_smoke` | The reviewed experiment is blocked by missing reusable real model artifacts; running a fresh training job would change the intervention from evaluation-only to retraining-plus-evaluation and would no longer answer the brief as written | Recover a saved real checkpoint/export pair from prior run storage, or explicitly approve a fresh controlled retrain that generates one pair before repeating the comparison |
| 2026-03-23 | `eval_001_asset_recovery_expanded_storage_audit` | infra | Baseline blocker state after the prior workspace audit vs one more bounded search across additional accessible prior-run storage on this machine | If a reusable real checkpoint/export pair exists outside the repo workspace, a bounded storage audit should recover it and unblock the real FineWeb evaluation-only comparison without retraining | The evaluation code, tokenizer, and validation shard are already confirmed; only prior-run storage discovery could satisfy the brief without changing the intervention | Storage search scope only: `/newcpfs/lxh/physicslm_backups`, `/newcpfs/lxh/.cache`, `/newcpfs/lxh/.codex/tmp`, `/newcpfs/lxh/.qwen/tmp`, and a bounded filename sweep across the remaining non-`parameter-golf` roots under `/newcpfs/lxh` | Evaluation script, tokenizer path, FineWeb validation shard path, metric definition, export expectations, no training, no export, no code changes | Either recover one real matching pair with exact provenance or produce a definitive negative result for all additionally accessible storage | No candidate `final_model.pt`, exported `.ptz`, or other plausible checkpoint/export pair was found in any newly searched location; the only experiment-like backup was `/newcpfs/lxh/physicslm_backups/hardtok_analysis_20260306_040545`, whose metadata points to `/tmp/physicslm_hardtok_analysis` and whose `landscape_probe.json` references `/tmp/physicslm_hardtok_demo/checkpoints/0000000050`, not accessible persistent storage for this repo | The evaluation-only brief remains blocked after expanding the search beyond the repo workspace; another recovery-only round would be redundant unless a genuinely new storage location is identified | Explicitly authorize a fresh controlled retrain-plus-evaluation round if the goal is still to measure the local `non_overlapping` vs `sliding_window stride=64` delta |
| 2026-03-24 | `eval_001_retrain_then_compare_real_pair` | evaluation | Fresh real baseline checkpoint/export pair from one baseline `train_gpt.py` run; compare `non_overlapping` vs `sliding_window stride=64` on that same checkpoint and same exported artifact | On the same real checkpoint and matching exported artifact, `sliding_window stride=64` should materially reduce measured pre-export and post-export `val_bpb` enough to justify adopting it as the local standard | Sliding-window scoring restores left context that `non_overlapping` scoring discards; the synthetic smoke already validated correctness, so the remaining question was the real FineWeb effect size | Fresh baseline asset generation only; evaluation mode `non_overlapping` vs `sliding_window`; `EVAL_STRIDE=64`; operationally narrowed execution from 1 GPU to the same baseline recipe on 8 GPUs because full real-asset dual-mode eval was impractical on one local L20Z | Code version, tokenizer, validation shard, architecture, sequence length, training hyperparameters, checkpoint weights, exported artifact bytes, quantization/export path, metric definition | Produce one real checkpoint plus one matching exported artifact and measure a clear `val_bpb` delta in both pre-export and post-export comparisons | Baseline completed in `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/eval_001_retrain_then_compare`; final pre-export at step `6180`: `1.2515` non-overlapping vs `1.2199` sliding-window (`-0.0316`) with runtimes `3825ms` vs `64626ms`; final post-export: `1.25689044` vs `1.22505202` (`-0.03183842`) with runtimes `3838ms` vs `64635ms`; artifact bytes `15785852`, total submission size `15840891` | The real local effect matches published evidence and is large enough to lock `sliding_window stride=64` as the local evaluation standard; runtime is much slower than `non_overlapping` but still practical on 8 GPUs, so the next bottleneck shifts to export/compression rather than evaluation | Keep `sliding_window stride=64` fixed in future local comparisons and run a controlled export-only compression experiment such as `zlib` vs `zstd` on the same checkpoint |
| 2026-03-24 | `export_001_zstd_only_after_eval_lockin` | export | Same real checkpoint and same `int8` quantized payload under fixed `sliding_window stride=64`; compare outer container `zlib-9` vs `zstd-22` | If only the outer container changes, `zstd-22` should reduce artifact bytes while leaving post-export `val_bpb` unchanged because decompression reconstructs the same quantized tensors bit-for-bit | Container compression is the lowest-risk post-eval export lever and can be isolated without retraining or changing precision policy | Added backend-selectable export/load helpers in root `train_gpt.py`; exported once with `zlib-9` and once with `zstd-22` via an export-only runner | Checkpoint weights, architecture, tokenizer, validation shard, eval mode/stride, quantization rule, tensor selection, metric definition, and runtime environment as much as practical | Smaller artifact bytes at zero quality cost; exact quantized-tensor roundtrip equality; measured export/load timings | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/export_001_zstd_only_after_eval_lockin`, checkpoint eval was `1.22005399` `val_bpb`; `zlib-9` artifact was `15785852` bytes and `zstd-22` artifact was `15748490` bytes (`-37362`, `-0.2367%`); both roundtripped exactly and both scored `1.22505202` post-export with identical quantization gap `+0.00499803`; `zstd-22` export was slower (`1935ms` vs `554ms`) but load was faster (`66ms` vs `162ms`) | `zstd-22` is a trustworthy replacement for `zlib-9` in the local export baseline because it saves bytes with zero measured quality cost, but the gain is small, so container compression alone is not the main remaining bottleneck | Keep `sliding_window stride=64` and `zstd-22` fixed, then test precision allocation such as pure `int6` or mixed `int6/int8` on the same checkpoint |
| 2026-03-24 | `export_002_uniform_int6_after_zstd_lockin` | export | Same locked checkpoint under fixed `sliding_window stride=64` and fixed `zstd-22`; compare regenerated `uniform int8` baseline vs `uniform int6` candidate | On the locked checkpoint, changing export precision from `uniform int8` to `uniform int6` should save more bytes than the prior container-only gain while worsening post-export `val_bpb` by no more than `+0.003` relative to `int8 + zstd-22` | With evaluation and container already locked, pure lower precision is the smallest falsifiable export-only test of whether quantization precision is the next real byte lever | Added minimum root export/load support for bit-packed `uniform int6` beside the existing uniform `int8` path and ran one managed precision comparison runner | Checkpoint weights, architecture, tokenizer, validation shard pattern, tensor selection, export schema, compression backend/level `zstd-22`, eval mode `sliding_window`, eval stride `64`, metric definition, runtime environment as much as practical | Large byte reduction with acceptable roundtrip quality if broad lower-bit export is viable | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/export_002_uniform_int6_after_zstd_lockin`, checkpoint eval was `1.22005399` `val_bpb`; regenerated `uniform int8 + zstd-22` scored `1.22505202` with artifact `15748155` bytes and quantization gap `+0.00499803`; `uniform int6 + zstd-22` scored `1.29818551` with artifact `12382907` bytes and quantization gap `+0.07813152`; both artifacts roundtripped exactly and loaded correctly; `int6` saved `3365248` artifact bytes (`-21.3692%`) but worsened `val_bpb` by `+0.07313349`; export/load times were `1946.59ms/99.67ms` for `int8` vs `1402.75ms/1373.97ms` for `int6` | Pure `uniform int6` is not promising on this checkpoint: the byte win is large, but the quality loss is far beyond the allowed band, so broad lower-bit export should be deprioritized in its current form | Pivot away from broad uniform lower-bit export and test either selective sensitive-tensor retention / mixed precision or training-side quantization-robustness changes |
| 2026-03-24 | `export_003_selective_precision_retention_after_uniform_int6_failure` | export | Same locked checkpoint under fixed `sliding_window stride=64` and fixed `zstd-22`; compare regenerated `uniform int8` baseline vs `mlp_int6_else_int8` candidate | If most of the `uniform int6` damage came from non-MLP tensors, lowering only `blocks.*.mlp.fc.weight` and `blocks.*.mlp.proj.weight` to `int6` should recover meaningful bytes with a much smaller post-export `val_bpb` penalty than broad `uniform int6` | MLP matrices are a large parameter share and the cleanest first selective subset; this isolates whether selective export remains viable without mixing training or evaluation changes | Added minimal per-name mixed-precision export support in root `train_gpt.py`, implemented one dedicated runner policy `mlp_int6_else_int8`, and ran one managed 8-GPU comparison | Checkpoint weights, architecture, tokenizer, validation shard pattern, eval mode `sliding_window`, eval stride `64`, compression backend/level `zstd-22`, tensor inclusion rules, metric definition, and runtime environment as much as practical | Save a meaningful fraction of the `uniform int6` bytes while keeping the post-export regression near or under the `+0.003` band relative to regenerated `uniform int8` | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/export_003_selective_precision_retention_after_uniform_int6_failure`, checkpoint eval was `1.22005399` `val_bpb`; regenerated `uniform int8 + zstd-22` scored `1.22505202` with artifact `15748335` bytes and quantization gap `+0.00499803`; `mlp_int6_else_int8 + zstd-22` scored `1.22768330` with artifact `13946844` bytes and quantization gap `+0.00762931`; both artifacts roundtripped exactly and loaded correctly; the candidate saved `1801491` artifact bytes (`-11.4392%`) and `1801491` total bytes while worsening `val_bpb` by only `+0.00263128`; export/load times were `1886.32ms/121.09ms` for `uniform int8` vs `1528.43ms/794.22ms` for `mlp_int6_else_int8` | Selective export is still viable on this checkpoint: MLP-only `int6` recovered about half of the broad `uniform int6` byte win with only a small quality penalty, far smaller than the failed `+0.07313349` broad regression | Continue selective export search with one additional narrow subset layered on top of the safe MLP policy; only pivot to training-side robustness if the next added subset breaks the quality guardrail |
| 2026-03-24 | `export_004_expand_selective_int6_beyond_safe_mlp_subset` | export | Same locked checkpoint under fixed `sliding_window stride=64` and fixed `zstd-22`; compare regenerated `uniform int8` baseline vs `mlp_int6_plus_attn_proj_int6_else_int8` candidate, with explicit reference to prior `mlp_int6_else_int8` | If some export-only headroom remains beyond the safe MLP subset, adding only `blocks.*.attn.proj.weight -> int6` should save extra bytes while keeping total regression versus regenerated `uniform int8` at or under the `+0.003` guardrail | `attn.proj.weight` is a single interpretable next subset: narrower than broad attention quantization but large enough to matter for bytes, making it a clean test of whether selective export is near saturation | Added one dedicated `export_004` comparison runner that reused the existing mixed-precision export path and changed only the candidate precision map to include `blocks.*.attn.proj.weight -> int6` on top of the established MLP rule | Checkpoint weights, architecture, tokenizer, validation shard pattern, eval mode `sliding_window`, eval stride `64`, compression backend/level `zstd-22`, tensor inclusion rules, export schema, metric definition, and runtime environment as much as practical | Save more bytes than `MLP-only int6` while keeping total regression versus regenerated `uniform int8` interpretable and within the explicit guardrail | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/export_004_expand_selective_int6_beyond_safe_mlp_subset`, checkpoint eval was `1.22005399` `val_bpb`; regenerated `uniform int8 + zstd-22` scored `1.22505202` with artifact `15748335` bytes and quantization gap `+0.00499803`; `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` scored `1.22805392` with artifact `13477497` bytes and quantization gap `+0.00799993`; both artifacts roundtripped exactly and loaded correctly; the candidate saved `2270838` artifact bytes (`-14.4195%`) vs regenerated `uniform int8` and `469347` artifact bytes (`-3.3653%`) vs prior `MLP-only int6`, but worsened `val_bpb` by `+0.00300190` vs regenerated `uniform int8` and `+0.00037062` vs prior `MLP-only int6`; export/load times were `1934.98ms/109.53ms` for `uniform int8` vs `1483.63ms/1053.59ms` for the candidate | The added `attn.proj` subset still buys bytes, but it pushes the total regression just past the explicit `+0.003` guardrail, so this checkpoint appears near the practical export-only selective limit | Pivot next to quantization-friendly optimization, keeping the current `MLP-only int6` selective export as the readout policy instead of adding more export subsets |
| 2026-03-24 | `opt_001_longer_warmdown_for_selective_export_robustness` | optimization | Locked baseline recipe from `eval_001_retrain_then_compare` and locked export readout from `export_003`; compare baseline `WARMDOWN_ITERS=1200` vs one retrain with `WARMDOWN_ITERS=3000` | Increasing warmdown to `3000` will reduce quantization damage enough to improve post-export `val_bpb` and quantization gap under fixed `mlp_int6_else_int8 + zstd-22` | Longer warmdown is a plausible compression-friendly schedule change and the smallest training-side robustness lever after export-only search neared saturation | Retrained once with only `WARMDOWN_ITERS=3000`; operationally kept the baseline training launch shape, including `EVAL_MODE=both`, to preserve the same wallclock-limited recipe, then scored the resulting checkpoint under fixed `sliding_window stride=64` with regenerated `uniform int8` and fixed `mlp_int6_else_int8` exports | Architecture, tokenizer, dataset/shard pattern, optimizer family and hyperparameters other than `WARMDOWN_ITERS`, total wallclock budget, distributed launch shape, evaluation readout for comparison `sliding_window stride=64`, export container `zstd-22`, export readout policy `mlp_int6_else_int8`, metric definitions | Lower post-export `val_bpb` and smaller quantization gap for `mlp_int6_else_int8` than the locked baseline, with little byte change | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/opt_001_longer_warmdown_for_selective_export_robustness`, the retrain finished at step `6194` versus baseline `6180`; checkpoint `sliding_window` `val_bpb` was `1.22014196` vs baseline `1.22005399` (`+0.00008797`); regenerated `uniform int8 + zstd-22` scored `1.22427718` with quantization gap `+0.00413521` and artifact `15743260` bytes versus baseline `1.22505202`, `+0.00499803`, and `15748335` bytes; fixed `mlp_int6_else_int8 + zstd-22` scored `1.22696259` with quantization gap `+0.00682063` and artifact `13943970` bytes versus baseline `1.22768330`, `+0.00762931`, and `13946844` bytes; both artifacts roundtripped exactly and loaded correctly | Longer warmdown modestly improved export robustness on this recipe even though the raw checkpoint metric did not improve: the fixed `MLP-only int6` post-export score improved by `-0.00072070` and its quantization gap shrank by `-0.00080867`, which is positive but not large | Re-test the prior `attn.proj.weight -> int6` near-miss on this warmdown-3000 checkpoint to see whether the modest robustness gain reopens a little selective-export headroom |
| 2026-03-24 | `export_005_retest_attn_proj_subset_after_warmdown_gain` | export | Warmdown-3000 checkpoint from `opt_001` under fixed `sliding_window stride=64` and fixed `zstd-22`; regenerate `uniform int8`, `mlp_int6_else_int8`, and `mlp_int6_plus_attn_proj_int6_else_int8` on the same improved checkpoint | If the modest `opt_001` robustness gain is operationally meaningful, then adding only `blocks.*.attn.proj.weight -> int6` on top of the safe `MLP-only int6` policy should now stay at or under `+0.003` post-export regression versus regenerated `uniform int8` | `export_004` was only a near-miss on the old checkpoint, so re-running the same narrow subset on the improved warmdown checkpoint is the cleanest falsifiable export-only retest | Added one dedicated `export_005` comparison runner that reuses the existing mixed-precision export path but regenerates all three required readouts in one managed run on the warmdown checkpoint | Checkpoint weights from `opt_001`, architecture, tokenizer, dataset/shard pattern, eval mode `sliding_window`, eval stride `64`, compression backend/level `zstd-22`, export schema, metric definition, and all non-`attn.proj` tensor precision rules | Candidate should save bytes versus `MLP-only int6` and, if warmdown really reopened the frontier, fall back under the `+0.003` guardrail versus regenerated `uniform int8` | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/export_005_retest_attn_proj_subset_after_warmdown_gain`, checkpoint eval was `1.22014196` `val_bpb`; regenerated `uniform int8 + zstd-22` scored `1.22427718` with artifact `15743260` bytes and quantization gap `+0.00413521`; regenerated `mlp_int6_else_int8 + zstd-22` scored `1.22696259` with artifact `13943970` bytes and quantization gap `+0.00682063`; `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` scored `1.22740875` with artifact `13473753` bytes and quantization gap `+0.00726679`; all three artifacts roundtripped exactly and loaded correctly; the candidate saved `2269507` artifact bytes (`-14.4157%`) versus regenerated `uniform int8` and `470217` artifact bytes (`-3.3722%`) versus regenerated `MLP-only int6`, but worsened `val_bpb` by `+0.00313158` versus regenerated `uniform int8` and `+0.00044616` versus regenerated `MLP-only int6`; versus historical `export_004`, the same candidate improved by `-0.00064517 val_bpb` and `-0.00073314` quantization gap while using the same packed payload bytes | The warmdown gain was real but still too small to reopen this export frontier: the retest improved materially relative to historical `export_004`, yet it missed the explicit `+0.003` guardrail by a wider absolute margin on the new checkpoint because the regenerated `uniform int8` baseline also improved | Pivot to a different single-variable optimization lever, keeping `WARMDOWN_ITERS=3000`, `sliding_window stride=64`, `zstd-22`, and `mlp_int6_else_int8` fixed while testing the optimizer schedule rather than extending export subsets |
| 2026-03-24 | `opt_002_longer_muon_momentum_warmup_for_selective_export_robustness` | optimization | Locked local baseline after `opt_001` and the negative `export_005` retest; compare warmdown-3000 recipe with `MUON_MOMENTUM_WARMUP_STEPS=500` vs one retrain with `MUON_MOMENTUM_WARMUP_STEPS=1500` | If export robustness is still limited by early optimizer dynamics, increasing Muon momentum warmup should reduce post-export quantization damage on the fixed `mlp_int6_else_int8 + zstd-22` readout | Local evidence already showed warmdown helped exported behavior more than raw checkpoint quality, so Muon momentum warmup was the next smallest schedule-only ablation already exposed in `train_gpt.py` | Retrained once with only `MUON_MOMENTUM_WARMUP_STEPS=1500`; kept architecture, tokenizer, dataset, optimizer family, `WARMDOWN_ITERS=3000`, wallclock-limited launch shape, and training launch mode `EVAL_MODE=both`; then scored the resulting checkpoint under fixed `sliding_window stride=64` with regenerated `uniform int8 + zstd-22` and regenerated `mlp_int6_else_int8 + zstd-22` | Architecture, tokenizer, dataset/shard pattern, optimizer family and all other optimizer hyperparameters, `WARMDOWN_ITERS=3000`, total wallclock budget, distributed launch shape, evaluation standard `sliding_window stride=64` for comparison, export container `zstd-22`, export schema, export readouts used for scoring, and metric definitions | Improve fixed `mlp_int6_else_int8 + zstd-22` post-export `val_bpb` and shrink its quantization gap versus `opt_001` without materially harming checkpoint quality | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/opt_002_longer_muon_momentum_warmup_for_selective_export_robustness`, the retrain finished at step `6196`; checkpoint `sliding_window` `val_bpb` was `1.22061644` versus `1.22014196` for `opt_001` (`+0.00047447`); regenerated `uniform int8 + zstd-22` scored `1.22429451` with quantization gap `+0.00367807` and artifact `15740717` bytes versus `1.22427718`, `+0.00413521`, and `15743260` bytes in `opt_001`; regenerated `mlp_int6_else_int8 + zstd-22` scored `1.22676204` with quantization gap `+0.00614561` and artifact `13939254` bytes versus `1.22696259`, `+0.00682063`, and `13943970` bytes in `opt_001`; both artifacts roundtripped exactly and loaded correctly | The longer Muon warmup improved the primary fixed exported readout and its quantization gap, but only modestly, and it did so while slightly worsening raw checkpoint quality and leaving regenerated `uniform int8` effectively flat-to-slightly worse on `val_bpb` | Stop spending more rounds on small optimizer schedule tweaks in this architecture family for now; keep the improved protocol fixed and test a single architecture-capacity lever such as `MLP_MULT=3` |
| 2026-03-24 | `arch_001_mlp_3x_capacity_under_locked_export_protocol` | architecture | Locked local optimization/export baseline after `opt_002`; compare `MLP_MULT=2` vs one retrain with `MLP_MULT=3` under fixed `sliding_window stride=64`, `zstd-22`, and regenerated `uniform int8` / `mlp_int6_else_int8` scoring | If the current recipe is more bottlenecked by under-capacity MLP blocks than by schedule details, then changing only `MLP_MULT` from `2` to `3` should improve checkpoint and exported `val_bpb` under the locked protocol | `MLP3x` is a recurring current leaderboard motif, and local schedule-only gains had become modest, so a single architecture-side capacity increase was the cleanest next test | Retrained once with only `MLP_MULT=3`; kept depth, width, tokenizer, dataset, optimizer family, optimizer hyperparameters, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, total wallclock budget, distributed launch shape, evaluation protocol, export container, export schema, and scored readouts fixed; export scoring required rerunning the fixed comparison tool with `MLP_MULT=3` in the environment so the model shape matched the checkpoint | Depth, width, tokenizer, dataset/shard pattern, optimizer family and hyperparameters, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, total wallclock-limited launch shape, evaluation standard `sliding_window stride=64`, export container `zstd-22`, export schema, scored readouts, and metric definitions | Improve exported `val_bpb`, ideally especially `mlp_int6_else_int8 + zstd-22`, without violating the byte cap | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/arch_001_mlp_3x_capacity_under_locked_export_protocol`, the retrain stopped at step `5600` under the same 600s cap; checkpoint `sliding_window` `val_bpb` improved to `1.20227788` versus `1.22061644` in `opt_002` (`-0.01833855`); regenerated `uniform int8 + zstd-22` scored `1.20534335` with quantization gap `+0.00306546` and artifact `19928545` bytes versus `1.22429451`, `+0.00367807`, and `15740717` bytes in `opt_002`; regenerated `mlp_int6_else_int8 + zstd-22` scored `1.20764933` with quantization gap `+0.00537144` and artifact `17265453` bytes versus `1.22676204`, `+0.00614561`, and `13939254` bytes in `opt_002`; both regenerated artifacts roundtripped exactly and loaded correctly, but total submission bytes were `19989838` and `17326746`, both above the 16 MB cap | `MLP3x` is a real quality lever in this recipe family and improved both exported readouts more than the recent schedule micro-tuning rounds, but this exact configuration is negative for the submission objective because both fixed exported artifacts exceed the byte cap by a wide margin | Return to a different single-variable architecture lever with a better byte trade, such as `NUM_LAYERS=10` while keeping `MLP_MULT=2`, rather than adopting `MLP3x` directly under the current export recipe |
| 2026-03-24 | `arch_002_depth_10l_under_locked_export_protocol` | architecture | Locked `opt_002` recipe and fixed export protocol; compare `NUM_LAYERS=9` vs one retrain with `NUM_LAYERS=10` under fixed `sliding_window stride=64`, `zstd-22`, and regenerated `uniform int8` / `mlp_int6_else_int8` scoring | If the recipe is capacity-limited but `MLP3x` is too byte-expensive, then one extra layer with `MLP_MULT=2` should improve exported `val_bpb` with a smaller byte increase and may keep the primary mixed-precision readout under the 16 MB cap | Current SOTA motifs repeatedly use `10L` or `11L`, and local `arch_001` already showed that more capacity helps; this isolates depth as a narrower byte-allocation lever than tripling every MLP block | Retrained once with only `NUM_LAYERS=10`; kept width, `MLP_MULT=2`, tokenizer, dataset, optimizer family, optimizer hyperparameters, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, wallclock cap, distributed launch shape, evaluation protocol, export container, export schema, and scored readouts fixed; export scoring reran the fixed comparison tool with `--num-layers 10` so the model shape matched the checkpoint | Width, `MLP_MULT=2`, tokenizer, dataset/shard pattern, optimizer family and hyperparameters, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, total wallclock-limited launch shape, evaluation standard `sliding_window stride=64`, export container `zstd-22`, export schema, scored readouts, and metric definitions | Improve checkpoint and exported `val_bpb` versus `opt_002`, ideally keeping both scored exports under the 16 MB cap and beating the `MLP3x` byte trade | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/arch_002_depth_10l_under_locked_export_protocol`, the retrain stopped at step `5580` under the same 600s cap; checkpoint `sliding_window` `val_bpb` improved to `1.21510822` versus `1.22061644` in `opt_002` (`-0.00550822`); regenerated `uniform int8 + zstd-22` scored `1.21806669` with quantization gap `+0.00295847`, artifact `17419187` bytes, and total `17480480` versus `1.22429451`, `+0.00367807`, `15740717`, and `15802010` in `opt_002`; regenerated `mlp_int6_else_int8 + zstd-22` scored `1.22052401` with quantization gap `+0.00541579`, artifact `15421424` bytes, and total `15482717` versus `1.22676204`, `+0.00614561`, `13939254`, and `14000547` in `opt_002`; both regenerated artifacts roundtripped exactly and loaded correctly, with export/load times `2196.90ms/117.70ms` and `1843.79ms/953.01ms` respectively | Extra depth is a viable capacity lever under the locked protocol in a narrower sense than `MLP3x`: it improved checkpoint and both exported readouts while keeping the primary `mlp_int6_else_int8` readout byte-safe, but regenerated `uniform int8` still missed the cap and raw gain-per-byte remained smaller than `MLP3x` | Keep the new 10-layer checkpoint lineage fixed and run one export-only follow-up, such as retesting `attn.proj.weight -> int6` on top of `mlp_int6_else_int8`, to see whether the stronger 10L checkpoint can buy extra bytes without crossing the `+0.003` guardrail |
| 2026-03-24 | `export_006_retest_attn_proj_subset_on_arch_002_checkpoint` | export | Completed 10-layer `arch_002` checkpoint under fixed `sliding_window stride=64` and `zstd-22`; regenerate `uniform int8`, `mlp_int6_else_int8`, and `mlp_int6_plus_attn_proj_int6_else_int8` on the same checkpoint, with explicit comparison to historical `export_005` | If the stronger `10L / MLP2x` checkpoint is materially more quantization-robust than the earlier 9-layer checkpoints, then adding only `blocks.*.attn.proj.weight -> int6` on top of the byte-safe `mlp_int6_else_int8` policy should save bytes while staying within the explicit `+0.003` regression guardrail versus regenerated `uniform int8` | `attn.proj` was a near-miss on weaker checkpoints; `arch_002` improved both exported baselines and left the primary mixed-precision readout under the cap, so one checkpoint-fixed export retest was justified without retraining | Added one new export-only runner `tools/export_006_retest_attn_proj_on_arch002.py` by minimally adapting the existing three-policy comparison path so the experiment metadata and historical comparison matched the reviewed brief; changed no training or model code and no export logic beyond the already-reviewed `attn.proj` precision override in the candidate policy | Checkpoint weights from `arch_002`, architecture `10L / MLP2x`, tokenizer, dataset/validation shard pattern, optimizer lineage embodied in the checkpoint, evaluation mode `sliding_window`, evaluation stride `64`, export container `zstd-22`, export schema, metric definitions, and the two regenerated baseline readouts | Save bytes versus regenerated `mlp_int6_else_int8 + zstd-22` while remaining within `+0.003 val_bpb` versus regenerated `uniform int8 + zstd-22`; secondary target was to remain under the 16 MB cap and improve on historical `export_005` | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/export_006_retest_attn_proj_subset_on_arch_002_checkpoint`, checkpoint `sliding_window` `val_bpb` revalidated at `1.21510822`; regenerated `uniform int8 + zstd-22` matched the locked baseline at `1.21806669`, artifact `17419187`, total `17480480`; regenerated `mlp_int6_else_int8 + zstd-22` matched the locked baseline at `1.22052401`, artifact `15421424`, total `15482717`; `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` scored `1.22086139`, artifact `14893681`, total `14954974`, with quantization gap `+0.00575317`; all three artifacts roundtripped exactly and loaded correctly; the candidate saved `527743` artifact bytes (`-3.4221%`) versus regenerated `mlp_int6_else_int8` and `2525506` bytes (`-14.4984%`) versus regenerated `uniform int8`, while regressing only `+0.00279470` versus regenerated `uniform int8` and `+0.00033737` versus regenerated `mlp_int6_else_int8`; versus historical `export_005`, the same policy improved by `-0.00654737 val_bpb` while staying byte-safe at `14954974` total bytes | The stronger 10-layer checkpoint did reopen this export frontier: the previously near-miss `attn.proj` subset is now clearly acceptable under the stated guardrail and becomes the new best byte-safe local export policy on this checkpoint family | Lock `arch_002 + mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` as the new export baseline and test one new single-variable architecture motif rather than another nearby export-subset retest |
| 2026-03-25 | `arch_003_bigramhash_4096_smeargate_under_export_006_protocol` | architecture | Locked local best byte-safe `arch_002 + export_006` baseline; compare `10L / MLP2x` vs one retrain adding `BigramHash(4096)` and `SmearGate`, then regenerate `uniform int8`, `mlp_int6_else_int8`, and `mlp_int6_plus_attn_proj_int6_else_int8` under fixed `zstd-22` | If the current `10L / MLP2x` line is still missing cheap local inductive bias, then adding the proven `BigramHash + SmearGate` stack should improve checkpoint and exported `val_bpb` relative to the locked byte-safe baseline while remaining under the 16 MB cap on the locked best readout | Current leaderboard entries show `BigramHash` alone and `SmearGate + BigramHash` together as successful low-cost feature-path motifs; local evidence already suggested architecture rather than another export tweak was the next open lever | Retrained once with only `BIGRAM_HASH_BUCKETS=4096`, `BIGRAM_HASH_DIM=128`, and `USE_SMEARGATE=1`; kept `NUM_LAYERS=10`, width, heads, `MLP_MULT=2`, tokenizer, dataset, optimizer family, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, wallclock-limited 8-GPU launch shape, evaluation protocol, and scored export policies fixed; added one dedicated export runner `tools/export_007_compare_arch003_features.py` so the augmented model shape could be evaluated under the locked export_006 comparison | Depth, width, heads, `MLP_MULT=2`, tokenizer, dataset/shard pattern, optimizer family, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, evaluation standard `sliding_window stride=64`, export container `zstd-22`, export schema, and scored readouts | Improve checkpoint and regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` beyond the current `1.22086139` while keeping the locked best readout under the 16 MB cap | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/arch_003_bigramhash_4096_smeargate_under_export_006_protocol`, the retrain stopped at step `5467` under the same 600s cap; checkpoint `sliding_window` `val_bpb` improved to `1.20992003` versus baseline `1.21510822` (`-0.00518819`); regenerated `uniform int8 + zstd-22` scored `1.21486895` with quantization gap `+0.00494892`, artifact `18010498`, and total `18074538`; regenerated `mlp_int6_else_int8 + zstd-22` scored `1.21774203` with quantization gap `+0.00782200`, artifact `16020055`, and total `16084095`; regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` scored `1.21797944` with quantization gap `+0.00805941`, artifact `15500176`, and total `15564216`; all three regenerated artifacts roundtripped exactly and loaded correctly; versus the prior locked best readout, the candidate improved `val_bpb` by `-0.00288195` while increasing total bytes by `+609242` | The cheap local feature stack works on this 10-layer byte-safe line in the primary sense: checkpoint and all exported readouts improved, and the locked best readout stayed under the cap; however, quantization robustness worsened materially and the same locked best readout is now `+0.00311049` versus regenerated `uniform int8` on this stronger checkpoint, while regenerated `mlp_int6_else_int8` also crossed the byte cap | Keep `arch_003` as the new quality baseline but pivot the next round toward a compression-friendly optimization lever such as `WD=0.04` or late averaging on this architecture to recover quantization robustness and byte headroom |
| 2026-03-25 | `opt_003_arch_003_muon_wd004_plus_late_swa` | optimization | Locked `arch_003` feature-stack baseline with checkpoint `1.20992003`, regenerated `uniform int8 + zstd-22` `1.21486895`, regenerated `mlp_int6_else_int8 + zstd-22` `1.21774203`, and regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` `1.21797944` | If the stronger `arch_003` checkpoint is under-regularized and insufficiently averaged for compression, then adding `Muon WD=0.04 + late SWA` should improve export robustness enough to beat the locked byte-safe readout and/or shrink quantization gaps on the same architecture | Nearby frontier entries pair `BigramHash` and `SmearGate` with Muon WD and late averaging; this directly reproduced the missing compression-friendly optimization layer on the exact local `arch_003` line | Added `MUON_WEIGHT_DECAY=0.04`; enabled `SWA_ENABLED=1` with a fixed late window collecting every `50` steps once the LR multiplier fell below `0.5`; added one dedicated export runner `tools/export_008_compare_opt003_wd_swa.py`; kept the scored export policies fixed | Architecture `10L / MLP2x + BigramHash(4096) + SmearGate`, width, heads, tokenizer, dataset/shard pattern, optimizer family, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, 8-GPU wallclock-limited launch shape, evaluation standard `sliding_window stride=64`, export container `zstd-22`, export schema, and reporting format | Improve the locked `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` readout beyond `1.21797944`, keep the best readout under 16 MB, and reduce quantization gaps on the stronger checkpoint family | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/opt_003_arch_003_muon_wd004_plus_late_swa`, training stopped at step `5449`; the last live pre-SWA checkpoint hit `1.1966` `val_bpb`, then SWA averaged `29` late checkpoints and the final scored checkpoint revalidated at `1.20495912`; regenerated `uniform int8 + zstd-22` scored `1.20880134` with artifact `14067767`, total `14133579`, and quantization gap `+0.00384222`; regenerated `mlp_int6_else_int8 + zstd-22` scored `1.23449862` with artifact `12706777`, total `12772589`, and quantization gap `+0.02953950`; regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` scored `1.23618602` with artifact `12181313`, total `12247125`, and quantization gap `+0.03122690`; all three artifacts roundtripped and loaded exactly | The bundled optimization reproduction produced a strong but mixed outcome: it failed the intended locked mixed-precision objective, because both prior 6-bit readouts collapsed badly, but it massively improved compression friendliness on the same checkpoint family and made regenerated `uniform int8 + zstd-22` byte-safe and decisively better than the prior local best | Keep `arch_003 + MUON WD=0.04` fixed and test `EMA` instead of late `SWA`, because the current result suggests averaging choice now matters more than adding more export subsets or another nearby architecture tweak |
| 2026-03-25 | `opt_004_arch_003_muon_wd004_plus_ema` | optimization | Direct baseline `opt_003_arch_003_muon_wd004_plus_late_swa` with checkpoint `1.20495912`, regenerated `uniform int8 + zstd-22` `1.20880134`, regenerated `mlp_int6_else_int8 + zstd-22` `1.23449862`, and regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` `1.23618602` | If `MUON WD=0.04` is the main gain but late `SWA` specifically over-smoothed the checkpoint for low-bit export, then replacing late `SWA` with a fixed late `EMA` should preserve the new `uniform int8` win while materially improving one or both mixed-precision readouts | `EMA` is a live leaderboard motif and the previous round showed that averaging choice can move the export optimum sharply on this exact architecture family | Disabled `SWA`; enabled one fixed late `EMA` path with `EMA_START_FRAC=0.5` and `EMA_DECAY=0.999`; added `tools/export_009_compare_opt004_ema.py`; kept the scored export policies and export container fixed | Architecture `10L / MLP2x + BigramHash(4096) + SmearGate`, width, heads, tokenizer, dataset/shard pattern, optimizer family, `MUON_WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, 8-GPU wallclock-limited launch shape, evaluation standard `sliding_window stride=64`, export container `zstd-22`, and scored export policies | Preserve or beat regenerated `uniform int8 + zstd-22` beyond `1.20880134`, or materially recover mixed-precision robustness versus `opt_003` while remaining comfortably under the 16 MB cap | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/opt_004_arch_003_muon_wd004_plus_ema`, training stopped at step `5101` under the same `600.099s` train-time cap; the live late-run checkpoint reached `1.2010 val_bpb` before EMA application, then the final EMA-scored checkpoint revalidated at `1.21491545`; regenerated `uniform int8 + zstd-22` scored `1.21883931` with artifact `14213479`, total `14280689`, and quantization gap `+0.00392386`; regenerated `mlp_int6_else_int8 + zstd-22` scored `1.23326469` with artifact `12861779`, total `12928989`, and quantization gap `+0.01834923`; regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` scored `1.23457111` with artifact `12336499`, total `12403709`, and quantization gap `+0.01965566`; all three regenerated artifacts roundtripped and loaded exactly | `EMA` partially recovered the severe mixed-precision collapse versus `opt_003`, but not enough to restore the old frontier, and it clearly failed the primary objective because checkpoint quality and regenerated `uniform int8` both regressed badly versus late `SWA`; the winning local regime remains `uniform int8` on the SWA checkpoint | Stop spending more rounds on nearby averaging variants for this stack; keep the stronger `arch_003 + MUON WD=0.04 + late SWA` baseline fixed and test a different frontier motif such as `GPTQ-lite` clip search or a larger-capacity stack |
| 2026-03-25 | `export_010_opt003_gptq_lite_clip_search_on_uniform_int8` | export | Locked `opt_003` checkpoint `1.20495912` and locked regenerated `uniform int8 + zstd-22` baseline `1.20880134` at total `14133579` | If plain `uniform int8` on the winning `opt_003` checkpoint still wastes quality on avoidable clipping, then one bounded `GPTQ-lite` clip search should improve post-export `val_bpb` without retraining or breaking the byte-safe regime | Current leaderboard evidence includes `GPTQ-lite`; on this checkpoint family the nearby averaging question is already answered, so export-side clip allocation was the smallest new aggressive-phase test | Added one dedicated export-only runner `tools/export_010_opt003_gptq_lite.py`; changed only the `uniform int8` quantization path by searching clip percentiles `[0.999, 0.9995, 0.9999, 0.99999, 1.0]` on large 2D float tensors and picking the minimum weight-reconstruction MSE per tensor; no activation calibration; all other export behavior reused the locked baseline path | Checkpoint weights from `opt_003`, architecture `10L / MLP2x + BigramHash(4096) + SmearGate`, tokenizer, dataset/validation shard pattern, optimizer lineage embodied in the checkpoint, evaluation mode `sliding_window`, evaluation stride `64`, `zstd-22`, artifact schema, and roundtrip/load validation protocol | Beat `1.20880134` post-export while staying under `16,000,000` total bytes and shrinking the quantization gap versus checkpoint `1.20495912` | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/export_010_opt003_gptq_lite_clip_search_on_uniform_int8`, the checkpoint revalidated at `1.20495912`; `uniform_int8_gptq_lite + zstd-22` scored `1.20884738` with artifact `14067534`, total `14134744`, and quantization gap `+0.00388827`; roundtrip/load correctness was exact; versus the locked baseline `uniform int8 + zstd-22`, artifact bytes improved by `-233` but post-export `val_bpb` worsened by `+0.00004605`; actual total bytes rose by `+1165` because current `train_gpt.py` code bytes are `67210` versus the locked baseline’s `65812`; the search touched `62` large 2D tensors and chose percentile `0.999` for `48`, `0.99999` for `11`, and `1.0` for `3` | This bounded GPTQ-lite export refinement is negative on the winning checkpoint family: it preserves exactness and byte safety, but it does not improve exported quality, so the remaining local gap is unlikely to be dominated by plain int8 clipping alone | Stop spending more rounds on nearby export-only clip refinements for `opt_003`; pivot next to a larger-capacity frontier test, such as `11L` on the same `arch_003 + MUON WD=0.04 + late SWA + uniform int8 + zstd-22` line |
| 2026-03-25 | `arch_004_11l_on_opt003_recipe_uniform_int8` | architecture | Locked local best `opt_003` line with checkpoint `1.20495912`, regenerated `uniform int8 + zstd-22` `1.20880134`, artifact `14067767`, and total `14133579` | If the remaining gap on the winning `opt_003` line is now more about under-capacity than nearby export clipping, then spending part of the remaining byte headroom on one extra layer should improve checkpoint and post-export `val_bpb` while remaining under the cap | The live leaderboard still clusters around `11L`, nearby `EMA` and bounded `GPTQ-lite` on this checkpoint family were already negative, and the current byte-safe winner still had about `1.87 MB` of headroom before this test | Retrained once with only `NUM_LAYERS=11`; added one dedicated export-only runner `tools/export_011_arch004_11l_uniform_int8.py` so the fixed `uniform int8 + zstd-22` readout could be compared directly against locked `opt_003` without regenerating unrelated mixed-precision policies | Width `512`, heads / KV heads, `MLP_MULT=2`, `BigramHash(4096)`, `SmearGate`, tokenizer, dataset/shard pattern, optimizer family, `MUON_WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, late `SWA`, 8-GPU wallclock-limited launch shape, evaluation `sliding_window stride=64`, and export container `zstd-22` | Beat regenerated `uniform int8 + zstd-22` beyond `1.20880134` while staying under `16,000,000` total bytes | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/arch_004_11l_on_opt003_recipe_uniform_int8`, training stopped at step `4989` under the same `600.041s` train-time cap; the live pre-SWA checkpoint hit `1.1883 val_bpb`, then SWA averaged `30` late checkpoints and the final scored checkpoint revalidated at `1.19786472`; regenerated `uniform int8 + zstd-22` scored `1.20150644` with artifact `15366915`, code `67210`, total `15434125`, and quantization gap `+0.00364172`; roundtrip/load correctness was exact; versus the locked `10L` baseline, checkpoint `val_bpb` improved by `-0.00709440`, post-export `val_bpb` improved by `-0.00729490`, artifact bytes rose by `+1299148`, total bytes rose by `+1300546`, and code bytes rose by `+1398`; the candidate stayed under the cap by `565875` bytes | The `11L` motif is clearly positive on the winning `opt_003` stack: it materially improves checkpoint and exported quality while remaining byte-safe, and it slightly improves the uniform-int8 quantization gap as well | Keep the stronger `11L` recipe fixed and spend the remaining headroom on a cheaper capacity lever than another full layer, such as a larger `BigramHash` allocation |
| 2026-03-25 | `arch_005_11l_bigramhash_8192_on_opt003_recipe_uniform_int8` | architecture | Locked local best `arch_004` line with checkpoint `1.19786472`, regenerated `uniform int8 + zstd-22` `1.20150644`, artifact `15366915`, code `67210`, and total `15434125` | If the stronger `11L` winner still has useful byte headroom, then increasing `BIGRAM_HASH_BUCKETS` from `4096` to `8192` should improve checkpoint and exported `val_bpb` while remaining under the cap | The live snapshot still rewards `BigramHash`, including a stronger `BigramHash(10240)` motif, and this is a direct capacity spend on the strongest local byte-safe stack with all other factors locked | Retrained once with only `BIGRAM_HASH_BUCKETS=8192`; added one dedicated export-only runner `tools/export_012_arch005_11l_uniform_int8.py` so the fixed `uniform int8 + zstd-22` readout could be compared directly against locked `arch_004` | `NUM_LAYERS=11`, width `512`, heads / KV heads, `MLP_MULT=2`, `BIGRAM_HASH_DIM=128`, `SmearGate`, tokenizer, dataset/shard pattern, optimizer family, `MUON_WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, late `SWA`, 8-GPU wallclock-limited launch shape, evaluation `sliding_window stride=64`, and export container `zstd-22` | Beat regenerated `uniform int8 + zstd-22` beyond `1.20150644` while staying under `16,000,000` total bytes | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/arch_005_11l_bigramhash_8192_on_opt003_recipe_uniform_int8`, training stopped at step `4988` under the same `599.991s` train-time cap; the live pre-SWA checkpoint hit `1.1876 val_bpb`, then SWA averaged `30` late checkpoints and the final scored checkpoint revalidated at `1.19748125`; regenerated `uniform int8 + zstd-22` scored `1.20023167` with artifact `15881366`, code `67210`, total `15948576`, and quantization gap `+0.00275042`; roundtrip/load correctness was exact; versus locked `arch_004`, checkpoint `val_bpb` improved by `-0.00038347`, post-export `val_bpb` improved by `-0.00127477`, artifact bytes rose by `+514451`, total bytes rose by `+514451`, and the candidate stayed under the cap by `51424` bytes | More `BigramHash` capacity is still positive on the winning `11L` recipe and even improves quantization robustness, but this specific byte-allocation path is now close to saturation under fixed `uniform int8 + zstd-22` because the remaining headroom is tiny | Pivot next to a byte-neutral frontier motif such as `warmdown3500` rather than spending more bytes on another nearby `BigramHash` increase |
| 2026-03-25 | `opt_005_arch_005_warmdown3500_uniform_int8` | optimization | Locked local best `arch_005` line with checkpoint `1.19748125`, regenerated `uniform int8 + zstd-22` `1.20023167`, artifact `15881366`, code `67210`, and total `15948576` | If the near-cap `11L + BigramHash(8192)` stack is still bottlenecked by end-of-training smoothness, then increasing `WARMDOWN_ITERS` from `3000` to `3500` should improve exported `uniform int8 + zstd-22` without materially changing bytes | `warmdown3500` is a live leaderboard motif, and earlier local evidence already showed that longer warmdown could improve exported behavior on weaker families even when raw checkpoint quality stayed flat | Retrained once with only `WARMDOWN_ITERS=3500`; reused the same managed 8-GPU launch shape and fixed `uniform int8 + zstd-22` export/eval path; minimally updated `tools/export_012_arch005_11l_uniform_int8.py` so the comparison key recorded the correct locked baseline experiment id | `NUM_LAYERS=11`, width `512`, heads / KV heads, `MLP_MULT=2`, `BIGRAM_HASH_BUCKETS=8192`, `BIGRAM_HASH_DIM=128`, `SmearGate`, tokenizer, dataset/shard pattern, optimizer family, `MUON_WEIGHT_DECAY=0.04`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, late `SWA`, train-time wallclock cap, distributed launch shape, evaluation `sliding_window stride=64`, export readout `uniform int8`, and export container `zstd-22` | Beat regenerated `uniform int8 + zstd-22` beyond `1.20023167` while staying under `16,000,000` total bytes | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/opt_005_arch_005_warmdown3500_uniform_int8`, training stopped at step `4985` under the same `600.054s` cap; the live pre-SWA checkpoint reached `1.1873 val_bpb`, then SWA averaged `35` late checkpoints and the final scored checkpoint revalidated at `1.19943901`; regenerated `uniform int8 + zstd-22` scored `1.20206766` with artifact `15780075`, code `67210`, total `15847285`, and quantization gap `+0.00262866`; roundtrip/load correctness was exact; versus locked `arch_005`, checkpoint `val_bpb` worsened by `+0.00195776`, post-export `val_bpb` worsened by `+0.00183599`, artifact bytes fell by `-101291`, total bytes fell by `-101291`, and the candidate stayed under the cap by `152715` bytes | `warmdown3500` is negative on the current winning `arch_005` stack: it slightly improves compressibility and the fixed-int8 quantization gap, but it worsens both the final scored checkpoint and the exported objective, so this line is not bottlenecked by this nearby warmdown extension | Pivot to a different byte-neutral frontier motif such as `XSA` rather than spending more rounds on nearby warmdown tuning |
| 2026-03-25 | `arch_006_arch_005_xsa4_uniform_int8` | architecture | Locked local best `arch_005` line with checkpoint `1.19748125`, regenerated `uniform int8 + zstd-22` `1.20023167`, artifact `15881366`, code `67210`, and total `15948576` | If the near-cap `11L + BigramHash(8192) + SmearGate + MUON WD=0.04 + late SWA` stack is missing a byte-neutral attention upgrade, then replacing only the deepest `4` attention blocks with `XSA` should improve checkpoint and exported `val_bpb` without breaking the byte cap | Current live leaderboard entries repeatedly use `XSA4` on `11L` stacks, so this directly tested the strongest untried attention-side motif on the best local byte-safe recipe | Added minimal `XSA_LAST_N` support plus efficient self-value subtraction in `/newcpfs/lxh/parameter-golf/train_gpt.py`; added `tools/export_013_arch006_11l_xsa4_uniform_int8.py`; smoke-tested instantiate/export/reload/eval, then retrained once and regenerated the fixed `uniform int8 + zstd-22` readout | `NUM_LAYERS=11`, width `512`, heads / KV heads, `MLP_MULT=2`, `BIGRAM_HASH_BUCKETS=8192`, `BIGRAM_HASH_DIM=128`, `SmearGate`, tokenizer, dataset/shard pattern, optimizer family, `MUON_WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, late `SWA`, train-time wallclock cap, distributed 8-GPU launch shape, evaluation `sliding_window stride=64`, export readout `uniform int8`, and export container `zstd-22` | Beat regenerated `uniform int8 + zstd-22` beyond `1.20023167` while staying under `16,000,000` total bytes | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/arch_006_arch_005_xsa4_uniform_int8`, the smoke path passed with exact roundtrip/load correctness; the main retrain stopped at step `4507` under the same `600.099s` cap, the live pre-SWA checkpoint reached `1.1893 val_bpb`, then SWA averaged `30` late checkpoints and the final scored checkpoint revalidated at `1.19825960`; regenerated `uniform int8 + zstd-22` scored `1.20101996` with artifact `15845227`, code `68366`, total `15913593`, and quantization gap `+0.00276037`; roundtrip/load correctness was exact; versus locked `arch_005`, checkpoint `val_bpb` worsened by `+0.00077834`, post-export `val_bpb` worsened by `+0.00078829`, artifact bytes fell by `-36139`, code bytes rose by `+1156`, total bytes fell by `-34983`, and the candidate stayed under the cap by `86407` bytes | `XSA4` is a controlled negative on the current winning `arch_005` stack: both checkpoint and exported `val_bpb` regressed, while bytes improved only slightly; this is the strongest failure case from the brief and suggests this recipe is not missing a simple byte-neutral attention swap under the fixed 600s schedule | Pivot away from nearby attention-only changes on `arch_005` and test a different frontier geometry motif such as `Partial RoPE + LN Scale` under the same fixed export protocol |
| 2026-03-25 | `arch_007_arch_005_partial_rope_lnscale_uniform_int8` | architecture | Locked local best `arch_005` line with checkpoint `1.19748125`, regenerated `uniform int8 + zstd-22` `1.20023167`, artifact `15881366`, code `67210`, and total `15948576` | If the near-cap `11L + BigramHash(8192) + SmearGate + MUON WD=0.04 + late SWA` stack is missing the geometry/stability part of the current leaderboard `#3` motif, then adding only `Partial RoPE (16/64)` plus layerwise `LN Scale = 1/sqrt(layer_idx+1)` should improve exported `uniform int8 + zstd-22` without materially increasing bytes | The live leaderboard `#3` run uses `11L Partial RoPE + LN Scale + EMA + XSA4`; local evidence had already answered nearby `warmdown3500` and isolated `XSA4` negatively, leaving the geometry sub-bundle as the clean remaining byte-neutral test | Added default-off `ROPE_DIMS` and `LN_SCALE` support in `/newcpfs/lxh/parameter-golf/train_gpt.py`; added `tools/export_014_arch007_11l_partial_rope_lnscale_uniform_int8.py`; smoke-tested instantiate/export/reload/eval on the locked `arch_005` checkpoint with exact roundtrip/load correctness, then retrained once and regenerated the fixed `uniform int8 + zstd-22` readout | `NUM_LAYERS=11`, width `512`, heads / KV heads, `MLP_MULT=2`, `BIGRAM_HASH_BUCKETS=8192`, `BIGRAM_HASH_DIM=128`, `SmearGate`, tokenizer, dataset/shard pattern, optimizer family, `MUON_WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, late `SWA`, train-time wallclock cap, distributed 8-GPU launch shape, evaluation `sliding_window stride=64`, export readout `uniform int8`, and export container `zstd-22` | Beat regenerated `uniform int8 + zstd-22` beyond `1.20023167` while staying under `16,000,000` total bytes | In `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/arch_007_arch_005_partial_rope_lnscale_uniform_int8`, the 8-GPU smoke path passed with exact roundtrip/load correctness; the main retrain stopped at step `4998` under the same `600.015s` cap, the live pre-SWA checkpoint reached `1.1887 val_bpb`, then SWA averaged `29` late checkpoints and the final scored checkpoint revalidated at `1.19731787`; regenerated `uniform int8 + zstd-22` scored `1.20071205` with artifact `15651578`, code `69665`, total `15721243`, and quantization gap `+0.00339417`; roundtrip/load correctness was exact; versus locked `arch_005`, checkpoint `val_bpb` improved by `-0.00016338`, post-export `val_bpb` worsened by `+0.00048037`, artifact bytes fell by `-229788`, code bytes rose by `+2455`, total bytes fell by `-227333`, and the candidate stayed under the cap by `278757` bytes | `Partial RoPE + LN Scale` is raw-positive but export-negative on the winning `arch_005` stack: the geometry bundle slightly improves checkpoint quality and bytes, but it worsens the fixed exported objective because the quantization gap expands from `+0.00275042` to `+0.00339417` | Pivot next to a larger frontier bundle rather than another isolated zero-parameter refinement, most plausibly reproducing the fuller `#3` motif with `EMA` and possibly `XSA4` on top of the new geometry support |
| 2026-03-25 | `arch_008_arch_005_full_partial_rope_lnscale_ema_xsa4_uniform_int8` | architecture + optimization | Locked local best `arch_005` line with checkpoint `1.19748125`, regenerated `uniform int8 + zstd-22` `1.20023167`, artifact `15881366`, code `67210`, and total `15948576`; mechanism baseline `arch_007` at checkpoint `1.19731787`, regenerated `1.20071205`, quantization gap `+0.00339417` | If `arch_007` was export-negative only because the geometry sub-bundle lacked its matching frontier companions, then adding the full live `#3` motif `Partial RoPE + LN Scale + EMA + XSA4` should convert the small raw gain into an exported win on the exact `arch_005` stack | This directly reproduced the still-unanswered bundle interaction from the live `#3` leaderboard motif after isolated `XSA4` and isolated `Partial RoPE + LN Scale` had both already been answered locally | Reused existing default-off `ROPE_DIMS`, `LN_SCALE`, and `XSA_LAST_N` support; reused the established EMA path with fixed local settings `EMA_START_FRAC=0.5`, `EMA_DECAY=0.999`; ran one 8-GPU smoke instantiate/export/reload/eval pass, one controlled retrain, then one fixed `uniform int8 + zstd-22` export/eval comparison | `NUM_LAYERS=11`, width `512`, heads / KV heads, `MLP_MULT=2`, `BIGRAM_HASH_BUCKETS=8192`, `BIGRAM_HASH_DIM=128`, `SmearGate`, tokenizer, dataset/shard pattern, optimizer family, `MUON_WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `MUON_MOMENTUM_WARMUP_STEPS=1500`, train-time wallclock cap, distributed 8-GPU launch shape, evaluation `sliding_window stride=64`, export readout `uniform int8`, export container `zstd-22`, and artifact accounting | Beat regenerated `uniform int8 + zstd-22` beyond `1.20023167`, improve the `arch_007` quantization gap, and stay under `16,000,000` total bytes | The smoke path passed with exact roundtrip/load correctness. The full retrain stopped at step `4185` under `600.069s`, EMA started at step `3028` and was applied at the end, the final checkpoint revalidated at `1.21495046`, and regenerated `uniform int8 + zstd-22` scored `1.21781093` with artifact `15807871`, code `69665`, total `15877536`, quantization gap `+0.00286047`, export `9320.83ms`, load `145.56ms`, and exact roundtrip/load correctness. Versus locked `arch_005`, checkpoint `val_bpb` worsened by `+0.01746921`, exported `val_bpb` worsened by `+0.01757926`, artifact bytes fell by `-73495`, total bytes fell by `-71040`, and quantization gap worsened by `+0.00011005`. Versus `arch_007`, checkpoint `val_bpb` worsened by `+0.01763259`, exported `val_bpb` worsened by `+0.01709888`, artifact bytes rose by `+156293`, total bytes rose by `+156293`, and quantization gap improved by `-0.00053370` | The full live `#3` bundle is a strong transfer failure on the local `arch_005` stack. This is not another export-only miss: raw checkpoint quality collapsed first, and the slightly better quantization gap versus `arch_007` did not matter because the underlying model was far worse | Stop spending more rounds on nearby `Partial RoPE + LN Scale + XSA4 + EMA` refinements on `arch_005`; pivot to a different frontier family, preferably a controlled byte-neutral activation or mechanism change rather than another geometry/averaging retry |
| 2026-03-25 | `repro_001_record06_10l_int5mlp_bh10240` | architecture + optimization + export | Locked local best `arch_005` at checkpoint `1.19748125`, regenerated `uniform int8 + zstd-22` `1.20023167`, artifact `15881366`, code `67210`, and total `15948576`; archived external fidelity reference `/newcpfs/lxh/parameter-golf/records/track_10min_16mb/2026-03-20_10L_Int5MLP_MuonWD04_SWA50` with published mean `1.14276` and seed-42 post-export `1.14271194` | If the local gap is mainly search-space lag rather than missing byte-neutral tweaks, then a faithful local reproduction of the validated `10L Int5-MLP + BigramHash(10240)` record stack should materially beat `1.20023167` under the fixed local evaluation standard | This directly tested a validated low-bit capacity-funded frontier stack using the archived record implementation as authority instead of designing a new hybrid recipe | Created run dir `/newcpfs/lxh/parameter-golf/research_lab_pg/projects/parameter_golf_science/runs/repro_001_record06_10l_int5mlp_bh10240`, saved a resolved-config summary, ran an 8-GPU smoke instantiate/export/reload/eval pass, then launched one full managed reproduction with the archived script; because the local torch/python runtime lacked several required APIs and Python `zstandard`, execution used a run-local `sitecustomize.py` compatibility shim and the runtime `zlib` artifact was losslessly recompressed afterward to `zstd-22` with identical quantized payload bytes | Archived authority script, archived recipe defaults, tokenizer, dataset shard pattern, evaluation `sliding_window stride=64`, target export precision policy, target outer container `zstd-22`, 8-GPU launch shape, `600s` cap, and artifact accounting methodology | Beat local best `1.20023167`, stay under `16,000,000`, and land materially closer to published `1.14276` while matching the archived run markers closely enough to treat the result as a clean stack readout | Smoke pass succeeded with exact roundtrip/load correctness. The full run stopped at `step=2223` under `600.069s`; `swa:start` occurred at `1050` and `swa:applying averaged 24 checkpoints` matched the archived count. The archived script log reported checkpoint `val_bpb=1.2260` and runtime `int6+zlib` artifact bytes `16835646`; exact roundtrip/load validation passed and the logged post-export result was `1.22520354`. Because the runtime lacked Python `zstandard`, the final artifact for comparison was corrected by lossless `zlib` decompress plus `zstd-22` recompress to `15918861` artifact bytes, `52930` code bytes, and `15971791` total bytes, still under the cap by `28209`. Versus locked local best, post-export `val_bpb` worsened by `+0.02497187`, total bytes rose by `+23215`, and artifact bytes rose by `+37495`; versus published `1.14276`, post-export `val_bpb` was worse by `+0.08244354`. The run reached `4486` fewer steps than the archived seed-42 reference near `6709` | This reproduction is negative as a local score, but it is not yet evidence that the archived stack itself fails here cleanly: the dominant signal is severe execution drift, with roughly `3x` slower step time, missing native runtime features, and a forced fallback container path | Audit local environment alignment first and only rerun this record stack if a shim-free torch/python path with native `zstandard` exists; otherwise pivot to a different validated low-bit frontier stack rather than repeating this same reproduction |
| 2026-03-25 | `infra_001_record06_env_alignment_audit` | evaluation | Shimmed local `repro_001` at checkpoint `1.2260`, post-export `1.22520354`, corrected `zstd-22` artifact `15918861`, total `15971791`, `step=2223`, `swa:start=1050`; archived seed-42 reference `1.14271194`, artifact `15913048`, total `15965978`, near `step=6709`, `swa:start=5550`; prior local best `arch_005` `1.20023167`, total `15948576` | If runtime-path drift caused the negative reproduction, then a shim-free local environment with native `zstandard` and native torch feature support should recover archived-like training geometry and materially improve the reproduced score over `1.22520354` | The prior run preserved the SWA count but not the archived step budget or runtime features, so environment fidelity was the highest-value remaining ambiguity on a still-valid frontier stack | Changed only the execution environment and launch path: audited local interpreters, selected `physicslm` (`Python 3.11.14`, `torch 2.10.0+cu128`, native `zstandard 0.25.0`), removed `PYTHONPATH`/`sitecustomize.py`, ran one managed 8-GPU smoke pass and one managed 8-GPU full rerun | Archived authority script, archived recipe defaults, tokenizer, dataset shard pattern, evaluation `sliding_window stride=64`, export precision policy, outer container target `zstd-22`, same local 8xL20Z hardware, `600s` cap, and artifact accounting | No shim, direct runtime `zstd-22`, materially more than `2223` steps in `600s`, and at least `-0.010` post-export improvement versus `1.22520354` | Capability audit rejected `system_py310`, `conda_base`, and `conda_loongflow_ml`; only `physicslm` had native `zstandard`, `torch.compile`, `enable_cudnn_sdp`, `F.rms_norm`, and `enable_gqa` with no shim required. The smoke pass succeeded and emitted a direct `zstd` artifact (`28 b5 2f fd` header) with exact roundtrip/load evaluation. The full rerun stopped at `step=6666` under `599961ms`; `swa:start` occurred at `5500` and `swa:applying averaged 24 checkpoints` matched the archived count. The last logged checkpoint `val_bpb` was `1.1537`; the direct-runtime exported artifact was `15705618` bytes with `52930` code bytes and `15758548` total bytes, and the archived script reported post-export `val_bpb=1.14323509` with exact roundtrip/load correctness. Versus `repro_001`, post-export `val_bpb` improved by `-0.08196845`, artifact bytes by `-213243`, total bytes by `-213243`, and steps by `+4443`. Versus prior local best `arch_005`, post-export `val_bpb` improved by `-0.05699658` and total bytes by `-190028`. Versus archived seed-42, post-export `val_bpb` was only `+0.00052315` worse, with `-43` steps and `-50` SWA-start offset | Environment fidelity was the main blocker. Once the run used a native-feature environment, the archived stack became the new local best and nearly matched archived seed-42 fidelity on this machine | Stop spending more time on environment-alignment retries for `record06`; use the clean `physicslm` path as the validated baseline and run a single controlled confirmation or extension next |
| 2026-03-25 | `repro_002_record06_seed1337_shimfree_confirm` | evaluation | Clean local seed-42 `infra_001` rerun at checkpoint `1.1537`, post-export `1.14323509`, artifact `15705618`, total `15758548`, `step=6666`; archived seed-1337 post-export `1.14298416`, artifact `15777256`, total `15830186`, `step=6711`; archived 3-seed mean `1.14276` | If runtime fidelity is genuinely fixed, then changing only `SEED=42 -> 1337` in the same shim-free environment should produce another archived-like run with near-matching geometry and score | `infra_001` already recovered the archived stack once in `physicslm`; a seed-only rerun is the cleanest remaining robustness test before treating `record06` as the new local baseline | Changed only `SEED=1337`; reused the archived authority script, the same `physicslm` environment, the same managed 8-GPU launch path, and the same artifact accounting | Archived `record06` architecture/export stack, dataset and tokenizer, evaluation `sliding_window stride=64`, native `zstandard`, no shimmed `PYTHONPATH`, same 8xL20Z hardware, and the same `600s` wallclock cap | Recover archived-like geometry, emit direct runtime `zstd`, preserve exact roundtrip/load correctness, and land within `0.002` of both clean local seed-42 and archived seed-1337 | The full managed run in `physicslm` stopped at `step=6679` under `600049ms`; `swa:start` matched the clean rerun at `5500`, `swa:applying averaged 24 checkpoints` matched the archive, and the artifact header `28 b5 2f fd` confirmed direct runtime `zstd`. The last logged checkpoint was `val_bpb=1.1543`; post-export `val_bpb` was `1.14374449` with artifact `15630471`, code `52930`, total `15683401`, and exact roundtrip/load correctness. Versus clean local seed-42, post-export `val_bpb` changed by `+0.00050940`, artifact and total bytes by `-75147`, and steps by `+13`. Versus archived seed-1337, post-export `val_bpb` changed by `+0.00076033`, artifact and total bytes by `-146785`, steps by `-32`, and `swa:start` by `-50`. Versus the archived 3-seed mean, post-export `val_bpb` changed by `+0.00098449` | The clean `record06` reproduction is stable across at least two archived seeds on this machine. The run stayed within the target score band, matched the archived geometry closely, and remains comfortably byte-safe, so `record06` is now strong enough to treat as the local baseline for the next aggressive extension round | Stop spending rounds on more seed-fidelity confirmation for `record06`; use this stable clean baseline for the first current-motif extension |
| 2026-03-25 | `opt_006_record06_late_qat_keep_swa` | optimization | Clean shim-free `record06` seed-42 baseline `infra_001` at checkpoint `1.1537`, post-export `1.14323509`, artifact `15705618`, total `15758548`, `step=6666`; local 2-seed mean `1.14348979`; archived 3-seed mean `1.14276` | If the remaining error on `record06` is dominated more by train/export mismatch than by raw checkpoint quality, then late QAT aligned to the exact final mixed `int5/int6` export map should improve post-export `val_bpb` beyond the clean seed-42 baseline | QAT is a live frontier motif and directly targets the train/export mismatch risk on this mixed low-bit baseline without changing architecture, evaluation, or final export policy | Added late fake-quant only for tensors already exported in low precision; `QAT_START_STEP=5500`; fake-quantized `20` MLP `int5` weights, `39` attention `int6` weights, and `2` bigram `int6` weights, with exact map verification against the final export policy | Archived `record06` architecture, `BigramHash(10240)`, `SmearGate`, `MLP3x`, `WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `SWA_ENABLED=1`, `SWA_START_FRAC=0.4`, `SWA_EVERY=50`, `SEED=42`, `physicslm`, no shimmed `PYTHONPATH`, managed 8-GPU launch, `600s` cap, `sliding_window stride=64`, and final mixed `int5/int6 + zstd-22` export | Beat clean seed-42 `1.14323509`, preferably beat the 2-seed mean, shrink the mismatch, and stay under `16,000,000` total bytes | Smoke verify passed with exact export-map/QAT-map match and exact roundtrip/load correctness. The full run stayed shim-free in `physicslm`, started `SWA` at `5450`, started QAT at `5500`, applied `24` SWA checkpoints, and stopped at `step=6608` in `600024ms`; the last logged checkpoint was `1.1613`, post-export `val_bpb` was `1.14339099`, artifact `16027864`, code `58624`, total `16086488`, and the direct runtime artifact header `28 b5 2f fd` confirmed `zstd`. Versus clean seed-42, post-export changed by `+0.00015590`, checkpoint quality worsened by `+0.0076`, artifact bytes by `+322246`, code bytes by `+5694`, total bytes by `+327940`, steps by `-58`, and the checkpoint-to-export gap moved from `-0.01046491` to `-0.01790901` (`-0.00744410`). Versus the local 2-seed mean, post-export improved by `-0.00009880`; versus the archived 3-seed mean it regressed by `+0.00063099` | Late QAT is negative on this exact `record06` stack for the submission objective: it slightly misses the clean same-seed baseline, materially worsens raw checkpoint quality, and pushes the final counted bytes over the cap, so the intervention is too intrusive for the `600s` recipe | Keep the clean `record06 + SWA` baseline locked and shift the next aggressive round to a byte-neutral structural transfer rather than another nearby late-phase optimization tweak |
| 2026-03-25 | `arch_009_record06_leakyrelu2_keep_swa_export` | architecture | Clean shim-free `record06` seed-42 baseline `infra_001` at checkpoint `1.1537`, post-export `1.14323509`, artifact `15705618`, total `15758548`, `step=6666`; clean seed-1337 `1.14374449`; local 2-seed mean `1.14348979`; archived 3-seed mean `1.14276` | If `LeakyReLU(0.5)^2` is a transferable architecture-side piece of the current top leaderboard motif independent of `TTT` and `Parallel Muon`, then replacing only `ReLU^2` on clean `record06` should improve checkpoint and post-export `val_bpb` without breaking byte-safe export behavior | `LeakyReLU^2` is the clearest current-top motif still untested on clean `record06`, and it is byte-neutral in principle, making it the cleanest next aggressive structural transfer after nearby late-QAT failed | Forked the archived `record06` script into a run-local directory and changed only the MLP activation line from `torch.relu(...)` to `F.leaky_relu(..., negative_slope=0.5)`; pre-run verification confirmed identical `106` state-dict tensor keys/shapes and identical `167` export tensor keys/meta | Archived `record06` geometry, `BigramHash(10240)`, `SmearGate`, `MLP3x`, `WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `SWA_ENABLED=1`, `SWA_START_FRAC=0.4`, `SWA_EVERY=50`, `SEED=42`, `physicslm`, no shimmed `PYTHONPATH`, managed 8-GPU launch, `600s` cap, `sliding_window stride=64`, final mixed `int5/int6 + zstd-22` export, parameter names/shapes, and export schema | Beat clean seed-42 `1.14323509` post-export, stay under `16,000,000`, and preserve direct runtime `zstd` plus exact roundtrip correctness | Structural smoke pass succeeded with direct-runtime `zstd` header `28 b5 2f fd` and exact roundtrip/load correctness. The full run stayed shim-free in `physicslm`, stopped at `step=6597` in `600028ms`, started `SWA` at `5450`, applied `23` SWA checkpoints, logged checkpoint `val_bpb=1.1509`, and scored `1.14068897` post-export with artifact `15908521`, code `52952`, and total `15961473`; exact roundtrip/load correctness passed and the container header again confirmed direct `zstd`. Versus clean seed-42, checkpoint improved by `-0.0028`, post-export improved by `-0.00254612`, artifact bytes rose by `+202903`, code bytes by `+22`, total bytes by `+202925`, steps changed by `-69`, and the checkpoint-to-export gap moved from `-0.01046491` to `-0.01021103` (`+0.00025388`). Versus clean seed-1337, post-export improved by `-0.00305552`; versus the clean 2-seed mean by `-0.00280082`; versus archived 3-seed mean by `-0.00207103` | This isolated activation transfer is clearly positive on the clean `record06` stack: it beats the clean same-seed baseline, the clean seed-1337 rerun, the old local 2-seed mean, and the archived 3-seed mean while preserving byte-safe direct-runtime `zstd`, exact roundtrip correctness, and unchanged export schema. The gain comes mainly from a better raw checkpoint, while byte headroom tightens materially and late-SWA geometry is slightly shorter | Re-run the same `LeakyReLU^2` transfer on `SEED=1337` before stacking another motif so the project can decide whether `record06 + LeakyReLU^2 + SWA` is the new stable local baseline or just a strong seed-42 landing |
| 2026-03-25 | `opt_007_record06_leakyrelu2_ema_warmdown3500` | optimization | Best local `record06` seed-42 result `arch_009_record06_leakyrelu2_keep_swa_export` at checkpoint `1.1509`, post-export `1.14068897`, artifact `15908521`, total `15961473`, `step=6597`, `swa:start step:5450`, `23` SWA checkpoints | If the current frontier `EMA + warmdown3500` bundle transfers onto the strong `record06 + LeakyReLU^2` line, then replacing late `SWA` with late `EMA` and extending warmdown from `3000` to `3500` should improve exported `val_bpb` beyond `1.14068897` | This is the cleanest still-untested optimization transfer from the live `#2` leaderboard motif onto the best local `record06` lineage while keeping architecture and export fixed | Added the existing local EMA path to the `arch_009` run-local fork and changed only `SWA_ENABLED=0`, `EMA_ENABLED=1`, `EMA_START_FRAC=0.5`, `EMA_DECAY=0.999`, and `WARMDOWN_ITERS=3500`; smoke verification confirmed identical `106` state-dict tensors and identical `167` export tensors plus identical export metadata versus `arch_009` | `LeakyReLU(0.5)^2`, archived `record06` geometry, `BigramHash(10240)`, `SmearGate`, `MLP3x`, `WEIGHT_DECAY=0.04`, `SEED=42`, `physicslm`, no shimmed `PYTHONPATH`, managed 8-GPU launch, `600s` cap, `sliding_window stride=64`, final mixed `int5/int6 + zstd-22` export, parameter names/shapes, and export schema | Beat `arch_009` post-export `1.14068897`, stay under `16,000,000`, and preserve direct runtime `zstd` plus exact roundtrip correctness | Smoke passed on 8 GPUs with exact roundtrip correctness, direct-runtime `zstd` header `28 b5 2f fd`, `106` identical state-dict tensors, `167` identical export tensors, and identical export metadata versus `arch_009`. The full run stayed shim-free in `physicslm`, stopped at `step=6058` in `600061ms`, started `EMA` at `step=4854`, applied `EMA decay=0.999` at the end, logged checkpoint `val_bpb=1.1547`, and scored `1.15374139` post-export with artifact `15681354`, code `54516`, and total `15735870`; exact roundtrip/load correctness passed and the container header again confirmed direct `zstd`. Versus `arch_009`, checkpoint regressed by `+0.0038`, post-export regressed by `+0.01305242`, artifact bytes fell by `-227167`, code bytes rose by `+1564`, total bytes fell by `-225603`, steps fell by `-539`, and the checkpoint-to-export gap moved from `-0.01021103` to `-0.00095861` (`+0.00925242`). Versus clean seed-42, post-export regressed by `+0.01050630`; versus clean seed-1337, by `+0.00999690` | This bundled frontier optimization transfer does not transfer cleanly to `record06 + LeakyReLU^2`: EMA made the checkpoint much more compression-friendly and the artifact smaller, but raw checkpoint quality regressed first and the final exported score failed badly versus both `arch_009` and the clean baselines | Reject `EMA + warmdown3500` on this lineage and return to `arch_009` as the active baseline; next, run the controlled `SEED=1337` confirmation on `arch_009` before stacking another motif |
| 2026-03-25 | `repro_003_record06_leakyrelu2_seed1337_confirm` | evaluation | Clean shim-free seed-1337 baseline `repro_002_record06_seed1337_shimfree_confirm` at checkpoint `1.1543`, post-export `1.14374449`, artifact `15630471`, total `15683401`, `step=6679`, `swa:start=5500`, `24` SWA checkpoints; mechanism baseline `arch_009_record06_leakyrelu2_keep_swa_export` at checkpoint `1.1509`, post-export `1.14068897`, artifact `15908521`, total `15961473`, `step=6597`, `swa:start=5450`, `23` SWA checkpoints | If the positive `LeakyReLU(0.5)^2` transfer is a real recipe gain rather than a seed-42-only landing, then rerunning the same shim-free recipe with only `SEED=1337` changed should beat the clean seed-1337 baseline and keep the updated transferred mean below the old clean 2-seed mean `1.14348979` | The seed-42 activation transfer was already clearly positive; this was the smallest controlled way to decide whether later aggressive bundling should build on the new line or on the older clean baseline | Reused the `arch_009` run-local script unchanged and changed only `SEED=42 -> 1337` in the managed launch command | `LeakyReLU(0.5)^2`, archived `record06` geometry, `MLP3x`, `BigramHash(10240)`, `SmearGate`, `WEIGHT_DECAY=0.04`, `WARMDOWN_ITERS=3000`, `SWA_ENABLED=1`, `SWA_START_FRAC=0.4`, `SWA_EVERY=50`, shim-free `physicslm`, no shimmed `PYTHONPATH`, same managed 8-GPU launch path, same `600s` cap, `sliding_window stride=64`, final mixed `int5/int6 + zstd-22` export, and export/load schema | Beat clean seed-1337 `1.14374449`, improve the updated transferred mean beyond `1.14348979`, stay under `16,000,000`, and preserve direct-runtime `zstd` plus exact roundtrip/load correctness | The full managed run stayed shim-free in `physicslm`, stopped at `step=6603` in `600031ms`, matched `swa:start step:5450`, applied `24` SWA checkpoints, emitted direct-runtime `zstd` with header `28 b5 2f fd`, and scored `1.14276256` post-export with artifact `15641575`, code `52952`, total `15694527`, checkpoint `val_bpb=1.1529`, and exact roundtrip/load correctness. Versus clean seed-1337, checkpoint improved by `-0.0014`, post-export improved by `-0.00098193`, artifact bytes rose by `+11104`, code bytes by `+22`, total bytes by `+11126`, and steps changed by `-76`. Versus seed-42 `arch_009`, checkpoint regressed by `+0.0020`, post-export regressed by `+0.00207359`, artifact and total bytes fell by `-266946`, steps changed by `+6`, and the checkpoint-to-export gap moved from `-0.01021103` to `-0.01013744` (`+0.00007359`). The updated transferred 2-seed mean became `1.14172576`, which beats the old clean local 2-seed mean by `-0.00176403` and the archived clean 3-seed mean by `-0.00103424` | This is a positive confirmation: the `LeakyReLU^2` transfer survives the second archived local seed, stays comfortably byte-safe, and is strong enough to promote `record06 + LeakyReLU^2 + SWA` to the new active local baseline. The only caution is mild seed variance: the seed-1337 result narrowly missed the brief's secondary `<=0.002` band versus seed-42 by `0.00007359` | Run the same confirmed transfer on archived seed `2024` to resolve the remaining variance question before stacking another frontier motif on top |
| 2026-03-25 | `repro_004_record02_11l_ema_gptqlite_warmdown3500_qat015_local_repro` | architecture + optimization + export | Active local best `arch_009_record06_leakyrelu2_keep_swa_export` at checkpoint `1.1509`, post-export `1.14068897`, artifact `15908521`, total `15961473`; documented record-`#2` target `1.12278022`, bytes `15555017`, seed-1337 steps `7101` | If the remaining local gap is mainly search-space lag rather than seed variance on `record06`, then a faithful local reproduction of the documented leaderboard `#2` stack should beat `1.14068897` and land materially closer to `1.12278022` | This directly tested the strongest currently documented portable non-TTT frontier stack by extracting the record-`#2` markdown script into a run-local `train_gpt.py` and reproducing its `11L + EMA + warmdown3500 + late QAT@0.15 + GPTQ-lite` recipe as a whole | Switched to the record-`#2` stack: `11L`, `MLP3x`, `XSA4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA decay=0.997`, tight `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite int6 clip search, `SEED=1337`; one explicit compatibility delta only: local fallback from missing `flash_attn_interface.flash_attn_func` to `torch.nn.functional.scaled_dot_product_attention(..., enable_gqa=True)` | Dataset path, tokenizer path, shim-free `physicslm`, managed 8-GPU launch, `600s` train-time cap, `zstd-22`, final comparison metric `sliding_window stride=64`, and artifact accounting under `16,000,000` | Beat local best `1.14068897`, stay under the byte cap, and reproduce the documented `#2` markers closely enough that any miss is interpretable as a portability/runtime delta rather than a missing-stack failure | Smoke passed instantiate/export/reload/eval on 8 GPUs with exact roundtrip/load correctness and direct-runtime `zstd`. The full run finished in `physicslm` at `step=6084` under `600017ms`, logged checkpoint `val_bpb=1.1446`, applied `EMA` at the end with diagnostic `val_bpb=1.1437`, started `SWA` at `5400`, enabled late QAT at `step=5564 scale=0.1498`, and scored `1.12837920` post-export under `sliding_window stride=64`; non-overlapping roundtrip score was `1.15214005`. Artifact bytes were `15733455`, code bytes `68137`, total bytes `15801592`, the artifact header was `28 b5 2f fd` (`zstd`), and exact roundtrip/load correctness passed. Versus `arch_009`, post-export improved by `-0.01230977` and total bytes by `-159881`. Versus documented record-`#2`, post-export remained `+0.00559898` worse, total bytes `+246575` larger, and step count `-1017` lower | The record-`#2` stack reproduced locally in the main aggressive-phase sense and is the new active baseline: it beat the prior local best decisively while staying byte-safe. The remaining miss is best interpreted as a portability/runtime issue rather than a clean negative on the stack because all intended motifs were active, but the run needed an SDPA fallback for missing FlashAttention bindings and reached materially fewer steps than the documented record | Isolate the remaining portability blocker inside this exact `11L` stack, starting with the attention/runtime path rather than returning to `record06` seed-variance work |
| 2026-03-25 | `opt_008_record02_cudnn_sdpa_fallback_isolation` | optimization | Active local baseline `repro_004_record02_11l_ema_gptqlite_warmdown3500_qat015_local_repro` at checkpoint `1.1446`, post-EMA `1.1437`, post-export `1.12837920`, artifact `15733455`, code `68137`, total `15801592`, `step=6084`; documented record-`#2` `1.12278022`, total `15555017`, seed-1337 `step=7101` | If the remaining gap on the reproduced `#2` stack is mainly caused by the local attention runtime path, then changing only the SDPA fallback backend selection from flash-only to cuDNN-only should recover steps and improve exported score without changing the wrapper or recipe | `repro_004` had already reproduced the full stack positively, leaving the backend mismatch as the cleanest remaining fidelity break; cuDNN SDPA might be the better local GQA-compatible fallback on `8xL20Z` | Changed only the SDPA backend flags to `cudnn=True flash=False mem_efficient=False math=False`, added one backend-request log line, and added one runtime probe showing SDPA eligibility plus observed dispatch; kept the fallback call itself as `F.scaled_dot_product_attention(..., enable_gqa=True)` | `SEED=1337`, run-local `repro_004` script outside the backend-toggle block, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, QAT threshold `0.15`, GPTQ-lite, `physicslm`, managed 8-GPU launch, `600s` cap, `zstd-22`, and `sliding_window stride=64` | Improve step count beyond `6084`, improve post-export score beyond `1.12837920`, preserve exact roundtrip/load correctness, and stay under the byte cap | Smoke passed instantiate/export/reload/eval with exact roundtrip/load correctness and a valid backend probe: `eligible_cudnn=True`, `eligible_flash=False`, `observed_backend=cudnn`, `observed_cudnn_active=True`. The full run finished at `step=6796` in `600032ms` with steady-state `88.29ms/step`, checkpoint `val_bpb=1.1407`, post-EMA `1.1397`, post-export non-overlapping `1.14806403`, post-export `sliding_window stride=64 val_bpb=1.12425244`, artifact `15484915`, code `71243`, total `15556158`, `swa:start=6150`, `late_qat:enabled step=6276 scale=0.1498`, and exact roundtrip/load correctness. Versus `repro_004`, steps improved by `+712`, exported score by `-0.00412676`, artifact bytes by `-248540`, and total bytes by `-245434`; versus documented record `#2`, the remaining gap shrank to `+0.00147222`, total bytes were only `+1141` higher, and steps were `-305` lower | This is a clean positive isolation result: the prior flash-only SDPA fallback was a real local blocker inside the reproduced `#2` stack, and cuDNN-only SDPA should replace it as the preferred local fallback for that lineage | Keep the cuDNN-only fallback fixed and run one seed-only confirmation next to decide whether the gain is robust across seeds before stacking another motif |
| 2026-03-25 | `repro_005_record02_cudnn_sdpa_seed42_confirm` | evaluation | Active local best `opt_008_record02_cudnn_sdpa_fallback_isolation` at checkpoint `1.1407`, post-EMA `1.1397`, post-export `1.12425244`, artifact `15484915`, code `71243`, total `15556158`, `step=6796`, `step_avg=88.29 ms`; contextual references `repro_004=1.12837920`, documented record-`#2` seed `1337=1.12278022`, documented seed `42≈1.1236` | If the cuDNN-only SDPA fallback is a real portability fix rather than a seed-1337-specific landing, then rerunning the exact same `opt_008` stack with only `SEED=42` changed should stay within `+0.0015` post-export `val_bpb` of `opt_008` and remain clearly better than `repro_004` | `opt_008` had already answered the backend selector question on one seed; the remaining uncertainty was robustness across seed, not backend choice itself | Reused the exact `opt_008` run-local script unchanged, copied it into a new run directory, verified the copied file stayed hash-identical at `71243` code bytes, and changed only `SEED=42` in the managed smoke/full launch commands | SDPA backend flags `cudnn=True flash=False mem_efficient=False math=False`, fallback wrapper `F.scaled_dot_product_attention(..., enable_gqa=True)`, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite, tokenizer/data paths, shim-free `physicslm`, managed 8-GPU launch, `600s` cap, `zstd-22`, and `sliding_window stride=64` | Stay near `1.12425244`, remain under the cap, preserve exact roundtrip/load correctness, and confirm the recovered cuDNN gain is not seed-fragile | Smoke passed on 8 GPUs with exact roundtrip/load correctness, direct-runtime `zstd`, and the same backend probe outcome as `opt_008`: `eligible_cudnn=True`, `eligible_flash=False`, `observed_backend=cudnn`, `observed_cudnn_active=True`. The full seed-42 run finished at `step=6796` in `600040ms` with steady-state `88.29ms/step`, checkpoint `val_bpb=1.1415`, post-EMA `1.1405`, post-export non-overlapping `1.14850831`, post-export `sliding_window stride=64 val_bpb=1.12484349`, artifact `15715799`, code `71243`, total `15787042`, `swa:start=6150`, `late_qat:enabled step=6276 scale=0.1499`, and exact roundtrip/load correctness. Versus `opt_008`, exported score changed by `+0.00059105`, steps by `+0`, artifact and total bytes by `+230884`, and code bytes by `+0`. Versus `repro_004`, exported score improved by `-0.00353571`, steps by `+712`, and total bytes by `-14550`. Versus documented references, the run was `+0.00206327` worse than documented seed-1337 and about `+0.00124349` worse than the documented seed-42 readout, while still meeting the predeclared `<=1.1258` threshold | This is a positive seed-robustness confirmation. The cuDNN-only fallback stayed within the declared `+0.0015` band of `opt_008`, kept identical code bytes and identical step geometry, and remained clearly better than the old flash-only fallback, so cuDNN-only SDPA should now be treated as the stable local fallback for the leaderboard-`#2` lineage. Because the two local seeds now cluster tightly, the remaining miss looks less like seed luck and more like a smaller remaining portability/runtime component | Keep the cuDNN-only fallback locked and shift the next round from seed confirmation to the remaining portability gap, starting with a bounded audit for a local native `flash_attn_interface.flash_attn_func` path |
| 2026-03-25 | `infra_002_record02_flashattention3_binding_audit` | evaluation | Active local best `opt_008_record02_cudnn_sdpa_fallback_isolation` at checkpoint `1.1407`, post-EMA `1.1397`, post-export `1.12425244`, artifact `15484915`, code `71243`, total `15556158`, `step=6796`; documented record-`#2` seed `1337=1.12278022`; seed-42 confirmation `repro_005=1.12484349` | If the remaining `~0.0013-0.0021` miss is mainly caused by the missing native `flash_attn_interface.flash_attn_func` path, then that exact binding should be importable and minimally executable inside the validated shim-free `physicslm` environment, justifying one controlled smoke and one full seed-`1337` comparison | `opt_008` and `repro_005` already stabilized the cuDNN-backed fallback across seeds, so the exact native FlashAttention 3 binding was the cleanest still-unmatched portability variable | Environment audit only: added one standalone audit helper and one managed 1-GPU audit run; no training script, architecture, optimization, evaluation, or export changes | Validated `physicslm` environment requirement for any scored run, exact reproduced leaderboard `#2` recipe, cuDNN-backed fallback baseline, dataset/tokenizer paths, and the rule that any scored comparison must change only the native attention path | Either prove the required binding exists in `physicslm` and proceed to smoke/full comparison, or prove it is unavailable and stop without launching a non-comparable scored run | Managed 1-GPU audit in `physicslm` reported `cuda_available=true`, `torch=2.10.0+cu128`, `flash_attn_interface_spec=null`, `flash_attn_spec=null`, and `status=missing_flash_attn_interface`, exiting `2`; no controlled smoke/full scored run was launched. Bounded availability checks found `loongflow_ml` also lacked top-level `flash_attn_interface` and native `zstandard`; a VLLM-packaged `flash_attn_interface.py` existed there but attempting to import `flash_attn_func` from it failed; `physicslm` exposed `xformers.flash_attn_3` only, not the audited binding | The native `flash_attn_interface.flash_attn_func` path is not locally controllable inside the validated `physicslm` environment, so this refinement question is answered negatively for now. Availability hints in other materially different package paths do not count as a controlled comparison, so environment chasing should stop here | Keep `opt_008` as the active best and pivot the next controlled refinement to a different locally controllable delta, such as removing the now-redundant backend probe/profiler code to recover code bytes and any tiny startup/runtime overhead without changing the cuDNN-backed recipe |
| 2026-03-26 | `arch_010_record02_leakyrelu2_keep_cudnn_recipe` | architecture | Active local best `opt_008_record02_cudnn_sdpa_fallback_isolation` at checkpoint `1.1407`, post-EMA `1.1397`, post-export non-overlapping `1.14806403`, post-export `sliding_window stride=64` `1.12425244`, artifact `15484915`, code `71243`, total `15556158`, `step=6796`, `88.29 ms/step`; secondary robustness reference `repro_005=1.12484349`; documented record-`#2` `1.12278022` | If `LeakyReLU(0.5)^2` is a portable improvement rather than a `record06`-specific effect, then changing only the MLP activation on the exact `opt_008` stack should improve post-export `sliding_window stride=64 val_bpb` beyond `1.12425244` with little or no change in step geometry | The live SOTA snapshot identifies `LeakyReLU(0.5)^2` as the defining current-`#1` motif, and the local `record06` lineage already transferred it positively across two seeds; this isolates that motif on the strongest reproduced leaderboard-`#2` stack | Changed only the MLP activation line in the copied `opt_008` run-local script: `torch.relu(...) -> F.leaky_relu(..., negative_slope=0.5)` | Exact `opt_008` script outside that one activation line, `SEED=1337`, cuDNN-only SDPA fallback flags, fallback wrapper, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite, tokenizer/data paths, shim-free `physicslm`, managed 8-GPU launch, `600s` cap, `zstd-22`, and `sliding_window stride=64` | Improve post-export `sliding_window stride=64 val_bpb` beyond `1.12425244` while keeping step geometry close enough that the result remains a clean activation-only comparison | Structural verification passed before launch: activation-only text diff, identical `121` state-dict keys/shapes, identical `190` export tensor keys, and identical export meta. Managed 8-GPU smoke passed with exact roundtrip/load correctness and the same cuDNN probe result as `opt_008`. The full seed-1337 run finished at `step=6734` in `599994ms` with steady-state `89.10 ms/step`, checkpoint `val_bpb=1.1384`, post-EMA `1.1375`, post-export non-overlapping `1.14584172`, post-export `sliding_window stride=64 val_bpb=1.12211549`, artifact `15555121`, code `71265`, total `15626386`, `swa:start=6050`, `late_qat:enabled step=6214 scale=0.1498`, exact roundtrip/load correctness, and observed cuDNN SDPA dispatch | This is a clean positive refinement result. The activation-only change improved checkpoint, post-EMA, non-overlapping export, and the primary post-export sliding score, beating `opt_008` by `-0.00213695`, beating `repro_005` by `-0.00272800`, and even edging the documented record-`#2` seed-1337 readout by `-0.00066473`. Step geometry moved only modestly (`-62` steps, `+0.81 ms/step`), so the gain is interpretable as an activation effect rather than a runtime confound | Promote `arch_010` to the new active local baseline and run a seed-only `SEED=42` confirmation next before stacking another motif |
| 2026-03-26 | `repro_006_record02_leakyrelu2_seed42_confirm` | evaluation | Active local best `arch_010_record02_leakyrelu2_keep_cudnn_recipe` at checkpoint `1.1384`, post-EMA `1.1375`, post-export non-overlapping `1.14584172`, post-export `sliding_window stride=64` `1.12211549`, artifact `15555121`, code `71265`, total `15626386`, `step=6734`, `89.10 ms/step`; seed-42 pre-activation reference `repro_005=1.12484349`; documented record-`#2` seed `1337=1.12278022` | If the `LeakyReLU(0.5)^2` gain on the reproduced leaderboard-`#2` stack is real rather than a seed-1337-only landing, then rerunning the exact `arch_010` script with only `SEED=42` changed should stay within `+0.0015` of `1.12211549` and still beat `1.12484349` | The seed-1337 activation transfer was already positive; the remaining refinement question was robustness on the exact new best script, not mechanism plausibility | Reused the copied `arch_010` run-local script unchanged and changed only `SEED=42` in the managed smoke/full launch commands | Exact `arch_010` script at `71265` code bytes, `LeakyReLU(0.5)^2`, cuDNN-only SDPA fallback flags, fallback wrapper, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite, tokenizer/data paths, shim-free `physicslm`, managed 8-GPU launch, `600s` cap, `zstd-22`, and `sliding_window stride=64` | Stay within `+0.0015` of `1.12211549`, still beat `1.12484349`, preserve identical code bytes and cuDNN dispatch, and keep step geometry near `6734 @ 89.10 ms/step` | Copy verification showed identical SHA-256 and identical `71265` code bytes. Smoke logged exact roundtrip/load correctness, `final_int6_roundtrip_exact val_bpb=4.10536258`, and the same cuDNN probe outcome as `arch_010`; the smoke runner then hung in distributed teardown after result capture and was terminated only after the outputs were already on disk. The full run exited cleanly with checkpoint `1.1390`, post-EMA `1.1381`, post-export non-overlapping `1.14644940`, post-export `sliding_window stride=64` `1.12281449`, artifact `15802352`, code `71265`, total `15873617`, `step=6737`, `89.07 ms/step`, `swa:start=6050`, `late_qat:enabled step=6215 scale=0.1499`, exact roundtrip/load correctness, and observed cuDNN SDPA dispatch | This is a positive seed-robustness confirmation. The seed-42 rerun stayed only `+0.00069900` behind `arch_010`, comfortably inside the declared band, and still beat the pre-activation seed-42 reference by `-0.00202900`, so `LeakyReLU(0.5)^2` should now be treated as the stable local baseline on the reproduced leaderboard-`#2` lineage. The main caveat is byte variance: the seed-42 artifact was `+247231` bytes larger than seed-1337 even though score and step geometry stayed close | If three-seed evidence is desired before stacking another motif, run one more exact seed-only confirmation at `SEED=2024`; otherwise use the now-stable `arch_010` lineage for the next beyond-`#2` controlled refinement |
| 2026-03-26 | `repro_007_record02_leakyrelu2_seed2024_confirm` | evaluation | Exact-script `arch_010` seed `1337` at post-export `1.12211549`, total `15626386`, `step=6734`; exact-script seed `42` at post-export `1.12281449`, total `15873617`, `step=6737`; pre-activation seed-42 reference `repro_005=1.12484349`; documented record-`#2` seed `1337=1.12278022`, mean band `~1.1233` | If `LeakyReLU(0.5)^2` is a stable improvement on the reproduced leaderboard-`#2` stack rather than a favorable 2-seed landing, then changing only `SEED -> 2024` on the exact `arch_010` script should stay within `+0.0015` of `1.12211549`, remain better than `1.12484349`, and keep the 3-seed mean at or below `~1.1233` | The first two exact-script seeds already clustered tightly with the same cuDNN-backed SDPA geometry, so a third exact seed was the smallest remaining test of whether this line was stable enough to lock | Reused the copied `arch_010` run-local script unchanged and changed only `SEED=2024` in the managed smoke/full launch commands; added one eval-only recovery helper after training only because the full log was interrupted before its final stride-64 export lines were flushed | Exact `arch_010` script at `71265` code bytes, `LeakyReLU(0.5)^2`, cuDNN-only SDPA fallback flags, fallback wrapper, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite, tokenizer/data paths, shim-free `physicslm`, managed 8-GPU launch, `600s` cap, `zstd-22`, and `sliding_window stride=64` | Stay within `+0.0015` of `1.12211549`, keep the exact 3-seed mean `<= 1.1233`, remain better than the pre-activation reference, preserve identical code bytes and cuDNN dispatch, and keep step geometry near `6734-6737 @ ~89.1 ms/step` | Copy verification showed identical SHA-256 and identical `71265` code bytes. Smoke logged exact roundtrip/load correctness, `final_int6_roundtrip_exact val_bpb=4.10666993`, and the same cuDNN probe outcome as `arch_010`; the smoke runner then hung in distributed teardown after result capture and was terminated only after the outputs were already on disk. The full training run reached the wallclock cap and wrote checkpoint `1.1389`, post-EMA `1.1380`, artifact `15761090`, total `15832355`, post-export non-overlapping `1.14585946`, `step=6725`, `89.22 ms/step`, `swa:start=6050`, and `late_qat:enabled step=6206 scale=0.1498` before the process remained alive in its final export-eval path. A separate managed 8-GPU eval-only recovery on the saved `final_model.pt` and `final_model.int6.ptz` then recovered post-export `sliding_window stride=64 val_bpb=1.12225480` with exact roundtrip/load correctness and the same direct-runtime `zstd` artifact header `28 b5 2f fd`. The exact 3-seed mean became `1.12239493`; deltas were `+0.00013931` vs `arch_010`, `-0.00055969` vs `repro_006`, `-0.00258869` vs `repro_005`, `-0.00052542` vs documented record `#2`, and `+0.00285480` vs live SOTA `1.1194` | This is a positive third-seed robustness confirmation. The exact `LeakyReLU(0.5)^2` script stayed well inside the predeclared band, remained better than the pre-activation reference, and kept the exact 3-seed mean below the documented record-`#2` mean band, so `arch_010` is now a credible locked 3-seed baseline for the next refinement round. The main residual caution remains byte variance rather than score variance | Stop spending rounds on more seed-only confirmation for this exact script and move to one controlled beyond-`#2` single-variable refinement on top of the locked `arch_010` baseline |
| 2026-03-26 | `eval_002_arch010_legal_ttt_seed1337_export_eval` | evaluation | Locked `arch_010` seed-1337 exported lineage at archived post-export `sliding_window stride=64 val_bpb=1.12211549`; compare same-script recovered no-TTT eval vs same-script legal score-first TTT on the exact same saved `final_model.pt` / `final_model.int6.ptz` source | If legal score-first TTT transfers to the locked `arch_010` lineage, then enabling the record-`#1` TTT recipe on the exact same saved seed-1337 export lineage should improve post-export `val_bpb` relative to the same-script no-TTT baseline | The current live `#1` evidence says legal TTT was the larger beyond-`#2` evaluation motif, while Parallel Muon was not the main pre-TTT contributor; this isolates that missing motif alone without retraining or export changes | Added only evaluation-side support to a run-local copy of `arch_010`: eval-only artifact loading, TTT env parsing, legal score-first chunk scheduling, eval-time TTT update path, and a final conditional TTT eval call; experimental variable was `TTT_ENABLED=0 -> 1` with fixed TTT recipe `LR=0.002`, `EPOCHS=3`, `CHUNK_TOKENS=32768`, `FREEZE_BLOCKS=0`, `MOMENTUM=0.9`, `BATCH_SEQS=32`, `GRAD_CLIP=1.0` | Saved seed-1337 `arch_010` checkpoint/export lineage, `LeakyReLU(0.5)^2`, cuDNN-backed SDPA fallback path, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite int6 export path, tokenizer/data paths, shim-free `physicslm`, `sliding_window stride=64`, no Parameter Banking, no Parallel Muon, no retraining, no artifact rewrite | Recover the archived no-TTT score within `±0.0002`, then see a useful TTT gain of roughly `-0.0010` to `-0.0025`, ideally at least `-0.0008` | The same-script no-TTT recovery scored `1.12211550`, only `+0.00000001` from the archived `1.12211549`, so the parity gate passed cleanly. The same-script legal TTT eval on the exact same saved artifact then scored `1.11971476`, a gain of `-0.00240074` versus the recovered no-TTT baseline, `-0.00268017` versus the locked 3-seed mean `1.12239493`, and `+0.00031476` versus live SOTA `1.1194`. Artifact bytes stayed unchanged at `15555121`; the run-local helper was `84059` code bytes for `15639180` total bytes. TTT logs recorded `1893` chunks of `32768` tokens, `score_first=True`, `last_chunk_untrained=True`, and `freeze_blocks=0`. Eval wallclock was `100456ms` for no-TTT and `396112ms` for TTT | This is a strong positive evaluation-only transfer. The parity gate shows the run-local helper is faithful, and legal score-first TTT closes almost the entire remaining gap to live SOTA without retraining or changing the artifact. Because the gain exceeds the brief’s strong-transfer threshold, the next useful step is a second-seed confirmation rather than a TTT hyperparameter sweep | Reuse the exact `eval_002` helper and run the same saved-artifact parity plus legal-TTT confirmation on the locked seed-42 export lineage before stacking any additional motif |
| 2026-03-26 | `eval_003_arch010_legal_ttt_seed42_confirm` | evaluation | Saved seed-42 `arch_010` export lineage at archived post-export `sliding_window stride=64 val_bpb=1.12281449`; compare same-script recovered no-TTT eval vs same-script legal score-first TTT on that exact saved `final_model.pt` / `final_model.int6.ptz` source | If the seed-1337 legal score-first TTT gain is a real evaluation-side transfer on locked `arch_010`, then applying the exact same same-script parity-plus-TTT protocol to the saved seed-42 export lineage should again beat the recovered same-script no-TTT baseline by at least `-0.0008` | Seed-1337 already answered the transfer question positively, so the next refinement question was robustness across a second saved export seed, not a broader stack change | Changed only the saved export lineage under evaluation from seed `1337 -> 42` while keeping the copied `eval_002` helper byte-identical at `84059`; operational toggle remained `TTT_ENABLED=0 -> 1` with fixed TTT recipe `LR=0.002`, `EPOCHS=3`, `CHUNK_TOKENS=32768`, `FREEZE_BLOCKS=0`, `MOMENTUM=0.9`, `BATCH_SEQS=32`, `GRAD_CLIP=1.0` | Exact copied eval helper, saved seed-42 `arch_010` checkpoint/export lineage, `LeakyReLU(0.5)^2`, cuDNN-backed SDPA fallback path, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite int6 export path, tokenizer/data paths, shim-free `physicslm`, `sliding_window stride=64`, no retraining, and no artifact rewrite | Recover the archived no-TTT score within `±0.0002`, then see another clearly positive legal-TTT gain near the seed-1337 landing | The same-script no-TTT recovery scored `1.12281449`, exactly matching the archived seed-42 baseline, so the parity gate passed cleanly. The same-script legal TTT eval on the exact same saved artifact then scored `1.12032675`, a gain of `-0.00248774` versus the recovered no-TTT baseline, `-0.00206818` versus the locked non-TTT 3-seed mean `1.12239493`, `+0.00061199` versus the seed-1337 TTT result `1.11971476`, and `+0.00092675` versus live SOTA `1.1194`. Artifact bytes stayed unchanged at `15802352`; helper code bytes stayed `84059` for `15886411` total bytes. TTT logs again recorded `1893` chunks of `32768` tokens, `score_first=True`, `last_chunk_untrained=True`, and `freeze_blocks=0`. Script eval wallclock was `86107ms` for no-TTT and `404338ms` for TTT; managed wallclock was `128s` and `444s` | This is a second strong positive evaluation-only transfer. The same-script parity gate again passed exactly, and the seed-42 legal-TTT gain is slightly stronger than the seed-1337 gain (`-0.00248774` vs `-0.00240074`), which is enough to promote legal score-first TTT as the stable evaluation-side regime on locked `arch_010` rather than a one-seed landing | Promote legal score-first TTT operationally on locked `arch_010`; if three-seed evaluation-only evidence is still desired before treating it as fully locked, run the exact same helper on the saved seed-2024 export lineage |
| 2026-03-26 | `eval_004_arch010_legal_ttt_seed2024_confirm` | evaluation | Saved seed-2024 `arch_010` export lineage at archived post-export `sliding_window stride=64 val_bpb=1.12225480`; compare same-script recovered no-TTT eval vs same-script legal score-first TTT on that exact saved `final_model.pt` / `final_model.int6.ptz` source | If legal score-first TTT is fully robust on locked `arch_010` rather than a favorable two-seed landing, then the exact same same-script parity-plus-TTT protocol on the saved seed-2024 export should again beat the recovered same-script no-TTT baseline by at least `-0.0008` | The first two saved export seeds were already strongly positive, so the remaining refinement question was whether the exact evaluation-only gain stays positive on the third locked saved export lineage | Changed only the saved export lineage under evaluation from seed `42 -> 2024` while keeping the copied `eval_003` helper byte-identical at `84059`; operational toggle remained `TTT_ENABLED=0 -> 1` with fixed TTT recipe `LR=0.002`, `EPOCHS=3`, `CHUNK_TOKENS=32768`, `FREEZE_BLOCKS=0`, `MOMENTUM=0.9`, `BATCH_SEQS=32`, `GRAD_CLIP=1.0` | Exact copied eval helper, saved seed-2024 `arch_010` checkpoint/export lineage, `LeakyReLU(0.5)^2`, cuDNN-backed SDPA fallback path, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite int6 export path, tokenizer/data paths, shim-free `physicslm`, `sliding_window stride=64`, no retraining, and no artifact rewrite | Recover the archived no-TTT score within `±0.0002`, then see another clearly positive legal-TTT gain near the first two saved-seed landings | The same-script no-TTT recovery scored `1.12225535`, only `+0.00000055` from the archived seed-2024 baseline, so the parity gate passed cleanly. The same-script legal TTT eval on the exact same saved artifact then scored `1.12027199`, a gain of `-0.00198336` versus the recovered no-TTT baseline, `-0.00212294` versus the locked non-TTT 3-seed mean `1.12239493`, `+0.00055723` versus the seed-1337 TTT result `1.11971476`, `-0.00005476` versus the seed-42 TTT result `1.12032675`, and `+0.00087199` versus live SOTA `1.1194`. Artifact bytes stayed unchanged at `15761090`; helper code bytes stayed `84059` for `15845149` total bytes. TTT logs again recorded `1893` chunks of `32768` tokens, `score_first=True`, `last_chunk_untrained=True`, and `freeze_blocks=0`. Script eval wallclock was `86179ms` for no-TTT and `400348ms` for TTT; managed wallclock was `126s` and `441s` | This is a third strong positive evaluation-only transfer. The same-script parity gate again passed cleanly, and although the seed-2024 gain is weaker than the first two by about `+0.0004` to `+0.0005`, it remains comfortably above the strong-positive threshold and does not collapse. Legal score-first TTT should now be treated as fully locked evaluation-side behavior on `arch_010` across all three saved export seeds | Stop spending more rounds on seed-only legal-TTT confirmation for locked `arch_010`; move to one new controlled beyond-SOTA variable, most directly the remaining current-`#1` training-side motif `Parallel Muon` |
| 2026-03-26 | `pending_opt_009_arch010_parallel_muon_seed1337_nottt` | optimization | Locked `arch_010` seed-`1337` no-TTT post-export `sliding_window stride=64 val_bpb=1.12211549`; context-only comparisons `3`-seed no-TTT mean `1.12239493`, legal-TTT `3`-seed mean `1.12010450`, live SOTA `1.1194` | If `Parallel Muon` is the remaining missing training-side lever on locked `arch_010`, then adding only that motif should improve the standard no-TTT exported `sliding_window stride=64 val_bpb` by at least `-0.0005` versus `1.12211549` | Live `#1` is `LeakyReLU² + Legal Score-First TTT + Parallel Muon`; on local `arch_010`, `LeakyReLU²` and legal TTT were already locked, so `Parallel Muon` was the clearest remaining unmatched training-side motif | Forked the exact locked `arch_010` script into a run-local directory and implemented the narrowest viable controlled transfer: a row-sharded async reduce-scatter/local Newton-Schulz/all-gather Muon path over the existing 2D parameter set, with `PARALLEL_MUON=1`, DDP disabled only for that path, manual replicated-grad all-reduce, and no Parameter Banking or export-schema change; a managed 8-GPU smoke and one managed 8-GPU full run were executed, followed by one managed 8-GPU eval-only recovery of the missing final stride-64 exported metric from the saved artifact after distributed teardown stalled | Exact `arch_010` architecture and feature stack, `SEED=1337`, `LeakyReLU(0.5)^2`, `11L / 512 / 8 heads / 4 KV heads / MLP3x`, `XSA_LAST_N=4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA decay=0.997`, `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, GPTQ-lite int6 export lineage, tokenizer/data paths, shim-free `physicslm`, managed 8-GPU launch, cuDNN SDPA fallback path, evaluation protocol `sliding_window stride=64`, no legal-TTT in the primary readout, and matched parameter/export schema | Improve no-TTT exported `sliding_window stride=64 val_bpb`, preserve structural parity, and ideally keep or improve step geometry versus `6734 @ 89.10 ms/step` | Structural parity smoke passed exactly: parameter count stayed `26993756`, state-dict keys stayed identical at `121`, export tensor keys stayed identical at `190`, export metadata stayed identical, and cuDNN SDPA dispatch stayed matched. The full run hit the `600s` cap at only `4939` steps with checkpoint `val_bpb=1.1577`, post-EMA `1.1573`, artifact `16826625`, code `77935`, total `16904560`, post-export non-overlapping `1.16429555`, and exact roundtrip/load correctness. Because the process stalled before flushing the final stride-64 export line, a separate managed 8-GPU eval-only recovery on the saved `final_model.pt` and `final_model.int6.ptz` measured post-export `sliding_window stride=64 val_bpb=1.14079891` with eval wallclock `86271ms`. Deltas were `+0.01868342` vs locked seed-1337 baseline, `+0.01840398` vs locked no-TTT 3-seed mean, `+0.02139891` vs live SOTA, `-1795` steps and `+32.38 ms/step` versus locked geometry, and `+904560` bytes over the cap | This is a clean controlled negative. The narrowed row-sharded `Parallel Muon` port preserved structure and backend behavior, but it severely degraded runtime geometry, triggered late QAT almost immediately, blew the byte cap, and missed the primary exported score by a wide margin, so `Parallel Muon` alone is not a viable next lever on locked `arch_010` under this control regime | Reject this `Parallel Muon` path on locked `arch_010`; do not restack it with legal TTT, and move to a different single motif rather than bundling more training/runtime changes |
| 2026-03-26 | `eval_005_arch010_legal_ttt_lr0025_seed1337` | evaluation | Locked seed-`1337` legal-TTT baseline `eval_002=1.11971476`; contextual comparisons same-script no-TTT parity `1.12211550`, locked legal-TTT 3-seed mean `1.12010450`, live SOTA `1.1194` | If the locked `arch_010` legal-TTT line is still mildly under-adapting at eval time, then increasing only `TTT_LR` from `0.002` to `0.0025` should improve the seed-`1337` legal-TTT score by at least `-0.0002` and possibly cross live SOTA | The locked seed-`1337` legal-TTT result already sat within `+0.00031476` of live SOTA, so a one-variable evaluation-side LR refinement was the cleanest remaining local test after the negative `Parallel Muon` round | Reused the exact saved seed-`1337` `arch_010` checkpoint/export lineage and copied the exact `eval_004` helper byte-for-byte into a fresh run directory; changed only `TTT_LR=0.002 -> 0.0025` | Exact helper lineage at `84059` bytes and `sha256=d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8`, exact saved seed-`1337` artifact hashes/bytes, legal score-first scheduling, `TTT_EPOCHS=3`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `sliding_window stride=64`, shim-free `physicslm`, managed 8-GPU launch, no retraining, no export rewrite, no artifact rewrite | Improve the locked seed-`1337` legal-TTT score by at least `-0.0002`, ideally cross `1.1194`, and keep eval time comparable to the locked `TTT_LR=0.002` run | Identity guardrail passed without drift: copied helper remained `84059` bytes with the same SHA-256, saved `final_model.pt` stayed `106178569` bytes with hash `b8291ad1...`, and saved `final_model.int6.ptz` stayed `15555121` bytes with hash `eb062c96...`. The single managed 8-GPU eval-only run then scored `legal_ttt_exact val_bpb=1.11956042` with `val_loss=1.89032587`, a gain of `-0.00015434` vs locked seed-`1337` TTT baseline `1.11971476`, `-0.00255508` vs same-script no-TTT parity baseline `1.12211550`, `-0.00054408` vs locked legal-TTT 3-seed mean `1.12010450`, and `+0.00016042` vs live SOTA `1.1194`; script eval wallclock was `397765ms` and managed wallclock `438s` | This is a controlled positive refinement, but smaller than target. The setting lands in the brief’s strong-positive absolute-score band and becomes the best local single-seed result, yet it misses the planned `-0.0002` gain threshold and still stops just above live SOTA, so the evidence supports only mild remaining LR headroom rather than a decisive misspecification | Repeat the exact locked-helper, locked-artifact `TTT_LR=0.0025` eval-only test on seeds `42` and `2024` if evaluation budget is available; otherwise move to a different single-variable refinement rather than assuming further nearby TTT-LR tuning will pay off |
| 2026-03-26 | `eval_006_arch010_legal_ttt_epochs4_seed1337` | evaluation | Locked seed-`1337` legal-TTT baseline `eval_005=1.11956042`; contextual comparisons `eval_002=1.11971476`, same-script no-TTT parity `1.12211550`, locked legal-TTT 3-seed mean `1.12010450`, live SOTA `1.1194` | If the locked seed-`1337` `arch_010` legal-TTT regime is still mildly adaptation-budget-limited after the confirmed LR gain, then increasing only `TTT_EPOCHS` from `3` to `4` should beat `1.11956042` without pushing eval cost too close to the practical `10 min` limit | Legal score-first TTT remains part of the live `#1` motif, the training-side `Parallel Muon` transfer already failed locally, and the eval-side `TTT_LR=0.0025` gain on this exact artifact suggested that any remaining headroom was more likely in adaptation budget than in another nearby training change | Reused the exact saved seed-`1337` `arch_010` checkpoint/export lineage and copied the exact `eval_005` helper byte-for-byte into a fresh run directory; changed only `TTT_EPOCHS=3 -> 4` | Exact helper lineage at `84059` bytes and `sha256=d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8`, exact saved seed-`1337` artifact hashes/bytes, legal score-first scheduling, `TTT_LR=0.0025`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `sliding_window stride=64`, shim-free `physicslm`, managed 8-GPU launch, no retraining, no export rewrite, no artifact rewrite | Beat `1.11956042`, ideally cross `1.1194`, and remain sufficiently below the `10 min` eval limit to keep the result promotable if it reproduces | Identity guardrail passed without drift: copied helper remained `84059` bytes with the same SHA-256, saved `final_model.pt` stayed `106178569` bytes with hash `b8291ad1...`, and saved `final_model.int6.ptz` stayed `15555121` bytes with hash `eb062c96...`. The single managed 8-GPU eval-only run then scored `legal_ttt_exact val_bpb=1.11935008` with `val_loss=1.88997072`, a gain of `-0.00021034` vs locked seed-`1337` `TTT_EPOCHS=3` baseline `1.11956042`, `-0.00036468` vs locked seed-`1337` `TTT_LR=0.002` baseline `1.11971476`, `-0.00276542` vs same-script no-TTT parity baseline `1.12211550`, `-0.00075442` vs locked legal-TTT 3-seed mean `1.12010450`, and `-0.00004992` vs live SOTA `1.1194`; script eval wallclock was `459677ms` and managed wallclock `502s` | This is a controlled positive refinement and a single-seed breakthrough. The setting beats the explicit `-0.0002` target, becomes the strongest local single-seed result, and crosses live SOTA while staying under the real eval budget, but it is still only one seed and the eval cost rises materially versus `eval_005` | Run the exact locked-helper, locked-artifact `TTT_LR=0.0025`, `TTT_EPOCHS=4` eval-only confirmation on seeds `42` and `2024`; if those stay positive and budget-safe, promote the new setting, otherwise stop nearby TTT-budget tuning |
| 2026-03-26 | `eval_007_arch010_legal_ttt_epochs4_seed2024_confirm` | evaluation | Primary decision baseline `eval_004_arch010_legal_ttt_seed2024_confirm=1.12027199`; context same-script no-TTT parity `1.12225535`, `eval_006=1.11935008`, locked legal-TTT 3-seed mean `1.12010450`, live SOTA `1.1194` | If the `eval_006` gain reflects real remaining eval-time adaptation headroom rather than a seed-`1337` one-off, then applying the exact same locked `TTT_LR=0.0025`, `TTT_EPOCHS=4` setting to the saved seed-`2024` artifact should improve over `1.12027199` while remaining under the practical eval-time budget | Seed `2024` previously had the weakest absolute landing among the three locked seeds at the older TTT setting, so it is the strongest discriminating confirmation seed for the refined `eval_006` recipe | Copied the exact `eval_006` helper byte-for-byte into a fresh run directory and changed only the evaluated artifact lineage from seed `1337 -> 2024` while keeping `TTT_LR=0.0025`, `TTT_EPOCHS=4`, and all other TTT settings fixed | Exact helper lineage at `84059` bytes and `sha256=d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8`, exact saved seed-`2024` artifact hashes/bytes (`final_model.pt` `106178569` / `302fe0ad...`, `final_model.int6.ptz` `15761090` / `479e5ccc...`), legal score-first scheduling, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `sliding_window stride=64`, shim-free `physicslm`, managed 8-GPU launch, no retraining, no export rewrite, no artifact rewrite, no same-script no-TTT rerun because identity guardrail passed | Beat the primary seed-`2024` baseline `1.12027199`, ideally land at or below `1.12010`, and remain sufficiently below the `10 min` eval limit to justify the final seed-`42` confirmation if positive | Identity guardrail passed without drift: copied helper remained `84059` bytes with the same SHA-256, and the saved seed-`2024` `final_model.pt` / `final_model.int6.ptz` stayed unchanged at `106178569` / `302fe0ad...` and `15761090` / `479e5ccc...`. The managed 8-GPU eval-only run scored `legal_ttt_exact val_bpb=1.11994397` with `val_loss=1.89097347`, a gain of `-0.00032802` vs the primary seed-`2024` legal-TTT baseline `1.12027199`, `-0.00231138` vs the inherited same-script no-TTT parity baseline `1.12225535`, `+0.00059389` vs `eval_006`, `-0.00016053` vs the locked legal-TTT 3-seed mean `1.12010450`, and `+0.00054397` vs live SOTA `1.1194`; script eval wallclock was `465406ms` and managed wallclock `506s` | This is a controlled positive refinement on the discriminating seed. The result beats the round’s primary decision baseline cleanly and stays in the “especially strong” band, but it is weaker than the seed-`1337` breakthrough and still above live SOTA, so it justifies the final seed-`42` confirmation rather than immediate full-line promotion | Run the exact locked-helper, locked-artifact `TTT_LR=0.0025`, `TTT_EPOCHS=4` eval-only confirmation on seed `42`; if that final seed also stays positive and budget-safe, promote the new setting, otherwise stop nearby TTT-budget tuning |
| 2026-03-26 | `eval_008_arch010_legal_ttt_epochs4_seed42_confirm` | evaluation | Primary decision baseline `eval_003_arch010_legal_ttt_seed42_confirm=1.12032675`; context same-script no-TTT parity `1.12281449`, `eval_006=1.11935008`, `eval_007=1.11994397`, locked legal-TTT 3-seed mean `1.12010450`, live SOTA `1.1194` | If the refined `TTT_LR=0.0025`, `TTT_EPOCHS=4` gain is real across the locked line rather than a two-seed coincidence, then applying the exact same locked setting to the saved seed-`42` artifact should improve over `1.12032675` while remaining under the practical eval-time budget | Seed `42` was the final unresolved locked seed, so this is the last controlled answer needed to decide whether the higher legal-TTT budget should be promoted across the line | Copied the exact `eval_007` helper byte-for-byte into a fresh run directory and changed only the evaluated artifact lineage from seed `2024 -> 42` while keeping `TTT_LR=0.0025`, `TTT_EPOCHS=4`, and all other TTT settings fixed | Exact helper lineage at `84059` bytes and `sha256=d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8`, exact saved seed-`42` artifact hashes/bytes (`final_model.pt` `106178569` / `c59cdd33...`, `final_model.int6.ptz` `15802352` / `3db765aa...`), legal score-first scheduling, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `sliding_window stride=64`, shim-free `physicslm`, managed 8-GPU launch, no retraining, no export rewrite, no artifact rewrite, no same-script no-TTT rerun because identity guardrail passed | Beat the primary seed-`42` baseline `1.12032675`, ideally land at or below `1.12010`, and if positive make the refined exact 3-seed mean beat the locked `TTT_EPOCHS=3` mean `1.12010450` while staying below the `10 min` eval limit | Identity guardrail passed without drift before and after launch: copied helper remained `84059` bytes with the same SHA-256, and the saved seed-`42` `final_model.pt` / `final_model.int6.ptz` stayed unchanged at `106178569` / `c59cdd33...` and `15802352` / `3db765aa...`. The managed 8-GPU eval-only run scored `legal_ttt_exact val_bpb=1.11998885` with `val_loss=1.89104925`, a gain of `-0.00033790` vs the primary seed-`42` legal-TTT baseline `1.12032675`, `-0.00282564` vs the inherited same-script no-TTT parity baseline `1.12281449`, `+0.00063877` vs `eval_006`, `+0.00004488` vs `eval_007`, and `+0.00058885` vs live SOTA `1.1194`; the refined exact 3-seed mean became `1.11976097`, which beats the locked legal-TTT 3-seed mean `1.12010450` by `-0.00034353`; script eval wallclock was `461177ms` and managed wallclock `503s` | This is a controlled positive refinement on the final locked seed. The result beats the round’s primary decision baseline cleanly, lands in the “especially strong” band, and makes the refined exact 3-seed mean clearly better than the locked `TTT_EPOCHS=3` mean while staying budget-safe, so the refined eval regime is now promotable across the locked `arch_010` line | Promote `TTT_LR=0.0025`, `TTT_EPOCHS=4` across the locked `arch_010` legal-TTT line and stop nearby TTT-budget tuning; the next round should change a different single variable |
| 2026-03-26 | `eval_009_arch010_legal_ttt_temperature_seed1337` | evaluation | Historical seed-`1337` legal-TTT baseline `eval_006=1.11935008`; required same-helper parity baseline after edit at `EVAL_TEMPERATURE=1.0`; context refined legal-TTT 3-seed mean `1.11976097`, live SOTA `1.1194` | If the locked `arch_010 + legal score-first TTT` evaluator is still slightly miscalibrated at scoring time, then one global temperature applied only to scoring logits can improve post-export `sliding_window stride=64 val_bpb` beyond `1.11935008` on the exact saved seed-`1337` artifact | This directly tested the user-priority eval-mixing motif while staying within refinement discipline: copy the locked helper, change only scoring calibration, exclude official-val tuning, and keep the TTT update path untouched | Added `EVAL_TEMPERATURE` to a copied `eval_008` helper; applied it only in `eval_val_sliding()` and `eval_val_sliding_ttt()` scoring logits; swept held-out temperatures `{0.95,0.975,1.00,1.025,1.05}` on one frozen train slice | Exact saved seed-`1337` `arch_010` checkpoint/export lineage (`106178569` / `b8291ad1...`, `15555121` / `eb062c96...`), legal score-first TTT, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `score_first=True`, `last_chunk_untrained=True`, `sliding_window stride=64`, cuDNN-backed SDPA fallback, tokenizer/data format, no retraining, no export rewrite, no artifact rewrite, official validation excluded from temperature selection | `T=1.0` should reproduce `1.11935008` within `±0.0002`; one off-baseline temperature should win on the held-out slice and then beat parity on full validation if simple global calibration helps | The edited helper changed from `84059` bytes / `d93fc02c...` to `84578` bytes / `3db38c0e...`; saved artifact hashes stayed unchanged before and after all runs. Full-val parity at `EVAL_TEMPERATURE=1.0` scored `1.11935013`, only `+0.00000005` vs historical `eval_006`, with script eval wallclock `461600ms` and managed wallclock `503s`, so the parity gate passed cleanly. The fixed held-out train slice was created from `fineweb_train_000000.bin` starting at token offset `8388608`, copying `2097153` tokens (`2097152` scored, exactly `64` TTT chunks). Held-out results were: `0.95 -> 1.11733953`, `0.975 -> 1.11620707`, `1.00 -> 1.11555286`, `1.025 -> 1.11614565`, `1.05 -> 1.11734831`; the selected temperature was `1.0`. Because the selected temperature matched parity exactly, the parity run is also the final full-val result for the selected temperature, so delta vs parity was `+0.00000000`, delta vs refined legal-TTT 3-seed mean was `-0.00041084`, and delta vs live SOTA was `-0.00004987` | The helper edit is valid, but the mechanism is negative: the frozen held-out slice selected `T=1.0`, so simple global temperature scaling did not improve the locked seed-`1337` legal-TTT line. This motif is locally exhausted on the controlled seed rather than mildly positive | Do not run nearby global-temperature sweeps on locked legal-TTT; keep the promoted `TTT_LR=0.0025`, `TTT_EPOCHS=4` regime fixed and move to a different controlled eval-side mixing motif |
| 2026-03-26 | `eval_010_arch010_legal_ttt_prefix_bigram_interp_seed1337` | evaluation | Historical seed-`1337` legal-TTT baseline `eval_006=1.11935008`; required same-helper parity baseline after edit at `EVAL_BIGRAM_LAMBDA=0.0`; context refined legal-TTT 3-seed mean `1.11976097`, live SOTA `1.1194` | If the locked `arch_010 + legal score-first TTT` evaluator still misses exact chunk-local continuation statistics after adaptation, then adding one prefix-only chunk-local bigram interpolation term during scoring can improve post-export `sliding_window stride=64 val_bpb` beyond `1.11935008` on the exact saved seed-`1337` artifact | This tested the next user-priority eval-mixing motif while staying within refinement discipline: copy the locked helper, change only scoring inside `eval_val_sliding_ttt()`, reuse the existing held-out slice, and keep TTT updates untouched | Added `EVAL_BIGRAM_LAMBDA` to a copied `eval_008` helper; modified only `eval_val_sliding_ttt()` scoring to mix `p_model` with one chunk-local prefix-only previous-token bigram distribution; swept held-out lambdas `{0.00,0.05,0.10,0.15,0.20}` on the frozen `eval_009` held-out slice | Exact saved seed-`1337` `arch_010` checkpoint/export lineage (`106178569` / `b8291ad1...`, `15555121` / `eb062c96...`), legal score-first TTT, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `score_first=True`, `last_chunk_untrained=True`, `sliding_window stride=64`, cuDNN-backed SDPA fallback, tokenizer/data format, no retraining, no export rewrite, no artifact rewrite, no temperature scaling, official validation excluded from lambda selection | `lambda=0.0` should reproduce `1.11935008` within `±0.0002`; one nonzero lambda should win on the held-out slice and then beat parity on full validation if simple chunk-local bigram mixing helps | The edited helper changed from `84059` bytes / `d93fc02c...` to `89121` bytes / `1cecd32e...`; saved artifact hashes stayed unchanged before and after all runs. Full-val parity at `EVAL_BIGRAM_LAMBDA=0.0` scored `1.11935127`, only `+0.00000119` vs historical `eval_006`, with script eval wallclock `457427ms` and managed wallclock `498s`, so the parity gate passed cleanly. The exact `eval_009` held-out slice was reused unchanged. Held-out results were: `0.00 -> 1.11556493`, `0.05 -> 1.12860428`, `0.10 -> 1.14760131`, `0.15 -> 1.16896766`, `0.20 -> 1.19233780`; the selected lambda was `0.0`. Because the selected lambda matched parity exactly, the parity run is also the final full-val result for the selected lambda, so delta vs parity was `+0.00000000`, delta vs refined legal-TTT 3-seed mean was `-0.00040970`, and delta vs live SOTA was `-0.00004873` | The helper edit is valid, but the mechanism is strongly negative: the frozen held-out slice selected `lambda=0.0`, and every nonzero lambda degraded sharply, so simple chunk-local prefix bigram interpolation is locally exhausted on the locked seed-`1337` legal-TTT line | Do not run nearby lambda sweeps or smoothing variants for this exact chunk-local prefix bigram mechanism; keep the promoted `TTT_LR=0.0025`, `TTT_EPOCHS=4` regime fixed and move to a different controlled eval-side mixing motif |
| 2026-03-26 | `eval_011_arch010_legal_ttt_backoff_ngram_seed1337` | evaluation | Historical seed-`1337` legal-TTT baseline `eval_006=1.11935008`; required same-helper parity baseline after edit at `EVAL_NGRAM_LAMBDA=0.0`; context refined legal-TTT 3-seed mean `1.11976097`, live SOTA `1.1194` | If the locked `arch_010 + legal score-first TTT` evaluator still leaves exploitable exact repeated local spans beyond order-2 statistics, then a fixed higher-order prefix-only backoff cache with small interpolation weight can improve post-export `sliding_window stride=64 val_bpb` beyond `1.11935008` on the exact saved seed-`1337` artifact | This tested the next controlled eval-side mechanism while staying within refinement discipline: copy the locked helper, change only scoring inside `eval_val_sliding_ttt()`, reuse the existing held-out slice, enforce fixed cache structure `orders 2..9 / min_count 2 / 4194304 buckets`, and keep TTT updates untouched | Added `EVAL_NGRAM_LAMBDA` to a copied `eval_010` helper; removed the old bigram path; modified only `eval_val_sliding_ttt()` scoring to mix `p_model` with one chunk-local prefix-only highest-order-first multi-order backoff distribution; swept held-out lambdas `{0.00,0.01,0.02,0.05,0.10}` on the frozen `eval_009` held-out slice | Exact saved seed-`1337` `arch_010` checkpoint/export lineage (`106178569` / `b8291ad1...`, `15555121` / `eb062c96...`), legal score-first TTT, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `score_first=True`, `last_chunk_untrained=True`, `sliding_window stride=64`, cuDNN-backed SDPA fallback, tokenizer/data format, no retraining, no export rewrite, no artifact rewrite, no temperature scaling, no bigram interpolation, no entropy-adaptive alpha, no complementary training, official validation excluded from lambda selection | `lambda=0.0` should reproduce `1.11935008` within `±0.0002`; one small nonzero lambda should win on the held-out slice and then beat parity on full validation if higher-order prefix reuse helps | The edited helper changed from `89121` bytes / `1cecd32e...` to `94173` bytes / `60779a33...`; saved artifact hashes stayed unchanged before and after all runs. Full-val parity at `EVAL_NGRAM_LAMBDA=0.0` scored `1.11935518`, only `+0.00000510` vs historical `eval_006`, with script eval wallclock `466883ms` and managed wallclock `508s`, so the parity gate passed cleanly. Held-out results were `0.00 -> 1.11556687`, `0.01 -> 1.11494470`, `0.02 -> 1.11689406`, `0.05 -> 1.12465863`, `0.10 -> 1.14061444`; the held-out selector chose `lambda=0.01`. The selected-lambda final full-val run scored `1.12044055`, which is `+0.00108537` worse than parity, with script eval wallclock `1859145ms`, managed wallclock `1901s`, and matched-order coverage any-match fractions `0.95245996` on held-out and `0.95310151` on full val | The helper edit is valid, but the mechanism is non-promotable: the held-out-selected nonzero weight overfit the selector slice, full validation regressed materially versus parity, and the final owner-path eval exceeded the practical `10 min` budget by more than `3x` | Close this higher-order count-based prefix-only backoff family on locked legal-TTT `arch_010` and move to a genuinely different single-variable direction rather than another nearby count-based sweep |
| 2026-03-26 | `eval_012_arch010_legal_ttt_logit_bias_seed1337` | evaluation | Historical seed-`1337` legal-TTT baseline `eval_006=1.11935008`; required same-helper parity baseline after edit at `EVAL_LOGIT_BIAS_LAMBDA=0.0`; context refined legal-TTT 3-seed mean `1.11976097`, live SOTA `1.1194` | If the locked `arch_010 + legal score-first TTT` evaluator is still limited by vocabulary-level unigram calibration, then a fixed off-val token log-prior bias with one tuned scalar can improve post-export `sliding_window stride=64 val_bpb` beyond `1.11935008` on the exact saved seed-`1337` artifact | This tested the user-priority logit-adjustment calibration direction while tightening interpretability after the nearby selector-overfit failure: copy the locked helper, change only scoring inside `eval_val_sliding_ttt()`, separate prior construction from lambda selection, and keep TTT updates untouched | Added `EVAL_LOGIT_BIAS_LAMBDA` plus one optional `EVAL_LOGIT_BIAS_PRIOR_PATH` to a copied `eval_008` helper; modified only `eval_val_sliding_ttt()` scoring to use `logits + lambda * log_prior`; split the frozen `eval_009` held-out slice deterministically into prior-build and selector halves; swept selector-half lambdas `{ -0.10, -0.05, 0.00, 0.05, 0.10 }` | Exact saved seed-`1337` `arch_010` checkpoint/export lineage (`106178569` / `b8291ad1...`, `15555121` / `eb062c96...`), legal score-first TTT, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `score_first=True`, `last_chunk_untrained=True`, `sliding_window stride=64`, cuDNN-backed SDPA fallback, tokenizer/data format, no retraining, no export rewrite, no artifact rewrite, no temperature scaling, no bigram interpolation, no higher-order backoff cache, official validation excluded from prior build and lambda selection | `lambda=0.0` should reproduce `1.11935008` within `±0.0002`; one nonzero lambda should win on the disjoint selector half and then beat parity on full validation if simple tokenwise unigram calibration still helps | The edited helper changed from `84059` bytes / `d93fc02c...` to `87184` bytes / `227f842b...`; saved artifact hashes stayed unchanged before and after all runs. The exact `eval_009` held-out slice `8b5e92cc...` was split into prior-build `b3102ee1...` and selector `286e6e4e...`, each `1048577` tokens / `1048576` scored. Full-val parity at `EVAL_LOGIT_BIAS_LAMBDA=0.0` scored `1.11935063`, only `+0.00000055` vs historical `eval_006`, with script eval wallclock `470956ms` and managed wallclock `512s`, so the parity gate passed cleanly. Selector-half results were `-0.10 -> 1.12511628`, `-0.05 -> 1.12345755`, `0.00 -> 1.12285060`, `0.05 -> 1.12334301`, `0.10 -> 1.12489128`; the selector chose `lambda=0.0`, so the parity run was intentionally reused as the final full-val result. Final deltas were `+0.00000055` vs historical `eval_006`, `-0.00041034` vs refined legal-TTT 3-seed mean, and `-0.00004937` vs live SOTA; total bytes were `15642305` | The helper edit is valid, but the mechanism is negative: after clean prior/selector separation, every nonzero fixed token-prior bias lost to `lambda=0.0`, so simple unigram logit adjustment is locally exhausted on the locked seed-`1337` legal-TTT line | Close simple fixed token-prior calibration on locked legal-TTT `arch_010` and move to a genuinely different single-variable direction rather than another nearby eval-time calibration tweak |
| 2026-03-26 | `eval_013_arch010_pr809_chunk_ngram_ttt_seed1337` | evaluation | Historical seed-`1337` legal-TTT baseline `eval_006=1.11935008`; required same-helper parity baseline after edit with `NGRAM_EVAL_ENABLED=0`; context PR `#813=0.6671`, official snapshot target `0.4416`, legality-pending PR `#809=0.2952` | If the 2026-03-26 leaderboard shift is mainly driven by the correct eval-time chunked n-gram mechanism, then porting the PR `#809`-style vectorized score-first chunk n-gram backoff stack onto the locked seed-`1337` `arch_010` legal-TTT evaluator should produce a very large BPB drop on the unchanged saved artifact | The latest snapshot made n-gram backoff the dominant motif and `eval_011` had already answered that the naive global-lambda prefix-only shape was the wrong algorithm; this round tested the explicit PR `#809` mechanism instead: vectorized lookup, separate context/full tables, highest-order-first backoff, entropy-adaptive alpha, per-order multipliers, and score-first `1M`-token cache updates | Copied the exact locked `eval_008` helper into a fresh `eval_013` run dir; added the PR `#809` hash/cache primitives plus score-first chunk update logic; integrated the cache into the existing legal score-first TTT path; enabled `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, `ALPHA_MIN=0.05`, `ALPHA_MAX=0.60`, `ENTROPY_CENTER=3.0`, `ENTROPY_SCALE=2.0`, `ORDER_MULTS=[0.3,0.3,0.97,2.0,2.0,2.0,2.0,2.0]` | Exact saved seed-`1337` `arch_010` checkpoint/export lineage (`106178569` / `b8291ad1...`, `15555121` / `eb062c96...`), legal score-first TTT, `TTT_LR=0.0025`, `TTT_EPOCHS=4`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, `TTT_GRAD_CLIP=1.0`, `score_first=True`, `last_chunk_untrained=True`, `sliding_window stride=64`, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite, no temperature scaling, no unigram logit bias, no prefix bigram interpolation, no naive global-lambda n-gram mixer | Disabled parity should reproduce `1.11935008` within `±0.0002`; enabled full-val should materially improve and ideally at least approach the PR `#813` baseline-n-gram regime while staying under the practical `10 min` eval budget | The edited helper changed from `84059` bytes / `d93fc02c...` to `106140` bytes / `03107a3c...`; saved artifact hashes stayed unchanged before and after all runs. Held-out smoke disabled scored `1.11555607`; held-out smoke enabled scored `1.20585025` with any-match `0.52316141` and avg alpha `0.29490703`, confirming live cache behavior after a one-line `detach()` bugfix. Full-val disabled parity scored `1.11934928`, only `-0.00000080` vs historical `eval_006`, with script eval wallclock `468636ms` and managed wallclock `509s`, so the parity gate passed. Full-val enabled scored `0.29118172`, improving by `-0.82816836` vs historical `eval_006`, `-0.37591828` vs PR `#813`, `-0.15041828` vs official `0.4416`, and `-0.00401828` vs PR `#809`; final any-match was `0.98387524`, avg alpha `0.64246907`, and order `9` dominated the match histogram with `52956834` matches. Total bytes were `15661261` | The mechanism transfer is strongly positive, not a non-transfer: the correct PR `#809` algorithmic shape works on the locked `arch_010` line and massively outperforms every previous local eval-side refinement. The only failure is runtime legality: the final enabled run took `683624ms` script time and `725s` managed wallclock, exceeding the practical `10 min` budget | Treat this as a strong BPB reproduction with a runtime-budget implementation failure; the next round should keep the mechanism fixed and change only one runtime-cost lever at a time until the path is promotable |
| 2026-03-26 | `eval_014_arch010_pr809_chunk_ngram_ttt_maxorder7_seed1337` | evaluation | Primary baseline `eval_013_arch010_pr809_chunk_ngram_ttt_seed1337` at `val_bpb=0.29118172`, script eval wallclock `683624ms`, managed wallclock `725s`, `NGRAM_EVAL_MAX_ORDER=9`; context official `0.4416`, PR `#813=0.6671` | If the runtime overrun in `eval_013` is driven mainly by the highest-order cache lookup/update path, then changing only `NGRAM_EVAL_MAX_ORDER=9 -> 7` on the exact same helper/artifact lineage should recover enough runtime to make the path legal while preserving most of the BPB gain | `eval_013` was dominated by order-`9` matches, so trimming orders `8-9` was the narrowest runtime-only lever with a plausible speed mechanism and no helper or artifact drift | Copied the exact `eval_013` helper into a fresh `eval_014` run dir with no edits and changed only `NGRAM_EVAL_MAX_ORDER=7` in the managed eval-only command | Exact helper bytes/hash `106140 / 03107a3c...`, exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, `NGRAM_EVAL_MIN_ORDER=2`, `NGRAM_EVAL_BUCKETS=4194304`, `NGRAM_EVAL_MIN_COUNT=2`, `NGRAM_EVAL_CHUNK_TOKENS=1000000`, `NGRAM_EVAL_ALPHA_MIN=0.05`, `NGRAM_EVAL_ALPHA_MAX=0.60`, `NGRAM_EVAL_ENTROPY_CENTER=3.0`, `NGRAM_EVAL_ENTROPY_SCALE=2.0`, `NGRAM_EVAL_ORDER_MULTS=[0.3,0.3,0.97,2.0,2.0,2.0,2.0,2.0]`, legal score-first TTT, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite | Recover legality under `600000ms` script / `600s` managed while keeping `val_bpb <= 0.4416`, ideally still near the sub-`0.35` band | Helper and artifact identity stayed unchanged before and after launch. The single managed `8x NVIDIA L20Z` eval-only run scored `legal_ttt_exact val_loss=0.58942811`, `val_bpb=0.34909253`, with script eval wallclock `659969ms` and managed wallclock `701s`; any-match was `0.98387505`, avg alpha `0.56869018`, histogram `order_2=154263`, `order_3=1155316`, `order_4=1520175`, `order_5=1734348`, `order_6=3628830`, `order_7=52828604`; BPB delta vs `eval_013` was `+0.05791081`, script-time delta `-23655ms`, managed delta `-24s`, delta vs official `0.4416` was `-0.09250747`, and total bytes stayed `15661261` | This is a controlled but non-promotable runtime ablation: reducing only `max_order` preserved a strong BPB result below the official target, but the runtime gain was too small to recover legality and the BPB regression is substantial for the cost saved. Classification: `still-runtime-illegal` | Do not promote `max_order=7` and do not spend the next round on another nearby order sweep; keep the mechanism fixed and attack a different runtime lever, most directly cache geometry or update granularity |
| 2026-03-26 | `eval_015_arch010_pr809_chunk_ngram_ttt_buckets2097152_seed1337` | evaluation | Primary baseline `eval_013_arch010_pr809_chunk_ngram_ttt_seed1337` at `val_bpb=0.29118172`, script eval wallclock `683624ms`, managed wallclock `725s`, `NGRAM_EVAL_BUCKETS=4194304`; context `eval_014=0.34909253` at `659969ms`, official `0.4416`, PR `#813=0.6671` | If the remaining runtime overrun in `eval_013` is driven mainly by count-table size and `np.bincount(..., minlength=num_buckets)` memory traffic rather than by orders `8-9`, then changing only `NGRAM_EVAL_BUCKETS=4194304 -> 2097152` on the exact same helper/artifact lineage should recover more runtime than `eval_014` while preserving the stronger order-`9` behavior and losing materially less BPB than the order-cap ablation | Halving bucket count directly attacks count-table footprint and full-length `bincount` traffic while leaving the validated score-first PR `#809` mechanism, full order depth, TTT settings, and artifact lineage fixed | Copied the exact `eval_013` helper into a fresh `eval_015` run dir with no edits and changed only `NGRAM_EVAL_BUCKETS=2097152` in the managed eval-only command | Exact helper bytes/hash `106140 / 03107a3c...`, exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, `NGRAM_EVAL_MIN_ORDER=2`, `NGRAM_EVAL_MAX_ORDER=9`, `NGRAM_EVAL_MIN_COUNT=2`, `NGRAM_EVAL_CHUNK_TOKENS=1000000`, `NGRAM_EVAL_ALPHA_MIN=0.05`, `NGRAM_EVAL_ALPHA_MAX=0.60`, `NGRAM_EVAL_ENTROPY_CENTER=3.0`, `NGRAM_EVAL_ENTROPY_SCALE=2.0`, `NGRAM_EVAL_ORDER_MULTS=[0.3,0.3,0.97,2.0,2.0,2.0,2.0,2.0]`, legal score-first TTT, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite | Recover legality under `600000ms` script / `600s` managed, beat `eval_014`'s runtime gain, and keep quality safely inside the refined guardrails | Helper and artifact identity stayed unchanged before and after launch. The single managed `8x NVIDIA L20Z` eval-only run scored `legal_ttt_exact val_loss=0.33725604`, `val_bpb=0.19974202`, with script eval wallclock `663957ms` and managed wallclock `705s`; any-match was `0.98387585`, avg alpha `0.65459776`, histogram `order_2=60166`, `order_3=346841`, `order_4=373090`, `order_5=332096`, `order_6=395043`, `order_7=644544`, `order_8=1634957`, `order_9=57234849`; BPB delta vs `eval_013` was `-0.09143970`, script-time delta `-19667ms`, managed delta `-20s`, delta vs `eval_014` was `-0.14935051` at `+3988ms` script time, delta vs official `0.4416` was `-0.24185798`, and total bytes stayed `15661261` | This is a controlled but non-promotable runtime ablation: halving bucket count produced the strongest measured BPB on this lineage, but runtime still missed legality by `63957ms` script and `105s` managed, and it failed the discriminating speed target because it was slower than `eval_014` | Do not promote smaller bucket geometry as the legality-recovery setting and do not spend the next round on another nearby bucket sweep; keep the mechanism fixed and attack update granularity or another non-bucket runtime lever |
| 2026-03-26 | `eval_016_arch010_pr809_chunk_ngram_ttt_chunk2000000_seed1337` | evaluation | Primary baseline `eval_013_arch010_pr809_chunk_ngram_ttt_seed1337` at `val_bpb=0.29118172`, script eval wallclock `683624ms`, managed wallclock `725s`, `NGRAM_EVAL_CHUNK_TOKENS=1000000`; context `eval_014=0.34909253` at `659969ms` and `eval_015=0.19974202` at `663957ms`; official `0.4416`, PR `#813=0.6671` | If the remaining legality failure is driven mainly by cache-update frequency and chunk-boundary overhead rather than by bucket geometry or order depth, then changing only `NGRAM_EVAL_CHUNK_TOKENS=1000000 -> 2000000` on the exact same helper/artifact lineage should materially reduce runtime while preserving most of the full-order BPB gain from `eval_013` | Doubling chunk size halves the number of score-first cache updates and chunk-boundary transitions while keeping the validated PR `#809` mechanism, artifact lineage, order depth, bucket geometry, and TTT settings fixed | Copied the exact `eval_013` helper into a fresh `eval_016` run dir with no edits and changed only `NGRAM_EVAL_CHUNK_TOKENS=2000000` in the managed eval-only command | Exact helper bytes/hash `106140 / 03107a3c...`, exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, `NGRAM_EVAL_MIN_ORDER=2`, `NGRAM_EVAL_MAX_ORDER=9`, `NGRAM_EVAL_BUCKETS=4194304`, `NGRAM_EVAL_MIN_COUNT=2`, `NGRAM_EVAL_ALPHA_MIN=0.05`, `NGRAM_EVAL_ALPHA_MAX=0.60`, `NGRAM_EVAL_ENTROPY_CENTER=3.0`, `NGRAM_EVAL_ENTROPY_SCALE=2.0`, `NGRAM_EVAL_ORDER_MULTS=[0.3,0.3,0.97,2.0,2.0,2.0,2.0,2.0]`, legal score-first TTT, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite | Recover legality under `600000ms` script / `600s` managed, save at least `30000ms` script time vs `eval_013`, and keep quality inside the refined guardrails | Helper and artifact identity stayed unchanged before and after launch. The single managed `8x NVIDIA L20Z` eval-only run scored `legal_ttt_exact val_loss=0.50306482`, `val_bpb=0.29794332`, with script eval wallclock `676651ms` and managed wallclock `719s`; any-match was `0.96775207`, avg alpha `0.64574429`, histogram `order_2=75362`, `order_3=601815`, `order_4=771244`, `order_5=706910`, `order_6=819542`, `order_7=1307785`, `order_8=3287826`, `order_9=52451079`; BPB delta vs `eval_013` was `+0.00676160`, script-time delta `-6973ms`, managed delta `-6s`, delta vs `eval_014` was `-0.05114921` at `+16682ms` script time, delta vs `eval_015` was `+0.09820130` at `+12694ms` script time, delta vs official `0.4416` was `-0.14365668`, and total bytes stayed `15661261` | This is a controlled negative on update granularity: quality stayed strong relative to the official target, but runtime barely improved, remained illegal by `76651ms` script and `119s` managed, and was slower than both prior legality-recovery ablations, so cache-update frequency is probably not the main bottleneck | Do not promote `NGRAM_EVAL_CHUNK_TOKENS=2000000` and do not spend the next round on another nearby chunk-size sweep; keep the mechanism fixed and target implementation-side update cost or another distinct non-bucket runtime lever |
| 2026-03-26 | `eval_017_arch010_pr809_chunk_ngram_ttt_precompute_ctxkeys_seed1337` | evaluation | Primary baseline `eval_013_arch010_pr809_chunk_ngram_ttt_seed1337` at `val_bpb=0.29118172`, script eval wallclock `683624ms`, managed wallclock `725s`; context `eval_014=0.34909253`, `eval_015=0.19974202`, `eval_016=0.29794332`; official `0.4416`, PR `#813=0.6671` | If the remaining legality failure is driven mainly by repeated context-hash construction inside `batch_lookup()` and `update_batch()`, then precomputing cached bucketized context keys once over `tokens_np` should materially reduce runtime while preserving `eval_013` semantics and BPB | Prior order-cap, bucket-geometry, and chunk-granularity ablations left implementation-side repeated `_batch_hash_ctx(...)` work as the clean next runtime mechanism; cached-key reuse attacks that cost while keeping the validated PR `#809` scoring path fixed | Copied the `eval_013` helper into a fresh `eval_017` run dir; added one cached-key precompute stage, one helper to combine cached context keys with targets, and optional cached-key reuse inside `NgramEvalCache`; ran one disabled parity eval and one enabled full-val eval-only comparison via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT, `sliding_window stride=64`, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite | Disabled parity within `±0.0002` of the locked disabled baseline, enabled `val_bpb` within `±0.002` of `eval_013`, and runtime legal under `600000ms` script / `600s` managed | Helper identity stayed unchanged before/after launch at `110161` bytes / `d2399931...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11935320`, only `+0.00000392` vs locked disabled baseline `1.11934928`, with `457900ms` script wallclock and `501s` managed. The helper logged `ngram_ctx_key_precompute dtype=uint32 resident_bytes=1984692256 elapsed_ms=20977`. Enabled full-val scored `legal_ttt_exact val_loss=0.49164452`, `val_bpb=0.29117957`, with `645995ms` script wallclock and `688s` managed; final any-match was `0.98387524`, avg alpha `0.64246789`, histogram `order_2=120864`, `order_3=808024`, `order_4=915947`, `order_5=767349`, `order_6=844813`, `order_7=1317753`, `order_8=3289964`, `order_9=52956834`; deltas were `-0.00000215` vs `eval_013`, `-37629ms` script and `-37s` managed vs `eval_013`, `-0.05791296` vs `eval_014`, `+0.09143755` vs `eval_015`, `-0.00676375` vs `eval_016`, and `-0.15042043` vs official `0.4416`; total bytes were `15665282` | This is a controlled positive-but-insufficient implementation result: cached-key reuse preserves the validated PR `#809` BPB almost exactly and materially improves runtime, so repeated context hashing was a real cost, but the path still misses legality by `45995ms` script and `88s` managed. Classification: `still-runtime-illegal` | Keep the cached-key helper path fixed and target lookup-table memory traffic or count-update layout next rather than another nearby order, bucket, or chunk sweep |
| 2026-03-26 | `eval_018_arch010_pr809_chunk_ngram_ttt_sparse_updates_seed1337` | evaluation | Primary baseline `eval_017_arch010_pr809_chunk_ngram_ttt_precompute_ctxkeys_seed1337` at `val_bpb=0.29117957`, script eval wallclock `645995ms`, managed wallclock `688s`; context `eval_013=0.29118172`, `eval_014=0.34909253`, `eval_015=0.19974202`, `eval_016=0.29794332`; official `0.4416` | If the remaining legality miss after `eval_017` is driven mainly by dense full-table count-update traffic in `NgramEvalCache.update_batch()`, then replacing full-table `np.bincount(..., minlength=num_buckets)` updates with one exact deterministic sparse touched-bin accumulation path should recover a meaningful fraction of the remaining runtime while preserving `eval_017` semantics and BPB | `eval_017` had already validated cached-key reuse, so dense count-update layout was the next clean remaining implementation-side bottleneck on the exact cached-key PR `#809` path | Copied the exact `eval_017` helper into a fresh `eval_018` run dir; added `_sparse_exact_add_counts(...)`; changed only `update_batch()` accumulation from dense full-bucket histograms to exact sparse unique-count indexed adds; added cumulative `update_batch()` timing telemetry; ran one disabled parity eval, one enabled short-slice semantic gate on the frozen `eval_009` held-out slice, and one enabled full official eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact cached-key `eval_017` helper outside the sparse-update change, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT, `sliding_window stride=64`, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite | Disabled parity within the locked band, enabled short-slice semantics near-identical to `eval_017`, enabled full-val BPB within `±0.0005` of `eval_017`, and runtime legal under `600000ms` script / `600s` managed | Helper identity stayed unchanged before/after launch at `111179` bytes / `7471e15d...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11935177`, only `-0.00000143` vs locked `eval_017` disabled baseline `1.11935320`, with `453755ms` script wallclock and `494s` managed. The enabled short-slice semantic gate scored `1.20585497`, only `+0.00001311` vs the locked `eval_017` same-slice baseline, with identical any-match `0.52316141`, identical matched-order histogram, avg alpha `0.29491847`, and `update_batch` timing `calls=3 elapsed_ms=1188`. Enabled full-val scored `legal_ttt_exact val_loss=0.49164261`, `val_bpb=0.29117845`, with `630501ms` script wallclock and `671s` managed; final `update_batch` timing was `calls=63 elapsed_ms=32959`, final any-match was `0.98387524`, avg alpha `0.64246856`, histogram `order_2=120864`, `order_3=808024`, `order_4=915947`, `order_5=767349`, `order_6=844813`, `order_7=1317753`, `order_8=3289964`, `order_9=52956834`; deltas were `-0.00000112` vs `eval_017`, `-0.00000327` vs `eval_013`, `-15494ms` script and `-17s` managed vs `eval_017`, `-53123ms` script and `-54s` managed vs `eval_013`, and `-0.15042155` vs official `0.4416`; total bytes were `15666300` | This is a controlled positive-but-insufficient count-layout result: sparse touched-bin updates preserve the validated cached-key PR `#809` semantics and recover another `15494ms` script time plus `17s` managed wallclock versus `eval_017`, so dense count-update layout was a real cost, but the path still misses legality by `30501ms` script and `71s` managed. Classification: `still-runtime-illegal` | Stop spending rounds on nearby sparse-update variants; keep the cached-key sparse path as evidence and target lookup-table read traffic next rather than another count-layout, order, bucket, or chunk sweep |
| 2026-03-26 | `eval_019_arch010_pr809_chunk_ngram_ttt_dedup_lookup_gathers_seed1337` | evaluation | Primary baseline `eval_018_arch010_pr809_chunk_ngram_ttt_sparse_updates_seed1337` at `val_bpb=0.29117845`, script eval wallclock `630501ms`, managed wallclock `671s`; context `eval_017=0.29117957`, `eval_013=0.29118172`, `eval_014=0.34909253`, `eval_015=0.19974202`, `eval_016=0.29794332`; official `0.4416` | If the remaining legality miss after `eval_018` is materially driven by repeated lookup-table read traffic in `NgramEvalCache.batch_lookup()`, then exact deduplication of repeated `ctx_keys` and `full_keys` before table reads should reduce runtime while preserving `eval_018` semantics and BPB | `eval_018` had already answered sparse update layout positively, so lookup-table read traffic was the next clean remaining implementation-side bottleneck on the exact cached-key sparse-update PR `#809` path | Copied the exact `eval_018` helper into a fresh `eval_019` run dir; added `_dedup_exact_gather(...)`; changed only the two table gathers in `batch_lookup()` to exact deduplicated gather/scatter reconstruction; added cumulative/per-order `batch_lookup()` telemetry; ran one disabled parity eval, one enabled short-slice semantic gate on the frozen `eval_009` held-out slice, and one enabled full official eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_018` helper outside the lookup-read change, cached-key precompute path from `eval_017`, sparse touched-bin `update_batch()` path from `eval_018`, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT, `sliding_window stride=64`, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite | Disabled parity within the locked band, enabled short-slice semantics near-identical to `eval_018`, enabled full-val BPB within `±0.0005` of `eval_018`, and runtime legal under `600000ms` script / `600s` managed | Helper identity stayed unchanged before/after launch at `114934` bytes / `f340e1e0...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11935159`, only `-0.00000018` vs locked `eval_018` disabled baseline `1.11935177`, with `461785ms` script wallclock and `504s` managed. The enabled short-slice semantic gate scored `1.20584817`, only `-0.00000680` vs the locked `eval_018` same-slice baseline `1.20585497`, with any-match `0.52316141`, avg alpha `0.29492288`, `batch_lookup` timing `calls=32737 elapsed_ms=11819`, and `update_batch` timing `calls=3 elapsed_ms=1172`. Enabled full-val scored `legal_ttt_exact val_loss=0.49164569`, `val_bpb=0.29118027`, with `645099ms` script wallclock and `687s` managed; final `batch_lookup` timing was `calls=969057 elapsed_ms=188358`, final `update_batch` timing `calls=63 elapsed_ms=32194`, final any-match `0.98387524`, avg alpha `0.64246909`, histogram `order_2=120864`, `order_3=808024`, `order_4=915947`, `order_5=767349`, `order_6=844813`, `order_7=1317753`, `order_8=3289964`, `order_9=52956834`; per-order dedup telemetry showed the highest-volume orders were already almost entirely unique; deltas were `+0.00000182` vs `eval_018`, `+14598ms` script and `+16s` managed vs `eval_018`, `+0.00000070` and `-896ms` vs `eval_017`, and `-0.15041973` vs official `0.4416`; total bytes were `15670055` | This is a controlled exact-but-negative lookup-dedup result: deduplicated gathers preserve the validated cached-key sparse-update PR `#809` semantics, but they make runtime worse than `eval_018`, so repeated lookup duplication is not a dominant remaining bottleneck at the observed uniqueness ratios. Classification: `still-runtime-illegal` | Stop spending rounds on lookup-key dedup variants; keep `eval_018` as the best legality baseline and target table-layout/read-locality or a more fused lookup structure next |
| 2026-03-26 | `eval_020_arch010_pr809_chunk_ngram_ttt_precompute_fullkeys_seed1337` | evaluation | Primary baseline `eval_018_arch010_pr809_chunk_ngram_ttt_sparse_updates_seed1337` at `val_bpb=0.29117845`, script eval wallclock `630501ms`, managed wallclock `671s`; context `eval_017=0.29117957`, `eval_019=0.29118027`, `eval_013=0.29118172`; official `0.4416` | If the remaining legality miss after `eval_018` is materially driven by repeated exact full-key construction inside `batch_lookup()` and `update_batch()`, then precomputing exact per-order `full_key_tables` once over `tokens_np` and reusing them should reduce runtime while preserving exact `eval_018` semantics and BPB | `eval_017` had already answered cached context-key reuse positively, `eval_018` had already answered sparse touched-bin updates positively, and `eval_019` had already answered lookup dedup negatively, leaving exact full-key construction as the clean remaining implementation-side cost on the locked evaluator | Copied the exact `eval_018` helper into a fresh `eval_020` run dir; added `_precompute_bucketized_full_keys(...)` and `_spot_check_precomputed_full_keys(...)`; changed only `NgramEvalCache` full-key sourcing so cached tables are used in `batch_lookup()` and `update_batch()`; added minimal full-key precompute telemetry plus retained cumulative `batch_lookup()` and `update_batch()` timing; ran one disabled parity eval, one enabled short-slice semantic gate on the frozen `eval_009` held-out slice, and one enabled full official eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_018` helper outside the full-key caching change, cached context-key precompute path from `eval_017`, sparse touched-bin `update_batch()` path from `eval_018`, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT, `sliding_window stride=64`, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite | Disabled parity within the locked band, enabled short-slice semantics near-identical to `eval_018`, enabled full-val BPB within `±0.0001` of `eval_018`, and runtime legal under `600000ms` script / `600s` managed | Helper identity stayed unchanged before/after launch at `116490` bytes / `0a2eb402...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11935153`, only `-0.00000024` vs locked `eval_018` disabled baseline `1.11935177`, with `459013ms` script wallclock and `501s` managed. The enabled short-slice semantic gate scored `1.20584349`, only `-0.00001148` vs the locked `eval_018` same-slice baseline `1.20585497`, with full-key precompute `67108896` bytes / `147ms`, exact spot-check `mismatch_count=0`, any-match `0.52316141`, avg alpha `0.29491807`, `batch_lookup` timing `calls=32737 elapsed_ms=5523`, and `update_batch` timing `calls=3 elapsed_ms=1117`. Enabled full-val scored `legal_ttt_exact val_loss=0.49164490`, `val_bpb=0.29117980`, with `633136ms` script wallclock and `677s` managed; full-key precompute was `1984692256` resident bytes and `11295ms`, exact spot-check `mismatch_count=0`, final `batch_lookup` timing was `calls=969057 elapsed_ms=91562`, final `update_batch` timing `calls=63 elapsed_ms=30501`, final any-match `0.98387524`, avg alpha `0.64246789`, histogram `order_2=120864`, `order_3=808024`, `order_4=915947`, `order_5=767349`, `order_6=844813`, `order_7=1317753`, `order_8=3289964`, `order_9=52956834`; deltas were `+0.00000135` vs `eval_018`, `+2635ms` script and `+6s` managed vs `eval_018`, `+0.00000023` and `-12859ms` vs `eval_017`, `-0.00000047` and `-11963ms` vs `eval_019`, and `-0.15042020` vs official `0.4416`; total bytes were `15671611` | This is a controlled exact-but-net-negative full-key caching result: cached full-key reuse preserves the validated cached-key sparse-update PR `#809` semantics and matches recomputed keys exactly, but the added `1.98 GB` full-key tables plus `11295ms` precompute cost erase the hot-path gain and leave the full run slightly slower than `eval_018`. Classification: `still-runtime-illegal` | Close full-key precompute expansion on this line; keep `eval_018` as the active legality baseline and target table-memory layout or a more fused lookup structure next |
| 2026-03-26 | `eval_021_arch010_pr809_chunk_ngram_ttt_fused_lookup_scratch_seed1337` | evaluation | Primary baseline `eval_018_arch010_pr809_chunk_ngram_ttt_sparse_updates_seed1337` at `val_bpb=0.29117845`, script eval wallclock `630501ms`, managed wallclock `671s`; context `eval_017=0.29117957`, `eval_019=0.29118027`, `eval_020=0.29117980`, `eval_013=0.29118172`; official `0.4416` | If the remaining legality miss after `eval_018` is now mostly exact per-call allocation, masking, and temporary gather overhead inside `batch_lookup()`, then replacing only that code with a reusable fused scratch-buffer implementation should reduce runtime while preserving exact PR `#809` semantics | `eval_019` had already rejected deduplication and `eval_020` had already rejected full-key precompute expansion, leaving per-call lookup-local allocation/compaction overhead as the clean remaining lookup-family implementation cost | Copied the exact `eval_018` helper into a fresh `eval_021` run dir; added one `_ensure_batch_lookup_scratch_capacity(...)` helper plus reusable scratch arrays owned by `NgramEvalCache`; rewrote only `batch_lookup()` to keep active unmatched positions in scratch-backed arrays, use scratch-backed `np.take(...)` gathers, combine cached context keys with targets in place, and compact unmatched indices through scratch-backed keep buffers; added minimal lookup telemetry; ran one disabled parity eval, one enabled short-slice semantic gate on the frozen `eval_009` held-out slice, and one enabled full official eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_018` helper outside the `batch_lookup()` rewrite, cached context-key precompute path from `eval_017`, sparse touched-bin `update_batch()` path from `eval_018`, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT, `sliding_window stride=64`, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite | Disabled parity within the locked band, enabled short-slice semantics near-identical to `eval_018`, enabled full-val BPB within `±0.0001` of `eval_018`, and runtime legal under `600000ms` script / `600s` managed | Helper identity stayed unchanged before/after launch at `117606` bytes / `328007f5...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11934470`, only `-0.00000707` vs locked `eval_018` disabled baseline `1.11935177`, with `468429ms` script wallclock and `512s` managed. The enabled short-slice semantic gate scored `1.20587221`, only `+0.00001724` vs the locked `eval_018` same-slice baseline `1.20585497`, with inherited context-key precompute `67108896` bytes / `272ms`, any-match `0.52316141`, avg alpha `0.29489710`, `batch_lookup` timing `calls=32737 elapsed_ms=8133 max_active_lookup_positions=2048`, and `update_batch` timing `calls=3 elapsed_ms=1173`. Enabled full-val scored `legal_ttt_exact val_loss=0.49164342`, `val_bpb=0.29117893`, with `641669ms` script wallclock and `682s` managed; inherited context-key precompute was `1984692256` resident bytes and `21953ms`, final `batch_lookup` timing was `calls=969057 elapsed_ms=111576 max_active_lookup_positions=2048`, final `update_batch` timing `calls=63 elapsed_ms=32425`, final any-match `0.98387524`, avg alpha `0.64247112`, histogram `order_2=120864`, `order_3=808024`, `order_4=915947`, `order_5=767349`, `order_6=844813`, `order_7=1317753`, `order_8=3289964`, `order_9=52956834`; deltas were `+0.00000048` vs `eval_018`, `-0.00000064` and `-4326ms` vs `eval_017`, `-0.00000134` and `-3430ms` vs `eval_019`, `-0.00000087` and `+8533ms` vs `eval_020`, and `-0.15042107` vs official `0.4416`; total bytes were `15672727` | This is a controlled exact-but-negative fused-lookup result: the scratch-backed `batch_lookup()` rewrite preserves the validated cached-key sparse-update PR `#809` semantics, but it remains slower than the active `eval_018` legality baseline and still misses the practical budget by `41669ms` script and `82s` managed. Classification: `still-runtime-illegal` | Close lookup-local scratch plumbing on this line; keep `eval_018` as the active legality baseline and pivot to a genuinely different runtime lever rather than another nearby lookup-family rewrite |
| 2026-03-26 | `eval_022_arch010_pr809_chunk_ngram_ttt_torch_cuda_tables_seed1337` | evaluation | Primary baseline `eval_018_arch010_pr809_chunk_ngram_ttt_sparse_updates_seed1337` at `val_bpb=0.29117845`, script eval wallclock `630501ms`, managed wallclock `671s`; context `eval_017=0.29117957`, `eval_020=0.29117980`, `eval_021=0.29117893`, `eval_013=0.29118172`; official `0.4416` | If the remaining legality miss after `eval_018` is mainly caused by CPU-side table locality and host-side lookup/update execution, then moving the exact PR `#809` cache tables and exact lookup/update path onto CUDA should reduce runtime while preserving `eval_018` semantics | `eval_017` and `eval_018` already removed repeated context hashing and dense full-table updates, while `eval_019`, `eval_020`, and `eval_021` had already rejected nearby CPU-side lookup-family ideas; backend placement was the clean remaining locality hypothesis | Copied the exact `eval_018` helper into a fresh `eval_022` run dir; preserved the original CPU cache as `CpuReferenceNgramEvalCache`; replaced only the active `NgramEvalCache` backend with CUDA-resident tables, exact device-side lookup/update execution, and minimal telemetry; added one bounded CPU-vs-CUDA preflight path; ran one disabled parity eval, one backend-equivalence preflight plus enabled short-slice semantic gate on the frozen `eval_009` held-out slice, and one enabled full official eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_018` scorer logic and TTT settings, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite, no algorithmic mixer change | Disabled parity within the locked band, preflight exact on the checked subset, enabled short-slice semantics near-identical to `eval_018`, enabled full-val BPB within `±0.0005` of `eval_018`, and runtime legal under `600000ms` script / `600s` managed | Helper identity stayed unchanged before/after launch at `128759` bytes / `932cf0e1...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11935044`, only `-0.00000133` vs locked `eval_018` disabled baseline `1.11935177`, with `457365ms` script wallclock and `498s` managed. The backend-equivalence preflight passed exactly with `matched_mask_mismatch_count=0`, `matched_order_mismatch_count=0`, `ngram_p_max_delta=0`, `ngram_p_mean_delta=0`, and `touched_subset_mismatch_count=0`. The enabled short-slice semantic gate scored `1.20586051`, only `+0.00000554` vs the locked `eval_018` same-slice baseline `1.20585497`, with context-key precompute/upload `67108896` bytes / `247ms + 14ms`, any-match `0.52316141`, avg alpha `0.29491496`, CUDA lookup timing `calls=4095 elapsed_ms=11815`, CUDA update timing `calls=3 elapsed_ms=15`, and peak CUDA memory `2580524032`. Enabled full-val scored `legal_ttt_exact val_loss=0.49164530`, `val_bpb=0.29118004`, with `842722ms` script wallclock and `883s` managed; full-val precompute/upload was `1984692256` resident bytes and `21018ms + 853ms`, final CUDA lookup timing `calls=121188 elapsed_ms=206036`, final CUDA update timing `calls=63 elapsed_ms=335`, final peak CUDA memory `4732566528`, final any-match `0.98387524`, avg alpha `0.64246992`, histogram `order_2=120864`, `order_3=808024`, `order_4=915947`, `order_5=767349`, `order_6=844813`, `order_7=1317753`, `order_8=3289964`, `order_9=52956834`; deltas were `+0.00000159` and `+212221ms` vs `eval_018`, `+0.00000047` and `+196727ms` vs `eval_017`, `+0.00000024` and `+209586ms` vs `eval_020`, `+0.00000111` and `+201053ms` vs `eval_021`, and `-0.15041996` vs official `0.4416`; total bytes were `15683880` | This is a controlled exact-but-strongly-negative backend-placement result: CUDA-resident tables preserve the validated PR `#809` semantics and keep tables plus cached keys resident on device, but they make the end-to-end run much slower, so CPU locality is not the main remaining legality bottleneck on this hardware. Classification: `still-runtime-illegal` | Close CUDA-table backend placement on this line; keep `eval_018` as the active legality baseline and pivot to a genuinely different non-overlapping end-to-end runtime lever |
| 2026-03-26 | `eval_023_arch010_pr809_chunk_ngram_ttt_entropy_pregate_h3p0_seed1337` | evaluation | Primary baseline `eval_018_arch010_pr809_chunk_ngram_ttt_sparse_updates_seed1337` at `val_bpb=0.29117845`, script eval wallclock `630501ms`, managed wallclock `671s`; context `eval_017=0.29117957`, `eval_021=0.29117893`, `eval_022=0.29118004`; required same-slice control baseline `1.20585497`; official `0.4416` | If a meaningful share of the remaining illegal runtime comes from doing n-gram lookup on low-entropy positions that already receive little effective n-gram trust, then preventing lookup at positions with `seg_entropy < 3.0` should reduce `batch_lookup()` cost enough to improve runtime materially with a bounded BPB penalty | The locked evaluator already computes per-token entropy before mixing, the active alpha rule is centered at `3.0`, and nearby `eval_017` through `eval_022` had tested implementation/backend details rather than lookup eligibility itself | Copied the exact `eval_018` helper into a fresh `eval_023` run dir; added one default-off env-controlled gate `NGRAM_EVAL_MIN_LOOKUP_ENTROPY`; changed only the enabled lookup path to build `lookup_mask = seg_entropy >= threshold`, call `cache.batch_lookup()` only on masked positions, scatter lookup outputs back into full segment indexing, and leave masked-out positions on pure neural predictions; added only the requested gate telemetry; ran one disabled parity eval, one enabled same-helper gate-off held-out control, one enabled held-out gate-on screen, and one enabled full official eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_018` helper outside the new gate, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings after a match is found, legal score-first TTT settings, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite, no bucket/order/chunk/backend sweep | Disabled parity within the locked band, enabled same-helper gate-off control within `±0.0001` of the locked same-slice baseline, enabled gate-on full-val under `0.4416`, and runtime legal under `600000ms` script / `600s` managed | The copied source helper from `eval_018` was `111179` bytes / `7471e15d...`; the new `eval_023` helper stayed fixed before/after launch at `113686` bytes / `cc3c211e...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11935003`, only `-0.00000174` vs the locked `eval_018` disabled baseline `1.11935177`, with `466947ms` script wallclock. The enabled same-helper gate-off control scored `1.20587243`, only `+0.00001746` vs the locked `eval_018` same-slice baseline `1.20585497`, with `ngram_lookup_candidate_count=2097152`, `ngram_entropy_gate_kept_fraction=1.00000000`, `ngram_entropy_gate_skipped_fraction=0.00000000`, and `ngram_lookup_masked_match_fraction=0.52316141`. The enabled held-out gate-on screen scored `1.17164650`, with `ngram_entropy_gate_kept_fraction=0.30025053`, `ngram_entropy_gate_skipped_fraction=0.69974947`, `ngram_lookup_masked_match_fraction=0.52615413`, any-match `0.15797806`, and avg alpha `0.54092532`. Enabled full-val scored `legal_ttt_exact val_loss=1.00820595`, `val_bpb=0.59711635`, with `636815ms` script wallclock and inferred managed wallclock `~678s`; full-val gate telemetry was `ngram_lookup_candidate_count=62021632`, `ngram_entropy_gate_kept_fraction=0.30216217`, `ngram_entropy_gate_skipped_fraction=0.69783783`, `ngram_lookup_masked_match_fraction=0.98383840`, any-match `0.29727875`, avg alpha `0.93260731`, and `ngram_update_batch_timing calls=63 elapsed_ms=33753`; deltas were `+0.30593790` and `+6314ms` vs `eval_018`, `+0.30593678` and `-9180ms` vs `eval_017`, `+0.30593742` and `-4854ms` vs `eval_021`, `+0.30593631` and `-205907ms` vs `eval_022`, and `+0.15551635` vs official `0.4416`; total bytes were `15668807` | This is a controlled negative on simple global lookup eligibility: the gate-off control passed, so semantics stayed valid, but globally skipping low-entropy lookups removed too much useful n-gram signal, collapsed full-val BPB far above the hard quality floor, and still failed runtime legality. Classification: `quality-regressed` | Close global entropy pre-gating on this line; keep `eval_018` as the active legality baseline and pivot to a genuinely different runtime lever rather than another threshold or lookup-eligibility sweep |
| 2026-03-26 | `eval_024_arch010_pr809_chunk_ngram_ttt_uint16_tables_seed1337` | evaluation | Primary baseline `eval_018_arch010_pr809_chunk_ngram_ttt_sparse_updates_seed1337` at `val_bpb=0.29117845`, script eval wallclock `630501ms`, managed wallclock `671s`; context `eval_017=0.29117957`, `eval_020=0.29117980`, `eval_023=0.59711635`; required same-slice control baseline `1.20585497`; official `0.4416` | If the remaining legality miss in `eval_018` is materially driven by count-table memory traffic rather than lookup policy or algorithm choice, then changing only `ctx_tables` and `full_tables` from `int32` to saturating `uint16` should reduce end-to-end runtime enough to improve legality while keeping BPB close to `eval_018` | `eval_018` missed script legality by only `30501ms`, the remaining open implementation question after `eval_017` through `eval_023` was table-memory traffic, and this kept the validated PR `#809` scorer logic fixed while changing only storage width | Copied the exact `eval_018` helper into a fresh `eval_024` run dir; added one default-off env control `NGRAM_EVAL_TABLE_BITS`; preserved exact `int32` behavior on the `32-bit` path; changed only the enabled `16-bit` path to allocate saturating `uint16` tables, perform sparse touched-bin saturating adds, cast gathered counts to a safe wider dtype for arithmetic, and log only the requested saturation telemetry; ran one disabled parity eval, one enabled same-helper held-out `32-bit` control, one enabled held-out `16-bit` screen, and one enabled full official `16-bit` eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_018` helper outside the table-width change, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT settings, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite, no order/bucket/chunk/backend/lookup-policy sweep | Disabled parity within the locked band, enabled same-helper `32-bit` control within `±0.0001` of the locked same-slice baseline, enabled `16-bit` full-val within `+0.02` BPB of `eval_018`, and runtime legal under `600000ms` script / `600s` managed | The copied source helper from `eval_018` was `111179` bytes / `7471e15d...`; the new `eval_024` helper stayed fixed before/after launch at `114357` bytes / `f8a21e70...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11935059`, only `-0.00000118` vs the locked `eval_018` disabled baseline `1.11935177`, with `474602ms` script wallclock and `516s` managed. The enabled same-helper `32-bit` control scored `1.20585207`, only `-0.00000290` vs the locked `eval_018` same-slice baseline `1.20585497`, with `ngram_update_batch_timing calls=3 elapsed_ms=1179` and zero saturation telemetry. The enabled `16-bit` held-out screen scored `1.20584253`, only `-0.00000954` vs the `32-bit` control, with `ngram_update_batch_timing calls=3 elapsed_ms=1030` and zero saturation telemetry, so the reviewed stop gate did not trigger. Enabled full-val scored `legal_ttt_exact val_loss=0.49164478`, `val_bpb=0.29117973`, with `637282ms` script wallclock and `679s` managed; full-val telemetry was `ngram_ctx_key_precompute resident_bytes=1984692256 elapsed_ms=22741`, `ngram_update_batch_timing calls=63 elapsed_ms=30050`, `ngram_ctx_saturation_events=31129706`, `ngram_full_saturation_events=495737`, `ngram_ctx_saturated_bins=7648`, `ngram_full_saturated_bins=283`, any-match `0.98387524`, avg alpha `0.64246969`, and the same matched-order histogram as `eval_018`; deltas were `+0.00000128` and `+6781ms` vs `eval_018`, `+0.00000016` and `-8713ms` vs `eval_017`, `-0.00000007` and `+4146ms` vs `eval_020`, and `-0.15042027` vs official `0.4416`; total bytes were `15669478` | This is a controlled negative on count-table precision as a legality lever: the controls passed and full-val BPB stayed effectively locked to `eval_018`, but runtime became slightly worse and full-val clipping was clearly nontrivial, so table width is not the right remaining runtime lever on this evaluator line. Classification: `non-promotable / still-runtime-illegal` | Abandon nearby `16/32-bit` table-width sweeps, keep `eval_018` as the active legality baseline, and pivot to a genuinely different runtime cost rather than more count-table precision work |
| 2026-03-26 | `eval_026_arch010_pr809_chunk_ngram_ttt_batch_torch_stats_seed1337` | evaluation | Primary baseline `eval_025_arch010_pr809_chunk_ngram_ttt_batch_lookup_by_batch_seed1337` at `val_bpb=0.29117983`, script eval wallclock `614228ms`, managed wallclock `657s`; continuity `eval_018=0.29117845`, script `630501ms`, managed `671s`; lookup-family context `eval_021=0.29117893`, script `641669ms`, managed `682s`; required same-slice control baseline `1.20584885`; official `0.4416` | If the remaining `eval_025` legality miss is materially driven by scorer-side per-row neural-stat extraction, then replacing repeated per-row `softmax` plus `log_softmax` work with one exact batched torch pass for `target_logp` and entropy should recover a meaningful fraction of the missing `14228ms` without materially changing BPB | `eval_025` already removed most outer lookup-call overhead, but the n-gram-specific timed sections still did not explain the full gap versus the locked no-ngram legal-TTT scorer; this kept the validated PR `#809` stack fixed and targeted the cleanest remaining scorer-side lever | Copied the exact `eval_025` helper into a fresh `eval_026` run dir; added one default-off env control `NGRAM_EVAL_BATCH_TORCH_STATS`; changed only `score_segments()` so the switch-off path preserves exact `eval_025` behavior, while the switch-on path computes exact `log_probs`, gathered `target_logp`, and entropy once per scorer batch in torch and reuses those tensors inside the existing row loop; left `NgramEvalCache`, scorer-batch lookup batching, score-first scheduling, TTT, alpha math, chunking, tokenizer, dataset, and artifact lineage fixed; added only the requested torch-stats telemetry; ran one disabled parity eval, one enabled same-helper held-out torch-stats-off control, one enabled held-out torch-stats-on screen, and one enabled full official torch-stats-on eval via `tools/gpu_experiment_runner.py` | Exact saved seed-`1337` `final_model.pt` `106178569 / b8291ad1...`, exact saved `final_model.int6.ptz` `15555121 / eb062c96...`, exact `eval_025` helper outside the new scorer-stats switch, exact `NGRAM_EVAL_BATCH_LOOKUP_BY_BATCH=1` behavior, exact `NgramEvalCache` lookup/update semantics, locked `NGRAM_EVAL_MIN_ORDER=2`, `MAX_ORDER=9`, `BUCKETS=4194304`, `MIN_COUNT=2`, `CHUNK_TOKENS=1000000`, locked alpha/entropy/order-mult settings, legal score-first TTT settings, tokenizer/dataset, no retraining, no export rewrite, no artifact rewrite, no order/bucket/chunk/backend/lookup-policy/scorer-batch-size sweep | Disabled parity within the locked band, enabled same-helper control within `±0.0001` of the locked same-slice baseline, enabled full-val within `±0.0005` of `eval_025`, and runtime legal under `600000ms` script / `600s` managed | The copied source helper from `eval_025` was `116390` bytes / `2d32bdbf...`; the new `eval_026` helper stayed fixed before/after launch at `119346` bytes / `62c19517...`; artifact identities stayed unchanged before/after launch. Disabled parity scored `1.11934940`, only `+0.00000147` vs the locked `eval_025` disabled-parity-equivalent baseline `1.11934793`, with `469094ms` script wallclock and `511s` managed. The enabled same-helper held-out control scored `1.20585418`, only `+0.00000533` vs the locked `eval_025` same-slice baseline `1.20584885`, with `ngram_batch_lookup_call_count=1040`, `ngram_batch_lookup_elapsed_ms=2019`, `ngram_torch_stats_batches=0`, `ngram_torch_stats_elapsed_ms=0`, and `ngram_update_batch_timing calls=3 elapsed_ms=1172`. The enabled torch-stats-on held-out screen scored `1.20584731`, only `-0.00000687` vs the same-helper control, with `ngram_batch_lookup_call_count=1040`, `ngram_batch_lookup_elapsed_ms=2226`, `ngram_torch_stats_batches=1040`, `ngram_torch_stats_elapsed_ms=489`, and `ngram_update_batch_timing calls=3 elapsed_ms=1286`, so the reviewed stop gate did not trigger. Enabled full-val scored `legal_ttt_exact val_loss=0.49164521`, `val_bpb=0.29117999`, with `606325ms` script wallclock and `647s` managed; full-val telemetry was `ngram_ctx_key_precompute resident_bytes=1984692256 elapsed_ms=21311`, `ngram_batch_lookup_call_count=30770`, `ngram_batch_lookup_elapsed_ms=19874`, `ngram_batch_lookup_positions_total=62021632`, `ngram_batch_lookup_max_positions_per_call=4032`, `ngram_torch_stats_batches=30770`, `ngram_torch_stats_elapsed_ms=3257`, `ngram_torch_stats_scored_positions_total=62021632`, `ngram_update_batch_timing calls=63 elapsed_ms=32367`, any-match `0.98387524`, avg alpha `0.64246912`, and the same matched-order histogram as `eval_025`; deltas were `+0.00000016` and `-7903ms` vs `eval_025`, `+0.00000154` and `-24176ms` vs `eval_018`, and `-0.15042001` vs official `0.4416`; total bytes were `15674467` | This is a controlled positive runtime probe but not full legality recovery. The controls passed and full-val BPB stayed effectively locked, while exact batched torch-side scorer stats recovered another `7903ms` of end-to-end runtime and showed nontrivial `ngram_torch_stats_elapsed_ms=3257`. But the run still missed the script budget by `6325ms` and the managed budget by `47s`. Classification: `runtime-improved-but-still-illegal` | Promote `eval_026` to the active legality-recovery baseline and target a different non-overlapping runtime lever on top of this batched scorer plus batched torch-stats line rather than revisiting scorer-side neural-stat extraction |
