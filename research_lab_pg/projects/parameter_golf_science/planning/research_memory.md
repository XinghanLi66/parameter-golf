# Research Memory

This file is the compact scientific memory for the project. Keep it updated so the planner can avoid redundant work.

## Working SOTA Anchor — 2026-03-26 (UPDATED)

**TARGET SOTA: PR #803 at 0.4416 BPB** — Complementary Training + Backoff N-gram (orders 2-10) + AdamW TTT.
(PR #809 at 0.2952 is excluded — under legality review by the user.)

This puts us in **AGGRESSIVE phase**:
- best local single seed: `1.11935008` from `eval_006`
- gap to target SOTA: **~0.722 BPB**
- required action: implement PR #803-style n-gram backoff + complementary training

`context/reference_materials/URGENT_ngram_backoff_breakthrough.md` is **authoritative and mandatory** context.
`context/reference_materials/latest_sota_snapshot.md` is **authoritative** — read it first every round.
The prior snapshot (1.1194) is obsolete. The new target is 0.4416 BPB.

## Confirmed Signals
- The controlled refinement `eval_012_arch010_legal_ttt_logit_bias_seed1337` answered the helper-validity part positively and the mechanism part negatively on locked seed-`1337` `arch_010`: after introducing one scoring-only `EVAL_LOGIT_BIAS_LAMBDA` hook plus one fixed off-val `log_prior`, the full-val parity run at `lambda=0.0` scored `1.11935063`, only `+0.00000055` from historical `eval_006=1.11935008`, so the helper edit is scientifically trustworthy.
- The `eval_012` helper/artifact guardrail stayed explicit: the copied helper started at `84059` bytes with SHA-256 `d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8` and ended at `87184` bytes with SHA-256 `227f842be584dacd24b77b4a58de423a7d8790a65b822d23133444abf0f2c8be`, while the saved seed-`1337` `final_model.pt` and `final_model.int6.ptz` stayed unchanged before and after all runs at `106178569` bytes / `b8291ad1...` and `15555121` bytes / `eb062c96...`.
- The frozen `eval_009` held-out slice is now recorded in three progressively tighter calibration uses on locked legal-TTT. In `eval_012`, it was deterministically split by token offset into a prior-build shard `b3102ee1...` and a selector shard `286e6e4e...`, each `1048577` tokens / `1048576` scored, with split boundaries `[0, 1048577)` and `[1048576, 2097153)` within the original `8b5e92cc...` slice.
- The `eval_012` prior rule is now fixed and explicit for future avoidance or reuse: count target-token frequencies over scored positions in the prior-build half only, apply add-one smoothing over the full vocab, normalize once, store `log_prior`, and use it only at scoring time inside `eval_val_sliding_ttt()`.
- On that disjoint selector half, every nonzero fixed token-prior bias was worse than `lambda=0.0`: `-0.10 -> 1.12511628`, `-0.05 -> 1.12345755`, `0.00 -> 1.12285060`, `0.05 -> 1.12334301`, `0.10 -> 1.12489128`. The selector therefore chose `EVAL_LOGIT_BIAS_LAMBDA=0.0`, which is direct evidence against this simple unigram calibration family even after separating prior construction from selection.
- Because the selector chose `lambda=0.0`, the parity run is also the final full-val result for `eval_012`: `1.11935063`, which is `-0.00041034` better than the refined legal-TTT 3-seed mean `1.11976097`, `-0.00004937` better than live SOTA `1.1194`, and still under the practical eval budget at `470956ms` script wallclock and `512s` managed wallclock.
- The controlled refinement `eval_011_arch010_legal_ttt_backoff_ngram_seed1337` answered the helper-validity part positively and the mechanism part negatively on locked seed-`1337` `arch_010`: after introducing one scoring-only `EVAL_NGRAM_LAMBDA` hook plus a fixed higher-order prefix-only backoff cache, the full-val parity run at `EVAL_NGRAM_LAMBDA=0.0` scored `1.11935518`, only `+0.00000510` from historical `eval_006=1.11935008`, so the helper edit is scientifically trustworthy.
- The `eval_011` helper/artifact guardrail stayed explicit: the copied helper started at `89121` bytes with SHA-256 `1cecd32edeb0d4c10fe29d3011a52d32324e8f39f068df478e06333763e1fde2` and ended at `94173` bytes with SHA-256 `60779a33716859ff74ca25ee82ef57e778350a4faba1588d2aa29b1285048ec3`, while the saved seed-`1337` `final_model.pt` and `final_model.int6.ptz` stayed unchanged before and after all runs at `106178569` bytes / `b8291ad1...` and `15555121` bytes / `eb062c96...`.
- The exact held-out selector slice from `eval_009` is now reused across three controlled eval-side refinement rounds on locked legal-TTT. In `eval_011`, the prescribed grid selected a small nonzero weight: `0.00 -> 1.11556687`, `0.01 -> 1.11494470`, `0.02 -> 1.11689406`, `0.05 -> 1.12465863`, `0.10 -> 1.14061444`, so the held-out selector chose `EVAL_NGRAM_LAMBDA=0.01` by `-0.00062217` versus `0.00`.
- That held-out-selected nonzero higher-order backoff weight did not transfer to full validation. The final full-val run at `EVAL_NGRAM_LAMBDA=0.01` scored `1.12044055`, which is `+0.00108537` worse than the parity run `1.11935518`, `+0.00109047` worse than historical `eval_006`, `+0.00067958` worse than the refined legal-TTT 3-seed mean `1.11976097`, and `+0.00104055` worse than live SOTA `1.1194`.
- The `eval_011` final full-val path is also now answered decisively negative on runtime: selected-lambda script eval wallclock was `1859145ms` and managed wallclock was `1901s`, more than `3x` the practical `10 min` eval budget. This makes the mechanism non-promotable even aside from its BPB regression.
- The `eval_011` matched-order statistics show that the failure is not explained by lack of higher-order reuse opportunities. On the selected held-out run, any-match coverage was `0.95245996`; on final full validation, any-match coverage was `0.95310151`, with order-2 and order-3 matches dominating both cases and still substantial higher-order match fractions through order 9. The local data clearly contains reusable prefixes, but this fixed count-based blend does not improve the locked legal-TTT line.
- The controlled refinement `eval_010_arch010_legal_ttt_prefix_bigram_interp_seed1337` answered the helper-validity part cleanly positively and the mechanism part strongly negatively on locked seed-`1337` `arch_010`: after introducing one scoring-only `EVAL_BIGRAM_LAMBDA` hook plus a chunk-local prefix-only bigram mixer, the full-val parity run at `EVAL_BIGRAM_LAMBDA=0.0` scored `1.11935127`, only `+0.00000119` from historical `eval_006=1.11935008`, so the helper edit is scientifically trustworthy.
- The `eval_010` helper/artifact guardrail stayed explicit: the copied helper started at `84059` bytes with SHA-256 `d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8` and ended at `89121` bytes with SHA-256 `1cecd32edeb0d4c10fe29d3011a52d32324e8f39f068df478e06333763e1fde2`, while the saved seed-`1337` `final_model.pt` and `final_model.int6.ptz` stayed unchanged before and after all runs at `106178569` bytes / `b8291ad1...` and `15555121` bytes / `eb062c96...`.
- The exact held-out selector slice from `eval_009` is now reused across two controlled eval-side refinement rounds on locked legal-TTT: `runs/eval_009_arch010_legal_ttt_temperature_seed1337/heldout_data/fineweb_val_000000.bin`, sourced from `fineweb_train_000000.bin` at token offset `8388608`, copied `2097153` tokens (`2097152` scored), covered exactly `64` TTT chunks, and hashed to `8b5e92cca71fcfb03dff3a5b2cbf37b25e15a476e8218d253006f1bbcb4db556`.
- On that frozen held-out slice, the full prescribed `eval_010` grid selected `EVAL_BIGRAM_LAMBDA=0.0`, not a nonzero mixer weight: `0.00 -> 1.11556493`, `0.05 -> 1.12860428`, `0.10 -> 1.14760131`, `0.15 -> 1.16896766`, `0.20 -> 1.19233780`. The degradation is monotonic once `lambda > 0`, which is direct evidence against this exact chunk-local prefix-bigram mixer on the locked legal-TTT line.
- Because the held-out sweep selected `lambda=0.0`, the selected-lambda final full-val result is the same as parity: `1.11935127`, which leaves the strongest local single-seed result effectively unchanged versus historical `eval_006`, remains `-0.00004873` better than live SOTA `1.1194`, and confirms that the score strength still comes from the existing refined legal-TTT regime rather than from the new bigram mechanism.
- The controlled refinement `eval_009_arch010_legal_ttt_temperature_seed1337` answered the helper-validity part cleanly positively and the mechanism part negatively on locked seed-`1337` `arch_010`: after introducing one scoring-only `EVAL_TEMPERATURE` hook, the full-val parity run at `EVAL_TEMPERATURE=1.0` scored `1.11935013`, only `+0.00000005` from the historical `eval_006=1.11935008`, so the helper edit is scientifically trustworthy.
- The `eval_009` helper/artifact guardrail stayed explicit: the copied helper started at `84059` bytes with SHA-256 `d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8` and ended at `84578` bytes with SHA-256 `3db38c0e2be23fc1a923d578fc07c096afe51ea28249777a4ac636b83ef936c9`, while the saved seed-`1337` `final_model.pt` and `final_model.int6.ptz` stayed unchanged before and after all runs at `106178569` bytes / `b8291ad1...` and `15555121` bytes / `eb062c96...`.
- The fixed held-out calibration slice for `eval_009` is now recorded exactly and can be avoided or reused deliberately in future work: it was carved from `/newcpfs/lxh/parameter-golf/data/datasets/fineweb10B_sp1024/fineweb_train_000000.bin` starting at token offset `8388608`, copied `2097153` tokens (`2097152` scored), covered exactly `64` TTT chunks at `32768` tokens each, and produced shard `runs/eval_009_arch010_legal_ttt_temperature_seed1337/heldout_data/fineweb_val_000000.bin` with SHA-256 `8b5e92cca71fcfb03dff3a5b2cbf37b25e15a476e8218d253006f1bbcb4db556`.
- On that frozen held-out train slice, the full prescribed grid selected `EVAL_TEMPERATURE=1.0`, not an off-baseline temperature: `0.95 -> 1.11733953`, `0.975 -> 1.11620707`, `1.00 -> 1.11555286`, `1.025 -> 1.11614565`, `1.05 -> 1.11734831`. The ordering is strongly peaked around `1.0`, which is direct evidence against simple global miscalibration on this locked legal-TTT line.
- Because the held-out sweep selected `T=1.0`, the selected-temperature final full-val result is the same as parity: `1.11935013`, which leaves the strongest local single-seed result effectively unchanged versus historical `eval_006`, remains `-0.00004987` better than live SOTA `1.1194`, and confirms that the score strength comes from the existing refined legal-TTT regime rather than from the new temperature mechanism.
- The controlled refinement `eval_008_arch010_legal_ttt_epochs4_seed42_confirm` is now answered positively on the final locked seed-`42` `arch_010` legal-TTT line: keeping the exact copied helper, the exact saved seed-`42` artifact lineage, and the winning `TTT_LR=0.0025`, `TTT_EPOCHS=4` setting fixed improved post-export `sliding_window stride=64 val_bpb` from `1.12032675` to `1.11998885`.
- The `eval_008` helper/artifact guardrail stayed explicit and fully locked: the copied helper remained byte-identical to the `eval_007` lineage at `84059` code bytes with SHA-256 `d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8`, while the saved seed-`42` `final_model.pt` and `final_model.int6.ptz` stayed unchanged before and after the run at `106178569` bytes / `c59cdd33...` and `15802352` bytes / `3db765aa...`.
- Because helper identity and artifact identity stayed locked in `eval_008`, the brief again allowed skipping a new no-TTT parity rerun. The scientifically relevant comparisons therefore remain valid against the locked seed-`42` legal-TTT baseline `1.12032675` and the inherited same-script no-TTT parity baseline `1.12281449` from `eval_003`.
- On the locked seed-`42` saved artifact, the refined setting improved post-export `val_bpb` by `-0.00033790` versus the locked seed-`42` legal-TTT baseline, by `-0.00282564` versus the same-script no-TTT parity baseline, by `+0.00063877` versus the seed-`1337` `eval_006` breakthrough, and by `+0.00004488` versus the seed-`2024` `eval_007` confirmation.
- The refined exact 3-seed mean for `TTT_LR=0.0025`, `TTT_EPOCHS=4` is now fully locked at `1.11976097` across seeds `1337`, `2024`, and `42`. That beats the older locked legal-TTT 3-seed mean `1.12010450` by `-0.00034353`, which is strong enough to promote the higher-budget eval regime across the locked `arch_010` line.
- Eval cost on `eval_008` increased materially versus the seed-`42` locked `TTT_EPOCHS=3` run but stayed below the practical limit, which keeps the promotion scientifically actionable: script TTT eval wallclock was `461177ms` versus `404338ms` (`+56839ms`), managed wallclock was `503s` versus `444s` (`+59s`), and the run still finished under `10 min` with about `97s` of managed headroom.
- The controlled refinement `eval_007_arch010_legal_ttt_epochs4_seed2024_confirm` is now answered positively on the locked seed-`2024` `arch_010` legal-TTT line: keeping the exact copied helper, the exact saved seed-`2024` artifact lineage, and the winning `TTT_LR=0.0025`, `TTT_EPOCHS=4` setting fixed improved post-export `sliding_window stride=64 val_bpb` from `1.12027199` to `1.11994397`.
- The `eval_007` helper/artifact guardrail stayed explicit and fully locked: the copied helper remained byte-identical to the `eval_006` lineage at `84059` code bytes with SHA-256 `d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8`, while the saved seed-`2024` `final_model.pt` and `final_model.int6.ptz` stayed unchanged at `106178569` bytes / `302fe0ad...` and `15761090` bytes / `479e5ccc...`.
- Because helper identity and artifact identity stayed locked in `eval_007`, the brief again allowed skipping a new no-TTT parity rerun. The scientifically relevant comparisons therefore remain valid against the locked seed-`2024` legal-TTT baseline `1.12027199` and the inherited same-script no-TTT parity baseline `1.12225535` from `eval_004`.
- On the locked seed-`2024` saved artifact, the `eval_006` setting improved post-export `val_bpb` by `-0.00032802` versus the locked seed-`2024` legal-TTT baseline, by `-0.00231138` versus the same-script no-TTT parity baseline, and by `-0.00016053` versus the locked legal-TTT 3-seed mean `1.12010450`.
- The seed-`2024` `eval_007` result is weaker than the seed-`1337` `eval_006` breakthrough by `+0.00059389`, but it still lands inside the brief’s “especially strong” band `<= 1.12010`, which is strong enough to justify the final exact seed-`42` confirmation before any full-line promotion decision.
- Eval cost on `eval_007` increased materially versus the seed-`2024` locked `TTT_EPOCHS=3` run but stayed below the practical limit, which keeps the result scientifically actionable: script TTT eval wallclock was `465406ms` versus `400348ms` (`+65058ms`), managed wallclock was `506s` versus `441s` (`+65s`), and the run still finished under `10 min` with about `94s` of managed headroom.
- The controlled refinement `eval_006_arch010_legal_ttt_epochs4_seed1337` is now answered positively on the locked seed-`1337` `arch_010` legal-TTT line: keeping the exact copied helper, the exact saved artifact lineage, and `TTT_LR=0.0025` fixed while changing only `TTT_EPOCHS` from `3` to `4` improved post-export `sliding_window stride=64 val_bpb` from `1.11956042` to `1.11935008`.
- The `eval_006` helper/artifact guardrail stayed explicit and fully locked rather than inferred: the copied helper remained byte-identical to the locked `eval_005` lineage at `84059` code bytes with SHA-256 `d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8`, while the saved seed-`1337` `final_model.pt` and `final_model.int6.ptz` again stayed unchanged at `106178569` bytes / `b8291ad1...` and `15555121` bytes / `eb062c96...`.
- Because helper identity and artifact identity stayed locked in `eval_006`, the brief again allowed skipping a new no-TTT parity rerun. The scientifically relevant comparisons therefore remain valid against the locked same-script no-TTT baseline `1.12211550`, the locked seed-`1337` TTT baseline `1.11971476`, and the locked seed-`1337` `TTT_EPOCHS=3` / `TTT_LR=0.0025` baseline `1.11956042`.
- On the locked seed-`1337` saved artifact, `TTT_EPOCHS=4` improved post-export `val_bpb` by `-0.00021034` versus the locked `TTT_EPOCHS=3` / `TTT_LR=0.0025` baseline, by `-0.00036468` versus the locked seed-`1337` `TTT_LR=0.002` legal-TTT baseline, by `-0.00276542` versus the same-script no-TTT parity baseline, and by `-0.00075442` versus the locked legal-TTT 3-seed mean `1.12010450`.
- The new single-seed result `1.11935008` is now the strongest local post-export readout seen so far and it is the first local result to cross live SOTA `1.1194`, doing so by `-0.00004992` while leaving bytes unchanged at artifact `15555121`, code `84059`, and total `15639180`.
- Eval cost on `eval_006` increased materially versus the locked `TTT_EPOCHS=3` run but stayed clearly below the practical limit, which keeps the result scientifically promotable if it reproduces: script TTT eval wallclock was `459677ms` versus `397765ms` (`+61912ms`), managed wallclock was `502s` versus `438s` (`+64s`), and the run still finished under `10 min` with about `98s` of managed headroom.
- The controlled refinement `eval_005_arch010_legal_ttt_lr0025_seed1337` is now answered positively on the locked seed-`1337` `arch_010` legal-TTT line: keeping the exact copied helper and exact saved artifact lineage fixed while changing only `TTT_LR` from `0.002` to `0.0025` improved post-export `sliding_window stride=64 val_bpb` from `1.11971476` to `1.11956042`.
- The `eval_005` helper/artifact guardrail is now explicit rather than assumed: the copied helper stayed byte-identical to the locked `eval_004` lineage at `84059` code bytes with SHA-256 `d93fc02cd20958d56d49af2df6b886527cef71d98ac46aa6304c4db048211bf8`, while the saved seed-`1337` `final_model.pt` and `final_model.int6.ptz` stayed unchanged at `106178569` bytes / `b8291ad1...` and `15555121` bytes / `eb062c96...`.
- Because helper identity and artifact identity stayed locked in `eval_005`, the brief’s guardrail allowed skipping a new no-TTT parity rerun. The scientifically relevant comparisons therefore remain valid against the locked same-script no-TTT baseline `1.12211550` and the locked seed-`1337` TTT baseline `1.11971476` from `eval_002`.
- On the locked seed-`1337` saved artifact, `TTT_LR=0.0025` improved post-export `val_bpb` by `-0.00015434` versus the locked seed-`1337` legal-TTT baseline, by `-0.00255508` versus the same-script no-TTT parity baseline, and by `-0.00054408` versus the locked legal-TTT 3-seed mean `1.12010450`.
- The new single-seed result `1.11956042` is now the strongest local post-export readout seen so far. It narrows the remaining live-SOTA gap from `+0.00031476` on `eval_002` to `+0.00016042` while leaving bytes unchanged at artifact `15555121`, code `84059`, and total `15639180`.
- Eval cost on `eval_005` stayed essentially flat relative to the locked `TTT_LR=0.002` run, which strengthens causal interpretation: script TTT eval wallclock was `397765ms` versus `396112ms` (`+1653ms`), and managed wallclock was `438s` versus `436s` (`+2s`).
- The controlled refinement `pending_opt_009_arch010_parallel_muon_seed1337_nottt` is now answered cleanly negatively on locked `arch_010`: a narrow row-sharded `Parallel Muon`-style port over the existing 2D parameter set preserved parameter count, state-dict keys, export tensor keys, export metadata, and cuDNN SDPA dispatch, yet still regressed the primary post-export no-TTT `sliding_window stride=64 val_bpb` from `1.12211549` to `1.14079891`.
- This round intentionally narrowed the published live-`#1` mechanism before implementation rather than silently mixing in Parameter Banking: the reviewed brief required structural parity, so the worker did not port the full banked runtime from the record note. The executed test used only the minimum locally controllable runtime changes needed to overlap Muon updates across ranks while keeping the exact `arch_010` parameter and export schema fixed.
- Structural parity evidence is now explicit rather than assumed: the managed 8-GPU smoke on the run-local script matched locked `arch_010` exactly at `26993756` parameters, identical `121` state-dict keys, identical `190` export tensor keys, and identical `121` export metadata keys and values. This means the negative result is scientifically usable as a single-variable training/runtime test rather than an invalidated architecture/export rewrite.
- The failure mode on `arch_010` was not subtle. Under the same `600s` wallclock cap, the `Parallel Muon` candidate reached only `4939` steps at `121.48 ms/step` versus the locked baseline’s `6734` at `89.10 ms/step`, a loss of `1795` steps and `+32.38 ms/step`. That step-budget collapse is large enough that late QAT enabled at `step=1`, so the training trajectory diverged immediately rather than only at the end.
- The negative exported result is now fully recorded despite distributed teardown noise: the full run wrote checkpoint `val_bpb=1.1577`, post-EMA `1.1573`, post-export non-overlapping `1.16429555`, artifact `16826625`, code `77935`, and total `16904560`; a separate managed 8-GPU eval-only recovery on the saved artifact then measured the missing exact primary readout `eval_only_int6_sliding_window_exact val_bpb=1.14079891`.
- The controlled `Parallel Muon` port is negative on all practical axes, not just the primary score: versus locked seed-`1337`, the exported stride-64 score regressed by `+0.01868342`, total bytes rose by `+1278174` to `16904560`, and the run exceeded the submission cap by `904560`. The artifact also regressed versus the exact no-TTT 3-seed mean by `+0.01840398` and versus live SOTA by `+0.02139891`.
- No evidence from this round supports later restacking of this failed training path with the already-locked legal-TTT evaluator. The controlled conclusion is narrower and stronger: on locked `arch_010`, this locally controllable `Parallel Muon` transfer is not the missing training-side lever, so the next refinement should move to a different single motif instead of bundling additional training/runtime changes onto this negative path.
- The controlled third-seed confirmation `eval_004_arch010_legal_ttt_seed2024_confirm` is now answered strongly positively on the locked `arch_010` seed-2024 export lineage: the exact copied `eval_003` helper stayed byte-identical at `84059` code bytes, recovered the archived no-TTT result at `1.12225535` with only `+0.00000055` delta versus `1.12225480`, then improved the exact same saved artifact source to `1.12027199` with legal score-first TTT.
- The no-TTT parity gate for `eval_004` again passed cleanly rather than loosely: the same-script recovered no-TTT seed-2024 score `1.12225535` stayed within `5.5e-7` of the archived `repro_007` seed-2024 result, so the third-seed TTT result is scientifically usable rather than confounded by helper drift.
- On the locked `arch_010` seed-2024 artifact lineage, legal score-first TTT improved post-export `val_bpb` by `-0.00198336` versus the recovered same-script no-TTT baseline, by `-0.00212294` versus the locked non-TTT 3-seed mean `1.12239493`, and landed `+0.00087199` above live SOTA `1.1194`.
- The seed-2024 legal-TTT gain is weaker than the first two saved export gains but does not collapse: versus seed `1337` the gain delta is `+0.00041738`, versus seed `42` it is `+0.00050438`, yet the result remains inside the brief’s strong-positive band and is effectively tied with seed `42` in absolute BPB (`-0.00005476`).
- The evaluation-only TTT regime on locked `arch_010` is now supported across all three locked saved export seeds rather than two: seed `1337` scored `1.11971476`, seed `42` scored `1.12032675`, seed `2024` scored `1.12027199`, and the resulting exact TTT 3-seed mean is `1.12010450`, only `+0.00070450` above live SOTA while changing no training or export bytes.
- The controlled seed-2024 TTT run preserved the same mechanism-level legality and recipe as the first two saved seeds: artifact bytes remained unchanged at `15761090`, the helper stayed unchanged at `84059` code bytes, and the TTT log again recorded `1893` chunks of `32768` tokens with `score_first=True`, `last_chunk_untrained=True`, `freeze_blocks=0`, and total windows `969088`.
- The evaluation cost of legal score-first TTT is now measured on the third saved export seed as well: same-script no-TTT eval on seed `2024` took `86179ms`, legal score-first TTT took `400348ms`, and managed wallclock was `126s` vs `441s`, so later comparisons can treat the TTT time cost as stable across all three saved export seeds rather than a two-seed coincidence.
- The controlled second-seed confirmation `eval_003_arch010_legal_ttt_seed42_confirm` is now answered strongly positively on the locked `arch_010` seed-42 export lineage: the exact copied `eval_002` helper stayed byte-identical at `84059` code bytes, recovered the archived no-TTT result exactly at `1.12281449`, then improved the exact same saved artifact source to `1.12032675` with legal score-first TTT.
- The no-TTT parity gate for `eval_003` passed exactly rather than approximately: the same-script recovered no-TTT seed-42 score `1.12281449` matched the archived `repro_006` seed-42 result with `+0.00000000` delta, so the seed-42 TTT result is scientifically usable rather than confounded by helper drift.
- On the locked `arch_010` seed-42 artifact lineage, legal score-first TTT improved post-export `val_bpb` by `-0.00248774` versus the recovered same-script no-TTT baseline, by `-0.00206818` versus the locked non-TTT 3-seed mean `1.12239493`, and landed only `+0.00092675` above live SOTA `1.1194`.
- The seed-42 legal-TTT gain is slightly stronger than the seed-1337 legal-TTT gain rather than collapsing: `-0.00248774` on seed `42` versus `-0.00240074` on seed `1337`, a gain difference of `-0.00008700`. Absolute BPB is `+0.00061199` worse on seed `42`, but both saved export seeds are now strongly positive with the same fixed recipe.
- The evaluation-only TTT regime on locked `arch_010` is now supported across two saved export seeds rather than one: seed `1337` scored `1.11971476`, seed `42` scored `1.12032675`, and the resulting local 2-seed TTT mean is `1.12002076`, which sits only `+0.00062076` above live SOTA while changing no training or export bytes.
- The controlled seed-42 TTT run preserved the same mechanism-level legality and recipe as seed `1337`: artifact bytes remained unchanged at `15802352`, the helper stayed unchanged at `84059` code bytes, and the TTT log again recorded `1893` chunks of `32768` tokens with `score_first=True`, `last_chunk_untrained=True`, `freeze_blocks=0`, and total windows `969088`.
- The evaluation cost of legal score-first TTT is now measured on the second saved export seed as well: same-script no-TTT eval on seed `42` took `86107ms`, legal score-first TTT took `404338ms`, and managed wallclock was `128s` vs `444s`, so later comparisons can treat the TTT time cost as stable rather than a seed-`1337` one-off.
- The controlled evaluation-only transfer `eval_002_arch010_legal_ttt_seed1337_export_eval` is now answered strongly positively on the locked `arch_010` seed-1337 export lineage: a run-local copy of `arch_010` with only evaluation-side TTT support recovered the archived no-TTT post-export `sliding_window stride=64 val_bpb` exactly at `1.12211550`, then improved the exact same saved artifact source to `1.11971476` with legal score-first TTT.
- The no-TTT parity gate for `eval_002` passed essentially perfectly rather than just loosely: the same-script recovered no-TTT score `1.12211550` is only `+0.00000001` from the archived `1.12211549`, so this round should be interpreted as a clean scientific readout of TTT transfer rather than an implementation drift.
- On the locked `arch_010` seed-1337 artifact lineage, legal score-first TTT improved post-export `val_bpb` by `-0.00240074` versus the recovered same-script no-TTT baseline and by `-0.00268017` versus the locked non-TTT 3-seed mean `1.12239493`; the result is only `+0.00031476` above live SOTA `1.1194`.
- The `eval_002` helper remained controlled at the mechanism level: edits were evaluation-side only, artifact bytes remained unchanged at `15555121`, and the TTT run logged `1893` chunks of `32768` tokens with `score_first=True`, `last_chunk_untrained=True`, and `freeze_blocks=0`, which matches the reviewed legal-TTT intent.
- The fixed TTT recipe that produced the positive transfer was `TTT_LR=0.002`, `TTT_EPOCHS=3`, `TTT_CHUNK_TOKENS=32768`, `TTT_FREEZE_BLOCKS=0`, `TTT_MOMENTUM=0.9`, `TTT_BATCH_SEQS=32`, and `TTT_GRAD_CLIP=1.0`; no retraining, no export rewrite, and no Parameter Banking or Parallel Muon were involved.
- The evaluation cost of legal TTT is now measured on the exact same saved artifact source: same-script no-TTT eval took `100456ms`, while legal score-first TTT took `396112ms`, so the gain is large but the eval-time cost is also materially higher and should remain explicit in later comparisons.
- The controlled third-seed confirmation `repro_007_record02_leakyrelu2_seed2024_confirm` is now answered positively on the exact `arch_010` script: keeping the copied run-local `train_gpt.py` byte-identical at `71265` bytes and changing only `SEED=2024` produced checkpoint `val_bpb=1.1389`, post-EMA diagnostic `1.1380`, post-export non-overlapping `1.14585946`, and recovered post-export `sliding_window stride=64 val_bpb=1.12225480`.
- On the exact reproduced leaderboard-`#2` lineage, the seed-2024 `LeakyReLU(0.5)^2` rerun stayed only `+0.00013931` behind the seed-1337 `arch_010` result and `-0.00055969` ahead of the seed-42 rerun, while still beating the pre-activation reference `repro_005` by `-0.00258869`.
- The exact `LeakyReLU(0.5)^2` recipe on the reproduced leaderboard-`#2` stack now has a local 3-seed mean of `1.12239493` (`1.12211549`, `1.12281449`, `1.12225480`), which stays below the documented record-`#2` mean band near `1.1233` and is strong enough to lock `arch_010` as a credible 3-seed baseline for the next refinement round.
- The seed-2024 rerun preserved the same backend state and nearly identical geometry as the first two seeds rather than drifting to a new runtime path: it again logged `eligible_cudnn=True`, `eligible_flash=False`, `observed_backend=cudnn`, `observed_cudnn_active=True`, matched `swa:start=6050`, reached `6725` steps in `600028ms`, and held steady-state throughput at `89.22 ms/step`.
- The full seed-2024 training run wrote checkpoint, post-EMA, artifact-byte, and exact roundtrip non-overlapping metrics successfully, but the process remained alive in its final export-eval path, so the missing stride-64 exported metric was recovered afterward from the saved `final_model.pt` and `final_model.int6.ptz` with a separate managed 8-GPU eval-only helper. That recovery path produced `1.12225480` and should be treated as part of the same scientific result, not as a new experiment variable.
- The controlled seed-only confirmation `repro_006_record02_leakyrelu2_seed42_confirm` is now answered positively on the exact `arch_010` script: keeping the copied run-local `train_gpt.py` byte-identical at `71265` bytes and changing only `SEED=42` produced checkpoint `val_bpb=1.1390`, post-EMA diagnostic `1.1381`, post-export non-overlapping `1.14644940`, and post-export `sliding_window stride=64 val_bpb=1.12281449`.
- On the exact reproduced leaderboard-`#2` lineage, the seed-42 `LeakyReLU(0.5)^2` rerun stayed only `+0.00069900` behind the seed-1337 `arch_010` result (`1.12211549 -> 1.12281449`), comfortably inside the predeclared `+0.0015` band, while still beating the pre-activation seed-42 reference `repro_005` by `-0.00202900`.
- The `repro_006` full run preserved the same backend state and training geometry as `arch_010`, not just the same requested flags: it again logged `eligible_cudnn=True`, `eligible_flash=False`, `observed_backend=cudnn`, `observed_cudnn_active=True`, matched `swa:start=6050`, reached `6737` steps in `600054ms`, and held steady-state throughput at `89.07 ms/step` versus `89.10 ms/step` on seed `1337`.
- The exact `LeakyReLU(0.5)^2` recipe on the reproduced leaderboard-`#2` stack now has a local 2-seed mean of `1.12246499` (`1.12211549`, `1.12281449`), which is below the documented record-`#2` 3-seed mean band near `1.1233` and supports promoting `arch_010` from a one-seed win to the stable local baseline for this lineage.
- The seed-42 rerun essentially ties the documented record-`#2` seed-1337 readout even on the local cuDNN-backed fallback path: `1.12281449` is only `+0.00003427` behind documented `1.12278022`, while remaining under the byte cap at `15873617` total bytes.
- The controlled activation-only transfer `arch_010_record02_leakyrelu2_keep_cudnn_recipe` is now answered positively on top of the best local reproduced leaderboard-`#2` stack: changing only the MLP activation from `ReLU^2` to `LeakyReLU(0.5)^2` improved checkpoint `val_bpb` to `1.1384`, post-EMA diagnostic `val_bpb` to `1.1375`, post-export non-overlapping `val_bpb` to `1.14584172`, and post-export `sliding_window stride=64 val_bpb` to `1.12211549`.
- The `arch_010` edit was structurally controlled before any GPU budget was spent: the copied run-local script differed from `opt_008` by one line only, code bytes changed only `71243 -> 71265` (`+22`), state-dict keys/shapes stayed identical at `121`, mixed-int6 export tensor keys stayed identical at `190`, and export meta keys/values stayed identical.
- The managed 8-GPU smoke and full runs for `arch_010` preserved the same cuDNN-backed fallback state as `opt_008`, not just the same requested flags: both runs logged `eligible_cudnn=True`, `eligible_flash=False`, `observed_backend=cudnn`, `observed_cudnn_active=True`, and cuDNN SDPA events including `aten::_scaled_dot_product_cudnn_attention`.
- On the exact reproduced leaderboard-`#2` lineage, the activation-only change beat the previous active best `opt_008` by `-0.00213695` post-export `sliding_window stride=64 val_bpb` (`1.12425244 -> 1.12211549`) while improving checkpoint quality by `-0.0023` and post-EMA quality by `-0.0022`.
- The `arch_010` result also beat the seed-42 robustness reference `repro_005` by `-0.00272800` post-export (`1.12484349 -> 1.12211549`), so the effect size is larger than the current two-seed spread on the pre-activation cuDNN-backed baseline.
- The new activation transfer slightly beats the documented record-`#2` seed-1337 readout despite staying on the local cuDNN-backed fallback path: `1.12211549` is `-0.00066473` better than documented `1.12278022`, though total bytes are still `+71369` higher and the run reached `6734` steps rather than documented `7101`.
- Step geometry stayed close enough to keep the result interpretable as a controlled refinement rather than a confounded runtime change: versus `opt_008`, the full run changed from `6796` to `6734` steps (`-62`) and from `88.29` to `89.10 ms/step` (`+0.81`), while holding the same backend probe outcome.
- The remaining gap to live SOTA shrank materially on this local line: `arch_010` reduced the live-SOTA miss from `+0.00485244` on `opt_008` to `+0.00271549`.
- The controlled portability audit `infra_002_record02_flashattention3_binding_audit` answered the native-FlashAttention question negatively inside the validated shim-free `physicslm` environment: a managed 1-GPU audit run in `physicslm` reported `cuda_available=true`, `torch=2.10.0+cu128`, but `flash_attn_interface_spec=null`, `flash_attn_spec=null`, and `status=missing_flash_attn_interface`, so no controlled smoke/full scored comparison was valid to launch this round.
- The same `infra_002` audit also bounded the “available elsewhere” escape hatch enough for planning: `loongflow_ml` lacked top-level `flash_attn_interface`, top-level `flash_attn`, and native `zstandard`; although a filesystem search found `vllm/vllm_flash_attn/flash_attn_interface.py` there, attempting to import `flash_attn_func` from it failed, so it does not qualify as a comparable alternate environment for the reproduced leaderboard `#2` stack.
- `physicslm` does expose `xformers.flash_attn_3`, but not the exact audited binding `flash_attn_interface.flash_attn_func`; under the reviewed brief, that means the native FA3 portability variable remains unavailable rather than “partially reproduced,” because using a different module path would require a different implementation change than this controlled refinement allowed.
- The controlled seed-only robustness confirmation `repro_005_record02_cudnn_sdpa_seed42_confirm` is now answered positively on top of the cuDNN-backed reproduced leaderboard `#2` stack: reusing the exact `opt_008` run-local script unchanged and changing only `SEED=42` produced `1.12484349` post-export at `15787042` total bytes, only `+0.00059105` behind the seed-1337 `opt_008` result.
- The `repro_005` smoke and full runs matched the `opt_008` backend state rather than drifting to a different path: both runs logged `eligible_cudnn=True`, `eligible_flash=False`, `observed_backend=cudnn`, `observed_cudnn_active=True`, and kept the same fallback wrapper `F.scaled_dot_product_attention(..., enable_gqa=True)` with code bytes unchanged at `71243`.
- On the exact `opt_008` stack, changing only the seed preserved the recovered cuDNN training geometry almost exactly: the seed-42 run reached the same `6796` steps in `600040ms`, the same steady-state `88.29 ms/step`, the same `swa:start=6150`, and the same `late_qat:enabled step=6276`, while remaining clearly better than `repro_004` by `-0.00353571` post-export.
- The two local cuDNN-backed reproduced `#2` seeds now cluster tightly at `1.12425244` and `1.12484349`, for a local 2-seed mean of `1.12454797`; this is strong enough to support the robustness hypothesis even though the seed-42 artifact bytes are larger by `230884`.
- The controlled backend-only fidelity isolation `opt_008_record02_cudnn_sdpa_fallback_isolation` is now answered positively on top of the reproduced leaderboard `#2` stack: keeping the fallback wrapper fixed as `F.scaled_dot_product_attention(..., enable_gqa=True)` and changing only the SDPA backend selection from flash-only to cuDNN-only produced a new best byte-safe local result of `1.12425244` post-export at `15556158` total bytes.
- The `opt_008` smoke and full runs both confirmed the intended backend state directly rather than inferring it from flags alone: the probe logged `eligible_cudnn=True`, `eligible_flash=False`, `observed_backend=cudnn`, `observed_cudnn_active=True`, and emitted cuDNN SDPA events including `aten::_scaled_dot_product_cudnn_attention`.
- On the exact `repro_004` baseline, the backend-only change recovered `712` steps inside the same `600s` cap (`6084 -> 6796`), improved steady-state throughput from `98.62` to `88.29 ms/step` (`-10.33`, about `+11.7%` tokens/s), improved checkpoint `val_bpb` from `1.1446` to `1.1407`, improved post-EMA `val_bpb` from `1.1437` to `1.1397`, and improved post-export `sliding_window stride=64 val_bpb` from `1.12837920` to `1.12425244` (`-0.00412676`).
- The same `opt_008` run also improved byte accounting despite slightly larger code: artifact bytes fell from `15733455` to `15484915` (`-248540`), code bytes rose from `68137` to `71243` (`+3106`) because of the diagnostic probe, and total bytes still fell from `15801592` to `15556158` (`-245434`).
- The remaining miss versus documented record `#2` is now much smaller than before the backend isolation: post-export gap shrank from `+0.00559898` on `repro_004` to `+0.00147222` on `opt_008`, step gap shrank from `-1017` to `-305`, and the current gap to live SOTA `1.1194` is now `+0.00485244`.
- The aggressive record-`#2` reproduction `repro_004_record02_11l_ema_gptqlite_warmdown3500_qat015_local_repro` is now answered positively in the main sense: extracting the record-`#2` markdown script into a run-local `train_gpt.py`, then running one smoke pass and one full managed 8-GPU run in `physicslm`, produced a byte-safe post-export `sliding_window stride=64 val_bpb` of `1.12837920` at `15801592` total bytes.
- This full-stack reproduction materially beats the prior active local best `arch_009_record06_leakyrelu2_keep_swa_export`: post-export `val_bpb` improved from `1.14068897` to `1.12837920` (`-0.01230977`), and total bytes fell from `15961473` to `15801592` (`-159881`), so the new `11L EMA + GPTQ-lite + warmdown3500 + QAT@0.15` line should replace `record06 + LeakyReLU^2 + SWA` as the active baseline.
- The local `repro_004` run reproduced the intended leaderboard-`#2` motifs rather than only a loose approximation: `11L`, `MLP3x`, `XSA4`, `ROPE_DIMS=16`, `LN_SCALE=1`, `VE128`, `BigramHash(2048,128)`, `SmearGate`, `EMA decay=0.997`, tight `SWA_EVERY=50`, `WARMDOWN_ITERS=3500`, late QAT threshold `0.15`, and GPTQ-lite int6 clip search with percentiles `[0.9990, 0.9995, 0.9999, 0.99999, 1.0]`.
- The smoke path for `repro_004` passed the required reproduction gates on 8 GPUs: instantiate, export, reload, direct-runtime `zstd` (`28 b5 2f fd` header), exact roundtrip/load correctness, and final `sliding_window stride=64` evaluation all completed successfully before the full run.
- The full `repro_004` run also hit the intended late-phase training markers locally: `swa:start step:5400`, `late_qat:enabled step:5564 scale:0.1498`, and `ema:applying EMA weights` all appeared in the runtime log before export.
- The remaining gap to the documented record-`#2` result is now narrowed to a portability/runtime issue rather than a stack-selection issue: the local run finished at `step=6084` versus the documented seed-1337 `7101` (`-1017`) and still landed only `+0.00559898` behind the documented `1.12278022`.
- The only explicit local fidelity break in `repro_004` is now identified and recorded: local `physicslm` lacks `flash_attn_interface.flash_attn_func`, so the run-local script needed a minimal compatibility fallback to `torch.nn.functional.scaled_dot_product_attention(..., enable_gqa=True)` to preserve the record attention contract.
- The controlled `record06 + LeakyReLU^2` second-seed confirmation `repro_003_record06_leakyrelu2_seed1337_confirm` is now answered positively on the clean shim-free line: reusing the `arch_009` run-local script unchanged with only `SEED=1337`, the managed 8-GPU `physicslm` run finished at `step=6603` in `600031ms`, matched `swa:start step:5450`, applied `24` SWA checkpoints, emitted direct-runtime `zstd`, and scored `1.14276256` post-export at `15694527` total bytes with exact roundtrip/load correctness.
- On the exact clean seed-1337 baseline, the `LeakyReLU^2` transfer improved both raw and exported quality while staying comfortably byte-safe: checkpoint `val_bpb` improved from `1.1543` to `1.1529` (`-0.0014`), post-export `val_bpb` improved from `1.14374449` to `1.14276256` (`-0.00098193`), and total bytes rose from `15683401` to `15694527` (`+11126`), still under the cap by `305473`.
- The updated transferred `LeakyReLU^2` 2-seed mean is now `1.14172576`, which beats the old clean local 2-seed mean `1.14348979` by `-0.00176403` and the archived clean 3-seed mean `1.14276` by `-0.00103424`; this is strong enough to promote `record06 + LeakyReLU^2 + SWA` to the new active local baseline.
- The seed-1337 confirmation preserved the same export/load regime as seed-42 `arch_009`: code bytes stayed `52952`, the runtime artifact header remained `28 b5 2f fd` (`zstd`), exact roundtrip/load correctness passed, and the checkpoint-to-export gap was nearly unchanged at `-0.01013744` versus `-0.01021103` on seed-42.
- The aggressive `record06 + LeakyReLU^2` optimization transfer `opt_007_record06_leakyrelu2_ema_warmdown3500` is now answered negatively on the local best lineage: after smoke parity verification against `arch_009`, the full shim-free `physicslm` run finished at `step=6058` in `600061ms`, started `EMA` at `step=4854`, applied `EMA decay=0.999`, and scored `1.15374139` post-export at `15735870` total bytes.
- On the exact `arch_009` seed-42 baseline, `EMA + warmdown3500` regressed both raw and exported quality even though it reduced bytes: checkpoint `val_bpb` worsened from `1.1509` to `1.1547` (`+0.0038`), post-export `val_bpb` worsened from `1.14068897` to `1.15374139` (`+0.01305242`), artifact bytes fell from `15908521` to `15681354` (`-227167`), and total bytes fell from `15961473` to `15735870` (`-225603`).
- The same `opt_007` run greatly improved the checkpoint-to-export gap on this lineage, from `-0.01021103` on `arch_009` to `-0.00095861` (`+0.00925242`), which suggests the bundle improved compression-friendliness but not enough to offset the weaker raw checkpoint inside the fixed `600s` budget.
- Smoke verification for `opt_007` confirmed that the EMA-enabled run-local fork preserved the exact `arch_009` schema: `106` state-dict tensors with identical keys/shapes/dtypes, `167` exported tensors with identical keys, identical export metadata contents, direct-runtime `zstd`, and exact roundtrip/load correctness.
- The controlled `record06` `LeakyReLU(0.5)^2` transfer is now answered positively on the clean shim-free baseline: `arch_009_record06_leakyrelu2_keep_swa_export` changed only the MLP activation, preserved identical `106` state-dict tensor keys/shapes and identical `167` export tensor keys/meta, stayed shim-free in `physicslm`, emitted direct-runtime `zstd`, and finished at `step=6597` in `600028ms` with post-export `val_bpb=1.14068897`.
- On the exact `record06` seed-42 baseline, the `LeakyReLU^2` transfer improved both raw and exported quality while staying byte-safe: checkpoint `val_bpb` improved from `1.1537` to `1.1509` (`-0.0028`), post-export `val_bpb` improved from `1.14323509` to `1.14068897` (`-0.00254612`), and total bytes rose from `15758548` to `15961473` (`+202925`), still under the cap by `38527`.
- The `record06` `LeakyReLU^2` gain comes mainly from a better raw checkpoint rather than better export robustness: the checkpoint-to-export gap moved from `-0.01046491` on the clean seed-42 baseline to `-0.01021103` (`+0.00025388`), so export remained slightly less favorable than baseline even though the final exported score improved.
- The new `record06` `LeakyReLU^2` seed-42 result already beats every current local clean reference point available without a second rerun: versus clean seed-1337 `1.14374449`, post-export `val_bpb` improved by `-0.00305552`; versus the old clean local 2-seed mean `1.14348979`, by `-0.00280082`; and versus the archived 3-seed mean `1.14276`, by `-0.00207103`.
- The controlled `record06` `LeakyReLU^2` run preserved direct-runtime `zstd` and exact roundtrip/load correctness in both smoke and full paths; the emitted artifact header remained `28 b5 2f fd`, confirming that the archived static `final_int8_zlib_roundtrip_exact` log label is not evidence of a schema or container change.
- The controlled `record06` late-QAT transfer is now answered on the clean shim-free baseline: `opt_006_record06_late_qat_keep_swa` verified an exact QAT/export-map match over `61` low-bit tensors (`20` MLP `int5`, `39` attention `int6`, `2` bigram `int6`), started `QAT` at `step=5500` while keeping late `SWA` active, and finished at `step=6608` in `600024ms` with post-export `val_bpb=1.14339099`.
- On the exact `record06` seed-42 baseline, late QAT did not improve the target objective: versus clean seed-42 `1.14323509`, the post-export result regressed slightly to `1.14339099` (`+0.00015590`), raw checkpoint quality regressed materially from `1.1537` to `1.1613` (`+0.0076`), and total counted bytes rose from `15758548` to `16086488` (`+327940`), which is over the submission cap.
- The `record06` late-QAT run stayed shim-free in `physicslm`, emitted direct runtime `zstd` (`28 b5 2f fd` header), preserved exact roundtrip/load correctness, and kept the intended schedule separation: `SWA` still started on the archived late window at `step=5450`, while `QAT` started separately at `step=5500`.
- The clean archived `record06` reproduction is now stable across at least two archived seeds in the validated `physicslm` environment: the new seed-1337 rerun reached `step=6679` in `600049ms`, matched `swa:start step:5500`, applied `24` SWA checkpoints, emitted direct runtime `zstd`, and scored `1.14374449` post-export at `15683401` total bytes with exact roundtrip/load correctness.
- A local environment-fidelity audit is now complete for the archived `record06` stack: among realistic local candidates, only `physicslm` (`Python 3.11.14`, `torch 2.10.0+cu128`, `zstandard 0.25.0`) natively supports `torch.compile`, `torch.backends.cuda.enable_cudnn_sdp`, `torch.nn.functional.rms_norm`, `scaled_dot_product_attention(..., enable_gqa=True)`, and direct `zstd` export with no run-local shim.
- The archived `2026-03-20_10L_Int5MLP_MuonWD04_SWA50` stack is now cleanly validated on this machine when run in that shim-free environment: the managed 8-GPU rerun reached `step=6666` in `599961ms`, hit `swa:start step:5500`, applied `24` SWA checkpoints, emitted `zstd` directly at runtime, and scored `1.14323509` post-export at `15758548` total bytes.
- The clean `record06` rerun nearly matches the archived seed-42 reference on the same stack: post-export `val_bpb` is only `+0.00052315` worse than archived `1.14271194`, step count is only `43` lower than archived `~6709`, and `swa:start` is only `50` steps earlier than archived `5550`.
- The clean seed-1337 rerun also stays near the archived seed-1337 reference: post-export `val_bpb` is only `+0.00076033` worse than archived `1.14298416`, step count is only `32` lower than archived `6711`, and `swa:start` is again `50` steps earlier than archived `5550`; artifact and total bytes are both smaller than the archived seed-1337 run by `146785`.
- The clean `record06` rerun decisively resolves the ambiguity left by `repro_001`: versus the prior shimmed run, post-export `val_bpb` improved by `-0.08196845`, artifact bytes by `-213243`, total bytes by `-213243`, and reached steps improved by `+4443`, so runtime-path drift rather than recipe weakness dominated the earlier failure.
- External leaderboard evidence shows that `sliding-window eval` is a large, repeatable gain and is now baseline table stakes for serious runs.
- External leaderboard evidence shows that current top runs win by combining `evaluation + compression-aware export + quantization-friendly optimization + extra capacity`.
- The current root `train_gpt.py` is still close to the simple baseline family: `9L / 512 / MLP 2x`, plain validation chunks, and `int8+zlib` export.
- The current root `train_gpt.py` already reports both pre-export and post-export metrics, so it is suitable for controlled comparison experiments.
- The root `train_gpt.py` now has explicit evaluation modes: `non_overlapping`, `sliding_window`, and `both`, with fixed `EVAL_STRIDE` support and mode/stride/time labels on both pre-export and post-export evaluation logs.
- The external repo under `/newcpfs/lxh/parameter-golf` contains the real FineWeb validation shard at `/newcpfs/lxh/parameter-golf/data/datasets/fineweb10B_sp1024/fineweb_val_000000.bin` and the real tokenizer at `/newcpfs/lxh/parameter-golf/data/tokenizers/fineweb_1024_bpe.model`.
- Local real-asset evidence now confirms that `sliding_window stride=64` is materially better on the same checkpoint and same exported artifact: final pre-export `val_bpb` improved from `1.2515` to `1.2199` (`-0.0316`) and final post-export improved from `1.25689044` to `1.22505202` (`-0.03183842`).
- On the available 8xL20Z machine, the local runtime cost of the locked evaluation protocol is acceptable for reuse: about `3.8s` for `non_overlapping` versus `64.6s` for `sliding_window stride=64` on the full FineWeb validation split.
- Local export-only evidence now confirms that changing only the outer container from `zlib-9` to `zstd-22` is lossless and modestly byte-positive on the same real `int8` payload: artifact bytes fell from `15785852` to `15748490` (`-37362`, `-0.2367%`) while post-export `val_bpb` stayed exactly `1.22505202` and quantized tensors matched bit-for-bit after roundtrip.
- Local export-only evidence now confirms that broad `uniform int6` on the locked checkpoint is not quality-safe despite strong byte savings: versus regenerated `uniform int8 + zstd-22`, artifact bytes fell from `15748155` to `12382907` (`-3365248`, `-21.3692%`) but post-export `val_bpb` worsened from `1.22505202` to `1.29818551` (`+0.07313349`), far beyond the allowed `+0.003` band; both artifacts still roundtripped and loaded exactly.
- Local export-only evidence now confirms that `MLP-only int6` is materially safer than broad `uniform int6` on the same locked checkpoint: versus regenerated `uniform int8 + zstd-22`, lowering only `blocks.*.mlp.fc.weight` and `blocks.*.mlp.proj.weight` to `int6` reduced artifact bytes from `15748335` to `13946844` (`-1801491`, `-11.4392%`) while post-export `val_bpb` rose only from `1.22505202` to `1.22768330` (`+0.00263128`); both artifacts still roundtripped and loaded exactly.
- Local export-only evidence now suggests the selective-export search is near saturation on this checkpoint: adding `blocks.*.attn.proj.weight -> int6` on top of `MLP-only int6` reduced artifact bytes further from `13946844` to `13477497` (`-469347`, `-3.3653%`) and from regenerated `uniform int8` by `-2270838` (`-14.4195%`), but raised post-export `val_bpb` from `1.22768330` to `1.22805392` (`+0.00037062`) and to `+0.00300190` over regenerated `uniform int8`, slightly beyond the explicit guardrail; both artifacts still roundtripped and loaded exactly.
- Local optimization evidence now shows that changing only `WARMDOWN_ITERS` from `1200` to `3000` on the locked baseline recipe modestly improves export robustness under the fixed `MLP-only int6` readout even though the raw checkpoint metric is flat-to-slightly worse: checkpoint `val_bpb` moved from `1.22005399` to `1.22014196` (`+0.00008797`), but regenerated `uniform int8 + zstd-22` improved from `1.22505202` to `1.22427718` and `mlp_int6_else_int8 + zstd-22` improved from `1.22768330` to `1.22696259`; the `MLP-only int6` quantization gap fell from `+0.00762931` to `+0.00682063`.
- Local export-only retest evidence now confirms that the warmdown-3000 gain did not reopen the prior `attn.proj` frontier on the improved checkpoint: versus regenerated `uniform int8 + zstd-22`, the retested `mlp_int6_plus_attn_proj_int6_else_int8` artifact reached `13473753` bytes (`-2269507`, `-14.4157%`) but still scored `1.22740875`, which is `+0.00313158` over regenerated `uniform int8`; all three regenerated readouts on the warmdown checkpoint roundtripped and loaded exactly.
- The `export_005` retest still improved meaningfully versus historical `export_004` on the old checkpoint: the same `MLP + attn.proj int6` policy improved from `1.22805392` to `1.22740875` (`-0.00064517`) and its quantization gap improved from `+0.00799993` to `+0.00726679` (`-0.00073314`), but the regenerated `uniform int8` baseline improved too, so the explicit guardrail remained missed.
- Local optimization evidence now shows that changing only `MUON_MOMENTUM_WARMUP_STEPS` from `500` to `1500` on top of `WARMDOWN_ITERS=3000` further improves the fixed `MLP-only int6` exported readout even though the raw checkpoint metric gets worse: checkpoint `val_bpb` moved from `1.22014196` to `1.22061644` (`+0.00047447`), regenerated `uniform int8 + zstd-22` was essentially flat at `1.22427718 -> 1.22429451` (`+0.00001733`), and regenerated `mlp_int6_else_int8 + zstd-22` improved from `1.22696259` to `1.22676204` (`-0.00020055`); the `MLP-only int6` quantization gap fell from `+0.00682063` to `+0.00614561` (`-0.00067503`) and both regenerated artifacts still roundtripped and loaded exactly.
- Local architecture evidence now shows that changing only `MLP_MULT` from `2` to `3` on top of the locked `opt_002` recipe produces a large quality gain under the fixed protocol: checkpoint `val_bpb` improved from `1.22061644` to `1.20227788` (`-0.01833855`), regenerated `uniform int8 + zstd-22` improved from `1.22429451` to `1.20534335` (`-0.01895116`), and regenerated `mlp_int6_else_int8 + zstd-22` improved from `1.22676204` to `1.20764933` (`-0.01911272`); both regenerated artifacts still roundtripped and loaded exactly.
- Local architecture evidence now shows that changing only `NUM_LAYERS` from `9` to `10` on top of the locked `opt_002` recipe improves checkpoint and both regenerated export readouts with smaller byte growth than `MLP3x`: checkpoint `val_bpb` improved from `1.22061644` to `1.21510822` (`-0.00550822`), regenerated `uniform int8 + zstd-22` improved from `1.22429451` to `1.21806669` (`-0.00622782`), and regenerated `mlp_int6_else_int8 + zstd-22` improved from `1.22676204` to `1.22052401` (`-0.00623803`); both regenerated artifacts still roundtripped and loaded exactly, and the primary `mlp_int6_else_int8` total stayed under the cap at `15482717`.
- Local export-only evidence now confirms that the stronger `arch_002` 10-layer checkpoint reopens the prior `attn.proj` near-miss: versus regenerated `uniform int8 + zstd-22`, `mlp_int6_plus_attn_proj_int6_else_int8` reached `14893681` artifact bytes (`-2525506`, `-14.4984%`) and `14954974` total bytes while scoring `1.22086139`, which is only `+0.00279470` over regenerated `uniform int8`; versus regenerated `mlp_int6_else_int8`, it saved `527743` artifact bytes (`-3.4221%`) for only `+0.00033737 val_bpb`; all three regenerated readouts on the `arch_002` checkpoint roundtripped and loaded exactly.
- Local architecture evidence now confirms that adding `BigramHash(4096) + SmearGate` on top of the locked `arch_002` recipe materially improves raw quality and exported readouts: checkpoint `val_bpb` improved from `1.21510822` to `1.20992003` (`-0.00518819`), regenerated `uniform int8 + zstd-22` improved from `1.21806669` to `1.21486895` (`-0.00319774`), regenerated `mlp_int6_else_int8 + zstd-22` improved from `1.22052401` to `1.21774203` (`-0.00278198`), and regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` improved from `1.22086139` to `1.21797944` (`-0.00288195`); all three roundtripped and loaded exactly.
- On the stronger `arch_003` checkpoint, the quality gain comes with materially worse quantization robustness and higher bytes: the locked best `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` readout remains byte-safe at `15564216`, but its quantization gap grew from `+0.00575317` to `+0.00805941` (`+0.00230625`) and it is now `+0.00311049` over regenerated `uniform int8` on the same checkpoint, slightly beyond the old explicit guardrail.
- Local optimization evidence now shows that adding `Muon WD=0.04 + late SWA` on top of `arch_003` radically improves compressibility and makes regenerated `uniform int8 + zstd-22` the new best local byte-safe readout: the final scored checkpoint reached `1.20495912` `val_bpb`, regenerated `uniform int8 + zstd-22` reached `1.20880134` at `14133579` total bytes, artifact bytes fell by `3942731` versus the prior `arch_003` `uniform int8` baseline, and the `uniform int8` quantization gap shrank from `+0.00494892` to `+0.00384222`.
- The same `arch_003 + WD=0.04 + late SWA` run also shows that averaging choice can sharply change the best export regime: both prior 6-bit selective readouts became much worse despite much smaller byte totals, with regenerated `mlp_int6_else_int8 + zstd-22` worsening to `1.23449862` and regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` worsening to `1.23618602`, so the new checkpoint family strongly favors `uniform int8` rather than the earlier mixed-precision policies.
- Local optimization evidence now shows that replacing late `SWA` with fixed late `EMA` on the same `arch_003 + MUON WD=0.04` stack does not preserve the win: the final EMA-scored checkpoint regressed to `1.21491545`, regenerated `uniform int8 + zstd-22` regressed to `1.21883931` at `14280689` total bytes, and its quantization gap was effectively flat-to-worse at `+0.00392386`.
- The same `EMA` run did partially recover mixed-precision robustness relative to the catastrophic `opt_003` SWA outcome, but not enough to reopen the old frontier: regenerated `mlp_int6_else_int8 + zstd-22` improved from `1.23449862` to `1.23326469` and regenerated `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22` improved from `1.23618602` to `1.23457111`, yet both remain far worse than the pre-`opt_003` `arch_003` mixed-precision results.
- Local export-only evidence now shows that one bounded `GPTQ-lite` int8 clip search does not improve the winning `opt_003` readout: on the same checkpoint, `uniform_int8_gptq_lite + zstd-22` scored `1.20884738` with quantization gap `+0.00388827` and exact roundtrip/load correctness, versus the locked regenerated `uniform int8 + zstd-22` baseline at `1.20880134` and `+0.00384222`; the candidate artifact was `14067534` bytes (`-233` vs baseline), but exported quality worsened by `+0.00004605 val_bpb`.
- The fixed `GPTQ-lite` search configuration used here was deterministic and weight-only: no activation calibration, search scope limited to `62` large 2D float tensors, clip percentiles `[0.999, 0.9995, 0.9999, 0.99999, 1.0]`, and tensor selection based on minimum weight-reconstruction MSE; `48` tensors chose `0.999`, `11` chose `0.99999`, and `3` chose `1.0`.
- Local architecture evidence now shows that changing only `NUM_LAYERS` from `10` to `11` on top of the winning `opt_003` recipe is strongly positive under the fixed `uniform int8 + zstd-22` objective: checkpoint `val_bpb` improved from `1.20495912` to `1.19786472` (`-0.00709440`), regenerated `uniform int8 + zstd-22` improved from `1.20880134` to `1.20150644` (`-0.00729490`), artifact bytes rose from `14067767` to `15366915` (`+1299148`), and total bytes rose from `14133579` to `15434125` (`+1300546`); the candidate remained under the cap and roundtripped exactly, while the uniform-int8 quantization gap improved slightly from `+0.00384222` to `+0.00364172`.
- Local architecture evidence now shows that changing only `BIGRAM_HASH_BUCKETS` from `4096` to `8192` on top of the winning `arch_004` recipe is still positive under the fixed `uniform int8 + zstd-22` objective: checkpoint `val_bpb` improved from `1.19786472` to `1.19748125` (`-0.00038347`), regenerated `uniform int8 + zstd-22` improved from `1.20150644` to `1.20023167` (`-0.00127477`), artifact bytes rose from `15366915` to `15881366` (`+514451`), and total bytes rose from `15434125` to `15948576` (`+514451`); the candidate remained under the cap and roundtripped exactly, while the uniform-int8 quantization gap improved from `+0.00364172` to `+0.00275042`.
- Local architecture evidence now shows that swapping only the deepest `4` attention blocks to `XSA` on top of the winning `arch_005` recipe is negative under the fixed `uniform int8 + zstd-22` objective: checkpoint `val_bpb` worsened from `1.19748125` to `1.19825960` (`+0.00077834`), regenerated `uniform int8 + zstd-22` worsened from `1.20023167` to `1.20101996` (`+0.00078829`), artifact bytes fell from `15881366` to `15845227` (`-36139`), code bytes rose from `67210` to `68366` (`+1156`), and total bytes fell from `15948576` to `15913593` (`-34983`); the candidate remained under the cap and roundtripped exactly, but the fixed-int8 quantization gap was effectively unchanged at `+0.00276037`.
- Local architecture evidence now shows that enabling only `Partial RoPE (16/64)` plus layerwise `LN Scale = 1/sqrt(layer_idx+1)` on top of the winning `arch_005` recipe is raw-positive but export-negative under the fixed `uniform int8 + zstd-22` objective: checkpoint `val_bpb` improved from `1.19748125` to `1.19731787` (`-0.00016338`), but regenerated `uniform int8 + zstd-22` worsened from `1.20023167` to `1.20071205` (`+0.00048037`); artifact bytes fell from `15881366` to `15651578` (`-229788`), code bytes rose from `67210` to `69665` (`+2455`), total bytes fell from `15948576` to `15721243` (`-227333`), and the quantization gap worsened from `+0.00275042` to `+0.00339417`; roundtrip/load correctness stayed exact.
- Local architecture + optimization evidence now shows that the full live `#3` motif `Partial RoPE + LN Scale + EMA + XSA4` does not transfer to the winning `arch_005` stack at all under the fixed `uniform int8 + zstd-22` objective: after an exact-roundtrip 8-GPU smoke pass, the controlled retrain finished at step `4185` under `600.069s`, the EMA-scored checkpoint revalidated at `1.21495046`, and regenerated `uniform int8 + zstd-22` scored `1.21781093` with artifact `15807871`, total `15877536`, and quantization gap `+0.00286047`; versus locked `arch_005`, checkpoint `val_bpb` worsened by `+0.01746921` and exported `val_bpb` worsened by `+0.01757926`, so this is a modeling failure first, not just another export-robustness miss.
- Local aggressive-phase reproduction evidence now shows that the archived `2026-03-20_10L_Int5MLP_MuonWD04_SWA50` stack does not currently reproduce competitively on this machine under the fixed local comparison standard: using the archived script directly, the full managed run stopped at `step=2223` under `600.069s`, logged checkpoint `val_bpb=1.2260`, and reached post-export `val_bpb=1.22520354`; after lossless `zstd-22` recompression of the runtime payload, the final artifact was `15918861` bytes with `52930` code bytes and `15971791` total bytes, still under the cap but worse than locked local best `1.20023167` by `+0.02497187`.
- The same reproduction preserved one important archival marker despite the weaker score: `swa:applying averaged 24 checkpoints` matched the archived seed-42 run, which suggests the main mismatch is throughput and runtime-path drift rather than a different high-level schedule branch.

## Negative Or Inconclusive Results
- Do not spend another immediate round on nearby fixed logit-bias lambda sweeps, alternative small prior strengths, or other simple unigram token-prior calibration variants on locked seed-`1337` legal-TTT. `eval_012` already separated prior construction from selector data and still selected `lambda=0.0`, so this cheap fixed-calibration family is locally exhausted on `arch_010`.
- Do not spend another immediate round on nearby lambda sweeps, order sweeps, or smoothing variants for the exact `eval_011` prefix-only higher-order backoff cache on locked seed-`1337` legal-TTT. The helper is valid, but the held-out-selected nonzero weight `0.01` regressed full validation by `+0.00108537` and pushed eval time to `1859145ms`, so the family is already answered as non-promotable.
- Do not spend another immediate round on nearby chunk-local prefix-bigram lambda sweeps around `lambda=0.0` for locked seed-`1337` legal-TTT. `eval_010` already answered the single-mechanism question negatively enough: parity passed exactly, the frozen held-out slice selected `lambda=0.0`, and even the smallest nonzero lambda `0.05` regressed held-out `val_bpb` by `+0.01303935`.
- `eval_010` does not justify a cross-seed confirmation round for this exact chunk-local prefix-bigram interpolation. The scientifically relevant signal is that the held-out selection itself collapsed to `lambda=0.0`, so there is no positive nonzero candidate to confirm on seeds `42` or `2024`.
- The `eval_010` helper edit increased code bytes from `84059` to `89121`, raising total bytes on the seed-`1337` artifact from `15639180` to `15644242`, but this byte increase is not actionable because the mechanism did not improve BPB at all.
- Do not spend another immediate round on nearby global-temperature sweeps around `T=1.0` for locked seed-`1337` legal-TTT. `eval_009` already answered the single-scalar calibration question negatively enough: parity passed exactly, the frozen held-out slice selected `T=1.0`, and no off-baseline temperature earned a final full-val test.
- `eval_009` does not justify a cross-seed confirmation round for global temperature scaling. The scientifically relevant signal is that the held-out selection itself collapsed to `T=1.0`, so there is no positive candidate temperature to confirm on seeds `42` or `2024`.
- The `eval_009` helper edit increased code bytes from `84059` to `84578`, raising total bytes on the seed-`1337` artifact from `15639180` to `15639699`, but this byte increase is not actionable because the mechanism did not improve BPB at all.
- Do not spend another immediate round on nearby legal-TTT budget tuning on locked `arch_010`. The final seed-`42` confirmation is now in hand, the refined exact 3-seed mean `1.11976097` already beats the locked `TTT_EPOCHS=3` mean `1.12010450`, and the eval-time budget remains acceptable, so more adjacent `TTT_LR` or `TTT_EPOCHS` sweeps would be redundant.
- Do not over-claim the promoted refined legal-TTT regime as a new live-SOTA result. The best single seed remains `1.11935008` and the refined exact 3-seed mean remains `+0.00036097` above live SOTA `1.1194`, so the result is strong local promotion evidence, not leaderboard-beating multi-seed evidence.
- Do not yet promote `TTT_EPOCHS=4` as the new locked default for the full `arch_010` legal-TTT line based on one seed alone, even though `eval_006` crossed live SOTA. The result is strong and budget-safe on seed `1337`, but it still needs locked seed-`42` and seed-`2024` confirmation before it should replace the fully locked `TTT_EPOCHS=3` multi-seed regime.
- The `eval_006` gain is no longer “too small,” but its time cost is large enough to matter operationally: versus `eval_005`, script eval time grew by about `15.6%` and managed wallclock by about `14.6%`. That is still acceptable under the real budget, but future nearby TTT-budget tuning should stop immediately if additional epochs fail to reproduce or if they erode the remaining time headroom further.
- The `eval_005` result only partially confirms the LR-limitation hypothesis. Although `TTT_LR=0.0025` is clearly better than `0.002` on the locked seed-`1337` artifact, the gain `-0.00015434` missed the planned `-0.0002` threshold and did not cross live SOTA `1.1194`, so the remaining headroom in this exact legal-TTT regime may be mild rather than large.
- Do not yet promote `TTT_LR=0.0025` as the new locked default for the full `arch_010` legal-TTT line based on one seed alone. The effect is real and above the research-memory non-actionable band, but it still needs seed-`42` and seed-`2024` confirmation if the project wants to treat it as a recipe change rather than a single-seed landing.
- Do not spend another immediate round on seed-only legal score-first TTT confirmation for locked `arch_010`; `eval_002`, `eval_003`, and `eval_004` now answer that robustness question positively across all three locked saved export seeds. The next informative move is one new controlled beyond-SOTA variable, not more seed repetition.
- Do not spend another immediate round rerunning the seed-42 legal-TTT confirmation on locked `arch_010`; that robustness question is now answered positively enough for planning. If more evaluation-only robustness evidence is still desired before treating TTT as fully locked, use the genuinely new saved seed `2024` export lineage instead.
- Do not treat legal score-first TTT as the new default on the locked `arch_010` line after one seed only, despite the strong `eval_002` gain; the next clean question is same-script confirmation on the saved seed-42 export lineage, not a TTT hyperparameter sweep.
- The `eval_002` helper is byte-safe but not code-byte-neutral: artifact bytes stayed fixed at `15555121`, but code bytes rose from the locked `arch_010` script’s `71265` to `84059`, so future TTT work should keep code-size discipline explicit even when the score gain is strong.
- Do not assume the managed runner will always exit cleanly after writing final metrics on the `arch_010` lineage; both the seed-2024 smoke and full runs wrote the required outputs first and then stalled afterward, so any future rerun on this exact script should treat the on-disk results as authoritative and be prepared to recover the final stride-64 exported metric from saved artifacts if teardown hangs again.
- Do not assume artifact bytes are seed-stable on the `arch_010` lineage just because BPB is now stable across two seeds; `repro_006` matched the score band cleanly but used `+247231` more artifact and total bytes than the seed-1337 `arch_010` run, shrinking headroom to `126383`.
- Do not spend another immediate round chasing `flash_attn_interface.flash_attn_func` across the current local environments; `infra_002` showed that the required top-level binding is absent in `physicslm`, absent as a top-level module in `loongflow_ml`, and not recoverable there via the observed VLLM package path, so this portability question is locally answered unless a genuinely new validated environment appears.
- The existence of `xformers.flash_attn_3` in `physicslm` should not be over-interpreted as a positive answer to the reviewed brief. The controlled question was specifically about the documented `flash_attn_interface.flash_attn_func` path inside the validated environment, and `infra_002` did not authorize bundling a different native attention implementation behind a new code path.
- Do not spend another immediate round on seed-only confirmation for the cuDNN-backed reproduced record-`#2` stack; `repro_005` stayed within `+0.0015` of `opt_008` with identical step geometry, so the remaining miss versus documented record-`#2` is now better explained by a smaller portability/runtime component than by obvious seed fragility.
- The old flash-only SDPA fallback used in `repro_004` should no longer be treated as the preferred local fallback for the leaderboard-`#2` lineage; `opt_008` answered that backend choice positively enough that future comparisons on this stack should keep cuDNN-only SDPA fixed unless a more faithful native FlashAttention path becomes available.
- `repro_004` should not be interpreted as a perfect as-authored reproduction of the documented record-`#2` script because local `physicslm` lacks the record's `flash_attn_interface` dependency; the SDPA+GQA fallback is the smallest controlled compatibility fix, but it remains a real fidelity break.
- The current `repro_004` miss versus the documented `1.12278022` is not yet evidence that the record-`#2` stack itself fails locally: the run reproduced the intended motifs and won decisively against the old local best, but it remained `1017` steps short of the documented seed-1337 step budget and required one explicit attention-kernel substitution, so the clean negative target has shifted from "does the stack transfer?" to "which portability/runtime component is still limiting it?"
- On the `record06 + LeakyReLU^2` lineage, the bundled frontier `EMA + warmdown3500` transfer is now answered negatively for the submission objective: despite a much smaller export gap and lower artifact bytes, it regressed raw checkpoint quality first, finished `539` steps earlier than `arch_009`, and missed the exported baseline by `+0.01305242`.
- Do not spend another immediate round on nearby `EMA + warmdown3500` retries on this exact lineage; this bundle already got the main export-friendliness effect it could plausibly offer, but the wallclock-bounded raw model quality collapse dominated the result.
- The now-confirmed `record06` `LeakyReLU^2` transfer still shows mild seed variance rather than perfectly flat behavior: the positive seed-1337 rerun landed `+0.00207359` behind the seed-42 transferred result, narrowly missing the earlier secondary `<=0.002` similarity band by `0.00007359`, so another seed is still the cleanest way to quantify stability before bundling more motifs on top.
- On the clean `record06` baseline, late QAT is now answered negatively for the submission objective: it slightly misses the same-seed exported baseline, worsens raw checkpoint quality first, and pushes the final counted bytes over `16,000,000`, so this exact recipe is not mainly limited by missing late fake-quant robustness.
- Because `opt_006` regressed checkpoint quality by about `+0.0076`, do not pair this late-QAT path next with `EMA`, extra warmdown, or other nearby schedule changes on `record06`; the brief’s “too intrusive for the 600s recipe” failure case is the best fit.
- `system_py310` remains unsuitable for clean archived-stack reproduction on this machine because it lacks native Python `zstandard`, `enable_cudnn_sdp`, `cudnn_sdp_enabled`, `F.rms_norm`, and `enable_gqa`; `conda_base` lacks torch entirely; `conda_loongflow_ml` has the needed torch APIs but still lacks Python `zstandard`, so all three still require a shim or fallback path and should be rejected for `record06` fidelity work.
- Pure broad lower-bit export is no longer an open generic question on this checkpoint: `uniform int6` failed badly on quality, so any follow-up lower-bit export must be selective or training-assisted rather than another blind uniform-bit reduction.
- The project no longer needs to ask whether selective export is viable at all on this checkpoint: `MLP-only int6` answered that positively enough to justify one more narrow selective round before pivoting to optimization changes.
- The old conclusion that `attn.proj.weight -> int6` was globally closed for this recipe family is no longer valid: the failure was checkpoint-dependent, and the stronger `arch_002` checkpoint turned the same export rule into a positive result.
- A tiny synthetic smoke run on identical weights and identical exported bytes produced effectively zero `val_bpb` delta between `non_overlapping` and `sliding_window`; this validates implementation and token accounting pathways only, not the real hypothesis.
- An expanded artifact-recovery audit across additional accessible storage under `/newcpfs/lxh` found no reusable real checkpoint/export pair beyond the already-audited repo workspace. The only new experiment-like backup root, `/newcpfs/lxh/physicslm_backups/hardtok_analysis_20260306_040545`, contains analysis outputs and references ephemeral `/tmp/physicslm_hardtok_demo/checkpoints/0000000050` rather than persistent recoverable artifacts.
- A 1-GPU execution of the same real-asset dual-mode baseline was not a practical way to close the question on local L20Z hardware because the full step-0 evaluation was too slow; distributed 8-GPU execution preserved the same scientific comparison while making the run practical.
- The warmdown-only gain is modest rather than transformative: `WARMDOWN_ITERS=3000` improved the fixed export readout by less than `0.001 val_bpb`, so another optimization round should stay single-variable and targeted, or reuse this improved checkpoint only for a narrowly justified export follow-up.
- The Muon-warmup gain is also modest rather than transformative: it improved the primary fixed exported readout by only `-0.00020055 val_bpb` and worsened checkpoint `val_bpb` by `+0.00047447`, so additional micro-schedule tuning on this architecture family is lower priority than a clean architecture-side test.
- `MLP3x` under the current locked export recipe is not submission-viable despite its strong quality gain: regenerated `uniform int8 + zstd-22` grew to `19989838` total bytes and regenerated `mlp_int6_else_int8 + zstd-22` grew to `17326746`, so both fixed readouts miss the 16 MB cap by a large margin.
- `10L / MLP2x` does not make plain regenerated `uniform int8 + zstd-22` byte-safe under the locked protocol: total bytes still rose to `17480480`, so depth alone does not solve the baseline export-cap problem.
- On the successful `BigramHash(4096) + SmearGate` checkpoint, regenerated `mlp_int6_else_int8 + zstd-22` is no longer byte-safe: total bytes rose from `15482717` on `arch_002` to `16084095`, so the stronger architecture consumed enough extra bytes that the old safe readout no longer fits.
- On `arch_003 + MUON WD=0.04 + late SWA`, the old selective export frontier is no longer viable at all: both `mlp_int6_else_int8` and `mlp_int6_plus_attn_proj_int6_else_int8` stay far under the byte cap but regress by `+0.02569728` and `+0.02738468` versus regenerated `uniform int8` on the same checkpoint, so another nearby 6-bit subset tweak is low value unless the training/export regime changes again.
- On `arch_003 + MUON WD=0.04`, nearby averaging variants now look lower value: `EMA` reduced the mixed-precision damage versus `SWA`, but it simultaneously destroyed the checkpoint and regenerated `uniform int8` gains, so the next aggressive move should shift to a different frontier motif rather than another averaging tweak.
- On the winning `opt_003` checkpoint family, nearby export-only `GPTQ-lite` clip refinements now also look lower value: the bounded int8 clip search preserved exactness and a tiny artifact-byte win, but it still regressed exported `val_bpb`, so another adjacent clip-grid variation is less promising than spending the remaining headroom on a larger-capacity stack.
- Do not rerun the same `10L -> 11L` depth test on the `opt_003` stack; that question is now answered positively enough for planning, and the next useful question is how to spend the remaining `~0.566 MB` headroom on the stronger `11L` regime.
- Do not assume another nearby `BigramHash` increase is still viable on the fixed `arch_005` `uniform int8 + zstd-22` line; `4096 -> 8192` used `514451` of the remaining `565875` bytes and leaves only `51424` bytes of headroom, so further table growth likely needs a separate byte-saving move first.
- On the near-cap `arch_005` line, extending `WARMDOWN_ITERS` from `3000` to `3500` is now answered negatively for the fixed `uniform int8 + zstd-22` objective: checkpoint `val_bpb` worsened from `1.19748125` to `1.19943901` and regenerated `uniform int8 + zstd-22` worsened from `1.20023167` to `1.20206766`, even though artifact bytes fell from `15881366` to `15780075`, total bytes fell from `15948576` to `15847285`, and the quantization gap improved slightly from `+0.00275042` to `+0.00262866`; roundtrip/load correctness still stayed exact.
- Do not spend another round on nearby warmdown extension for the fixed `arch_005` `uniform int8 + zstd-22` line; `WARMDOWN_ITERS=3000 -> 3500` made the artifact smaller and slightly improved the quantization gap, but it worsened both checkpoint and exported `val_bpb`, so this specific schedule question is now answered negatively on the winning stack.
- Do not spend another round on nearby attention-only `XSA` refinements for the fixed `arch_005` `uniform int8 + zstd-22` line; the direct `XSA4` reproduction made both checkpoint and exported `val_bpb` worse despite slightly smaller bytes, so this stack is likely not missing a simple byte-neutral attention swap under the fixed 600s recipe.
- Do not spend another round on isolated `Partial RoPE + LN Scale` refinements for the fixed `arch_005` `uniform int8 + zstd-22` line; the geometry bundle slightly improved raw checkpoint quality and bytes, but it worsened the exported objective because the quantization gap expanded materially, so the remaining open question is bundle interaction rather than this sub-bundle alone.
- Do not spend another round on nearby `Partial RoPE + LN Scale + XSA4 + EMA` refinements for the fixed `arch_005` `uniform int8 + zstd-22` line; the full live `#3` bundle has now been answered strongly negatively, and the failure is primarily raw-checkpoint collapse rather than a recoverable export-only issue.
- The old `repro_001_record06_10l_int5mlp_bh10240` clean-negative concern is now resolved: the stack itself is locally competitive once run in `physicslm`; the earlier weakness came from environment drift.
- Do not spend another round on seed-only confirmation for `record06`; the clean baseline has now reproduced competitively on both archived seeds `42` and `1337`, so the next unanswered question is extension rather than robustness.

## Redundancy Watchlist
- Do not spend a round rerunning same-script no-TTT parity on seed `1337` for `eval_006`; the helper and artifact identity checks already satisfied the brief’s guardrail, so that parity comparison remains scientifically inherited from `eval_002`.
- Do not spend a round rerunning same-script no-TTT parity on seed `1337` for `eval_005`; the helper and artifact identity checks already satisfied the brief’s guardrail, so that parity comparison remains scientifically inherited from `eval_002`.
- Do not spend the next round restacking the failed controlled `Parallel Muon` port with legal score-first TTT on `arch_010`; the no-TTT training-side test already failed decisively on score, throughput, and bytes, so a later TTT gain on that artifact would not rescue the underlying training-side hypothesis.
- Do not spend the next round re-asking whether legal score-first TTT survives a second saved export seed on `arch_010`; `eval_003` answered that positively. The only remaining evaluation-only robustness question would be an optional third saved seed such as `2024`.
- Do not spend the next round sweeping legal-TTT hyperparameters on `arch_010`; `eval_002` already answered the transfer question positively on seed `1337`, so the next informative move is one same-script seed-42 confirmation on the saved artifact lineage.
- Do not rerun the completed seed-42 confirmation on `arch_010`; that question is now answered positively enough for planning. If more robustness evidence is desired before stacking another motif, use a genuinely new third seed such as `2024`.
- Do not spend the next round on more environment-chasing for top-level `flash_attn_interface` unless a new shim-free environment with native `zstandard` and the exact binding is identified first; the current accessible environments have already answered this negatively enough for planning.
- Do not spend another round on seed-42 or seed-1337 confirmation for `record06 + LeakyReLU^2`; that question is now answered positively enough for planning, and any further variance work should use a genuinely new seed such as `2024`.
- Do not spend the next round on nearby late-QAT variants for `record06`; the controlled `QAT_START_STEP=5500` transfer already answered that direction negatively enough, and the next informative move should be structural rather than another late-phase optimization edit.
- Do not spend rounds rediscovering that `sliding-window eval` helps in general; only test it locally once to lock the repo's evaluation protocol.
- Do not bundle `SmearGate`, `BigramHash`, `SWA`, depth changes, and export changes into one first experiment.
- Do not compare architecture variants while the evaluation protocol is fixed only implicitly; use explicit `sliding_window stride=64`.
- Do not compare export policies while using mixed evaluation setups.
- Do not spend another round on container-only compression unless the payload format itself changes materially; the `zlib-9` vs `zstd-22` question is now answered for the current `int8` payload.
- Do not run another blind broad `uniform int6` export round on this checkpoint; that gating question is now answered negatively.
- Do not rerun `MLP-only int6` as a generic confirmation round; that selective gating question is now answered well enough for planning.
- Do not re-run the old 9-layer `attn.proj.weight -> int6` near-miss variants again; the stronger 10-layer checkpoint has already answered the relevant checkpoint-dependence question more cleanly.
- Do not spend the next round on another arbitrary adjacent export subset unless it is justified by a new mechanism; `export_006` already promoted the most obvious near-miss candidate into the accepted frontier.
- Do not rerun `arch_003` as a generic confirmation round just to re-measure the same cheap-feature gain; the useful unanswered question is now how to recover quantization robustness or byte headroom on this stronger checkpoint family.
- Do not run another generic artifact-recovery sweep unless a specific new storage root is identified; the accessible `/newcpfs/lxh` roots have now been checked without finding a recoverable pair.
- Do not rerun `MLP_MULT=3` under the current locked export recipe as a generic confirmation round; the quality win is real, but the fixed exported artifacts are far over the byte cap.

## Current Best Known Configuration
- export: the strongest local post-export result seen so far is now `eval_006_arch010_legal_ttt_epochs4_seed1337`, which reused the saved `arch_010` seed-`1337` export lineage unchanged and the exact copied legal-TTT helper unchanged, while changing only `TTT_EPOCHS=3 -> 4` with `TTT_LR=0.0025` held fixed; it scored `1.11935008` at unchanged artifact bytes `15555121`, code bytes `84059`, and total bytes `15639180`.
- export: the strongest fully locked multi-seed evidence now belongs to the refined legal-TTT regime at `TTT_LR=0.0025`, `TTT_EPOCHS=4`, whose three saved export seeds are `1.11935008`, `1.11994397`, and `1.11998885` for an exact 3-seed mean of `1.11976097`. This replaces the older locked `TTT_LR=0.002`, `TTT_EPOCHS=3` regime at mean `1.12010450` as the promoted local evaluation-side default on `arch_010`.
- export: the evaluation-only TTT regime on locked `arch_010` is now fully locked rather than just provisionally promoted: all three saved export seeds remained under the byte cap with unchanged artifacts, all three improved versus their recovered same-script no-TTT baselines, and the exact 3-seed TTT mean now sits within about `0.0007` of live SOTA without retraining or export rewrite.
- export: the strongest local byte-safe architecture/export combination tested so far is now `arch_010_record02_leakyrelu2_keep_cudnn_recipe`, the activation-only `LeakyReLU(0.5)^2` transfer on top of the cuDNN-backed reproduced leaderboard-`#2` stack, which scored `1.12211549` post-export at `15626386` total bytes with exact roundtrip correctness, direct-runtime `zstd`, and confirmed cuDNN SDPA dispatch.
- export: that best single run is now backed by controlled second and third seeds on the exact same script: `repro_006_record02_leakyrelu2_seed42_confirm` scored `1.12281449` at `15873617` total bytes, and `repro_007_record02_leakyrelu2_seed2024_confirm` scored `1.12225480` at `15832355` total bytes, both with identical `71265` code bytes, the same confirmed cuDNN SDPA dispatch, and near-identical step geometry, giving the exact `LeakyReLU(0.5)^2` lineage a local 3-seed mean of `1.12239493`.
- export: relative to the prior active best `opt_008`, the new baseline improves checkpoint `val_bpb` by `-0.0023`, post-EMA by `-0.0022`, post-export non-overlapping by `-0.00222231`, and post-export `sliding_window stride=64` by `-0.00213695`, while spending only `+70228` total bytes and changing step geometry only modestly.
- export: before `arch_010`, the strongest local byte-safe architecture/export combination was `opt_008_record02_cudnn_sdpa_fallback_isolation`, the cuDNN-only SDPA fallback version of the reproduced leaderboard `#2` stack, which scored `1.12425244` post-export at `15556158` total bytes with exact roundtrip correctness, direct-runtime `zstd`, and confirmed cuDNN SDPA dispatch.
- export: the new active baseline improved the prior `repro_004` local reproduction by `-0.00412676 val_bpb` and `-245434` total bytes while recovering `+712` steps, and the seed-only confirmation `repro_005` now supports treating the cuDNN fallback as stable across at least two local seeds (`1.12425244`, `1.12484349`) rather than a seed-1337-only landing.
- architecture: external frontier clusters around `9L-11L`, width `512`, `MLP 3x`, plus small local feature additions such as `SmearGate` and `BigramHash`.
- optimization: external frontier repeatedly uses stronger Muon momentum warmup, longer warmdown, and nontrivial weight decay to improve quantization robustness.
- optimization: external frontier repeatedly uses stronger Muon momentum warmup, longer warmdown, nontrivial weight decay, and late averaging; local evidence now confirms that the exact averaging choice can move the export optimum between `uniform int8` and mixed 6-bit policies.
- evaluation: local baseline should now use `sliding-window eval` with `stride=64` as the fixed standard; the measured local gain on the same real checkpoint/export pair was about `-0.0318 val_bpb` post-export.
- export: the local clean baseline should now use `int8 + zstd-22` instead of `int8 + zlib-9`; the byte gain is only about `0.24%`, so the next likely leverage remains lower-bit or mixed-precision export under the same container.
- export: the strongest local byte-safe export frontier is now `arch_002` with `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22`, scoring `1.22086139` at `14954974` total bytes; on that checkpoint it improves on regenerated `mlp_int6_else_int8` by saving `527743` artifact bytes for only `+0.00033737 val_bpb`, and it remains within the explicit `+0.003` guardrail versus regenerated `uniform int8`.
- export: before `arch_009`, the strongest local byte-safe architecture/export combination was the clean archived `record06` rerun from `infra_001_record06_env_alignment_audit`, using the archived `10L Int5-MLP + BigramHash(10240) + WD=0.04 + SWA` script unchanged in the `physicslm` environment and scoring `1.14323509` at `15758548` total bytes.
- export: the same clean `record06` stack has now been confirmed on a second archived seed: seed-1337 scored `1.14374449` at `15683401` total bytes, so the stable local 2-seed mean is `1.14348979` with both runs preserving direct runtime `zstd`, exact roundtrip correctness, and near-archival geometry.
- optimization: the first controlled current-motif extension on top of clean `record06`, late QAT at `step=5500` with `SWA` fixed, is now answered negatively: the candidate slightly missed the clean same-seed exported score and exceeded the byte cap, so the winning local regime remains the clean archived `record06 + SWA` baseline rather than a QAT-augmented variant.
- export: before `infra_001`, `arch_007` remained worse than the then-local-best `arch_005`; its regenerated `uniform int8 + zstd-22` result was `1.20071205` at `15721243` total bytes.
- export: before `infra_001`, `arch_008` also remained worse than the then-local-best `arch_005`; despite staying under the cap at `15877536` total bytes, its EMA-scored checkpoint collapsed to `1.21495046` and its regenerated `uniform int8 + zstd-22` result collapsed to `1.21781093`.
- export: before `infra_001`, the shimmed archived-stack run `repro_001_record06_10l_int5mlp_bh10240` also remained worse than the then-local-best `arch_005`; after lossless correction to the intended `zstd-22` container, it scored `1.22520354` at `15971791` total bytes.
- optimization: for the locked baseline recipe, `WARMDOWN_ITERS=3000` is now the stronger local export-robustness setting than the original `1200` if the readout of interest is exported `uniform int8` or `mlp_int6_else_int8`, but the observed gain is modest and does not materially improve the raw checkpoint metric.
- optimization: for the fixed selective-export objective, the strongest local schedule variant tested so far is now `WARMDOWN_ITERS=3000` plus `MUON_MOMENTUM_WARMUP_STEPS=1500`; this slightly improves the fixed `mlp_int6_else_int8` exported score and quantization gap versus `opt_001`, but it does not improve the raw checkpoint metric and leaves regenerated `uniform int8` effectively flat.
- architecture: `MLP3x` is now confirmed as a strong quality lever on the locked schedule/export recipe, but not a byte-safe one; the next architecture-side question should spend capacity more efficiently than widening every MLP block.
- architecture: the previous best byte-safe architecture/export combination before `arch_003` and `arch_004` was `10L / MLP2x` on top of the locked `opt_002` schedule with `mlp_int6_plus_attn_proj_int6_else_int8 + zstd-22`, scoring `1.22086139` at `14954974` total bytes; this remains the clearest positive historical example that checkpoint quality can reopen a previously failed selective-export subset.
- optimization: on the stronger `arch_003` line, `MUON WD=0.04 + late SWA` remains the most effective compression-friendly optimization tested so far if the target export policy is regenerated `uniform int8`; the follow-up `EMA` ablation answered the main averaging question negatively enough that the next aggressive move should shift to a different frontier motif such as `GPTQ-lite` or a larger-capacity stack.
- optimization: on the stronger `arch_003` line, `MUON WD=0.04 + late SWA` remains the most effective compression-friendly optimization tested so far if the target export policy is regenerated `uniform int8`; the follow-up `EMA` and bounded `GPTQ-lite` export ablations both answered their nearby refinement questions negatively enough that the next aggressive move should shift to a larger-capacity stack rather than more local averaging or clip-search tweaks.

## Open Hypotheses
- **OVERRIDING eval_011**: The `eval_011` conclusion that "n-gram backoff is negative" is WRONG as a general statement. `eval_011` tested a slow naive implementation (global lambda, per-segment processing, 1901s). The correct vectorized chunk-based implementation (PR #803/#813 style) achieves ~0.44–0.67 BPB in well under 600s. The next experiment MUST implement the correct approach from `docs/sota_records/sota_code_pr803_0.442bpb_ComplementaryTraining.py` or `docs/sota_records/sota_code_pr813_0.667bpb_BackoffNgramMixer.py`. Do not cite eval_011 as evidence against n-gram caching.
- The next clean question is whether the correctly implemented vectorized chunk n-gram (PR #803/#813 style: entropy-adaptive alpha, per-order multipliers, 1M-token chunk updates) achieves the expected ~0.67 BPB on our locked `arch_010` artifact within the 600s eval budget.
- The next clean question is no longer whether one chunk-local prefix-only previous-token bigram interpolation helps on locked seed-`1337` legal-TTT; `eval_010` answered that negatively with a parity-clean helper and a held-out selection that collapsed to `lambda=0.0`.
- The next clean question is no longer whether one global scoring temperature helps on locked seed-`1337` legal-TTT; `eval_009` answered that negatively with a parity-clean helper and a held-out selection that collapsed to `T=1.0`.
- The next clean question is no longer whether the refined `TTT_LR=0.0025`, `TTT_EPOCHS=4` legal-TTT setting transfers to the final locked seed `42`; `eval_008` answered that positively enough to promote the refined evaluation-side regime.
- The next clean question is no longer whether legal score-first TTT can transfer at all to `arch_010`; `eval_002` answered that positively on seed `1337`.
- The next clean question is no longer whether the same exact evaluation-only legal-TTT path survives on the second or third locked saved export seed; `eval_003` and `eval_004` answered that positively enough to lock the regime.
- The next clean question is no longer whether the locally controllable `Parallel Muon` transfer improves locked `arch_010`; `pending_opt_009_arch010_parallel_muon_seed1337_nottt` answered that negatively under preserved structure.
- **SUPERSEDED** (see Working SOTA Anchor above): the "next single-variable refinement" framing is obsolete. We are now in AGGRESSIVE phase with a ~0.722 BPB gap to target SOTA (PR #803, 0.4416 BPB). The immediate task is implementing the correct vectorized chunk n-gram backoff cache (PR #803/#813 style) and complementary training.
