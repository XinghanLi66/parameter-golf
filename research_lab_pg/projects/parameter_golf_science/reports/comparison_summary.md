# Comparison Summary

Use this file to keep apples-to-apples comparisons easy to scan.

| Comparison | Metric | Baseline | Candidate | Delta | Notes |
| --- | --- | --- | --- | --- | --- |
| External reference: naive baseline vs sliding-window eval record | post-export `val_bpb` | `1.2244` | `1.1925` | `-0.0319` | Published repo evidence suggests evaluation is the cleanest first local gap to close. |
| External reference: root-style baseline vs current frontier | leaderboard `val_bpb` | `1.2244` | `1.1428` | `-0.0816` | The frontier gap is too large for a bundled first leap; local protocol lock-in is required first. |
| Local smoke fixture: pre-export non-overlapping vs sliding-window | `val_bpb` | `3.96925568` | `3.96925571` | `+0.00000003` | Same synthetic weights and same synthetic validation split; confirms implementation only, not contest-relevant effect. |
| Local smoke fixture: post-export non-overlapping vs sliding-window | `val_bpb` | `3.96925568` | `3.96925571` | `+0.00000003` | Same synthetic int8+zlib artifact bytes; runtime changed from `68ms` to `160ms`. |
| Local real-asset follow-up: availability audit | required asset pair | none found | none found | n/a | Real tokenizer and FineWeb validation shard exist in `/newcpfs/lxh/parameter-golf/data`, but no reusable real checkpoint plus matching exported artifact were found; no apples-to-apples real comparison could be run this round. |
| Expanded accessible-storage audit: prior blocker state vs additional `/newcpfs/lxh` storage | required asset pair | none found in prior workspace audit | none found in any additionally accessible storage | n/a | Newly searched `/newcpfs/lxh/physicslm_backups`, `/newcpfs/lxh/.cache`, `/newcpfs/lxh/.codex/tmp`, `/newcpfs/lxh/.qwen/tmp`, and a bounded sweep of the remaining non-`parameter-golf` roots; no recoverable matching pair was present, so evaluation-only remains blocked. |
| Local real pair final checkpoint: pre-export non-overlapping vs sliding-window | `val_bpb` | `1.2515` | `1.2199` | `-0.0316` | Same real checkpoint from the fresh baseline run at step `6180`; runtimes `3825ms` vs `64626ms` on the full FineWeb validation split. |
| Local real pair final artifact: post-export non-overlapping vs sliding-window | `val_bpb` | `1.25689044` | `1.22505202` | `-0.03183842` | Same real `final_model.int8.ptz` bytes; runtimes `3838ms` vs `64635ms`; artifact bytes `15785852`, total submission size `15840891`. |
| Local export-only real pair: `int8+zlib-9` vs `int8+zstd-22` | artifact bytes | `15785852` | `15748490` | `-37362` | Same checkpoint, same quantized payload, same `sliding_window stride=64` eval, and exact roundtrip quantized-tensor equality; post-export `val_bpb` stayed exactly `1.22505202` in both cases. |
| Local export-only locked checkpoint: regenerated `uniform int8+zstd-22` vs `uniform int6+zstd-22` | artifact bytes | `15748155` | `12382907` | `-3365248` | Same checkpoint, same tensor selection, same `sliding_window stride=64` eval, same `zstd-22`; both artifacts roundtripped exactly and loaded correctly. |
| Local export-only locked checkpoint: regenerated `uniform int8+zstd-22` vs `uniform int6+zstd-22` | post-export `val_bpb` | `1.22505202` | `1.29818551` | `+0.07313349` | The byte win is large, but the quality loss is far beyond the `+0.003` decision threshold, so broad `uniform int6` is not promising on this checkpoint. |
| Local export-only locked checkpoint: regenerated `uniform int8+zstd-22` vs `mlp_int6_else_int8+zstd-22` | artifact bytes | `15748335` | `13946844` | `-1801491` | Same checkpoint, same `sliding_window stride=64` eval, same `zstd-22`; only `blocks.*.mlp.fc.weight` and `blocks.*.mlp.proj.weight` moved to `int6`; both artifacts roundtripped exactly and loaded correctly. |
| Local export-only locked checkpoint: regenerated `uniform int8+zstd-22` vs `mlp_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.22505202` | `1.22768330` | `+0.00263128` | This selective policy recovered a meaningful byte win while staying near the quality guardrail, so selective export remains viable on this checkpoint. |
| Local export-only locked checkpoint: failed `uniform int6+zstd-22` vs selective `mlp_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.29818551` | `1.22768330` | `-0.07050222` | `MLP-only int6` is dramatically safer than broad `uniform int6` while still recovering about half of its artifact-byte savings. |
| Local export-only locked checkpoint: regenerated `uniform int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | artifact bytes | `15748335` | `13477497` | `-2270838` | Same checkpoint, same `sliding_window stride=64` eval, same `zstd-22`; only `blocks.*.attn.proj.weight` was added to the previously safe MLP-only `int6` subset; both artifacts roundtripped exactly and loaded correctly. |
| Local export-only locked checkpoint: regenerated `uniform int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.22505202` | `1.22805392` | `+0.00300190` | The byte win is larger than `MLP-only int6`, but the total regression lands just beyond the explicit `+0.003` guardrail, so this is not a clear new frontier. |
| Local selective-export frontier: prior `mlp_int6_else_int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | artifact bytes | `13946844` | `13477497` | `-469347` | The added `attn.proj.weight -> int6` rule buys a modest extra `3.3653%` artifact-byte reduction over the prior selective frontier. |
| Local selective-export frontier: prior `mlp_int6_else_int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.22768330` | `1.22805392` | `+0.00037062` | The extra quality cost versus `MLP-only int6` is small in isolation, but it is enough to push the total regression over the round’s guardrail versus regenerated `uniform int8`. |
| Local optimization locked recipe: baseline `WARMDOWN_ITERS=1200` vs candidate `WARMDOWN_ITERS=3000` | checkpoint `val_bpb` | `1.22005399` | `1.22014196` | `+0.00008797` | Same baseline recipe lineage and same scored comparison protocol `sliding_window stride=64`; the retrain preserved the same wallclock-limited launch shape and finished at step `6194` vs baseline `6180`. |
| Local optimization locked recipe: baseline `WARMDOWN_ITERS=1200` vs candidate `WARMDOWN_ITERS=3000` | regenerated `uniform int8+zstd-22` post-export `val_bpb` | `1.22505202` | `1.22427718` | `-0.00077484` | Longer warmdown slightly improved the regenerated export baseline even though the raw checkpoint metric did not improve. |
| Local optimization locked recipe: baseline `WARMDOWN_ITERS=1200` vs candidate `WARMDOWN_ITERS=3000` | `mlp_int6_else_int8+zstd-22` post-export `val_bpb` | `1.22768330` | `1.22696259` | `-0.00072070` | This is the primary readout from `opt_001`; the export policy stayed fixed and only training warmdown changed. |
| Local optimization locked recipe: baseline `WARMDOWN_ITERS=1200` vs candidate `WARMDOWN_ITERS=3000` | `mlp_int6_else_int8` quantization gap | `+0.00762931` | `+0.00682063` | `-0.00080867` | The gain is modest but directionally clean: longer warmdown made the same selective export safer. |
| Local optimization locked recipe: baseline `MUON_MOMENTUM_WARMUP_STEPS=500` vs candidate `1500` on top of `WARMDOWN_ITERS=3000` | checkpoint `val_bpb` | `1.22014196` | `1.22061644` | `+0.00047447` | Same warmdown-3000 recipe lineage and same scored comparison protocol `sliding_window stride=64`; the retrain preserved the same wallclock-limited launch shape and finished at step `6196` vs `6194`. |
| Local optimization locked recipe: baseline `MUON_MOMENTUM_WARMUP_STEPS=500` vs candidate `1500` on top of `WARMDOWN_ITERS=3000` | regenerated `uniform int8+zstd-22` post-export `val_bpb` | `1.22427718` | `1.22429451` | `+0.00001733` | The regenerated export baseline stayed effectively flat on quality while artifact bytes fell slightly. |
| Local optimization locked recipe: baseline `MUON_MOMENTUM_WARMUP_STEPS=500` vs candidate `1500` on top of `WARMDOWN_ITERS=3000` | regenerated `uniform int8+zstd-22` quantization gap | `+0.00413521` | `+0.00367807` | `-0.00045714` | The checkpoint got a bit worse, so the smaller gap does not translate into a better exported `uniform int8` score. |
| Local optimization locked recipe: baseline `MUON_MOMENTUM_WARMUP_STEPS=500` vs candidate `1500` on top of `WARMDOWN_ITERS=3000` | `mlp_int6_else_int8+zstd-22` post-export `val_bpb` | `1.22696259` | `1.22676204` | `-0.00020055` | This is the primary readout from `opt_002`; the export policy stayed fixed and only Muon momentum warmup changed. |
| Local optimization locked recipe: baseline `MUON_MOMENTUM_WARMUP_STEPS=500` vs candidate `1500` on top of `WARMDOWN_ITERS=3000` | `mlp_int6_else_int8` quantization gap | `+0.00682063` | `+0.00614561` | `-0.00067503` | The fixed selective export became safer again, but the gain is smaller than the accompanying checkpoint regression. |
| Local architecture locked recipe: baseline `MLP_MULT=2` vs candidate `3` on top of `opt_002` | checkpoint `val_bpb` | `1.22061644` | `1.20227788` | `-0.01833855` | Same warmdown-3000 and Muon-warmup-1500 recipe lineage, same `sliding_window stride=64`, and same wallclock-limited 8-GPU launch shape; `MLP3x` substantially improved raw checkpoint quality. |
| Local architecture locked recipe: baseline `MLP_MULT=2` vs candidate `3` on top of `opt_002` | regenerated `uniform int8+zstd-22` post-export `val_bpb` | `1.22429451` | `1.20534335` | `-0.01895116` | Quality improved strongly and the quantization gap also shrank from `+0.00367807` to `+0.00306546`, but total submission bytes rose from `15802010` to `19989838`, which is over the cap. |
| Local architecture locked recipe: baseline `MLP_MULT=2` vs candidate `3` on top of `opt_002` | `mlp_int6_else_int8+zstd-22` post-export `val_bpb` | `1.22676204` | `1.20764933` | `-0.01911272` | This is the decisive exported readout for `arch_001`; the quantization gap also improved from `+0.00614561` to `+0.00537144`, but total submission bytes rose from `14000547` to `17326746`, also over the cap. |
| Local architecture locked recipe: baseline `MLP_MULT=2` vs candidate `3` on top of `opt_002` | regenerated `uniform int8+zstd-22` artifact bytes | `15740717` | `19928545` | `+4187828` | The quality gain is real, but the byte increase is too large for this exact configuration to be submission-viable. |
| Local architecture locked recipe: baseline `MLP_MULT=2` vs candidate `3` on top of `opt_002` | `mlp_int6_else_int8+zstd-22` artifact bytes | `13939254` | `17265453` | `+3326199` | Even the stronger fixed selective export remains above the cap after the MLP expansion, so `MLP3x` needs a different byte strategy or a different architecture trade. |
| Local export-only warmdown checkpoint: regenerated `uniform int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | artifact bytes | `15743260` | `13473753` | `-2269507` | Same `opt_001` checkpoint, same `sliding_window stride=64` eval, same `zstd-22`; all three regenerated artifacts roundtripped exactly and loaded correctly in one run. |
| Local export-only warmdown checkpoint: regenerated `uniform int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.22427718` | `1.22740875` | `+0.00313158` | The retested candidate improved versus historical `export_004`, but it still misses the explicit `+0.003` guardrail on the improved checkpoint. |
| Local selective-export warmdown frontier: regenerated `mlp_int6_else_int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | artifact bytes | `13943970` | `13473753` | `-470217` | Adding `blocks.*.attn.proj.weight -> int6` still buys a modest extra `3.3722%` artifact-byte reduction over the warmdown checkpoint’s safe `MLP-only int6` frontier. |
| Local selective-export warmdown frontier: regenerated `mlp_int6_else_int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.22696259` | `1.22740875` | `+0.00044616` | The incremental quality cost stayed small, but not small enough relative to regenerated `uniform int8` to make the candidate acceptable. |
| Historical `attn.proj` candidate: `export_004` old checkpoint vs `export_005` warmdown checkpoint | post-export `val_bpb` | `1.22805392` | `1.22740875` | `-0.00064517` | The same export policy improved on the warmdown checkpoint, confirming the optimization gain was real even though the guardrail still failed. |
| Local architecture locked recipe: baseline `NUM_LAYERS=9` vs candidate `10` on top of `opt_002` | checkpoint `val_bpb` | `1.22061644` | `1.21510822` | `-0.00550822` | Same warmdown-3000 and Muon-warmup-1500 recipe lineage, same `sliding_window stride=64`, and same wallclock-limited 8-GPU launch shape; one extra layer improved raw checkpoint quality but reached only `5580` steps under the same 600s cap. |
| Local architecture locked recipe: baseline `NUM_LAYERS=9` vs candidate `10` on top of `opt_002` | regenerated `uniform int8+zstd-22` post-export `val_bpb` | `1.22429451` | `1.21806669` | `-0.00622782` | Quality improved and the quantization gap shrank from `+0.00367807` to `+0.00295847`, but total submission bytes rose from `15802010` to `17480480`, so regenerated `uniform int8` remains over the cap. |
| Local architecture locked recipe: baseline `NUM_LAYERS=9` vs candidate `10` on top of `opt_002` | `mlp_int6_else_int8+zstd-22` post-export `val_bpb` | `1.22676204` | `1.22052401` | `-0.00623803` | This is the decisive exported readout for `arch_002`; the quantization gap also improved from `+0.00614561` to `+0.00541579`, and total submission bytes rose from `14000547` to `15482717` but stayed under the cap. |
| Local architecture trade: `arch_001 MLP3x` vs `arch_002 10L` relative to `opt_002` | regenerated `mlp_int6_else_int8+zstd-22` gain / artifact-byte growth | `-0.01911272 / +3326199` | `-0.00623803 / +1482170` | smaller gain, much smaller byte growth | `MLP3x` bought about 3.06x the exported quality gain for about 2.24x the byte growth, but only the 10-layer candidate remained byte-safe on the primary mixed-precision readout. |
| Local export-only 10-layer checkpoint: regenerated `uniform int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | artifact bytes | `17419187` | `14893681` | `-2525506` | Same `arch_002` checkpoint, same `sliding_window stride=64` eval, same `zstd-22`; all three regenerated artifacts roundtripped exactly and loaded correctly in one run. |
| Local export-only 10-layer checkpoint: regenerated `uniform int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.21806669` | `1.22086139` | `+0.00279470` | This is the first successful `attn.proj` subset retest on a local checkpoint family: the candidate stays inside the explicit `+0.003` guardrail while cutting bytes materially. |
| Local selective-export 10-layer frontier: regenerated `mlp_int6_else_int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | artifact bytes | `15421424` | `14893681` | `-527743` | Adding `blocks.*.attn.proj.weight -> int6` now buys a modest extra `3.4221%` artifact-byte reduction over the 10-layer checkpoint’s safe MLP-only frontier. |
| Local selective-export 10-layer frontier: regenerated `mlp_int6_else_int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.22052401` | `1.22086139` | `+0.00033737` | The incremental quality cost is now small enough that the total regression versus regenerated `uniform int8` remains acceptable. |
| Historical `attn.proj` candidate: `export_005` warmdown checkpoint vs `export_006` 10-layer checkpoint | post-export `val_bpb` | `1.22740875` | `1.22086139` | `-0.00654737` | The same export policy became decisively viable on the stronger 10-layer checkpoint family, showing the earlier failures were checkpoint-limited rather than export-schema failures. |
| Local architecture locked recipe: baseline `arch_002` checkpoint vs candidate `BigramHash(4096)+SmearGate` | checkpoint `val_bpb` | `1.21510822` | `1.20992003` | `-0.00518819` | Same `10L / MLP2x`, same optimizer family, same `WARMDOWN_ITERS=3000`, same `MUON_MOMENTUM_WARMUP_STEPS=1500`, same wallclock-limited 8-GPU launch shape; changed only the cheap local feature stack. |
| Local architecture locked recipe: baseline `arch_002` regenerated `uniform int8+zstd-22` vs `BigramHash(4096)+SmearGate` | post-export `val_bpb` | `1.21806669` | `1.21486895` | `-0.00319774` | Exported quality improved, but total submission bytes rose from `17480480` to `18074538`, so this readout remains far over the cap. |
| Local architecture locked recipe: baseline `arch_002` regenerated `mlp_int6_else_int8+zstd-22` vs `BigramHash(4096)+SmearGate` | post-export `val_bpb` | `1.22052401` | `1.21774203` | `-0.00278198` | Exported quality improved, but total bytes rose from `15482717` to `16084095`, pushing the old safe MLP-only readout just over the cap. |
| Local best byte-safe frontier: prior `arch_002` locked best vs `arch_003 BigramHash(4096)+SmearGate` locked best | post-export `val_bpb` | `1.22086139` | `1.21797944` | `-0.00288195` | This is the primary success metric for the round: the locked best readout improves and remains byte-safe, but total bytes rise from `14954974` to `15564216`. |
| Local best byte-safe frontier: prior `arch_002` locked best vs `arch_003 BigramHash(4096)+SmearGate` locked best | total submission bytes | `14954974` | `15564216` | `+609242` | The quality win spends about `0.609 MB` of the prior headroom and leaves about `0.436 MB` under the cap. |
| Local selective-export on the stronger `arch_003` checkpoint: regenerated `uniform int8+zstd-22` vs locked best `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` | post-export `val_bpb` | `1.21486895` | `1.21797944` | `+0.00311049` | On this stronger checkpoint the locked best readout is now just outside the old `+0.003` guardrail versus regenerated `uniform int8`, indicating reduced quantization robustness despite the better raw quality. |
| Local optimization on `arch_003`: baseline checkpoint vs `MUON WD=0.04 + late SWA` final checkpoint | checkpoint `val_bpb` | `1.20992003` | `1.20495912` | `-0.00496091` | Same `10L / MLP2x + BigramHash(4096) + SmearGate`, same `WARMDOWN_ITERS=3000`, same `MUON_MOMENTUM_WARMUP_STEPS=1500`, same 8-GPU wallclock cap; candidate used `MUON_WEIGHT_DECAY=0.04` and SWA every `50` steps once the LR multiplier fell below `0.5`. |
| Local optimization on `arch_003`: baseline regenerated `uniform int8+zstd-22` vs `MUON WD=0.04 + late SWA` regenerated `uniform int8+zstd-22` | artifact bytes | `18010498` | `14067767` | `-3942731` | The optimization bundle dramatically improved compressibility on the same architecture family while keeping exact roundtrip/load correctness. |
| Local optimization on `arch_003`: baseline regenerated `uniform int8+zstd-22` vs `MUON WD=0.04 + late SWA` regenerated `uniform int8+zstd-22` | post-export `val_bpb` | `1.21486895` | `1.20880134` | `-0.00606761` | This is the main positive result from `opt_003`: the candidate makes regenerated `uniform int8` byte-safe and improves exported quality materially. |
| Local best byte-safe frontier: prior `arch_003` locked best vs `MUON WD=0.04 + late SWA` regenerated `uniform int8+zstd-22` | post-export `val_bpb` | `1.21797944` | `1.20880134` | `-0.00917810` | The new best local byte-safe readout changes export regime entirely: `uniform int8` becomes best and clearly beats the prior mixed-precision winner. |
| Local best byte-safe frontier: prior `arch_003` locked best vs `MUON WD=0.04 + late SWA` regenerated `uniform int8+zstd-22` | total submission bytes | `15564216` | `14133579` | `-1430637` | The new best score is also much smaller, leaving about `1.867 MB` under the cap. |
| Local optimization on `arch_003`: baseline locked best `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` vs `MUON WD=0.04 + late SWA` same policy | post-export `val_bpb` | `1.21797944` | `1.23618602` | `+0.01820658` | The intended locked readout fails badly on the new checkpoint family even though bytes fall sharply, showing that the optimization bundle changed the export optimum rather than simply making all readouts safer. |
| Local optimization on `arch_003`: regenerated `uniform int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` on the `MUON WD=0.04 + late SWA` checkpoint | post-export `val_bpb` | `1.20880134` | `1.23618602` | `+0.02738468` | On this checkpoint family, the old 6-bit selective winner is no longer a near-miss; it is decisively worse than regenerated `uniform int8`, so the next round should change averaging/export-aware training rather than add another adjacent low-bit subset. |
| Local optimization on `arch_003 + MUON WD=0.04`: late `SWA` vs late `EMA` | checkpoint `val_bpb` | `1.20495912` | `1.21491545` | `+0.00995633` | Same architecture, same `WARMDOWN_ITERS=3000`, same `MUON_MOMENTUM_WARMUP_STEPS=1500`, same `sliding_window stride=64`, same 8-GPU wallclock cap; changed only the late averaging path. |
| Local optimization on `arch_003 + MUON WD=0.04`: late `SWA` regenerated `uniform int8+zstd-22` vs late `EMA` regenerated `uniform int8+zstd-22` | post-export `val_bpb` | `1.20880134` | `1.21883931` | `+0.01003797` | This is the primary negative result from `opt_004`: EMA failed to preserve the new best byte-safe regime. |
| Local optimization on `arch_003 + MUON WD=0.04`: late `SWA` regenerated `uniform int8+zstd-22` vs late `EMA` regenerated `uniform int8+zstd-22` | total submission bytes | `14133579` | `14280689` | `+147110` | EMA also spent more bytes while producing a worse score, so it does not offer a win-by-different-trade. |
| Local optimization on `arch_003 + MUON WD=0.04`: late `SWA` `mlp_int6_else_int8+zstd-22` vs late `EMA` same policy | post-export `val_bpb` | `1.23449862` | `1.23326469` | `-0.00123393` | EMA partially repaired the severe mixed-precision damage, but the readout still remains far worse than the pre-`opt_003` frontier and far worse than regenerated `uniform int8` on the EMA checkpoint. |
| Local optimization on `arch_003 + MUON WD=0.04`: late `SWA` `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` vs late `EMA` same policy | post-export `val_bpb` | `1.23618602` | `1.23457111` | `-0.00161490` | The second mixed-precision policy shows the same pattern: small recovery versus SWA, but nowhere near enough to make the policy competitive again. |
| Local optimization on `arch_003 + MUON WD=0.04`: regenerated `uniform int8+zstd-22` vs `mlp_int6_plus_attn_proj_int6_else_int8+zstd-22` on the late `EMA` checkpoint | post-export `val_bpb` | `1.21883931` | `1.23457111` | `+0.01573180` | EMA narrows the catastrophic gap from the SWA checkpoint, but mixed precision still clearly loses to regenerated `uniform int8` on the same EMA checkpoint family. |
| Local export-only opt_003 checkpoint: locked regenerated `uniform int8+zstd-22` vs `uniform_int8_gptq_lite+zstd-22` | artifact bytes | `14067767` | `14067534` | `-233` | Same checkpoint, same `sliding_window stride=64` eval, same `zstd-22`; candidate used clip-percentile search `[0.999, 0.9995, 0.9999, 0.99999, 1.0]` on `62` large 2D float tensors with no activation calibration; roundtrip/load correctness stayed exact. |
| Local export-only opt_003 checkpoint: locked regenerated `uniform int8+zstd-22` vs `uniform_int8_gptq_lite+zstd-22` | total submission bytes | `14133579` | `14134744` | `+1165` | The artifact got slightly smaller, but current total bytes rose because current `/newcpfs/lxh/parameter-golf/train_gpt.py` code bytes are `67210` versus the locked baseline summary’s `65812`; this code-byte drift is outside the export mechanism itself. |
| Local export-only opt_003 checkpoint: locked regenerated `uniform int8+zstd-22` vs `uniform_int8_gptq_lite+zstd-22` | post-export `val_bpb` | `1.20880134` | `1.20884738` | `+0.00004605` | This bounded GPTQ-lite refinement failed the primary objective: the candidate stayed under the cap and preserved exactness, but it slightly worsened exported quality instead of improving it. |
| Local architecture on winning `opt_003` stack: baseline `10L` vs candidate `11L` | checkpoint `val_bpb` | `1.20495912` | `1.19786472` | `-0.00709440` | Same `MLP2x + BigramHash(4096) + SmearGate + MUON WD=0.04 + late SWA`, same `sliding_window stride=64`, same 8-GPU wallclock cap; changed only `NUM_LAYERS=10 -> 11`. |
| Local architecture on winning `opt_003` stack: baseline `10L` regenerated `uniform int8+zstd-22` vs candidate `11L` | post-export `val_bpb` | `1.20880134` | `1.20150644` | `-0.00729490` | This is the primary success metric for the round: the deeper candidate improves the exported score materially while staying under the cap. |
| Local architecture on winning `opt_003` stack: baseline `10L` regenerated `uniform int8+zstd-22` vs candidate `11L` | artifact bytes | `14067767` | `15366915` | `+1299148` | The extra layer costs about `1.30 MB` of artifact budget but still fits under the 16 MB submission cap when paired with the winning `uniform int8 + zstd-22` export path. |
| Local architecture on winning `opt_003` stack: baseline `10L` regenerated `uniform int8+zstd-22` vs candidate `11L` | total submission bytes | `14133579` | `15434125` | `+1300546` | Current code bytes are `67210` for the candidate summary versus the locked baseline summary’s `65812`, so total bytes rise slightly more than artifact bytes. |
| Local architecture on winning `opt_003` stack: baseline `10L` regenerated `uniform int8+zstd-22` vs candidate `11L` | quantization gap `bpb` | `+0.00384222` | `+0.00364172` | `-0.00020050` | The deeper checkpoint is not only better raw; it is also slightly more robust to the fixed uniform-int8 export readout on this recipe family. |
| Local architecture on winning `11L` stack: baseline `BigramHash(4096)` vs candidate `BigramHash(8192)` | checkpoint `val_bpb` | `1.19786472` | `1.19748125` | `-0.00038347` | Same `11L / MLP2x + SmearGate + MUON WD=0.04 + late SWA`, same `sliding_window stride=64`, same 8-GPU wallclock cap; changed only `BIGRAM_HASH_BUCKETS=4096 -> 8192`. |
| Local architecture on winning `11L` stack: baseline `BigramHash(4096)` regenerated `uniform int8+zstd-22` vs candidate `BigramHash(8192)` | post-export `val_bpb` | `1.20150644` | `1.20023167` | `-0.00127477` | This is the primary success metric for the round: the larger hash table improves the exported score and remains under the cap, but only `51424` bytes of headroom remain. |
| Local architecture on winning `11L` stack: baseline `BigramHash(4096)` regenerated `uniform int8+zstd-22` vs candidate `BigramHash(8192)` | artifact bytes | `15366915` | `15881366` | `+514451` | The larger hash table spends almost all remaining artifact budget on this fixed export path. |
| Local architecture on winning `11L` stack: baseline `BigramHash(4096)` regenerated `uniform int8+zstd-22` vs candidate `BigramHash(8192)` | total submission bytes | `15434125` | `15948576` | `+514451` | Code bytes stayed fixed at `67210`, so the total-byte increase exactly matches the artifact-byte increase. |
| Local architecture on winning `11L` stack: baseline `BigramHash(4096)` regenerated `uniform int8+zstd-22` vs candidate `BigramHash(8192)` | quantization gap `bpb` | `+0.00364172` | `+0.00275042` | `-0.00089130` | The larger hash table is not only better raw; it is also more robust to the fixed uniform-int8 export readout on this checkpoint family. |
| Local optimization on winning `arch_005` stack: baseline `WARMDOWN_ITERS=3000` vs candidate `3500` | checkpoint `val_bpb` | `1.19748125` | `1.19943901` | `+0.00195776` | Same `11L / MLP2x + BigramHash(8192) + SmearGate + MUON WD=0.04 + late SWA`, same `sliding_window stride=64`, same 8-GPU wallclock cap; changed only `WARMDOWN_ITERS=3000 -> 3500`. |
| Local optimization on winning `arch_005` stack: baseline `WARMDOWN_ITERS=3000` vs candidate `3500` regenerated `uniform int8+zstd-22` | post-export `val_bpb` | `1.20023167` | `1.20206766` | `+0.00183599` | This is the primary result from `opt_005`: the longer warmdown made the exported score worse on the near-cap winning stack. |
| Local optimization on winning `arch_005` stack: baseline `WARMDOWN_ITERS=3000` vs candidate `3500` regenerated `uniform int8+zstd-22` | artifact bytes | `15881366` | `15780075` | `-101291` | Bytes improved slightly even though score did not, so the failure is not a budget overrun but a quality regression. |
| Local optimization on winning `arch_005` stack: baseline `WARMDOWN_ITERS=3000` vs candidate `3500` regenerated `uniform int8+zstd-22` | total submission bytes | `15948576` | `15847285` | `-101291` | Code bytes stayed fixed at `67210`, so the total-byte change exactly matches the artifact-byte change. |
| Local optimization on winning `arch_005` stack: baseline `WARMDOWN_ITERS=3000` vs candidate `3500` regenerated `uniform int8+zstd-22` | quantization gap `bpb` | `+0.00275042` | `+0.00262866` | `-0.00012177` | The longer warmdown slightly improved fixed-int8 robustness, but not enough to offset the weaker checkpoint. |
| Local architecture on winning `arch_005` stack: baseline attention vs candidate `XSA_LAST_N=4` | checkpoint `val_bpb` | `1.19748125` | `1.19825960` | `+0.00077834` | Same `11L / MLP2x + BigramHash(8192) + SmearGate + MUON WD=0.04 + late SWA`, same `sliding_window stride=64`, same 8-GPU wallclock cap; changed only the deepest `4` attention blocks to `XSA`. |
| Local architecture on winning `arch_005` stack: baseline attention vs candidate `XSA_LAST_N=4` regenerated `uniform int8+zstd-22` | post-export `val_bpb` | `1.20023167` | `1.20101996` | `+0.00078829` | This is the primary negative result from `arch_006`: the direct `XSA4` reproduction made the exported score worse on the near-cap winning stack. |
| Local architecture on winning `arch_005` stack: baseline attention vs candidate `XSA_LAST_N=4` regenerated `uniform int8+zstd-22` | artifact bytes | `15881366` | `15845227` | `-36139` | The candidate artifact is slightly smaller, so the failure is not a byte-budget overrun. |
| Local architecture on winning `arch_005` stack: baseline attention vs candidate `XSA_LAST_N=4` regenerated `uniform int8+zstd-22` | code bytes | `67210` | `68366` | `+1156` | The root-script support for selectable `XSA` increased counted code bytes modestly. |
| Local architecture on winning `arch_005` stack: baseline attention vs candidate `XSA_LAST_N=4` regenerated `uniform int8+zstd-22` | total submission bytes | `15948576` | `15913593` | `-34983` | Despite the code-byte increase, the smaller artifact leaves the candidate under the cap by `86407` bytes; roundtrip/load correctness stayed exact. |
| Local architecture on winning `arch_005` stack: baseline attention vs candidate `XSA_LAST_N=4` regenerated `uniform int8+zstd-22` | quantization gap `bpb` | `+0.00275042` | `+0.00276037` | `+0.00000994` | The fixed-int8 quantization gap is effectively unchanged, so the regression comes from weaker model quality rather than a changed export failure mode. |
| Local architecture on winning `arch_005` stack: baseline full RoPE vs candidate `Partial RoPE (16/64) + LN Scale` | checkpoint `val_bpb` | `1.19748125` | `1.19731787` | `-0.00016338` | Same `11L / MLP2x + BigramHash(8192) + SmearGate + MUON WD=0.04 + late SWA`, same `sliding_window stride=64`, same 8-GPU wallclock cap; changed only `ROPE_DIMS=16` and `LN_SCALE=1`. |
| Local architecture on winning `arch_005` stack: baseline full RoPE vs candidate `Partial RoPE (16/64) + LN Scale` regenerated `uniform int8+zstd-22` | post-export `val_bpb` | `1.20023167` | `1.20071205` | `+0.00048037` | This is the primary result from `arch_007`: the geometry bundle slightly improves raw checkpoint quality but worsens the fixed exported objective. |
| Local architecture on winning `arch_005` stack: baseline full RoPE vs candidate `Partial RoPE (16/64) + LN Scale` regenerated `uniform int8+zstd-22` | artifact bytes | `15881366` | `15651578` | `-229788` | The candidate is materially smaller on artifact bytes, so the export failure is not caused by a budget overrun. |
| Local architecture on winning `arch_005` stack: baseline full RoPE vs candidate `Partial RoPE (16/64) + LN Scale` regenerated `uniform int8+zstd-22` | total submission bytes | `15948576` | `15721243` | `-227333` | Code bytes rose from `67210` to `69665`, but the much smaller artifact still leaves the candidate under the cap by `278757` bytes. |
| Local architecture on winning `arch_005` stack: baseline full RoPE vs candidate `Partial RoPE (16/64) + LN Scale` regenerated `uniform int8+zstd-22` | quantization gap `bpb` | `+0.00275042` | `+0.00339417` | `+0.00064375` | The raw checkpoint gain does not survive the fixed export path; the gap widens enough to more than erase the small checkpoint improvement. |
| Local aggressive bundle on winning `arch_005` stack: baseline recipe vs full live `#3` motif | checkpoint `val_bpb` | `1.19748125` | `1.21495046` | `+0.01746921` | Same `11L / MLP2x + BigramHash(8192) + SmearGate + Muon WD=0.04 + warmdown3000 + momentum warmup1500`, same `sliding_window stride=64`, same 8-GPU wallclock cap; changed only `ROPE_DIMS=16`, `LN_SCALE=1`, `XSA_LAST_N=4`, and late `EMA` replacing late `SWA`. |
| Local aggressive bundle on winning `arch_005` stack: baseline recipe vs full live `#3` motif regenerated `uniform int8+zstd-22` | post-export `val_bpb` | `1.20023167` | `1.21781093` | `+0.01757926` | This is the decisive result from `arch_008`: the full bundle transfer fails badly on the locked local winner even though the candidate stays byte-safe and roundtrips exactly. |
| Local aggressive bundle on winning `arch_005` stack: baseline recipe vs full live `#3` motif regenerated `uniform int8+zstd-22` | total submission bytes | `15948576` | `15877536` | `-71040` | The failure is not a budget overrun; the candidate is actually smaller and still leaves `122464` bytes of headroom under the cap. |
| Local aggressive bundle on winning `arch_005` stack: baseline recipe vs full live `#3` motif regenerated `uniform int8+zstd-22` | quantization gap `bpb` | `+0.00275042` | `+0.00286047` | `+0.00011005` | Export robustness is only slightly worse than the locked winner, so the large regression is mainly a checkpoint-quality collapse rather than an export-only pathology. |
| Mechanism baseline: `arch_007 Partial RoPE + LN Scale` vs `arch_008 Partial RoPE + LN Scale + EMA + XSA4` | post-export `val_bpb` | `1.20071205` | `1.21781093` | `+0.01709888` | Adding the missing frontier companions did not rescue the geometry sub-bundle locally; it made both raw and exported quality far worse. |
| Mechanism baseline: `arch_007 Partial RoPE + LN Scale` vs `arch_008 Partial RoPE + LN Scale + EMA + XSA4` | quantization gap `bpb` | `+0.00339417` | `+0.00286047` | `-0.00053370` | The full bundle slightly improves fixed-int8 robustness versus `arch_007`, but that improvement is irrelevant because the underlying checkpoint quality collapses. |
| External archived record mean vs local `repro_001` archived-stack reproduction | post-export `val_bpb` | `1.14276` | `1.22520354` | `+0.08244354` | Same nominal recipe family, but the local run required runtime compatibility shims and reached only `2223` steps under `600.069s`, so this is a fidelity check rather than a leaderboard-quality apples-to-apples result. |
| Local best `arch_005 uniform int8+zstd-22` vs local `repro_001` archived-stack reproduction | post-export `val_bpb` | `1.20023167` | `1.22520354` | `+0.02497187` | The archived-stack reproduction stayed under the byte cap after lossless `zstd-22` correction, but it did not beat the locked local best. |
| Local best `arch_005 uniform int8+zstd-22` vs local `repro_001` archived-stack reproduction | total submission bytes | `15948576` | `15971791` | `+23215` | The reproduction is still byte-safe, but it uses slightly more total bytes than the locked local best even though its counted code bytes are smaller (`52930` vs `67210`). |
| Local `repro_001` runtime artifact vs corrected comparison artifact | artifact bytes | `16835646` | `15918861` | `-916785` | The runtime artifact used `zlib` because the local python environment lacked `zstandard`; the corrected comparison artifact was produced by lossless `zlib` decompress plus `zstd-22` recompress and preserves the exact quantized payload bytes. |
| Archived seed-42 reference vs local `repro_001` reproduction | steps reached within `600s` | `6709` | `2223` | `-4486` | SWA count still matched at `24`, but the local run was about `3x` slower per step, which is the clearest evidence of environment/runtime drift. |
| Shimmed `repro_001` vs shim-free `infra_001` on archived `record06` | post-export `val_bpb` | `1.22520354` | `1.14323509` | `-0.08196845` | Same archived script and same local hardware; only the execution environment changed, removing the run-local shim and restoring native `zstd` plus native torch APIs. |
| Shimmed `repro_001` vs shim-free `infra_001` on archived `record06` | steps reached within `600s` | `2223` | `6666` | `+4443` | The clean environment almost exactly recovered the archived step budget, showing the prior negative result was dominated by runtime drift rather than recipe weakness. |
| Shimmed `repro_001` vs shim-free `infra_001` on archived `record06` | artifact bytes | `15918861` | `15705618` | `-213243` | Both are compared as `zstd-22`; the clean rerun emitted `zstd` directly at runtime instead of requiring post-hoc correction. |
| Local best `arch_005 uniform int8+zstd-22` vs shim-free `infra_001 record06` | post-export `val_bpb` | `1.20023167` | `1.14323509` | `-0.05699658` | The clean archived-stack rerun is the new best local byte-safe result and also uses fewer total bytes (`15758548` vs `15948576`). |
| Archived seed-42 reference vs shim-free `infra_001 record06` | post-export `val_bpb` | `1.14271194` | `1.14323509` | `+0.00052315` | The clean local rerun nearly matches archived fidelity: steps `6709 -> 6666`, `swa:start 5550 -> 5500`, and SWA count `24 -> 24`. |
| Clean local archived-stack seed-42 vs clean local archived-stack seed-1337 | post-export `val_bpb` | `1.14323509` | `1.14374449` | `+0.00050940` | Same archived script and same shim-free `physicslm` path; only `SEED` changed from `42` to `1337`, and both runs preserved direct runtime `zstd` plus exact roundtrip/load correctness. |
| Clean local archived-stack seed-42 vs clean local archived-stack seed-1337 | total submission bytes | `15758548` | `15683401` | `-75147` | The seed-1337 run is slightly smaller while keeping near-identical training geometry: steps `6666 -> 6679`, `swa:start 5500 -> 5500`, SWA count `24 -> 24`. |
| Archived seed-1337 reference vs clean local archived-stack seed-1337 | post-export `val_bpb` | `1.14298416` | `1.14374449` | `+0.00076033` | The clean local seed-1337 rerun remains near archival fidelity: steps `6711 -> 6679`, `swa:start 5550 -> 5500`, and SWA count `24 -> 24`. |
| Archived 3-seed mean vs clean local archived-stack seed-1337 | post-export `val_bpb` | `1.14276` | `1.14374449` | `+0.00098449` | This keeps the second clean local seed within `0.001` of the archived 3-seed mean, which is strong enough to treat the stack as a stable local baseline. |
| Clean `record06` seed-42 baseline vs `opt_006` late QAT + fixed SWA | checkpoint `val_bpb` | `1.1537` | `1.1613` | `+0.0076` | Same archived `record06` stack, same shim-free `physicslm` path, same seed, same export policy, and same evaluation protocol; changed only by adding late QAT starting at `step=5500`. |
| Clean `record06` seed-42 baseline vs `opt_006` late QAT + fixed SWA | post-export `val_bpb` | `1.14323509` | `1.14339099` | `+0.00015590` | This is the primary result from `opt_006`: late QAT does not beat the clean same-seed baseline on the exported objective. |
| Clean `record06` seed-42 baseline vs `opt_006` late QAT + fixed SWA | total submission bytes | `15758548` | `16086488` | `+327940` | The candidate is over the cap by `86488`, so even its tiny gain versus the 2-seed mean is not usable for submission. |
| Clean `record06` seed-42 baseline vs `opt_006` late QAT + fixed SWA | quantization gap `bpb` | `-0.01046491` | `-0.01790901` | `-0.00744410` | The checkpoint-to-export gap becomes more negative, but that does not rescue the run because raw checkpoint quality regresses first and the final exported score still ends up slightly worse. |
| Clean local 2-seed `record06` mean vs `opt_006` late QAT + fixed SWA | post-export `val_bpb` | `1.14348979` | `1.14339099` | `-0.00009880` | The candidate barely edges the 2-seed mean, but the effect is too small to matter given the byte-cap failure and the same-seed regression. |
| Clean `record06` seed-42 baseline vs `arch_009` LeakyReLU² + fixed SWA | checkpoint `val_bpb` | `1.1537` | `1.1509` | `-0.0028` | Same archived `record06` stack, same shim-free `physicslm` path, same seed, same export policy, and same evaluation protocol; changed only the MLP activation from `ReLU^2` to `LeakyReLU(0.5)^2`. |
| Clean `record06` seed-42 baseline vs `arch_009` LeakyReLU² + fixed SWA | post-export `val_bpb` | `1.14323509` | `1.14068897` | `-0.00254612` | This is the primary result from `arch_009`: the isolated activation transfer clearly beats the clean same-seed baseline while preserving byte-safe direct-runtime `zstd` and exact roundtrip correctness. |
| Clean `record06` seed-42 baseline vs `arch_009` LeakyReLU² + fixed SWA | total submission bytes | `15758548` | `15961473` | `+202925` | The candidate stays under the cap, but only by `38527` bytes, so the remaining headroom is now tight. |
| Clean `record06` seed-42 baseline vs `arch_009` LeakyReLU² + fixed SWA | quantization gap `bpb` | `-0.01046491` | `-0.01021103` | `+0.00025388` | The gain comes mainly from a stronger raw checkpoint; export remains slightly less favorable than the clean seed-42 baseline, but not enough to erase the checkpoint improvement. |
| Clean `record06` seed-1337 baseline vs `arch_009` LeakyReLU² seed-42 result | post-export `val_bpb` | `1.14374449` | `1.14068897` | `-0.00305552` | The new candidate already beats the clean second archived seed, which is strong evidence that the transfer is not just a tiny same-seed effect. |
| Clean local 2-seed `record06` mean vs `arch_009` LeakyReLU² seed-42 result | post-export `val_bpb` | `1.14348979` | `1.14068897` | `-0.00280082` | Even before a second rerun, the new activation transfer beats the previous clean 2-seed mean by a substantial margin. |
| Archived 3-seed `record06` mean vs `arch_009` LeakyReLU² seed-42 result | post-export `val_bpb` | `1.14276` | `1.14068897` | `-0.00207103` | The isolated activation transfer also beats the archived 3-seed mean while remaining under the byte cap, making it the strongest local readout yet on this family. |
| Local `record06 + LeakyReLU²` winner: late `SWA` vs `EMA + warmdown3500` | checkpoint `val_bpb` | `1.1509` | `1.1547` | `+0.0038` | Same `record06 + LeakyReLU(0.5)^2`, same mixed `int5/int6 + zstd-22` export, same shim-free `physicslm` path, same seed, same `600s` cap; changed only `SWA -> EMA` and `WARMDOWN_ITERS=3000 -> 3500`. |
| Local `record06 + LeakyReLU²` winner: late `SWA` vs `EMA + warmdown3500` | post-export `val_bpb` | `1.14068897` | `1.15374139` | `+0.01305242` | This is the decisive result from `opt_007`: the frontier optimization bundle fails badly on the local best lineage. |
| Local `record06 + LeakyReLU²` winner: late `SWA` vs `EMA + warmdown3500` | total submission bytes | `15961473` | `15735870` | `-225603` | The candidate stayed comfortably under the cap and even got smaller, so the failure is not a byte-budget overrun. |
| Local `record06 + LeakyReLU²` winner: late `SWA` vs `EMA + warmdown3500` | checkpoint-to-export gap `bpb` | `-0.01021103` | `-0.00095861` | `+0.00925242` | EMA plus longer warmdown greatly reduced the export penalty, but the raw checkpoint got worse first, so the exported result still regressed sharply. |
| Clean `record06` seed-1337 baseline vs `repro_003` `LeakyReLU²` seed-1337 | checkpoint `val_bpb` | `1.1543` | `1.1529` | `-0.0014` | Same archived `record06` stack, same shim-free `physicslm` path, same mixed `int5/int6 + zstd-22` export, and same evaluation protocol; changed only the MLP activation from `ReLU^2` to `LeakyReLU(0.5)^2`. |
| Clean `record06` seed-1337 baseline vs `repro_003` `LeakyReLU²` seed-1337 | post-export `val_bpb` | `1.14374449` | `1.14276256` | `-0.00098193` | This is the primary confirmation result: the transferred line also beats the clean seed-1337 baseline while staying under the cap with direct-runtime `zstd` and exact roundtrip correctness. |
| Seed-42 `arch_009` `LeakyReLU²` vs seed-1337 `repro_003` `LeakyReLU²` | post-export `val_bpb` | `1.14068897` | `1.14276256` | `+0.00207359` | The transfer survives the second local seed, but the seed-1337 result lands slightly behind seed-42, narrowly missing the earlier secondary `<=0.002` similarity band by `0.00007359`. |
| Old clean local 2-seed mean vs updated `LeakyReLU²` 2-seed mean | post-export `val_bpb` | `1.14348979` | `1.14172576` | `-0.00176403` | The updated transferred mean is clearly better than the prior clean local baseline mean, which is strong enough to promote `record06 + LeakyReLU² + SWA` to the new active local line. |
| Local active best `arch_009` vs `repro_004` record-`#2` reproduction | post-export `val_bpb` | `1.14068897` | `1.12837920` | `-0.01230977` | This is the decisive aggressive-phase outcome: the full documented `#2` stack beats the prior local best by a large margin while staying under the byte cap. |
| Local active best `arch_009` vs `repro_004` record-`#2` reproduction | total submission bytes | `15961473` | `15801592` | `-159881` | The stronger `11L EMA + GPTQ-lite + warmdown3500 + QAT@0.15` reproduction is also smaller than the prior active baseline despite larger code bytes, because the packed artifact is materially smaller. |
| Documented record `#2` vs local `repro_004` reproduction | post-export `val_bpb` | `1.12278022` | `1.12837920` | `+0.00559898` | The local run reproduced the intended `#2` motifs and won locally, but it still missed the documented result by about `0.0056 BPB`, so the remaining blocker is now portability/runtime rather than stack selection. |
| Documented record `#2` vs local `repro_004` reproduction | steps reached within `600s` | `7101` | `6084` | `-1017` | This is the clearest current fidelity gap: the local reproduction reached materially fewer steps despite matching the intended late-phase markers, and it required one explicit SDPA fallback because `flash_attn_interface` is unavailable locally. |
| Local backend isolation on reproduced `#2` stack: flash-only SDPA fallback vs cuDNN-only SDPA fallback | steps reached within `600s` | `6084` | `6796` | `+712` | Same reproduced `11L EMA + GPTQ-lite + warmdown3500 + QAT@0.15` stack, same seed `1337`, same fallback wrapper `F.scaled_dot_product_attention(..., enable_gqa=True)`; changed only SDPA backend flags and added logging/probe. |
| Local backend isolation on reproduced `#2` stack: flash-only SDPA fallback vs cuDNN-only SDPA fallback | steady-state `step_avg ms` | `98.62` | `88.29` | `-10.33` | Throughput improved by about `11.70%` tokens/s under the same `786432` tokens/step workload. |
| Local backend isolation on reproduced `#2` stack: flash-only SDPA fallback vs cuDNN-only SDPA fallback | post-export `val_bpb` | `1.12837920` | `1.12425244` | `-0.00412676` | This is the main result from `opt_008`: the backend-only change recovers most of the remaining fidelity gap while keeping the fallback wrapper unchanged and confirming actual cuDNN dispatch. |
| Local backend isolation on reproduced `#2` stack: flash-only SDPA fallback vs cuDNN-only SDPA fallback | total submission bytes | `15801592` | `15556158` | `-245434` | Artifact bytes fell by `248540`; code bytes rose by `3106` because the experiment added a diagnostic probe, but the total still improved materially. |
| Documented record `#2` vs local `opt_008` cuDNN-only fallback | post-export `val_bpb` | `1.12278022` | `1.12425244` | `+0.00147222` | The backend isolation shrank the prior reproduced-gap from `+0.00559898` to `+0.00147222`, making the local line nearly record-fidelity. |
| Documented record `#2` vs local `opt_008` cuDNN-only fallback | total submission bytes | `15555017` | `15556158` | `+1141` | The cuDNN-backed local candidate is almost byte-identical to the documented record while still carrying the experiment’s extra probe code. |
| Documented record `#2` vs local `opt_008` cuDNN-only fallback | steps reached within `600s` | `7101` | `6796` | `-305` | The remaining geometry gap is now much smaller than on `repro_004`, which supports the interpretation that the old flash-only fallback was the dominant local runtime blocker. |
| Local cuDNN-backed reproduced `#2` stack: seed `1337` `opt_008` vs seed `42` `repro_005` | post-export `val_bpb` | `1.12425244` | `1.12484349` | `+0.00059105` | This is the decisive robustness result: the seed-42 rerun stays comfortably inside the predeclared `+0.0015` band, so the cuDNN-only fallback is not a seed-1337-only landing. |
| Local cuDNN-backed reproduced `#2` stack: seed `1337` `opt_008` vs seed `42` `repro_005` | steps reached within `600s` | `6796` | `6796` | `+0` | The seed-only confirmation preserved the same training geometry, including the same step budget and the same steady-state `88.29 ms/step` throughput. |
| Local cuDNN-backed reproduced `#2` stack: seed `1337` `opt_008` vs seed `42` `repro_005` | total submission bytes | `15556158` | `15787042` | `+230884` | Code bytes stayed exactly equal at `71243`; the total-byte increase came entirely from a larger exported artifact on seed `42`. |
| Local reproduced leaderboard `#2` stack: pre-cuDNN `repro_004` vs seed-42 cuDNN `repro_005` | post-export `val_bpb` | `1.12837920` | `1.12484349` | `-0.00353571` | The seed-42 confirmation remains clearly better than the old flash-only fallback, so the backend gain transfers beyond a single seed. |
| Documented record `#2` seed `42≈1.1236` vs local `repro_005` seed `42` | post-export `val_bpb` | `1.1236` | `1.12484349` | `+0.00124349` | This reference is contextual only because the published seed-42 readout is approximate, but the local rerun still lands in the same general band while staying under the cap. |
| Controlled record-`#2` portability audit: required native binding in `physicslm` vs actual `physicslm` state | `flash_attn_interface.flash_attn_func` availability | `required for controlled smoke/full run` | `missing` | `blocked` | Managed 1-GPU audit in `physicslm` reported `status=missing_flash_attn_interface` with `cuda_available=true` and `torch 2.10.0+cu128`, so no scored smoke/full comparison was launched this round. |
| Controlled record-`#2` portability audit: validated `physicslm` vs bounded `loongflow_ml` availability note | comparable native binding path | `physicslm: missing top-level binding` | `loongflow_ml: only non-comparable package fragments` | `non-comparable` | `loongflow_ml` lacked native `zstandard`, lacked top-level `flash_attn_interface`, and its VLLM-packaged `flash_attn_interface.py` did not expose an importable `flash_attn_func`; this was recorded as availability information only. |
| Local reproduced leaderboard `#2` stack: `opt_008` ReLU² baseline vs `arch_010` LeakyReLU² transfer | checkpoint `val_bpb` | `1.1407` | `1.1384` | `-0.0023` | Same cuDNN-only SDPA fallback, same reproduced `11L EMA + GPTQ-lite + warmdown3500 + QAT@0.15` stack, same seed `1337`; changed only the MLP activation line. |
| Local reproduced leaderboard `#2` stack: `opt_008` ReLU² baseline vs `arch_010` LeakyReLU² transfer | post-EMA diagnostic `val_bpb` | `1.1397` | `1.1375` | `-0.0022` | The raw checkpoint gain survives EMA application, so the activation helps before export rather than only shifting quantization behavior. |
| Local reproduced leaderboard `#2` stack: `opt_008` ReLU² baseline vs `arch_010` LeakyReLU² transfer | post-export non-overlapping `val_bpb` | `1.14806403` | `1.14584172` | `-0.00222231` | Non-overlapping export also improves, which matches the checkpoint-side gain. |
| Local reproduced leaderboard `#2` stack: `opt_008` ReLU² baseline vs `arch_010` LeakyReLU² transfer | post-export `sliding_window stride=64 val_bpb` | `1.12425244` | `1.12211549` | `-0.00213695` | This is the primary round result: the activation-only transfer beats the prior active best and becomes the new local baseline. |
| Local reproduced leaderboard `#2` stack: `opt_008` ReLU² baseline vs `arch_010` LeakyReLU² transfer | total submission bytes | `15556158` | `15626386` | `+70228` | Artifact bytes rose by `70206` and code bytes by `22`; the candidate remains safely under the 16 MB cap with `373614` bytes of headroom. |
| Local reproduced leaderboard `#2` stack: `opt_008` ReLU² baseline vs `arch_010` LeakyReLU² transfer | step geometry | `6796 @ 88.29 ms/step` | `6734 @ 89.10 ms/step` | `-62 steps, +0.81 ms/step` | Geometry changed only modestly, so the score gain is still interpretable as a controlled activation effect rather than a backend/runtime confound. |
| Local seed-42 pre-activation reference vs new seed-1337 activation transfer | post-export `sliding_window stride=64 val_bpb` | `1.12484349` | `1.12211549` | `-0.00272800` | Even before a seed-42 rerun on the new script, the activation transfer beats the pre-activation seed-42 reference by a wide margin. |
| Documented record `#2` seed `1337` vs local `arch_010` LeakyReLU² transfer | post-export `val_bpb` | `1.12278022` | `1.12211549` | `-0.00066473` | The new local baseline edges the documented record-`#2` single-seed readout on BPB, though it uses `+71369` more total bytes and reaches fewer steps under the local runtime path. |
| Local reproduced leaderboard `#2` stack: `arch_010` seed `1337` vs `repro_006` seed `42` on the exact same `LeakyReLU²` script | post-export `sliding_window stride=64 val_bpb` | `1.12211549` | `1.12281449` | `+0.00069900` | This is the decisive seed-robustness result: the seed-42 rerun stays comfortably inside the predeclared `+0.0015` band while preserving the same cuDNN-backed recipe. |
| Local reproduced leaderboard `#2` stack: `arch_010` seed `1337` vs `repro_006` seed `42` on the exact same `LeakyReLU²` script | step geometry | `6734 @ 89.10 ms/step` | `6737 @ 89.07 ms/step` | `+3 steps, -0.03 ms/step` | Training geometry is essentially unchanged, so the small score delta is interpretable as seed noise rather than a runtime confound. |
| Local reproduced leaderboard `#2` stack: `arch_010` seed `1337` vs `repro_006` seed `42` on the exact same `LeakyReLU²` script | total submission bytes | `15626386` | `15873617` | `+247231` | BPB is stable across seeds, but artifact bytes vary materially, so byte headroom should be tracked explicitly on this line. |
| Local seed-42 pre-activation reference vs seed-42 `LeakyReLU²` confirmation | post-export `sliding_window stride=64 val_bpb` | `1.12484349` | `1.12281449` | `-0.00202900` | This removes the seed confound from the activation comparison: the exact same seed now still beats the pre-activation reference cleanly. |
| Documented record `#2` seed `1337` vs local `repro_006` seed `42` `LeakyReLU²` confirmation | post-export `val_bpb` | `1.12278022` | `1.12281449` | `+0.00003427` | The controlled seed-42 rerun is effectively tied with the documented record-`#2` single-seed readout on BPB, though it uses more total bytes. |
| Local reproduced leaderboard `#2` stack: `arch_010` seed `1337` vs `repro_007` seed `2024` on the exact same `LeakyReLU²` script | post-export `sliding_window stride=64 val_bpb` | `1.12211549` | `1.12225480` | `+0.00013931` | This is the decisive third-seed result: the seed-2024 rerun stays comfortably inside the predeclared `+0.0015` band while preserving the same cuDNN-backed recipe. |
| Local reproduced leaderboard `#2` stack: `repro_006` seed `42` vs `repro_007` seed `2024` on the exact same `LeakyReLU²` script | post-export `sliding_window stride=64 val_bpb` | `1.12281449` | `1.12225480` | `-0.00055969` | The third seed lands between the first two rather than widening the line, which is the expected signature of a stable exact-script recipe. |
| Local reproduced leaderboard `#2` stack: `arch_010` seed `1337` vs `repro_007` seed `2024` on the exact same `LeakyReLU²` script | step geometry | `6734 @ 89.10 ms/step` | `6725 @ 89.22 ms/step` | `-9 steps, +0.12 ms/step` | Training geometry stayed in the same narrow band, so the tiny score delta remains interpretable as seed noise rather than a runtime confound. |
| Local reproduced leaderboard `#2` stack: `arch_010` seed `1337` vs `repro_007` seed `2024` on the exact same `LeakyReLU²` script | total submission bytes | `15626386` | `15832355` | `+205969` | The third seed remains byte-safe but again shows that artifact bytes vary materially even when BPB stays stable. |
| Local seed-42 pre-activation reference vs seed-2024 `LeakyReLU²` confirmation | post-export `sliding_window stride=64 val_bpb` | `1.12484349` | `1.12225480` | `-0.00258869` | The third seed still beats the pre-activation local reference cleanly, so the activation gain is not a fragile 2-seed effect. |
| Documented record `#2` seed `1337` vs local `repro_007` seed `2024` `LeakyReLU²` confirmation | post-export `val_bpb` | `1.12278022` | `1.12225480` | `-0.00052542` | The third seed also beats the documented record-`#2` single-seed readout on BPB, though it uses more total bytes. |
| Documented record `#2` mean band vs exact local `LeakyReLU²` 3-seed mean | post-export `val_bpb` | `1.1233` | `1.12239493` | `-0.00090507` | The exact local 3-seed mean now stays below the documented record-`#2` mean band, which is strong enough to lock `arch_010` as the baseline for the next refinement round. |
| Locked `arch_010` archived seed-1337 export vs `eval_002` same-script no-TTT recovery | post-export `sliding_window stride=64 val_bpb` | `1.12211549` | `1.12211550` | `+0.00000001` | Same saved `final_model.pt` and `final_model.int6.ptz` lineage, same copied eval helper with TTT default-off; the parity gate passed cleanly. |
| `eval_002` same-script no-TTT vs same-script legal score-first TTT on saved `arch_010` seed-1337 artifact | post-export `val_bpb` | `1.12211550` | `1.11971476` | `-0.00240074` | Same saved artifact source, fixed TTT recipe `LR=0.002`, `EPOCHS=3`, `CHUNK_TOKENS=32768`, `FREEZE_BLOCKS=0`; logs recorded `1893` chunks, `score_first=True`, and `last_chunk_untrained=True`. |
| Locked `arch_010` 3-seed mean vs `eval_002` legal score-first TTT | post-export `val_bpb` | `1.12239493` | `1.11971476` | `-0.00268017` | The evaluation-only TTT readout on the saved seed-1337 export lineage beats the locked non-TTT mean by a large margin without retraining or rewriting the artifact. |
| Live SOTA vs `eval_002` legal score-first TTT | post-export `val_bpb` | `1.1194` | `1.11971476` | `+0.00031476` | Strong transfer, but still slightly above the live leaderboard best; this is the closest local result yet to the current SOTA on the `arch_010` line. |
| Locked `arch_010` archived seed-42 export vs `eval_003` same-script no-TTT recovery | post-export `sliding_window stride=64 val_bpb` | `1.12281449` | `1.12281449` | `+0.00000000` | Same saved seed-42 `final_model.pt` and `final_model.int6.ptz` lineage, same copied eval helper with TTT default-off; the parity gate passed exactly. |
| `eval_003` same-script no-TTT vs same-script legal score-first TTT on saved `arch_010` seed-42 artifact | post-export `val_bpb` | `1.12281449` | `1.12032675` | `-0.00248774` | Same saved artifact source, fixed TTT recipe `LR=0.002`, `EPOCHS=3`, `CHUNK_TOKENS=32768`, `FREEZE_BLOCKS=0`; logs again recorded `1893` chunks, `score_first=True`, and `last_chunk_untrained=True`. |
| Saved seed-1337 legal TTT vs saved seed-42 legal TTT on locked `arch_010` artifacts | post-export `val_bpb` | `1.11971476` | `1.12032675` | `+0.00061199` | Seed `42` is slightly worse in absolute BPB, but its gain versus its own no-TTT baseline is slightly stronger (`-0.00248774` vs `-0.00240074`). |
| Locked `arch_010` 3-seed mean vs `eval_003` legal score-first TTT | post-export `val_bpb` | `1.12239493` | `1.12032675` | `-0.00206818` | The second saved-export TTT confirmation still beats the locked non-TTT mean by a large margin without retraining or rewriting the artifact. |
| Live SOTA vs `eval_003` legal score-first TTT | post-export `val_bpb` | `1.1194` | `1.12032675` | `+0.00092675` | Strong second-seed confirmation, though not quite as close to live SOTA as the seed-1337 TTT landing. |
| Locked `arch_010` archived seed-2024 export vs `eval_004` same-script no-TTT recovery | post-export `sliding_window stride=64 val_bpb` | `1.12225480` | `1.12225535` | `+0.00000055` | Same saved seed-2024 `final_model.pt` and `final_model.int6.ptz` lineage, same copied eval helper with TTT default-off; the parity gate passed cleanly. |
| `eval_004` same-script no-TTT vs same-script legal score-first TTT on saved `arch_010` seed-2024 artifact | post-export `val_bpb` | `1.12225535` | `1.12027199` | `-0.00198336` | Same saved artifact source, fixed TTT recipe `LR=0.002`, `EPOCHS=3`, `CHUNK_TOKENS=32768`, `FREEZE_BLOCKS=0`; logs again recorded `1893` chunks, `score_first=True`, and `last_chunk_untrained=True`. |
| Saved seed-1337 legal TTT vs saved seed-2024 legal TTT on locked `arch_010` artifacts | post-export `val_bpb` | `1.11971476` | `1.12027199` | `+0.00055723` | Seed `2024` is slightly worse in absolute BPB than seed `1337`, and its gain is weaker by `+0.00041738`, but it remains strongly positive. |
| Saved seed-42 legal TTT vs saved seed-2024 legal TTT on locked `arch_010` artifacts | post-export `val_bpb` | `1.12032675` | `1.12027199` | `-0.00005476` | Seed `2024` is effectively tied with seed `42` and slightly better in absolute BPB. |
| Locked `arch_010` 3-seed mean vs `eval_004` legal score-first TTT | post-export `val_bpb` | `1.12239493` | `1.12027199` | `-0.00212294` | The third saved-export TTT confirmation still beats the locked non-TTT mean by a large margin without retraining or rewriting the artifact. |
| Current 2-seed legal TTT mean vs saved seed-2024 legal TTT | post-export `val_bpb` | `1.12002076` | `1.12027199` | `+0.00025123` | The third seed lands only about `2.5e-4` above the prior 2-seed mean, so the three-seed TTT line stays tight. |
| Live SOTA vs `eval_004` legal score-first TTT | post-export `val_bpb` | `1.1194` | `1.12027199` | `+0.00087199` | Third-seed confirmation remains within `0.001` of live SOTA without retraining or artifact rewrite. |
| Live SOTA vs exact local legal-TTT 3-seed mean on locked `arch_010` artifacts | post-export `val_bpb` | `1.1194` | `1.12010450` | `+0.00070450` | All three saved export confirmations are now in hand, so the evaluation-side TTT regime should be treated as fully locked on `arch_010`. |
